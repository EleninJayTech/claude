# Claude Code 통합 설치 — 선택형 셀렉터 (드롭인 적용)

> **문서 버전: v1.19** · 최종 갱신: **2026-08-12** · **최근 재검증: 2026-08-04** · 기준: Claude Code v2.1.226
>
> | 버전 | 날짜 | 변경 내용 |
> | --- | --- | --- |
> | v1.0 | 2026-07-23 | 초판 — 01~03 문서를 선택 목록으로 통합 설치하는 셀렉터 |
> | v1.1 | 2026-07-28 | 참조 버전 갱신(v2.1.220, Opus 5 출시) — §B 목록·설치 절차 변경 없음(하위 문서는 01 v1.8·02 v1.7로 갱신됨) |
> | v1.2 | 2026-07-28 | **§B에 04 검증 자동화 추가** + **2단계 선택** 명시(문서 선택 → 각 문서 안에서 항목 선택, 01·02도 부분 선택 지원) + 추천 조합(§C) 갱신 |
> | v1.3 | 2026-07-28 | **운용 프로필 도입**(STEP 3: 표준/절약 — Pro·한도 관리 사용자 권장, 선택이 하위 문서 실행에 전달) + §C에 Pro 행 |
> | v1.4 | 2026-08-04 | **적용 기록 도입**(STEP 5: 대상 CLAUDE.md에 `dropin-applied` 한 줄 — 문서별 적용 버전·프로필·미선택) + **재적용 분기**(STEP 2: 기록 우선 확인, STEP 3: 버전 diff 요약·버전 동일해도 기존/미선택 항목 재질문) |
> | v1.5 | 2026-08-04 | **전면 재검증**(당일 공식 문서 조회, v2.1.221) — 본문 유효 확인·내용 변경 없음. **"최근 재검증" 표기 분리**(형식 갱신 날짜와 내용 재검증 날짜 구분, 전 문서 공통) |
> | v1.6 | 2026-08-04 | **§B에 05 출력스타일·06 루틴 자동화 추가**(신규 문서 2종, 당일 공식 문서 조회로 작성) + STEP 2 감지 항목·§C 추천 조합 갱신 |
> | v1.7 | 2026-08-05 | **설치 모드·번들 도입**(STEP 3 개편): 기반 모드 4종(최소/권장/전체/사용자 — 사용자 외엔 내부 항목 질문 생략) + 목적 번들 합집합(디자인·검증 강화·무인 자동화·회사 수정자) + §C를 모드·번들 정의 표(C-1·C-2)로 개편 + `dropin-applied`에 모드 기록 |
> | v1.8 | 2026-08-10 | **정합성 전수 점검**(DEC-20260810-bsjeong87-07): STEP 5 기록 예시의 버전 하드코딩을 `vX.Y` 플레이스홀더로(기록할 버전은 각 문서 최상단 "문서 버전" 표기에서 읽음 — 예시가 낡아도 잘못 기록되지 않게), §B 01 행·STEP 5 검증 목록에 점검 스킬(`/dropin-check`·`/dropin-update`) 반영 |
> | v1.9 | 2026-08-10 | **기록에 부분 완료 표기 허용**(DEC-20260810-bsjeong87-08): dropin-applied 괄호 메모에 "사용자 조치 대기"를 남기게 명시(예: `gh 설치, 인증 대기`) — 반쪽 상태가 재적용까지 방치된 실사례(2026-08-06 gh 미인증) 해소 |
> | v1.10 | 2026-08-12 | **기록에 `출처=` 필드 신설**(사본/클론 <경로>/raw — STEP 1 확보 경로를 STEP 5에 기록): 클론 참조 설치에서 `/dropin-update`가 "사본 없음"만 보고하던 공백(2026-08-12 wave-project 실사례) 해소 — 점검·최신화 스킬이 이 필드로 설치 유형을 판별해 세 유형 모두 지원 |
> | v1.11 | 2026-08-12 | **STEP 2 흔적 감지에 로컬 전용 산출물 조회 추가**(DEC-20260812-bsjeong87-02): 02 감지를 로스터 파일 존재만 보던 것에서 rubric·핸드오프 산출물 + `.gitignore`·`.git/info/exclude` 조회로 확장 — 02 산출물이 전부 로컬 전용이라 "미설치"로 오판한 실사례(2026-08-10 nastvad) 해소 |
> | v1.12 | 2026-08-12 | **/audit 중·하 등급 일괄 반영**(docs/audit-20260812.md): STEP 5에 통합 워크스페이스 기록 위치(M12)·글로벌 항목 병기(M17)·교체 시 기존 메모 보존(h7)·`게이트 차단(사유)` 어휘(h8), STEP 1 로컬 클론 탐색(h6), STEP 3 재적용에 출처=사본이면 /dropin-update 선행 권장(h5), 불변 ② "06 §1 게이트 전부"(h15), 헤더 기준 버전 v2.1.226 동기화+§L 동기화 규칙(h2) |
> | v1.13 | 2026-08-12 | **적용 기록의 자기 검증·머지 충돌 방침**(STEP 5): ① 기록을 쓴 뒤 **다시 읽어** 건드린 스코프(프로젝트·글로벌)가 전부 반영됐는지·버전이 문서 헤더 실측값과 맞는지 확인 — "갱신했다고 보고했는데 한쪽만 바뀐" 실패(2026-08-12 글로벌 기록 6개 버전 방치)를 절차로 차단 ② 여러 PC의 재적용이 이 한 줄에서 충돌할 때 `merge=union` 금지(교체 방식이라 두 줄로 남음)와 병합 방침 명시 |
> | v1.14 | 2026-08-12 | **/audit 2차 반영**(docs/audit-20260812-2.md): STEP 3 재적용 질문에 **③ 조치 대기·`게이트 차단` 해소 확인 → 메모 제거**(S2 — 닫는 절차 부재로 신호가 영구 반복되던 것)+**역방향 버전 중단**(M14), STEP 5 검증 목록에 04·05·06(M7)·`CLAUDE.md` 부재 시 최소 골격 생성(M15)·`출처=`는 병합 대신 로컬 판별로 덮어씀(M13), STEP 2 06 감지를 "이 repo 대상 루틴"으로 한정(L14), STEP 1 확보 0건 중단(L17), 질문 "2개→3개"(L12), §B 04행 hooks 스코프(L13)·확장 규칙 "05~→07~"(L5) |
> | v1.15 | 2026-08-12 | **/audit 3차(회귀 감사) 반영**(docs/audit-20260812-3.md): STEP 3 질문 ③을 **2분기+증거 확인**으로 교정(S2 — 2차가 "해소 시 메모 제거"만 넣어 **재설치 경로 없이 상태를 지우던** 것. 해소 시 설치 대상에 넣고 §검증으로 확인한 뒤에만 제거, 미해소면 최초 발생일 보존), STEP 5 보존 규칙에 ③ 예외 명시(M6)·최소 골격의 위치/내용/승인/커밋 정의(M7)·**차단 기록 2형식**(문서 전체 vs 항목 단위, M19)·글로벌 기록의 `머신=` 한정자 정식 스펙화+미러 대조 예외(M4)·`출처=사본 <경로>`(M13), STEP 2 기록 탐색을 `dropin-applied` 접두 매칭으로·`~/.claude/agents/` 감지(M10), STEP 4 안전 질문에 **스코프 선택**(M21), §L 버전 열 오지시 정정(L1)·대표 검증에 04 §3·06 §3(L2)·자기 검증을 "이번에 적용한 문서"로 한정(L3)·확보 0건 시 흔적(L14) |
> | v1.16 | 2026-08-12 | **/audit 4차(앵커 없는 전수조사) 반영**(docs/audit-20260812-4.md): STEP 1 확보 경로에 **`사본 <경로>` 판별**(M10 — git repo 여부를 안 보고 전부 "클론"으로 적어 /dropin-update가 fetch에서 실패)·부분 확보 실패 `NN 소스없음(사유)`·확보 0건 시 `CLAUDE.md` 부재 대응(M9·L30), STEP 2에 **통합 워크스페이스 각각 읽기**(M11)·02 산출물 경로를 02 실제 명세로 교체(M17), STEP 3 최소 모드 설명을 01 §A STEP 3(ⓑⓒⓓ)과 동기화(M6)·역방향 중단 후 **복귀 경로**(L29)·**검증 명령 없는 게이트의 증거 규칙**(M12)·질문 ②에서 `해당 없음` 제외(L22), STEP 4 안전 질문 스코프에 **03 스킬·MCP**(M14), STEP 5 글로벌 병기 조건을 **글로벌 스코프 전체로 확장**(M13 — 02 §E가 인용하는데 조건에 안 걸리던 것)·최소 골격 위치를 스코프 기준으로(L24)·대표 검증에 03(L23)·기록 어휘 `소스없음`/`해당 없음` 신설(L22) |
> | v1.17 | 2026-08-12 | **/audit 5차(회귀 감사) 반영**(docs/audit-20260812-5.md): v1.16이 만든 자기모순 정리 — STEP 2의 02 산출물 인용을 **`docs/핸드오프-규약.md` + ⓐ~ⓓ**로(R4·R5: 같은 커밋에서 02가 기호를 바꿨는데 앵커가 낡았고, 02가 "00이 이 파일명으로 감지한다"고 주장하는데 00엔 그 이름이 없었다), `해당 없음` 표기를 **STEP 5 형식 한 벌로 통일**(R8 — 어휘 구분을 신설한 커밋이 자기 어휘를 두 표기로 만들었다), **`소스없음`을 "그 모드에서 설치 대상이었을 문서만"으로 한정**+해소 시 미선택 전환 분기·확보 자체를 증거로(R14·R29 — 고른 적 없는 문서가 영구 재질문·강제 편입되던 것), 확보 0건 중단 시 **스코프 확정 선행+글로벌 한정자**(R15), 최소 골격의 커밋 안내를 프로젝트 한정으로(R21), 대표 검증을 04·06 v1.9/v1.6 문구와 동기화+03의 MCP 검증을 `/mcp`로 정정(R12), 차단 기록 ② 예시의 `04 v1.7` 하드코딩 제거(R17) |
> | v1.18 | 2026-08-12 | **실적용 검증(playground) 발견 반영 + 밀도 정리**: ① **사본 판별을 파일명→헤더로**(F1 — `setup_1.md`처럼 이름이 다른 사본을 `NN_*.md`로만 찾아 "없음"으로 판정, 사본 2개가 3~4버전 낡은 채 방치돼 있었다. `출처=`에 클론+사본 병기도 함께) ② **02 핸드오프 산출물 이름 고정을 완화**(F2 — v1.16이 `docs/핸드오프-규약.md`로 못박아 기존 적용본(`docs/모델-분담-규약.md`)을 "미적용"으로 오판. 기본 이름 + 기록 괄호의 실제 이름 우선으로) ③ **STEP 1·STEP 5 문단 분해** — 2,070자·1,533자 한 덩어리를 사다리·소절·표로 쪼개고 드문 경우(최소 골격·글로벌 한정자·통합 워크스페이스·글로벌 병기·머지 충돌)를 접이식 소절로 이동. 내용 삭제 없음, 본문 밀도만 낮춤([[DEC-20260723-bsjeong87-03]] 얇게 유지 원칙 복귀) |
> | v1.19 | 2026-08-12 | **실적용 2회차 발견 반영**: STEP 5 ③에 **회차 서술 누적 금지**(G3 — "교체하되 보존"만 있고 회차 블록을 쌓지 말라는 말이 없어, 재적용 두 번에 기록 줄이 3,993자까지 자랐다. `CLAUDE.md`는 매 세션 자동 로드라 비용이 상시 발생 — 이 줄은 이력이 아니라 현재 상태다) |
>
> ※ 갱신 시: 이 표에 한 줄 추가 + §B 문서 목록 동기화 + 하단 "문서 정보" 날짜 수정.

> **사용법**: 이 파일 하나를 아무 프로젝트 루트(또는 `docs/`)에 넣고 Claude에게
> **"이 문서를 학습해서 이 PC(또는 이 프로젝트)에 적용해줘"** 라고 하면 된다.
> Claude는 ① 하위 문서(01~) 확보 → ② 환경·기존 구성 감지 → ③ **설치 모드·번들 선택**(최소/권장/전체/사용자 + 목적 번들) → ④ 확정된 것만 해당 문서의 프로토콜대로 설치 → ⑤ 통합 검증·보고 한다.
>
> **기본 원칙**: 1순위 = 최적의 결과물, 토큰 절약 = 품질 유지 전제의 2순위.
> 이 문서는 **라우터**다 — 설치 로직을 중복해 갖지 않고, 각 하위 문서가 자기 절차의 정본이다.

---

## A. Claude 실행 프로토콜 (적용하면 이 순서대로)

**STEP 1 — 하위 문서 확보.** §B 목록의 문서를 아래 사다리로 찾는다.

1. **같은 폴더(또는 `docs/`)의 사본** — **파일명이 아니라 헤더로 판별한다**(`# <제목>` + `> **문서 버전: vX.Y**`). 사본은 `setup_1.md`처럼 다른 이름일 수 있어 `NN_*.md` 패턴으로만 찾으면 **있는데도 "없음"으로 판정**해 클론·raw로 넘어가고, 그 사본은 최신화 대상에서 영영 빠진다(2026-08-12 실적용 발견 — 사본 2개가 3~4버전 낡은 채 방치).
2. **로컬 클론** — remote가 `EleninJayTech/claude`인 폴더(`/dropin-check`와 같은 사다리).
3. **저장소 raw** — §B의 원본(WebFetch, 파일명은 URL 인코딩).

- **확보 경로를 기억해 STEP 5 `출처=`에 남긴다** — 프로젝트 안 사본 / 사용자가 지정한 폴더(**git repo면 `클론 <경로>`, 아니면 `사본 <경로>`** — git 여부를 안 보고 전부 "클론"으로 적으면 `/dropin-update`가 `git fetch`에서 실패한다) / raw. **클론과 사본이 함께 있으면 둘 다 적는다.**
- **일부만 확보 실패** → 선택 목록에 "⚠️ 소스 없음"으로 표시하고 설치 대상에서 제외하되, **STEP 5에 `NN 소스없음(사유)`로 남긴다**(재시도 대상이라 "미선택"과 구분). 단 **그 모드에서 설치 대상이었을 문서만** — 확보는 모드 선택보다 앞서므로, 최소 모드에서 04가 실패한 것을 `소스없음`으로 남기면 고른 적 없는 문서를 영구 재질문하게 된다.
- **확보 0건**(폐쇄망 등) → 모드를 묻지 말고 저장소 URL·클론 방법을 안내하고 **중단**한다. 빈 목록으로 STEP 3까지 가면 아무것도 설치하지 않은 채 "완료"로 보고된다. 중단해도 대상 `CLAUDE.md`에 `<!-- dropin-applied: 적용일 · 확보 실패(사유) -->`를 남긴다(`/resume`·`/dropin-check`가 읽는다). **기록 전에 대상 스코프부터 확정**한다 — PC인지 이 프로젝트인지 모호하면 먼저 묻고, 글로벌이면 `~/.claude/CLAUDE.md`에 STEP 5의 `머신=` 한정자 형식으로. 중단 경로는 STEP 3에 도달하지 못하므로 여기서 정하지 않으면 스코프가 어긋난 자리에 기록이 생긴다. 대상에 `CLAUDE.md`가 아예 없으면 STEP 5의 최소 골격 규칙을 따른다.

**STEP 2 — 환경·기존 구성 감지 (질문 전에 조사).**
- **적용 기록 확인(최우선)**: 대상 `CLAUDE.md` 하단의 `<!-- dropin-applied` 로 시작하는 한 줄(STEP 5 형식 — 글로벌 기록엔 `dropin-applied(글로벌 · 머신=…)`처럼 괄호 한정자가 붙으므로 리터럴 `dropin-applied:`로만 찾지 않는다) → 어떤 문서가 몇 버전으로 언제 적용됐고 프로필·미선택 문서가 무엇인지. 기록이 있으면 아래 흔적 감지보다 이 기록을 1차 근거로 쓴다(없을 때만 흔적으로 추정). **통합 워크스페이스**(상위 폴더 아래 독립 repo 여러 개)면 루트와 하위 repo **각각**의 `CLAUDE.md` 기록을 읽는다(STEP 5·`/dropin-check`·`/dropin-update`와 같은 패턴) — 루트만 보면 하위 repo 적용분을 "미적용"으로 오판해 버전 diff 없이 전체를 다시 깐다.
- 글로벌: `~/.claude/CLAUDE.md`·`settings.json`·`skills/` 존재 여부 → 01 §D(글로벌 셋업)가 이미 됐는지.
- 프로젝트: git repo 여부·`CLAUDE.md`·`docs/`·`.claude/` 존재 → 01 §E가 이미 됐는지, **01 §B 표의 어느 유형인지**(유형 이름을 여기 열거하지 않는다 — 표에 행이 늘면 이 줄만 낡아 새 유형이 감지에서 빠진다. 실측: 혼합형·비-git 폴더가 그렇게 빠져 있었다).
- `.claude/agents/` 로스터 존재 → 02가 이미 적용됐는지. **`~/.claude/agents/`(개인 스코프)도 함께 본다** — 02는 개인 배치를 허용하고 동명이면 개인이 이기므로, 프로젝트 경로만 보면 "미적용"으로 오판해 실행되지도 않는 중복 로스터를 만든다. **로스터 파일만 보지 말 것** — 02가 실제로 명세하는 산출물(`docs/난이도-매트릭스.md`·`docs/spec/<module>.md`·`docs/survey/<area>.md`·핸드오프 규약 문서 — 기본 이름은 `docs/핸드오프-규약.md`이나 **기존 적용본은 다른 이름일 수 있으니**(예: `docs/모델-분담-규약.md`) 기록 괄호의 실제 이름을 먼저 보고, 없으면 `docs/`에서 규약 성격 문서를 찾는다. 02 STEP 3 ⓐ~ⓓ)까지 확인하고 `.gitignore`·`.git/info/exclude`도 조회한다(02 산출물은 전부 로컬 전용일 수 있어 **파일 부재≠미적용, git 추적 여부≠적용 범위**).
- statusLine·skills 폴더 → 03의 항목별 감지(상세는 03 문서가 수행).
- hooks(PostToolUse)·`gh`/`glab`·CI 워크플로 → 04의 항목별 감지(상세는 04 문서가 수행).
- `output-styles/` 폴더(양 스코프)·settings `outputStyle` 키 → 05가 이미 적용됐는지.
- `/schedule list`에서 **이 repo를 대상으로 하는** 루틴이 있는지 + `docs/ROUTINES.md` → 06이 이미 적용됐는지(루틴은 **계정 스코프**라 목록에 뭔가 있다는 것만으론 이 프로젝트 적용 근거가 못 된다. 요금제 Pro+·조직 승인은 코드로 알 수 없어 질문).

**STEP 3 — 설치 모드·번들 선택.** 질문은 기본 3개다(AskUserQuestion, 감지 결과 병기 — 아래 1·2 + 운용 프로필).

1. **기반 모드**(하나 선택 — 정의는 §C-1):
   - **최소** — 01만(보안+`/resume`·`/wrap` 스킬+기록 체계 — 항목 매핑의 정본은 01 §A STEP 3). 문서 내부 항목도 필수만.
   - **권장**(기본 추천) — STEP 2 감지 결과에 §C-1 권장 규칙을 적용한 조합. 무엇이 왜 골라졌는지 한 줄로 보여준다(예: "개발 repo 감지 → 01+02+04").
   - **전체** — 01~06 전부. 해당 없음(비개발 repo의 02 등)은 제외하고 고지하며 **STEP 5 형식대로 `미선택: NN(해당 없음—사유)`로 남긴다**(고지만 하고 안 남기면 재적용 때마다 다시 묻는다. 형식은 STEP 5가 정본 — 여기서 다른 표기를 쓰면 질문 ②의 미선택 스캔에 걸리지 않는다).
   - **사용자** — 현행 방식: §B 문서 목록 → 각 문서 안 항목의 2단계 선택. 각 선택지에 감지 결과 병기(예: "03 — claude-hud 이미 설치됨").
2. **목적 번들**(0개 이상 복수 선택 — 정의는 §C-2): 기반 모드의 선택 상태에 **합집합**으로 얹는다. 감지 결과로 추천 체크를 병기한다(예: 프론트엔드 스택 감지 → "디자인" 기본 체크).

- **모드·번들과 무관한 불변**: ① 이미 적용·유사 구성 항목은 **유지(건너뜀)/재구성(어긋난 부분만)/교체**를 묻는다 — 전체 모드여도 말없이 덮어쓰지 않는다. ② 04 CI 조직 정책·**06 §1 게이트 전부**·**03 §4 MCP·§5 브라우저의 조직 승인**은 어떤 모드에서도 건너뛰지 않는다(셋 다 사내 데이터가 외부로 나가거나 사내 세션에 접근하는 항목이다).
- **재적용(적용 기록 있음)이면**: 기록된 버전과 현재 문서 버전을 비교해 **변경분을 요약 제시**한다(각 문서 최상단 버전 표에서 그 사이 행만 읽으면 된다). **출처=사본이면 비교 전 `/dropin-update`로 사본 최신화를 권장**(사본이 낡은 채 비교하면 버전 diff가 공백으로 나온다). **기록 버전이 현재 문서 버전보다 높으면**(역방향 — 다른 PC가 최신본으로 적용했는데 이쪽 사본·클론이 낡음) 비교를 중단하고 소스 최신화를 먼저 한다 — 그대로 진행하면 **구버전 절차로 덮어쓴다**. 최신화(`/dropin-update`)가 끝나면 **여기 STEP 3부터 재적용을 다시 시작한다** — 문서 갱신은 구성 반영이 아니라서, 최신화만 하고 끝내면 실구성은 그대로다. **버전이 같아도** ① 기존 적용 항목의 재구성(어긋난 부분 점검) 여부 ② 이전에 선택하지 않았던 문서·항목의 추가 여부(**`해당 없음`으로 기록된 것은 제외** — 구조적으로 적용 불가한 것을 매번 다시 묻지 않는다) ③ **기록의 조치 대기 메모·`게이트 차단(사유)`·`소스없음(사유)`/`확보 실패(사유)`가 해소됐는지**(예: `gh 인증 대기` → 인증 완료? / `06 게이트 차단(조직 미승인)` → 승인됐나? / `04 소스없음(네트워크 차단)` → 이번엔 확보되나?)를 묻는다. 답에 따라 갈린다:
  - **해소됨** → 그 문서·항목을 **이번 STEP 4의 설치 대상에 넣는다**(단 `소스없음`은 "확보됐지만 여전히 설치는 원하지 않는다"가 가능하다 — 그때는 편입하지 말고 **`미선택`으로 전환**한다. 확보 실패의 증거는 §검증 명령이 아니라 **확보 자체**다 — 문서 헤더의 버전 표기를 읽어 보이면 해소로 친다)(차단 기록은 "미선택"과 구분되므로 위 ①②에 걸리지 않는다 — 여기서 넣지 않으면 어디서도 설치되지 않는다). 자기신고만 믿지 말고 **해당 문서의 §검증 명령 1개를 실행해 증거로 확인**한다(예: `gh auth status`). **실행할 검증 명령이 없는 게이트**(조직 승인·요금제처럼 조회 대상이 아닌 것)는 **그 문서의 게이트 질문을 다시 물은 답**을 증거로 삼고 무엇을 근거로 해소 판정했는지 괄호에 남긴다(예: `06 vX.Y(2026-○-○ 조직 승인 확인)`) — 명령이 없다는 이유로 판정이 어느 쪽으로도 확정되지 않으면 그 항목은 승인이 난 뒤에도 영영 차단 상태로 남는다. 설치·검증이 성공한 뒤에**만** STEP 5에서 그 메모를 제거한다(괄호 메모 보존 규칙의 예외).
  - **아직 아님·확인 불가** → 메모를 **유지**하고 사유만 갱신한다. 적용일이 새로 찍혀도 괄호에 **최초 발생일을 보존**한다(예: `gh 인증 대기(2026-08-06~)`) — 재적용을 반복할수록 대기가 새것처럼 보이면 방치가 길어진 사실이 지워진다.
  > ⚠️ 메모 제거는 **상태를 닫는 것**이지 지우는 게 아니다. 설치 없이 메모만 없애면 그 문서는 기록에서 "미선택"조차 아닌 상태가 돼 `/resume`·`/dropin-check` 어느 쪽도 다시 알려주지 않는다 — 신호 반복보다 **신호 소실**이 나쁘다.
- 적용 대상이 모호하면(PC인지 이 프로젝트인지) 먼저 확인한다.
- **운용 프로필**을 함께 묻는다: **표준** / **절약**(Pro 요금제·한도 관리 사용자 권장 — Max에서도 한도 임박 시 선택 가능). 선택은 하위 문서 실행에 전달된다(01 §D-6 effort 기본값, 02 §F 절약 배정, 03 claude-hud 기본 체크). 절약 프로필도 **품질 불변 지점**(트리아지·최종 리뷰·검증 게이트)은 줄이지 않는다 — 기본값 하향과 낭비 제거만.

**STEP 4 — 확정된 문서만, 그 문서의 프로토콜대로 실행.** §B의 권장 순서를 따르되, 각 문서를 읽고 **그 문서의 "실행 프로토콜/체크리스트"를 그대로 수행**한다. **내부 항목 선택은 모드가 정한다** — 사용자 모드면 2단계(문서 → 각 문서 안 항목: 01 ⓐ~ⓕ, 02 에이전트별, 03·04 항목별), 그 외 모드면 모드·번들이 정한 기본값으로 내부 항목을 자동 확정하고 질문을 생략한다. 단 하위 문서의 **안전 질문**은 모드와 무관하게 그대로 진행한다 — 유지/재구성/교체 · 게이트 · 감지 불가 항목 확인 · **스코프 선택**(04 §1 hooks 3택, 05 활성화 키, **03 §2·§3 스킬 스코프**, **03 §4 MCP 스코프**). 스코프는 잘못 고르면 팀 전원에게 커밋되거나(04·03 §4의 `.mcp.json`) 다른 프로젝트에서 안 켜지거나(05) **개인 스코프 동명에 가려 설치해도 실행되지 않으므로**(03 — enterprise > personal > project) 기본값으로 밀어붙이지 않는다.

**STEP 5 — 통합 검증·보고.** ① 검증 → ② 커밋 안내 → ③ 적용 기록 → ④ 기록 자기 검증 순으로 한다.

**① 검증** — 각 문서의 §"검증"을 실행해 **증거(출력)로 보고**한다. 대표 항목:

| 문서 | 대표 검증 |
| --- | --- |
| 01 | `/resume`·`/wrap`·`/dropin-check`·`/dropin-update` 자동완성 |
| 02 | 로스터 파일 생성(+뜬 것이 설치한 스코프의 것인지) |
| 03 | `statusLine` 키 · 생성한 스킬 파일 경로(설치 스코프 확인) · MCP는 03 §4 검증(`/mcp` connected + 도구 1회 호출 — `claude mcp list`는 감지 명령이지 검증이 아니다) |
| 04 | **hooks 설정 파일 증거 선행**(시연은 가능할 때만 — 세션 재로딩 불확실성은 04 §1·§L) · `gh pr list` · 워크플로/Secret |
| 05 | `outputStyle` 키 · 스타일 파일 경로 |
| 06 | **MCP를 쓰는 루틴이면** `claude mcp list`(안 쓰면 "해당 없음" 명시) · `/schedule run` 세션 URL · `docs/ROUTINES.md` · `.gitattributes` |

**② 커밋 안내** — 프로젝트에 생성된 파일만. **글로벌 산출물은 커밋 대상이 아니다**(백업 반영은 `/wrap`의 미러 대조가 담당).

**③ 적용 기록** — 대상 `CLAUDE.md` 맨 아래에 아래 한 줄을 추가한다(글로벌만 적용했으면 `~/.claude/CLAUDE.md`). 재적용이면 기존 줄을 **교체**하되 **기존 문서 항목·괄호 메모는 보존**하고 바뀐 부분만 — **단 STEP 3 ③에서 "해소됨"으로 확인되고 이번에 설치·검증까지 마친 메모는 제거**한다.
**회차 서술은 쌓지 않는다** — 이 줄은 이력이 아니라 **현재 상태**다. `[00 vX.Y 재적용 — …]` 같은 회차 블록을 덧붙이면 재적용마다 자라고(실측: 두 회차 만에 3,993자), `CLAUDE.md`는 **매 세션 자동 로드**돼 그 비용이 상시 발생한다. 무엇이 왜 바뀌었는지는 git 히스토리·`docs/PROGRESS.md`가 갖는다 — 이 줄엔 문서별 **현재 버전 + 살아있는 괄호 메모**만 남긴다(2026-08-12 실적용 발견).

```
<!-- dropin-applied: 2026-08-10 · 모드=권장+디자인 · 프로필=표준 · 01 vX.Y(글로벌만) · 02 vX.Y · 03 vX.Y(디자인 3종) · 04 vX.Y · 미선택: 05, 06 · 출처=클론 <경로> -->
```

**기록 필드**
- **적용일 · 모드**(기반+번들, 사용자 모드는 `모드=사용자`) **· 프로필**.
- **문서별 버전** — `vX.Y`는 적용 시점에 **각 문서 헤더에서 읽은 실측값**(예시 숫자를 베끼지 않는다). **부분 완료·사용자 조치 대기는 괄호 메모**(예: `04 vX.Y(gh 설치, 인증 대기)`) — 미완이 기록에 없으면 다음 재적용까지 발견되지 않는다.
- **상태 어휘 4종은 섞지 않는다** — 셋을 한 어휘로 뭉치면 앞의 둘은 잊히고 마지막은 매번 다시 묻게 된다:

  | 상태 | 형식 | 다음 재적용에서 |
  | --- | --- | --- |
  | 고르지 않음 | `미선택: NN` | 질문 ②가 추가 여부를 물음 |
  | 막힘(게이트) | 전체 차단 `NN 게이트 차단(사유)` / 항목 차단 `NN vX.Y(설치분 — <항목> 게이트 차단(사유))` | 질문 ③이 해소를 물음 |
  | 못 받음(확보 실패) | `NN 소스없음(사유)` / 확보 0건이면 줄 전체를 `확보 실패(사유)` | 질문 ③이 재시도를 물음 |
  | 적용 불가(구조적) | `미선택: NN(해당 없음—사유)` | **②③ 둘 다에서 제외** |

- **`출처=`** — STEP 1에서 문서를 어디서 읽었는지: `사본`(프로젝트 안) / `사본 <경로>`(프로젝트 밖 폴더) / `클론 <경로>` / `raw`. 두 스킬이 이 값으로 최신화 경로를 정한다. **클론과 사본이 함께 있으면 둘 다 적는다**(`출처=클론 <경로> + 사본 <파일들>`) — 한 값만 적으면 나머지가 최신화에서 통째로 빠져 낡은 사본이 다음 적용에 다시 쓰인다(2026-08-12 실적용 발견).

**④ 기록 자기 검증** — 쓴 뒤 그 줄을 **다시 읽어** 확인한다: 건드린 스코프(프로젝트·글로벌)가 **전부** 반영됐는지, **이번에 적용한 문서**의 버전이 헤더 실측값과 맞는지. 손대지 않은 문서의 기록은 **적용 시점 스냅샷이라 현재 버전과 달라도 정상**이다(억지로 앞당기면 적용하지도 않은 버전이 기록된다). "남겼다고 보고했는데 한쪽만 바뀐" 실패가 조용히 생기는 자리다(2026-08-12 실사례).

<details><summary><b>드문 경우</b> — 해당할 때만 편다</summary>

- **대상에 `CLAUDE.md`가 없고 01도 미선택**(예: 03만·05만 설치) → 기록만을 위한 **최소 골격**을 만든다: 제목 한 줄 + `dropin-applied` 한 줄(본문은 비운다 — 정식 내용은 나중에 01 §E-1·`/init`이 채움). **위치는 기록할 스코프를 따른다**(프로젝트 적용분=프로젝트 루트 / 글로벌 전용=`~/.claude/CLAUDE.md`). 모든 세션에 자동 로드되므로 **만들기 전 승인**을 받고, 프로젝트 골격만 커밋 안내에 포함한다. 나중에 01을 적용할 때 이 껍데기를 "기존 구성"으로 보고 §E-1 생성을 건너뛰지 않는다(불변 ① 질문에서 "골격뿐"임을 명시). 기록이 없으면 `/dropin-check`가 문서 비교를 생략해 그 문서가 영영 최신화에서 빠진다.
- **글로벌 기록은 스코프·머신 한정자**를 붙인다 — `<!-- dropin-applied(글로벌 · 머신=<이름>): … -->`. 글로벌은 PC당 하나라 밝히지 않으면 백업 미러에서 여러 PC 기록이 서로를 덮는다. 이 한정자 때문에 **기록은 `dropin-applied:` 리터럴이 아니라 `dropin-applied`로 시작하는 줄**로 찾는다(점검 스킬도 동일). 미러 대조에서 **이 줄의 차이는 머신 종속으로 보고 차이로 치지 않는다**([[DEC-20260812-bsjeong87-11]]).
- **통합 워크스페이스** — repo별 적용분은 각 repo `CLAUDE.md`에, 루트 레이어분(01 §E-4)은 루트에. 루트가 얇은 라우터면 단위 규칙을 루트에 복제하지 않는다(기록은 각 단위에 위임).
- **글로벌 병기** — 프로젝트 설치에 **글로벌 스코프 항목이 하나라도** 포함됐으면 `~/.claude/CLAUDE.md`에도 글로벌분을 병기한다: 01 §D · **02의 개인 배치**(`~/.claude/agents/`) · 03의 글로벌 스킬·claude-hud · 05의 글로벌 스타일·활성화 키 · 04 hooks의 PC 스코프. 조건을 01 §D로만 좁히면 02 §E가 인용하는 이 규칙이 02엔 걸리지 않아 "적용됐다는데 흔적 없음" 모순이 난다.
- **머지 충돌**(여러 PC가 같은 repo에 재적용) — 교체 방식이라 `merge=union`을 걸면 안 된다(두 줄로 남아 더 나쁘다). **적용일이 최신인 줄 기준**으로 문서 항목·괄호 메모를 합치되, **`출처=`는 합치지 말고 그 PC의 판별 결과로 덮어쓴다**(로컬 값이라 남의 경로가 남으면 raw로 후퇴한다).
</details>

> 원칙: 승인 없이 대량 변경·커밋하지 않는다. 시크릿(.env·키·인증서)은 읽지도 커밋하지도 않는다.

---

## B. 설치 가능 문서 목록 (이 표가 선택 목록의 원본)

| # | 문서(파일명) | 무엇을 설치하나 | 대상 | 의존/순서 |
| --- | --- | --- | --- | --- |
| 01 | `01_Claude-Code-통합구성-범용마스터.md` | 글로벌 셋업(§D: CLAUDE.md·deny·hooks·스킬) + 프로젝트 셋업(§E·F: 환경 판별 B/A-1/A-2/MSA, docs 기록 체계, `/resume`·`/wrap`·점검 스킬 dropin-check/update) | PC + 프로젝트 | **기반 — 가장 먼저** |
| 02 | `02_모델분담-범용-플레이북.md` | 난이도 라우팅 `.claude/agents/` 로스터(surveyor~parity-tester) + 핸드오프 규약 | 프로젝트 | 01의 docs/ 체계 전제(권장), 개발 프로젝트에만 의미 있음 |
| 03 | `03_확장기능-설치-체크리스트.md` | 확장 5종: claude-hud(사용량 HUD)·svg-design·frontend-design·MCP 서버·브라우저 검증 — 자체 체크리스트로 재질문 | PC 또는 프로젝트 | 독립(단독 적용 가능) |
| 04 | `04_검증-자동화.md` | 검증 3종: hooks 품질 게이트·호스트 CLI(gh/glab)·CI 통합(GitHub Actions 또는 headless 폴백) — git 호스트 감지로 분기 | 프로젝트(hooks는 PC 스코프도 선택 가능 · CLI는 PC) | 독립. CI 통합은 **조직 정책 확인 후**(회사 repo) |
| 05 | `05_출력스타일.md` | 응답의 역할·톤·형식 커스텀: 내장 스타일 4종 전환 + 커스텀 스타일(`output-styles/*.md`) 생성 — `keep-coding-instructions` 함정 안내 포함 | PC 또는 프로젝트 | 독립(단독 적용 가능) |
| 06 | `06_루틴-자동화.md` | 클라우드 스케줄 에이전트(스케줄·API·GitHub 트리거): repo 준비물(`.mcp.json` 등)+루틴 생성+정의 기록(docs/ROUTINES.md) | 프로젝트(+계정) | 독립. **Pro+ 요금제 · research preview · 회사 repo는 조직 승인 후** 🔴 |

- **저장소 원본**(로컬에 없을 때): `https://raw.githubusercontent.com/EleninJayTech/claude/main/<파일명>` (한글 파일명은 URL 인코딩해 요청).
- **권장 순서**: 01 → 02 → 03 → 04 (05·06은 완전 독립 — 필요할 때 언제든). 단 사용자가 일부만 선택하면 그것만 실행한다(예: 문서 전용 repo엔 02·04 불필요, HUD만 원하면 03만).
- 새 문서(07~)가 생기면 이 표에 한 줄 추가하고 버전 표에 기록한다 — 그 순간부터 선택 목록에 자동 포함된다(§C에도 한 줄 — 아래 §C-2 끝 참조).

---

## C. 설치 모드·번들 정의 (STEP 3의 원본)

### C-1. 기반 모드

| 모드 | 무엇을 설치하나 |
| --- | --- |
| **최소** | 01만 — 보안+`/resume`·`/wrap` 스킬+기록 체계(기반. 항목 매핑 정본은 01 §A STEP 3). 내부 항목도 필수만 |
| **권장** (기본) | 감지 상황별 자동 조합: **새 PC** → 01(글로벌)+03(claude-hud) / **개발 프로젝트** → 01+02+04(hooks 게이트) / **문서·비개발 repo** → 01만 / **이미 01 적용된 대상** → 미적용 문서 중 상황에 맞는 것만 제안 |
| **전체** | 01~06 전부 — 해당 없음(비개발 repo의 02 등)은 제외 후 고지, 게이트는 그대로 |
| **사용자** | §B 목록에서 직접 — 문서 → 문서 내 항목 2단계 선택 |

> Pro 요금제·한도 관리는 모드가 아니라 **운용 프로필=절약**으로 — 어느 모드와도 조합된다(+03 claude-hud 권장: 한도 가시화가 절약의 전제).

### C-2. 목적 번들 (기반 모드에 합집합으로 얹음, 복수 선택)

| 번들 | 얹는 것 | 추천 체크 조건(감지) |
| --- | --- | --- |
| **디자인**(웹 퍼블리싱) | 03의 svg-design·frontend-design·브라우저 검증 | 프론트엔드 스택 감지 시 기본 체크 |
| **검증 강화** | 04 전체(hooks 게이트+호스트 CLI+CI) | 테스트·CI 구성 감지 시 기본 체크 |
| **무인 자동화** | 06(+04 CI 병용 권장) — Pro+·조직 승인 게이트 그대로 | 감지 불가 — 사용자 요청 시만 |
| **회사** | 새 문서 없음 — **게이트 강화 수정자**: 04 CI는 조직 정책 확인 필수·03 §4 MCP는 허용 서버만·03 §5 브라우저는 사이트 권한 최소화·06은 조직 승인 필수(게이트 실행은 각 문서 STEP이 담당 — 00 §A STEP 3 불변 ②) | 회사 repo라고 답한 경우 자동 적용 |
| **응답 스타일** | 05(내장 전환 또는 커스텀 생성) | 감지 불가 — 사용자 요청 시만 |

- 새 문서(07~)가 생기면: §B에 한 줄 + 여기(C-1 권장 규칙 또는 C-2 소속)에 한 줄 — 그 순간부터 모드·번들 조합에 자동 포함된다.

---

## L. 유지보수
- §B의 파일명이 실제 저장소와 일치하는지(문서 개편 시 이 표부터 갱신). **버전 표기는 §B가 아니라 저장소 `CLAUDE.md` 구성물 표가 정본** — §B에 버전 열을 만들지 않는다(드롭인끼리 서로의 버전을 들면 bump마다 7×7 갱신이 된다).
- 저장소 원본 URL 생존 확인(`github.com/EleninJayTech/claude`, 기본 브랜치 `main`).
- 하위 문서가 늘면(07~) §B·§C에 반영.
- 재검증 시 00~06 헤더·푸터의 "기준/참조: Claude Code v○" 표기를 같은 값으로 동기화(문서 간 불일치 방지).

---

**문서 정보** — 통합 설치 셀렉터 **v1.19** · 최종 갱신: 2026-08-12 · 최근 재검증: 2026-08-04 · 참조: Claude Code v2.1.226. 설치 절차의 정본은 각 하위 문서(01~06)이며 이 문서는 확보→감지→모드 선택→위임만 담당한다.
