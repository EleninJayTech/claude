# Claude Code 통합 설치 — 선택형 셀렉터 (드롭인 적용)

> **문서 버전: v1.41** · 최종 갱신: **2026-09-02** · **최근 재검증: 2026-08-20** · 기준: Claude Code v2.1.237
>
> | 버전 | 날짜 | 변경 내용 |
> | --- | --- | --- |
> | v1.0~v1.13 | 2026-07-23~2026-08-12 | 초판 셀렉터 → §B에 04·05·06 편입·2단계 선택 → 운용 프로필(표준/절약)·설치 모드 4종+목적 번들 합집합 → **적용 기록 체계**(`dropin-applied` 도입 → 재적용 분기 → 조치 대기 메모 → `출처=` 필드 → 기록 자기 검증, 머지 충돌은 `merge=union` 금지·교체 방식) → STEP 2 흔적 감지에 로컬 전용 산출물, STEP 5 통합 워크스페이스 기록 위치·`게이트 차단(사유)` 어휘, 점검 스킬 반영. 행별 상세는 저장소 git 이력 |
> | v1.14 | 2026-08-12 | 재적용 질문 **③ 조치 대기·차단 해소 확인** 신설 + 역방향 버전 중단, STEP 5 검증 목록에 04·05·06·최소 골격 생성, 06 감지 "이 repo 대상" 한정, 확보 0건 중단, 질문 3개 |
> | v1.15 | 2026-08-12 | 질문 ③을 **2분기+증거 확인**으로(해소 시 설치·검증 후에만 메모 제거, 미해소면 최초 발생일 보존), 차단 기록 2형식(문서/항목), 글로벌 기록 `머신=` 한정자+미러 대조 예외, 기록 탐색 접두 매칭, `~/.claude/agents/` 감지, STEP 4 스코프 선택 |
> | v1.16 | 2026-08-12 | STEP 1 `사본 <경로>` 판별·`NN 소스없음(사유)`·확보 0건 대응, STEP 2 통합 워크스페이스 각각 읽기, 최소 모드=ⓑⓒⓓ 동기화, 역방향 복귀 경로, 검증 명령 없는 게이트의 증거 규칙, 스코프에 03 스킬·MCP, 글로벌 병기 조건 확장, `해당 없음` 어휘 |
> | v1.17 | 2026-08-12 | STEP 2 02 산출물 앵커 현행화(`docs/핸드오프-규약.md`+ⓐ~ⓓ), `해당 없음` 표기 한 벌 통일, `소스없음`을 그 모드의 설치 대상 문서로 한정+해소 시 미선택 전환, 확보 0건 중단 시 스코프 확정 선행, 최소 골격 커밋 안내 프로젝트 한정, 대표 검증 동기화 |
> | v1.18 | 2026-08-12 | **사본 판별을 파일명→헤더로**(`출처=`에 클론+사본 병기), 02 핸드오프 이름 고정 완화(기록 괄호의 실제 이름 우선), STEP 1·5 문단을 사다리·표·접이식 소절로 분해 |
> | v1.19 | 2026-08-12 | STEP 5 ③ **회차 서술 누적 금지** — 기록 줄은 이력이 아니라 현재 상태(문서별 현재 버전+살아있는 메모만) |
> | v1.20 | 2026-08-13 | STEP 5 대표 검증 01 행을 **두 축**으로(글로벌 스킬 자동완성 + 프로젝트 산출물 경로 조회), STEP 2 유형 감지의 열거 제거(01 §B 표를 가리킴) |
> | v1.21 | 2026-08-13 | STEP 2에 **프로젝트 기록의 글로벌 항목은 이 PC 실구성 증거가 아님**(글로벌분은 `~/.claude/CLAUDE.md`·실물로 판정), STEP 4 스코프 목록에 02 로스터 배치, STEP 5 ② 커밋 안내에 개인 파일(`settings.local.json`·`CLAUDE.local.md`) 커밋 금지 |
> | v1.22 | 2026-08-13 | **최적화**: 버전 표·본문 압축(규칙 불변). STEP 1 클론+사본 병기 시 **읽기는 헤더 버전 높은 쪽**, STEP 3 ③ 보류 시 `미선택` 전환 일반화, STEP 5 기록에 **00 자신 버전 포함** + 통합 워크스페이스 **위임 표기** 정의(하위 단위→루트 참조, 질문 ①~③ 제외) |
> | v1.23 | 2026-08-13 | STEP 2 통합 워크스페이스에 **회사/개인 단위별 판정**(루트 remote 하나로 답하면 하위 회사 repo가 게이트 없이 통과) + **하위에서 설치 시 상위 라우터·기록 역방향 감지**(루트 레이어 중복 설치 차단) + **clone 직후 하위 부재 시 단일 repo 오판 방지**, §B 표 01행 유형 열거 제거·02행 대상에 개인 배치 병기 |
> | v1.24 | 2026-08-20 | STEP 5 대표 검증 03 행 동기화 — **`/claude-hud:*` 명령 가용성·추천 구성 `config.json` 실측·MCP 스코프 확인**(03 v1.15 검증 배선 반영) |
> | v1.25 | 2026-08-20 | STEP 5 대표 검증 **01 행을 01 §검증 위임으로 일원화**(열거 제거)·06 행에 세션 로그 축(`claude mcp list`는 감지 명령), §B 04 행 CI 3분기·06 "독립" 한정, STEP 1 클론에 **behind 가드**, 확보 실패 해소 시 신규 설치 취급, 전체 모드의 `해당 없음` 재판정 금지, ② 커밋 안내에 적용 기록 줄 포함 |
> | v1.26 | 2026-08-20 | 전면 재검증(v2.1.237) — 기준 CLI 버전 동기, §B 파일 목록·raw URL 생존 확인 |
> | v1.27 | 2026-08-20 | **최적화**: 버전 표 압축(규칙 불변). 재적용 델타의 "그 사이 행"은 **기록된 버전 행 제외**로 명시 |
> | v1.28 | 2026-08-20 | **최적화**: R20 적용 — 구 행 v1.0~v1.13을 축 요약 한 줄로 |
> | v1.29 | 2026-08-20 | STEP 3 병합 구간 폴백 정밀화 — 구간분만 갈음·구간 뒤 상세는 유지, 버전 비교는 정수, 거절 경로·`git log -p` 복원 |
> | v1.30 | 2026-08-21 | **다인 축** — STEP 3 재적용에 **신규 합류 분기**(글로벌 구성 없으면 기본값=유지, 팀 구성을 새 사람 판단으로 덮지 않음), STEP 5 기록에 **머신 로컬 상태는 글로벌이 정본**(조치 대기·`프로필=`은 프로젝트 줄에 안 적는다 — 공유돼 전원에게 상속된다) |
> | v1.31 | 2026-08-21 | **sfa형 시나리오 검증 반영** — 위임 표기의 **루트 부재 폴백**(그 하위 repo만 클론한 팀원이면 위임을 무시하고 미적용으로 취급해 질문 ①~③에 되돌린다 — 정본 없는 위임은 "적용됨"이 아니라 확인 불가) |
> | v1.32 | 2026-08-21 | **wave형 시나리오 검증 반영** — 조치 대기 해소에 **셋째 증거 경로**(게이트도 §검증 명령도 없는 대기는 메모가 지목한 대상을 직접 확인, 관찰 불가면 질문), 미완 메모의 **어휘 형식 고정**(`… 대기(사유)` — 자유 어휘는 소비자 문자열 탐색에 걸리지 않는다), **게이트 차단의 스코프를 사유로 분기**(조직·정책=팀 사실 / 요금제·계정=글로벌) |
> | v1.33 | 2026-08-21 | **/audit 7차(회귀) 반영** — 비설치성 조치 대기는 **설치 없이도 메모 제거**(셋째 증거 경로의 종결), 대기 어휘를 `… 대기(사유, YYYY-MM-DD~)` **한 형식**으로 통합, `프로필=`을 예시·필드에서 글로벌 줄로 이관 + **머신 로컬 항목이 생기면 글로벌 줄을 만들고 기존 메모는 이관**, 신규 합류 판정을 **기록의 흔적** 기준으로, 확보 사다리에 **사용자 지정 폴더** rung + pull 불가 폴백, 위임 표기 정리, 05 스타일 5종 |
> | v1.34 | 2026-08-24 | **산출물 언어 축 신설** — STEP 3 질문 3개→4개(`한국어`/`English`, 기본=대화 언어·기존 산출물 우선), STEP 5 기록에 `언어=ko\|en` 필드. 드롭인 본문은 번역하지 않는다(Claude는 어느 언어로든 같게 실행) |
> | v1.35 | 2026-08-26 | STEP 2의 **02 산출물 열거를 걷어냄**(R14) — `ⓐ~ⓓ` 고정 범위가 02에 선택지가 늘 때마다 낡아, 새 산출물을 "미적용"으로 오판했다. 이제 **02 STEP 3 목록을 정본으로 열어 대조**하고 괄호는 예시로만 둔다 |
> | v1.36 | 2026-08-30 | **저장소 2분리 반영** — §A 확보 사다리 2의 로컬 클론 탐색이 remote 이름 하나(`claude`)만 봐서 **정본을 못 찾았다**(정본은 `claude-dev`로 갈렸다). 두 이름을 모두 인정한다. raw·API 경로는 배포 저장소가 이름 `claude`를 승계해 무변경 |
> | v1.37 | 2026-08-31 | **사실 정정** — 서브에이전트 동명 충돌은 개인이 아니라 **작업 디렉터리에 가까운 쪽**(프로젝트)이 이긴다. 스킬과 방향이 반대이므로 STEP 2 감지 근거를 교체 |
> | v1.38 | 2026-08-31 | STEP 5 `머신=`에 **OS 호스트명 대신 별칭** — 이 줄은 커밋돼 공유되므로 호스트명이 사람·조직을 특정하는 실마리가 된다(§6 익명화) |
> | v1.39 | 2026-08-31 | §L에 **자기 점검(갱신 의식)** 한 줄 — 당일 공식 문서 조회 원칙 + 버전 4축·기계 검사 배선(골격 정본은 `docs/DOC_CONTRACT.md`) |
> | v1.40 | 2026-08-31 | STEP 1 로컬 클론 판별에 **정본 우선·배포본 폴백** 순서 — 두 저장소가 한 PC에 공존하면 배포본이 기준이 되어 정본의 미푸시 개정이 안 보인다(오류 없이 낡은 기준을 쓴다). 폴더 이름 예시 제거 |
> | v1.41 | 2026-09-02 | **/audit 8차(회귀) 반영 3건** — STEP 5 상태 어휘 표에 **적용 범위 메모** 행(재적용 시 현 문서 선택 목록과 대조해 폐지된 선택지를 갱신/제거 — 보존만 하면 `02 vX.Y(ⓕ …)` 같은 유령 메모가 남는다), §B의 개수 하드코딩 제거("확장 5종"·"내장 스타일 5종" — R14), STEP 2 05 감지를 **세 곳**+05 위임으로 |
> ※ 갱신 시: 이 표에 한 줄 추가 + §B 문서 목록 동기화 + 하단 "문서 정보" 날짜 수정.

> **사용법**: 이 파일 하나를 아무 프로젝트 루트(또는 `docs/`)에 넣고 Claude에게
> **"이 문서를 학습해서 이 PC(또는 이 프로젝트)에 적용해줘"** 라고 하면 된다.
> Claude는 ① 하위 문서(01~) 확보 → ② 환경·기존 구성 감지 → ③ **설치 모드·번들 선택**(최소/권장/전체/사용자 + 목적 번들) → ④ 확정된 것만 해당 문서의 프로토콜대로 설치 → ⑤ 통합 검증·보고 한다.
>
> **기본 원칙**: 1순위 = 최적의 결과물, 토큰 절약 = 품질 유지 전제의 2순위.
> 이 문서는 **라우터**다 — 설치 로직을 중복해 갖지 않고, 각 하위 문서가 자기 절차의 정본이다.

---

## A. Claude 실행 프로토콜 (적용하면 이 순서대로)

**STEP 1 — 하위 문서 확보.** §B 목록의 문서를 아래 사다리로 찾는다.

1. **같은 폴더(또는 `docs/`)의 사본** — **파일명이 아니라 헤더로 판별한다**(`# <제목>` + `> **문서 버전: vX.Y**`). 사본은 `setup_1.md`처럼 다른 이름일 수 있어 `NN_*.md` 패턴으로만 찾으면 **있는데도 "없음"으로 판정**해 클론·raw로 넘어가고, 그 사본은 최신화 대상에서 영영 빠진다.
2. **로컬 클론** — remote로 판별한다: **정본 `EleninJayTech/claude-dev`가 우선이고, 배포본 `EleninJayTech/claude`는 폴백**이다(배포본은 정본 push 시점에만 동기화돼 **정본의 미푸시 개정이 안 보인다** — 오류 없이 낡은 기준을 쓰게 된다). **경로·폴더 이름으로 고르지 않는다**(폴더 이름은 PC마다 다르고, 저장소 이름이 재사용되면 같은 이름이 다른 저장소를 가리킨다). 기준으로 쓰기 전 `git fetch`로 뒤처짐을 확인하고 **`behind > 0`이면 알린 뒤 pull 여부를 묻는다** — 두 점검 스킬은 이미 같은 규칙을 갖는다. 없으면 몇 달 묵은 클론으로 설치하고 그 낡은 버전이 기록에 "적용 완료"로 확정된다. **더티·ff 불가로 pull이 막히면** `git show origin/main:<파일>`을 기준으로 읽는다(중단하지 않는다).
2-1. **사용자가 지정한 폴더** — 위 둘이 다 실패하면 경로를 묻는다(`/dropin-check`와 같다). git repo면 `클론 <경로>`, 아니면 `사본 <경로>`로 판별해 `출처=`에 남긴다 — 이 rung이 없으면 폐쇄망에서 프로젝트 밖 최신 사본이 있어도 "확보 0건"으로 중단된다.
3. **저장소 raw** — §B의 원본(WebFetch, 파일명은 URL 인코딩).

- **확보 경로를 기억해 STEP 5 `출처=`에 남긴다** — 프로젝트 안 사본 / 사용자가 지정한 폴더(**git repo면 `클론 <경로>`, 아니면 `사본 <경로>`** — git 여부를 안 보고 전부 "클론"으로 적으면 `/dropin-update`가 `git fetch`에서 실패한다) / raw. **클론과 사본이 함께 있으면 둘 다 적되, 읽기는 헤더 버전이 높은 쪽으로 한다**(보통 클론 — 낡은 쪽은 `/dropin-update` 대상으로 안내. 우선순위 없이 사다리 순서대로 읽으면 최신 클론을 옆에 두고 낡은 사본 절차로 설치한다).
- **일부만 확보 실패** → 선택 목록에 "⚠️ 소스 없음"으로 표시하고 설치 대상에서 제외하되, **STEP 5에 `NN 소스없음(사유)`로 남긴다**(재시도 대상이라 "미선택"과 구분). 단 **그 모드에서 설치 대상이었을 문서만** — 확보는 모드 선택보다 앞서므로, 최소 모드에서 04가 실패한 것을 `소스없음`으로 남기면 고른 적 없는 문서를 영구 재질문하게 된다.
- **확보 0건**(폐쇄망 등) → 모드를 묻지 말고 저장소 URL·클론 방법을 안내하고 **중단**한다. 빈 목록으로 STEP 3까지 가면 아무것도 설치하지 않은 채 "완료"로 보고된다. 중단해도 대상 `CLAUDE.md`에 `<!-- dropin-applied: 적용일 · 확보 실패(사유) -->`를 남긴다(`/resume`·`/dropin-check`가 읽는다). **기록 전에 대상 스코프부터 확정**한다 — PC인지 이 프로젝트인지 모호하면 먼저 묻고, 글로벌이면 `~/.claude/CLAUDE.md`에 STEP 5의 `머신=` 한정자 형식으로. 중단 경로는 STEP 3에 도달하지 못하므로 여기서 정하지 않으면 스코프가 어긋난 자리에 기록이 생긴다. 대상에 `CLAUDE.md`가 아예 없으면 STEP 5의 최소 골격 규칙을 따른다.

**STEP 2 — 환경·기존 구성 감지 (질문 전에 조사).**
- **적용 기록 확인(최우선)**: 대상 `CLAUDE.md` 하단의 `<!-- dropin-applied` 로 시작하는 한 줄(STEP 5 형식 — 글로벌 기록엔 `dropin-applied(글로벌 · 머신=…)`처럼 괄호 한정자가 붙으므로 리터럴 `dropin-applied:`로만 찾지 않는다) → 어떤 문서가 몇 버전으로 언제 적용됐고 프로필·미선택 문서가 무엇인지. 기록이 있으면 아래 흔적 감지보다 이 기록을 1차 근거로 쓴다(없을 때만 흔적으로 추정). 단 **프로젝트 기록의 글로벌 스코프 항목(01 §D 등)은 이 PC의 실구성 증거가 아니다** — 커밋된 기록은 다른 PC의 설치일 수 있으니 글로벌분은 `~/.claude/CLAUDE.md`의 글로벌 기록과 실물로 판정한다. **통합 워크스페이스**(상위 폴더 아래 독립 repo 여러 개)면 루트와 하위 repo **각각**의 `CLAUDE.md` 기록을 읽는다(STEP 5·`/dropin-check`·`/dropin-update`와 같은 패턴) — 루트만 보면 하위 repo 적용분을 "미적용"으로 오판해 버전 diff 없이 전체를 다시 깐다. **회사/개인 판정도 단위별로** 한다 — remote가 단위마다 다르므로 루트 하나로 답하면 하위 회사 repo가 게이트 없이 통과한다(01 §E-2). 반대로 **하위 repo에서 설치할 때는 상위 폴더의 라우터 `CLAUDE.md`·기록도 본다** — 루트 레이어 적용분을 "미적용"으로 오판해 중복 설치하는 것을 막는다. 루트 라우터가 하위 단위를 라우팅하는데 **실물 폴더가 없으면**(clone 직후 — 하위 repo는 보통 루트 `.gitignore`로 제외돼 함께 clone되지 않는다) 단일 repo로 판정하지 말고 하위 repo clone 배치를 먼저 안내한다.
- 글로벌: `~/.claude/CLAUDE.md`·`settings.json`·`skills/` 존재 여부 → 01 §D(글로벌 셋업)가 이미 됐는지.
- 프로젝트: git repo 여부·`CLAUDE.md`·`docs/`·`.claude/` 존재 → 01 §E가 이미 됐는지, **01 §B 표의 어느 유형인지**(유형 이름을 여기 열거하지 않는다 — 표에 행이 늘면 이 줄만 낡아 혼합형·비-git 폴더 같은 새 유형이 감지에서 빠진다).
- `.claude/agents/` 로스터 존재 → 02가 이미 적용됐는지. **`~/.claude/agents/`(개인 스코프)도 함께 본다** — 02는 개인 배치를 허용하므로, 프로젝트 경로만 보면 개인 배치로 이미 적용된 것을 "미적용"으로 오판해 중복 로스터를 만든다. **서브에이전트는 동명일 때 작업 디렉터리에 가장 가까운 정의가 이긴다**(프로젝트 > 개인) — 스킬의 `enterprise > personal > project`와 **방향이 반대**이므로 한쪽 직관을 옮겨 적지 않는다. **로스터 파일만 보지 말 것** — 02가 실제로 명세하는 산출물까지 확인한다 — **목록은 여기 열거하지 않는다**(선택지가 늘 때마다 낡는다. R14): **02 STEP 3의 선택 목록이 정본**이니 그 문서를 열어 각 항목의 산출물을 읽고 대조한다(현재 `docs/난이도-매트릭스.md`·`docs/spec/<module>.md`·`docs/survey/<area>.md`·핸드오프 규약 문서·모델 운용표 등 — **이 괄호는 예시지 목록이 아니다**). 이름이 고정되지 않는 산출물은 **기록 괄호의 실제 이름을 먼저 보고**(예: 핸드오프가 `docs/모델-분담-규약.md`), 없으면 `docs/`에서 그 성격의 문서를 찾는다. 이어서 `.gitignore`·`.git/info/exclude`도 조회한다(02 산출물은 전부 로컬 전용일 수 있어 **파일 부재≠미적용, git 추적 여부≠적용 범위**).
- statusLine·skills 폴더 → 03의 항목별 감지(상세는 03 문서가 수행).
- hooks(PostToolUse)·`gh`/`glab`·CI 워크플로 → 04의 항목별 감지(상세는 04 문서가 수행).
- `output-styles/` 폴더(양 스코프)·settings `outputStyle` 키(**세 곳** — `.claude/settings.local.json` 포함) → 05가 이미 적용됐는지. 상세는 05 문서가 수행.
- `/schedule list`에서 **이 repo를 대상으로 하는** 루틴이 있는지 + `docs/ROUTINES.md` → 06이 이미 적용됐는지(루틴은 **계정 스코프**라 목록에 뭔가 있다는 것만으론 이 프로젝트 적용 근거가 못 된다. 요금제 Pro+·조직 승인은 코드로 알 수 없어 질문).

**STEP 3 — 설치 모드·번들 선택.** 질문은 기본 4개다(AskUserQuestion, 감지 결과 병기 — 아래 1·2 + 운용 프로필 + 산출물 언어).

1. **기반 모드**(하나 선택 — 정의는 §C-1):
   - **최소** — 01만(보안+`/resume`·`/wrap` 스킬+기록 체계 — 항목 매핑의 정본은 01 §A STEP 3). 문서 내부 항목도 필수만.
   - **권장**(기본 추천) — STEP 2 감지 결과에 §C-1 권장 규칙을 적용한 조합. 무엇이 왜 골라졌는지 한 줄로 보여준다(예: "개발 repo 감지 → 01+02+04").
   - **전체** — 01~06 전부. 해당 없음(비개발 repo의 02 등)은 제외하고 고지하며(**기록에 이미 `해당 없음`이 있으면 재판정하지 않는다** — 질문 ②·③의 영구 제외가 정본이고, 전체 모드도 예외가 아니다) **STEP 5 형식대로 `미선택: NN(해당 없음—사유)`로 남긴다**(고지만 하고 안 남기면 재적용 때마다 다시 묻는다. 형식은 STEP 5가 정본 — 여기서 다른 표기를 쓰면 질문 ②의 미선택 스캔에 걸리지 않는다).
   - **사용자** — 현행 방식: §B 문서 목록 → 각 문서 안 항목의 2단계 선택. 각 선택지에 감지 결과 병기(예: "03 — claude-hud 이미 설치됨").
2. **목적 번들**(0개 이상 복수 선택 — 정의는 §C-2): 기반 모드의 선택 상태에 **합집합**으로 얹는다. 감지 결과로 추천 체크를 병기한다(예: 프론트엔드 스택 감지 → "디자인" 기본 체크).

- **모드·번들과 무관한 불변**: ① 이미 적용·유사 구성 항목은 **유지(건너뜀)/재구성(어긋난 부분만)/교체**를 묻는다 — 전체 모드여도 말없이 덮어쓰지 않는다. ② 04 CI 조직 정책·**06 §1 게이트 전부**·**03 §4 MCP·§5 브라우저의 조직 승인**은 어떤 모드에서도 건너뛰지 않는다(셋 다 사내 데이터가 외부로 나가거나 사내 세션에 접근하는 항목이다).
- **재적용(적용 기록 있음)이면**: **먼저 이 PC가 처음인지 본다** — **이 PC의 글로벌 기록·프로젝트 산출물 실물에 이 기록의 흔적이 없으면** 재적용이 아니라 **신규 합류**다(다른 사람이 커밋한 기록 — `~/.claude/` 유무로만 가르면 Claude Code를 이미 쓰던 합류자 대다수를 재적용으로 오판한다). 이때 **위** 불변 ①의 기본값은 **"이 repo의 기존 구성을 그대로 따른다(유지)"**로 제시한다 — 팀이 맞춰둔 구성을 새로 온 사람의 판단으로 덮지 않는다. 이어 기록된 버전과 현재 문서 버전을 비교해 **변경분을 요약 제시**한다(각 문서 최상단 버전 표에서 그 사이 행만 읽으면 된다 — **기록된 버전 행은 제외**한다. 이미 적용된 회차다. **기록 버전이 버전 표 병합 구간(`vA~vB` 한 행) 안이면**(마이너는 정수로 비교 — `v1.9`는 `v1.16`보다 낮다) **그 구간분만 축 요약으로 갈음하고 구간 뒤 상세 행은 그대로 제시**한다 — 병합 행은 위 "기록된 버전 행 제외"의 대상이 아니다. 이어 **그 문서를 전항목 재적용**할지 묻고(§C-1의 `전체` 모드와 다르다 — 문서 선택은 건드리지 않는다), 거절하면 상세 행만으로 진행한다. `출처=클론`이면 `git log -p`로 병합 구간의 원래 행을 복원할 수 있다). **출처=사본이면 비교 전 `/dropin-update`로 사본 최신화를 권장**(사본이 낡은 채 비교하면 버전 diff가 공백으로 나온다). **기록 버전이 현재 문서 버전보다 높으면**(역방향 — 다른 PC가 최신본으로 적용했는데 이쪽 사본·클론이 낡음) 비교를 중단하고 소스 최신화를 먼저 한다 — 그대로 진행하면 **구버전 절차로 덮어쓴다**. 최신화(`/dropin-update`)가 끝나면 **여기 STEP 3부터 재적용을 다시 시작한다** — 문서 갱신은 구성 반영이 아니라서, 최신화만 하고 끝내면 실구성은 그대로다. **버전이 같아도** ① 기존 적용 항목의 재구성(어긋난 부분 점검) 여부 ② 이전에 선택하지 않았던 문서·항목의 추가 여부(**`해당 없음`으로 기록된 것은 제외** — 구조적으로 적용 불가한 것을 매번 다시 묻지 않는다) ③ **기록의 조치 대기 메모·`게이트 차단(사유)`·`소스없음(사유)`/`확보 실패(사유)`가 해소됐는지**(예: `gh 인증 대기` → 인증 완료? / `06 게이트 차단(조직 미승인)` → 승인됐나? / `04 소스없음(네트워크 차단)` → 이번엔 확보되나?)를 묻는다. 답에 따라 갈린다:
  - **해소됨** → 그 문서·항목을 **이번 STEP 4의 설치 대상에 넣는다**(단 어느 항목이든 **"해소됐지만 설치는 원하지 않는다"(보류)면 편입하지 말고 `미선택`으로 전환**한다 — 사용자가 보류한 항목을 규칙이 강제 편입하면 의도와 반대가 된다. 확보 실패의 증거는 §검증 명령이 아니라 **확보 자체**다 — 문서 헤더의 버전 표기를 읽어 보이면 해소로 친다)(차단 기록은 "미선택"과 구분되므로 위 ①②에 걸리지 않는다 — 여기서 넣지 않으면 어디서도 설치되지 않는다). 자기신고만 믿지 말고 **해당 문서의 §검증 명령 1개를 실행해 증거로 확인**한다(예: `gh auth status`). **실행할 검증 명령이 없는 게이트**(조직 승인·요금제처럼 조회 대상이 아닌 것)는 **그 문서의 게이트 질문을 다시 물은 답**을 증거로 삼고 무엇을 근거로 해소 판정했는지 괄호에 남긴다(예: `06 vX.Y(2026-○-○ 조직 승인 확인)`) — 명령이 없다는 이유로 판정이 어느 쪽으로도 확정되지 않으면 그 항목은 승인이 난 뒤에도 영영 차단 상태로 남는다. **게이트도 §검증 명령도 없는 조치 대기**(아카이브 승인 등)는 **메모가 지목한 대상을 직접 확인**하고(파일 줄 수·포인터 등) 관찰이 안 되면 사용자에게 묻는다 — 이 경로가 없으면 이미 해소된 대기가 안 닫혀 `/resume`이 상시 오보한다. **이 경로로 해소가 관찰되면 설치 없이도 STEP 5에서 메모를 제거한다**(근거를 괄호에 남긴다 — 설치할 것이 없는 대기라 아래 게이트가 영영 충족되지 않는다). 그 밖에는 설치·검증이 성공한 뒤에**만** STEP 5에서 그 메모를 제거한다(괄호 메모 보존 규칙의 예외).
  - **아직 아님·확인 불가** → 메모를 **유지**하고 사유만 갱신한다. 적용일이 새로 찍혀도 괄호에 **최초 발생일을 보존**한다(형식은 아래 `… 대기(사유, YYYY-MM-DD~)` — 예: `gh 인증 대기(미인증, 2026-08-06~)`) — 재적용을 반복할수록 대기가 새것처럼 보이면 방치가 길어진 사실이 지워진다.
  > ⚠️ 메모 제거는 **상태를 닫는 것**이지 지우는 게 아니다. 설치 없이 메모만 없애면 그 문서는 기록에서 "미선택"조차 아닌 상태가 돼 `/resume`·`/dropin-check` 어느 쪽도 다시 알려주지 않는다 — 신호 반복보다 **신호 소실**이 나쁘다.
- 적용 대상이 모호하면(PC인지 이 프로젝트인지) 먼저 확인한다.
- **산출물 언어**를 함께 묻는다: **한국어** / **English** — 이 드롭인 문서들은 한국어지만 Claude는 어느 언어로든 같게 실행하므로, 이 값은 **문서 언어가 아니라 생성물 언어**다(대상 `CLAUDE.md` 본문·`docs/` 기록·커밋 메시지·산출물 파일명 — 01 §F-1, 02 STEP 3). **기본값은 사용자와 대화 중인 언어**를 감지해 병기하고, 기존 산출물이 있으면 그 언어를 우선한다(섞이면 `/resume`이 읽는 기록이 갈린다). 값은 STEP 5 기록에 `언어=ko|en`으로 남겨 재적용 때 유지한다.
- **운용 프로필**을 함께 묻는다: **표준** / **절약**(Pro 요금제·한도 관리 사용자 권장 — Max에서도 한도 임박 시 선택 가능). 선택은 하위 문서 실행에 전달된다(01 §D-6 effort 기본값, 02 §F 절약 배정, 03 claude-hud 기본 체크). 절약 프로필도 **품질 불변 지점**(트리아지·최종 리뷰·검증 게이트)은 줄이지 않는다 — 기본값 하향과 낭비 제거만.

**STEP 4 — 확정된 문서만, 그 문서의 프로토콜대로 실행.** §B의 권장 순서를 따르되, 각 문서를 읽고 **그 문서의 "실행 프로토콜/체크리스트"를 그대로 수행**한다. **내부 항목 선택은 모드가 정한다** — 사용자 모드면 2단계(문서 → 각 문서 안 항목: 01 ⓐ~ⓕ, 02 에이전트별, 03·04 항목별), 그 외 모드면 모드·번들이 정한 기본값으로 내부 항목을 자동 확정하고 질문을 생략한다. 단 하위 문서의 **안전 질문**은 모드와 무관하게 그대로 진행한다 — 유지/재구성/교체 · 게이트 · 감지 불가 항목 확인 · **스코프 선택**(04 §1 hooks 3택, 05 활성화 키, **02 로스터 배치**(프로젝트/개인 — 02 §E 서두), **03 §2·§3 스킬 스코프**, **03 §4 MCP 스코프**). 스코프는 잘못 고르면 팀 전원에게 커밋되거나(04·03 §4의 `.mcp.json`) 다른 프로젝트에서 안 켜지거나(05) **개인 스코프 동명에 가려 설치해도 실행되지 않으므로**(03 — enterprise > personal > project) 기본값으로 밀어붙이지 않는다.

**STEP 5 — 통합 검증·보고.** ① 검증 → ② 커밋 안내 → ③ 적용 기록 → ④ 기록 자기 검증 순으로 한다.

**① 검증** — 각 문서의 §"검증"을 실행해 **증거(출력)로 보고**한다. 대표 항목:

| 문서 | 대표 검증 |
| --- | --- |
| 01 | **두 축 다 — 01 §A STEP 4 §검증을 그대로 실행**한다(항목을 여기 옮겨 적지 않는다 — 옮기면 01 개정마다 두 곳이 갈린다). ①만 보면 아무것도 안 만들어도 통과하므로 ② 산출물 경로 조회가 필수이고, **글로벌 전용 대상이면 ②는 `해당 없음` 명시로 갈음**한다 |
| 02 | 로스터 파일 생성(+뜬 것이 설치한 스코프의 것인지) |
| 03 | `statusLine` 키 + **`/claude-hud:*` 명령 가용성**(HUD 표시만으론 스코프 실패를 못 잡는다 — 03 §1 검증) · 추천 표시 구성 선택 시 `config.json` 실측 · 생성한 스킬 파일 경로(설치 스코프 확인) · MCP는 03 §4 검증(`/mcp` connected + 도구 1회 호출 + 스코프 확인 — `claude mcp list`는 감지 명령이지 검증이 아니다) |
| 04 | **hooks 설정 파일 증거 선행**(시연은 가능할 때만 — 세션 재로딩 불확실성은 04 §1·§L) · `gh pr list` · 워크플로/Secret |
| 05 | `outputStyle` 키 · 스타일 파일 경로 |
| 06 | **MCP를 쓰는 루틴이면** `.mcp.json` 커밋 + **`/schedule run` 세션 로그에서 그 MCP 도구 호출이 실제로 성공했는지**(`claude mcp list`는 등록만 보이는 감지 명령 — 03행과 같은 기준. 무인 실행이라 연결 실패를 볼 자리가 여기뿐이다. 안 쓰면 "해당 없음" 명시) · `/schedule run` 세션 URL · `docs/ROUTINES.md` · `.gitattributes` |

**② 커밋 안내** — 프로젝트에 생성된 파일만(**③에서 추가할 `CLAUDE.md` 적용 기록 줄도 포함**한다 — 순서상 아직 없지만 빼면 그 줄만 미커밋으로 남아 다른 PC에서 "적용 기록 없음"이 된다). **글로벌 산출물은 커밋 대상이 아니다**(백업 반영은 `/wrap`의 미러 대조가 담당). 프로젝트 안의 **개인 파일**(`.claude/settings.local.json`·`CLAUDE.local.md`)도 커밋하지 않는다(01 §F-5 `.gitignore`가 막는다 — 01 미선택 조합이면 이 한 줄이 유일한 안내다).

**③ 적용 기록** — 대상 `CLAUDE.md` 맨 아래에 아래 한 줄을 추가한다(글로벌만 적용했으면 `~/.claude/CLAUDE.md`). 재적용이면 기존 줄을 **교체**하되 **기존 문서 항목·괄호 메모는 보존**하고 바뀐 부분만 — **단 STEP 3 ③에서 "해소됨"으로 확인되고 이번에 설치·검증까지 마친 메모는 제거**한다.
**회차 서술은 쌓지 않는다** — 이 줄은 이력이 아니라 **현재 상태**다. `[00 vX.Y 재적용 — …]` 같은 회차 블록을 덧붙이면 재적용마다 자라고, `CLAUDE.md`는 **매 세션 자동 로드**돼 그 비용이 상시 발생한다. 무엇이 왜 바뀌었는지는 git 히스토리·`docs/PROGRESS.md`가 갖는다 — 이 줄엔 문서별 **현재 버전 + 살아있는 괄호 메모**만 남긴다.

```
<!-- dropin-applied: 2026-08-10 · 모드=권장+디자인 · 언어=ko · 01 vX.Y(글로벌만) · 02 vX.Y · 03 vX.Y(디자인 3종) · 04 vX.Y · 미선택: 05, 06 · 출처=클론 <경로> -->
```

**기록 필드**
- **적용일 · 모드**(기반+번들, 사용자 모드는 `모드=사용자`) — **`프로필=`은 글로벌 줄에만**(아래 머신 로컬 규칙).
- **문서별 버전** — `vX.Y`는 적용 시점에 **각 문서 헤더에서 읽은 실측값**(예시 숫자를 베끼지 않는다). **부분 완료·사용자 조치 대기는 괄호 메모**(예: `04 vX.Y(gh 설치, 인증 대기)`) — 미완이 기록에 없으면 다음 재적용까지 발견되지 않는다. 메모는 **`… 대기(사유, YYYY-MM-DD~)`** 한 형식으로 적는다(사유와 최초 발생일이 같은 괄호를 쓴다) — 소비자가 `대기` 문자열로 찾으므로 `미정`·`검토 중` 같은 자유 어휘는 어느 안내에도 걸리지 않는다.
- **머신 로컬 상태는 글로벌 기록이 정본** — 그 PC에서만 참인 것(인증·설치 대기 등 조치 대기 메모, `프로필=`)은 **`~/.claude/CLAUDE.md`에만** 남기고 프로젝트 줄에는 적지 않는다. 프로젝트 `CLAUDE.md`는 커밋돼 팀 전원이 공유하므로, 한 사람의 `gh 인증 대기`가 재적용의 "괄호 메모 보존"을 타고 **전원에게 상속**되고 `/resume`이 모두에게 해소를 안내한다. 프로젝트 줄에 남는 것은 **팀 사실**(어떤 문서가 몇 버전으로 적용됐나·미선택·게이트 차단)뿐이다. **머신 로컬 항목이 하나라도 생기면 글로벌 줄을 만들어**(없으면 최소 골격으로) 거기 남긴다 — 프로젝트 전용 설치라 글로벌 줄이 없으면 그 상태는 어디에도 기록되지 않는다. 재적용 중 **프로젝트 줄에서 머신 로컬 메모를 발견하면 글로벌 줄로 옮기고 프로젝트 줄에선 제거**한다(이관은 괄호 메모 보존·제거 게이트의 예외 — 규칙을 앞으로만 적용하면 이미 상속된 메모는 영영 남는다). 단 **게이트 차단은 사유로 가른다** — 조직 승인·정책은 팀 사실이지만 **요금제·계정 연결은 그 사람 것**이라 글로벌에 남긴다(아니면 한 사람의 미확인이 전원에게 상속된다).
- **상태 어휘 4종은 섞지 않는다** — 셋을 한 어휘로 뭉치면 앞의 둘은 잊히고 마지막은 매번 다시 묻게 된다:

  | 상태 | 형식 | 다음 재적용에서 |
  | --- | --- | --- |
  | 고르지 않음 | `미선택: NN` | 질문 ②가 추가 여부를 물음 |
  | 막힘(게이트) | 전체 차단 `NN 게이트 차단(사유)` / 항목 차단 `NN vX.Y(설치분 — <항목> 게이트 차단(사유))` | 질문 ③이 해소를 물음 |
  | 못 받음(확보 실패) | `NN 소스없음(사유)` / 확보 0건이면 줄 전체를 `확보 실패(사유)` | 질문 ③이 재시도를 물음 — **줄 전체가 확보 실패면 문서 항목이 0건이라 ①②③이 다 비므로, 해소되면 신규 설치로 취급해 STEP 3 질문 1·2(모드·번들)부터 다시 한다** |
  | 적용 불가(구조적) | `미선택: NN(해당 없음—사유)` | **②③ 둘 다에서 제외** |
  | 적용 범위 메모(부분 적용) | `NN vX.Y(…만 — 사유)` | **현 문서의 선택 목록과 대조**해 없는 항목은 지목하고 갱신/제거를 묻는다 — 보존만 하면 폐지된 선택지를 계속 주장한다 |

- **`출처=`** — STEP 1에서 문서를 어디서 읽었는지: `사본`(프로젝트 안) / `사본 <경로>`(프로젝트 밖 폴더) / `클론 <경로>` / `raw`. 두 스킬이 이 값으로 최신화 경로를 정한다. **클론과 사본이 함께 있으면 둘 다 적는다**(`출처=클론 <경로> + 사본 <파일들>`) — 한 값만 적으면 나머지가 최신화에서 통째로 빠져 낡은 사본이 다음 적용에 다시 쓰인다.
- **`00 vX.Y`** — 문서별 버전 나열에 **이 문서(00) 자신도 포함**한다. 없으면 다음 재적용이 00 절차 자체의 델타를 계산할 기준이 없어 회차 문구 같은 우회 표기가 생긴다.

**④ 기록 자기 검증** — 쓴 뒤 그 줄을 **다시 읽어** 확인한다: 건드린 스코프(프로젝트·글로벌)가 **전부** 반영됐는지, **이번에 적용한 문서**의 버전이 헤더 실측값과 맞는지. 손대지 않은 문서의 기록은 **적용 시점 스냅샷이라 현재 버전과 달라도 정상**이다(억지로 앞당기면 적용하지도 않은 버전이 기록된다). "남겼다고 보고했는데 한쪽만 바뀐" 실패가 조용히 생기는 자리다(2026-08-12 실사례).

<details><summary><b>드문 경우</b> — 해당할 때만 편다</summary>

- **대상에 `CLAUDE.md`가 없고 01도 미선택**(예: 03만·05만 설치) → 기록만을 위한 **최소 골격**을 만든다: 제목 한 줄 + `dropin-applied` 한 줄(본문은 비운다 — 정식 내용은 나중에 01 §E-1·`/init`이 채움). **위치는 기록할 스코프를 따른다**(프로젝트 적용분=프로젝트 루트 / 글로벌 전용=`~/.claude/CLAUDE.md`). 모든 세션에 자동 로드되므로 **만들기 전 승인**을 받고, 프로젝트 골격만 커밋 안내에 포함한다. 나중에 01을 적용할 때 이 껍데기를 "기존 구성"으로 보고 §E-1 생성을 건너뛰지 않는다(불변 ① 질문에서 "골격뿐"임을 명시). 기록이 없으면 `/dropin-check`가 문서 비교를 생략해 그 문서가 영영 최신화에서 빠진다.
- **글로벌 기록은 스코프·머신 한정자**를 붙인다 — `<!-- dropin-applied(글로벌 · 머신=<이름>): … -->`. 글로벌은 PC당 하나라 밝히지 않으면 백업 미러에서 여러 PC 기록이 서로를 덮는다. 이 한정자 때문에 **기록은 `dropin-applied:` 리터럴이 아니라 `dropin-applied`로 시작하는 줄**로 찾는다(점검 스킬도 동일). 미러 대조에서 **이 줄의 차이는 머신 종속으로 보고 차이로 치지 않는다**([[DEC-20260812-bsjeong87-11]]). **`<이름>`에는 OS 호스트명을 그대로 쓰지 않는다** — 이 줄은 커밋되어 공유되므로 호스트명이 사람·조직을 특정하는 실마리가 된다(§6 익명화 규칙). 사용자가 고른 **별칭**을 쓰고, PC를 구별하는 목적만 달성한다.
- **통합 워크스페이스** — repo별 적용분은 각 repo `CLAUDE.md`에, 루트 레이어분(01 §E-4)은 루트에. 루트가 얇은 라우터면 단위 규칙을 루트에 복제하지 않는다(기록은 각 단위에 위임). 하위 단위 기록엔 루트 레이어 적용분을 `02·05·06은 루트 CLAUDE.md 기록 참조` 식으로 **위임 표기**할 수 있다 — 위임된 문서는 그 단위의 재적용 질문 ①~③에서 제외한다(루트 기록이 정본). **단 위임 대상 루트가 실제로 없으면**(그 하위 repo만 클론한 팀원 — 개별 repo 기본 정책에선 흔하다) **위임 표기를 무시하고 그 문서를 미적용으로 취급**해 질문 ①~③에 되돌린다 — 정본이 없는 위임은 "적용됐다"가 아니라 **확인 불가**이고, 제외한 채 두면 그 문서는 영영 설치되지 않는다. 그 문서를 이번에 설치했으면 **위임 표기에서 그 문서를 뺀다**(옛 위임과 새 설치 기록이 한 줄에 공존하면 정본이 둘로 갈린다).
- **글로벌 병기** — 프로젝트 설치에 **글로벌 스코프 항목이 하나라도** 포함됐으면 `~/.claude/CLAUDE.md`에도 글로벌분을 병기한다: 01 §D · **02의 개인 배치**(`~/.claude/agents/`) · 03의 글로벌 스킬·claude-hud · 05의 글로벌 스타일·활성화 키 · 04 hooks의 PC 스코프. 조건을 01 §D로만 좁히면 02 §E가 인용하는 이 규칙이 02엔 걸리지 않아 "적용됐다는데 흔적 없음" 모순이 난다.
- **머지 충돌**(여러 PC가 같은 repo에 재적용) — 교체 방식이라 `merge=union`을 걸면 안 된다(두 줄로 남아 더 나쁘다). **적용일이 최신인 줄 기준**으로 문서 항목·괄호 메모를 합치되, **`출처=`는 합치지 말고 그 PC의 판별 결과로 덮어쓴다**(로컬 값이라 남의 경로가 남으면 raw로 후퇴한다).
</details>

> 원칙: 승인 없이 대량 변경·커밋하지 않는다. 시크릿(.env·키·인증서)은 읽지도 커밋하지도 않는다.

---

## B. 설치 가능 문서 목록 (이 표가 선택 목록의 원본)

| # | 문서(파일명) | 무엇을 설치하나 | 대상 | 의존/순서 |
| --- | --- | --- | --- | --- |
| 01 | `01_Claude-Code-통합구성-범용마스터.md` | 글로벌 셋업(§D: CLAUDE.md·deny·hooks·스킬) + 프로젝트 셋업(§E·F: 환경 판별(유형은 01 §B 표가 정본), docs 기록 체계, `/resume`·`/wrap`·점검 스킬 dropin-check/update) | PC + 프로젝트 | **기반 — 가장 먼저** |
| 02 | `02_모델분담-범용-플레이북.md` | 난이도 라우팅 `.claude/agents/` 로스터(surveyor~parity-tester) + 핸드오프 규약 | 프로젝트(개인 스코프 `~/.claude/agents/` 배치도 선택 가능) | 01의 docs/ 체계 전제(권장), 개발 프로젝트에만 의미 있음 |
| 03 | `03_확장기능-설치-체크리스트.md` | 확장 항목(§1~§5): claude-hud(사용량 HUD)·svg-design·frontend-design·MCP 서버·브라우저 검증 — 자체 체크리스트로 재질문 | PC 또는 프로젝트 | 독립(단독 적용 가능) |
| 04 | `04_검증-자동화.md` | 검증 3종: hooks 품질 게이트·호스트 CLI(gh/glab)·CI 통합(GitHub 공식 액션 · GitLab 공식 CI/CD 통합(beta) · 기타는 headless 폴백) — git 호스트 감지로 분기 | 프로젝트(hooks는 PC 스코프도 선택 가능 · CLI는 PC) | 독립. CI 통합은 **조직 정책 확인 후**(회사 repo) |
| 05 | `05_출력스타일.md` | 응답의 역할·톤·형식 커스텀: 내장 스타일 전환(목록 정본은 `/config` 메뉴) + 커스텀 스타일(`output-styles/*.md`) 생성 — `keep-coding-instructions` 함정 안내 포함 | PC 또는 프로젝트 | 독립(단독 적용 가능) |
| 06 | `06_루틴-자동화.md` | 클라우드 스케줄 에이전트(스케줄·API·GitHub 트리거): repo 준비물(`.mcp.json` 등)+루틴 생성+정의 기록(docs/ROUTINES.md) | 프로젝트(+계정) | 독립(단 §2 준비물 ②④는 01의 `CLAUDE.md` 기록 체계를 전제 — 골격뿐이면 06 §2 검증 ④의 분기를 따른다). **Pro+ 요금제 · research preview · 회사 repo는 조직 승인 후** 🔴 |

- **저장소 원본**(로컬에 없을 때): `https://raw.githubusercontent.com/EleninJayTech/claude/main/<파일명>` (한글 파일명은 URL 인코딩해 요청).
- **권장 순서**: 01 → 02 → 03 → 04 (05는 완전 독립, 06은 §2 준비물이 01 산출물을 전제 — 둘 다 필요할 때 언제든). 단 사용자가 일부만 선택하면 그것만 실행한다(예: 문서 전용 repo엔 02·04 불필요, HUD만 원하면 03만).
- 새 문서(07~)가 생기면 이 표에 한 줄 추가하고 버전 표에 기록한다 — 그 순간부터 선택 목록에 자동 포함된다(§C에도 한 줄 — 아래 §C-2 끝 참조).

---

## C. 설치 모드·번들 정의 (STEP 3의 원본)

### C-1. 기반 모드

| 모드 | 무엇을 설치하나 |
| --- | --- |
| **최소** | 01만 — 보안+`/resume`·`/wrap` 스킬+기록 체계(기반. 항목 매핑 정본은 01 §A STEP 3). 내부 항목도 필수만 |
| **권장** (기본) | 감지 상황별 자동 조합: **새 PC** → 01(글로벌)+03(claude-hud) / **개발 프로젝트** → 01+02+04(hooks 게이트) / **문서·비개발 repo** → 01만 / **이미 01 적용된 대상** → 미적용 문서 중 상황에 맞는 것만 제안 |
| **전체** | 01~06 전부 — 해당 없음(비개발 repo의 02 등)은 제외 후 고지, 게이트는 그대로 |
| **사용자** | §B 목록에서 직접 — 문서 → 문서 내 항목 2단계 선택 |

> Pro 요금제·한도 관리는 모드가 아니라 **운용 프로필=절약**으로 — 어느 모드와도 조합된다(+03 claude-hud 권장: 한도 가시화가 절약의 전제).

### C-2. 목적 번들 (기반 모드에 합집합으로 얹음, 복수 선택)

| 번들 | 얹는 것 | 추천 체크 조건(감지) |
| --- | --- | --- |
| **디자인**(웹 퍼블리싱) | 03의 svg-design·frontend-design·브라우저 검증 | 프론트엔드 스택 감지 시 기본 체크 |
| **검증 강화** | 04 전체(hooks 게이트+호스트 CLI+CI) | 테스트·CI 구성 감지 시 기본 체크 |
| **무인 자동화** | 06(+04 CI 병용 권장) — Pro+·조직 승인 게이트 그대로 | 감지 불가 — 사용자 요청 시만 |
| **회사** | 새 문서 없음 — **게이트 강화 수정자**: 04 CI는 조직 정책 확인 필수·03 §4 MCP는 허용 서버만·03 §5 브라우저는 사이트 권한 최소화·06은 조직 승인 필수(게이트 실행은 각 문서 STEP이 담당 — 00 §A STEP 3 불변 ②) | 회사 repo라고 답한 경우 자동 적용 |
| **응답 스타일** | 05(내장 전환 또는 커스텀 생성) | 감지 불가 — 사용자 요청 시만 |

- 새 문서(07~)가 생기면: §B에 한 줄 + 여기(C-1 권장 규칙 또는 C-2 소속)에 한 줄 — 그 순간부터 모드·번들 조합에 자동 포함된다.

---

## L. 유지보수
- §B의 파일명이 실제 저장소와 일치하는지(문서 개편 시 이 표부터 갱신). **버전 표기는 §B가 아니라 저장소 `CLAUDE.md` 구성물 표가 정본** — §B에 버전 열을 만들지 않는다(드롭인끼리 서로의 버전을 들면 bump마다 7×7 갱신이 된다).
- 저장소 원본 URL 생존 확인(`github.com/EleninJayTech/claude`, 기본 브랜치 `main`).
- 하위 문서가 늘면(07~) §B·§C에 반영.
- 재검증 시 00~06 헤더·푸터의 "기준/참조: Claude Code v○" 표기를 같은 값으로 동기화(문서 간 불일치 방지).
- **자기 점검(갱신 의식)** — 재검증·개정은 기억이 아니라 **당일 공식 문서** 조회로 한다(확인 못 한 항목엔 🟡을 단다). 고쳤으면 ① 버전 표에 한 줄(무엇을 왜) ② 헤더·푸터의 버전과 날짜 ③ 저장소 `CLAUDE.md` 구성물 표 ④ 이 체크리스트 ⑤ 기계 검사 통과를 빠짐없이 한다. 버전은 네 곳에 있고 **하나만 빠져도 4축이 어긋난다** — 골격 규칙의 정본은 `docs/DOC_CONTRACT.md`다.

---

**문서 정보** — 통합 설치 셀렉터 **v1.41** · 최종 갱신: 2026-09-02 · 최근 재검증: 2026-08-20 · 참조: Claude Code v2.1.237. 설치 절차의 정본은 각 하위 문서(01~06)이며 이 문서는 확보→감지→모드 선택→위임만 담당한다.
