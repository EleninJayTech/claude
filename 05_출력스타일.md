# Claude Code 출력 스타일(Output Styles) — 드롭인 적용

> **문서 버전: v1.11** · 최종 갱신: **2026-08-20** · **최근 재검증: 2026-08-20** · 기준: Claude Code v2.1.237
>
> | 버전 | 날짜 | 변경 내용 |
> | --- | --- | --- |
> | v1.0 | 2026-08-04 | 초판 — 내장 4종 전환+커스텀 생성 체크리스트(스코프·keep-coding-instructions 질문) |
> | v1.1 | 2026-08-10 | 적용 기록 표기 자기 참조화(하드코딩 제거) |
> | v1.2 | 2026-08-12 | §2 frontmatter 표에 `force-for-plugin` 행, 01 §D-2 동명 혼동 주의 반 줄 |
> | v1.3 | 2026-08-12 | STEP 2 **활성화 키 스코프 질문**(파일 위치와 별개 축), STEP 4 조치 대기 표기+파일 기반 증거, 기준 v2.1.226 |
> | v1.4 | 2026-08-12 | §1·§2 검증을 파일 기반 증거로(`/status` 표시는 현 세션 적용 근거 아님), 제거를 양 스코프 키 조회로, 사용법 요약에 적용 기록 |
> | v1.5 | 2026-08-12 | STEP 4 `CLAUDE.md` 부재 시 최소 골격 위임, 동시 선택 시 활성화 대상 확정(`outputStyle`은 단일 값), 제거 키 조회를 세 곳으로 |
> | v1.6 | 2026-08-12 | STEP 1 감지도 세 곳으로 동기화(프로젝트 공유 키 누락 방지), 설치 말미 활성화를 STEP 2 답변에 조건화 |
> | v1.7 | 2026-08-13 | 글로벌 `outputStyle` 키 기입 경고 — PC당 하나라 다른 프로젝트의 선택을 덮어씀 |
> | v1.8 | 2026-08-13 | **최적화**(릴리즈 정비): 버전 표 서술 압축(규칙 불변) |
> | v1.9 | 2026-08-20 | 감지·검증에 **`force-for-plugin`·managed 축**(사용자 키를 덮으므로 키 값만으론 적용 근거가 아니다), STEP 2에 **팀 공유 스코프** 3번째 선택지 + "선택은 각자" 한정, STEP 4에 개인 파일 커밋 금지·**글로벌 병기 인라인** |
> | v1.10 | 2026-08-20 | 내장 스타일 **5종**으로 정정 — Concise 추가(v2.1.237) |
> | v1.11 | 2026-08-20 | §2 감지 정정 — 동명 우선순위 "가까운 쪽"은 **프로젝트 스코프 안**의 중첩 폴더 규칙이다(스코프 간 우선순위는 공식 미명시). §L에 그 공백을 축으로 추가 |
>
> ※ 갱신 시: 이 표에 한 줄 추가 + 하단 "문서 정보" 날짜 수정 + §L 재검증.

> **사용법**: 이 파일을 아무 프로젝트 루트(또는 `docs/`)에 넣고 Claude에게
> **"이 문서를 학습해서 이 PC(또는 이 프로젝트)에 적용해줘"** 라고 하면 된다.
> Claude는 ① 현재 스타일 구성을 감지 → ② **체크리스트로 무엇을 할지 확인 요청** → ③ 선택된 것만 생성·설정 → ④ 검증·**적용 기록** 후 보고 한다.
>
> **무엇**: 출력 스타일은 Claude Code의 **시스템 프롬프트를 수정**해 응답의 역할·톤·형식을 세션 전체에 걸쳐 바꾼다. CLAUDE.md(사용자 메시지로 주입되는 사실·규칙)와 층이 다르다 — 역할 자체를 바꾸려면 스타일, 프로젝트 사실·컨벤션은 CLAUDE.md. (이름 겹침 주의: 01 §D-2 글로벌 CLAUDE.md 예시의 "## 5. Output Style" 절은 행동 규칙 항목명일 뿐 이 기능과 무관.)
> **기본 원칙**: 1순위 = 최적의 결과물, 토큰 절약 = 품질 유지 전제의 2순위 (01 §0과 동일).

---

## A. Claude 실행 프로토콜 (03 §A와 같은 방식)

**STEP 1 — 감지.**
- 현재 활성 스타일: settings의 `outputStyle` 키를 **세 곳 모두** 조회(`~/.claude/settings.json`·`.claude/settings.json`·`.claude/settings.local.json`) — 없으면 Default. **활성 플러그인의 `force-for-plugin` 스타일·조직 managed 설정도 함께 확인**한다(둘은 사용자 `outputStyle` 키를 덮으므로, 키만 보면 "켰다"는 증거가 나와도 실제 응답은 안 바뀐다). 프로젝트 **공유** `.claude/settings.json`을 빼면 팀이 커밋한 스타일 키를 놓쳐 "Default"로 오판한다(§1·§2 제거 절과 같은 세 곳 기준).
- 커스텀 스타일 존재: `~/.claude/output-styles/`·`.claude/output-styles/`의 `.md` 파일 목록. 있으면 "유지/재구성/교체" 질문(덮어쓰지 않음).

**STEP 2 — 체크리스트 확인 요청** (AskUserQuestion, 감지 결과 병기):
1. **무엇을 할까** (multiSelect): §1 내장 스타일 전환(파일 생성 없음, 설정만) / §2 커스텀 스타일 생성. **둘 다 고르면 활성화는 하나만 된다** — `outputStyle`은 단일 값이라, 커스텀을 만들되 지금 켤 것은 무엇인지 함께 묻는다(만들고 안 켜는 것도 유효한 선택. STEP 4가 보고할 키 값이 그 답으로 정해진다).
2. §2 선택 시 — **용도**(어떤 역할·톤·출력 형식을 원하나: 예. 한국어 보고서 톤, 다이어그램 우선, 데이터 분석가 역할), **스코프**(글로벌 `~/.claude/output-styles/` = 이 PC 전 프로젝트 / 프로젝트 `.claude/output-styles/` = 이 repo, 커밋해 팀 공유), **코딩 지침 유지 여부**(아래 🔴 주의 — 코딩 작업을 병행하면 반드시 유지).
3. **활성화 키를 어디에 쓸지**도 함께 묻는다 — **파일 위치와 별개 축**이다. `/config` 피커로 고르면 `.claude/settings.local.json`(이 repo·개인)에 저장되므로, 글로벌 스코프에 스타일을 만들어도 **다른 프로젝트에선 켜지지 않는다**. PC 전체에 적용하려면 `~/.claude/settings.json`의 `outputStyle` 키를 직접 기입한다(이 키는 PC당 하나다 — 다른 프로젝트에서 고른 스타일을 덮어쓰므로 기존 값을 확인하고 기입한다). **팀 전원의 기본값으로 삼으려면 `.claude/settings.json`**(프로젝트 공유·커밋 — 회사 repo면 커밋 전 합의). 이 세 번째 선택지를 빼면 "팀에 적용해줘"에 개인 스코프를 켜고 완료로 보고하게 된다. 제거 시에도 어느 스코프의 키를 지울지 같은 기준으로 판단한다.

**STEP 3 — 선택된 것만 생성·설정.**

**STEP 4 — 검증·보고.** §"검증"을 실행해 **증거(출력)로 보고**. 프로젝트 스코프 **스타일 파일**은 커밋 안내 — 단 `.claude/settings.local.json`은 개인 파일이라 커밋 대상이 아니다(`/config` 피커가 기본으로 쓰는 곳이 여기다). **글로벌 스코프에 적용했으면 기록도 `~/.claude/CLAUDE.md`에 병기**한다(00 §A STEP 5의 글로벌 병기 규칙 — 프로젝트 기록에만 남기면 파일이 그 repo에 없어 다른 PC 점검에서 모순이 된다). **적용 기록**: 대상 `CLAUDE.md` 맨 아래 `<!-- dropin-applied: … -->` 한 줄에 이 문서의 **현재 버전**(최상단 "문서 버전" 표기에서 읽음)을 `05 vX.Y(항목)` 형식으로 추가/갱신한다(형식·재적용 규칙은 00 §A — 단독 적용 시에도 남긴다. **대상에 `CLAUDE.md`가 없으면** 00 §A STEP 5의 최소 골격 규칙대로 승인 후 만들어 기록한다 — 단독 드롭인이면 그 규칙이 읽히지 않아 기록이 통째로 사라지고, 00 STEP 5가 든 대표 사례가 하필 "05만 설치"다). **부분 완료·사용자 조치 대기도 괄호에 남긴다**(예: `05 vX.Y(스타일 생성, 활성화·새 세션 확인 대기)`) — 이 문서의 검증은 **세션 경계에 걸려**(§1 주의: 세션 중 변경은 현재 세션에 적용되지 않는다) 그 자리에서 출력으로 증명할 수 없다. 그 자리에서 낼 수 있는 **파일 기반 증거**(스타일 파일 경로 + settings의 `outputStyle` 키 값)를 먼저 보고하고, 응답 형태 변화 확인은 대기로 남긴다.

> 원칙: 승인 없이 기존 구성을 덮어쓰지 않는다. settings 수정은 해당 키만 병합.

---

## 1. 내장 스타일 5종 — 전환만 (파일 생성 없음)

| 스타일 | 용도 |
| --- | --- |
| **Default** | 기본 소프트웨어 엔지니어링(코딩 최적) |
| **Proactive** | 자율 실행 강화 — 즉시 실행·루틴 결정은 추정 선호(권한 모드는 그대로 적용됨) |
| **Explanatory** | 학습 지원 — 구현 선택·패턴을 "Insights"로 설명하며 진행 |
| **Learning** | 협력 학습 — 설명 + 일부 코드를 사용자가 직접 구현하도록 `TODO(human)` 유도 |
| **Concise** | 결과를 먼저 제시하고 서론·중계 서술을 생략 — 작업의 철저함은 그대로(v2.1.237+) |

### 전환 방법 (2026-08-04 현행)
- **`/config` 메뉴 → Output style** 선택(선택은 `.claude/settings.local.json`의 `outputStyle` 키에 저장), 또는 settings에 `"outputStyle": "<이름>"` 직접 기입.
- ⚠️ **(구)`/output-style` 슬래시 명령은 제거됨**(v2.1.73 deprecated → v2.1.91 제거) — 옛 자료의 안내를 따르지 않는다.
- ⚠️ **세션 중 변경은 현재 세션에 적용되지 않는다** — 시스템 프롬프트는 세션 시작 시 1회만 읽힌다. 변경 후 `/clear` 또는 새 세션.

### 검증
**파일 기반 증거를 먼저 보고**한다 — settings의 `outputStyle` 키 값(어느 스코프인지 함께). **`force-for-plugin` 스타일이나 managed 설정이 있으면 키 값은 적용 근거가 아니다** — 그 사실을 함께 보고하고 통과로 치지 않는다. `/status`(또는 `/context`) 표시는 **설정값일 뿐 현 세션 적용 근거가 아니다**(위 주의: 세션 중 변경은 현 세션에 적용되지 않음). 응답 형태 변화 확인은 **새 세션에서 사용자가** 하는 항목이므로 STEP 4 기록에 대기로 남긴다.

### 제거(기본 복귀)
**`outputStyle` 키가 있을 수 있는 세 곳을 모두 조회해** 그 스타일을 가리키는 키를 전부 제거한다 — `~/.claude/settings.json`(개인 전역) · `.claude/settings.json`(프로젝트 공유, 커밋됨) · `.claude/settings.local.json`(프로젝트 개인). `/config`에서 Default 재선택은 `.claude/settings.local.json`만 건드리므로 나머지 두 곳에 직접 기입한 키가 남아 다른 프로젝트·다른 팀원에게서 계속 활성이다("양 스코프"로 세면 프로젝트 공유 파일이 빠진다).

---

## 2. 커스텀 스타일 생성

**무엇**: 선택된 스코프의 `output-styles/<이름>.md` 한 파일. frontmatter + 본문(시스템 프롬프트 끝에 추가될 지시).

### 감지
양 스코프 `output-styles/` 폴더의 동명·유사 목적 파일. 있으면 유지/교체 질문. 같은 이름이 **작업 디렉터리~repo 루트 사이의 중첩 `.claude/output-styles/` 여러 곳**에 있으면 **작업 디렉터리에 가까운 쪽이 우선**(v2.1.178+) — 이건 **프로젝트 스코프 안의 규칙**이다. 🟡 **개인↔프로젝트 스코프 간 우선순위는 공식 문서에 명시가 없다**(2026-08-20 확인) — 스킬·설정의 스코프 우선순위(01 §F-2)를 그대로 옮겨 적용하지 말고, 충돌이 의심되면 실제 활성 스타일을 §검증으로 확인한다.

### 설치
사용자의 용도 답변으로 본문을 작성해 생성한다. frontmatter 키(2026-08-04 확정):

| 키 | 기본값 | 설명 |
| --- | --- | --- |
| `name` | 파일명 | 표시 이름 |
| `description` | 없음 | `/config` 피커에 표시 — 권장 |
| `keep-coding-instructions` | **false** | 🔴 **false면 Claude Code의 내장 코딩 지침이 통째로 제거**된다(검증·수술적 변경 습관 포함). **코딩을 병행하는 스타일은 반드시 `true`** — false는 비코딩 역할 전환(작가·분석가 등)에만 |
| `force-for-plugin` | false | **플러그인 배포 스타일 전용**(2026-08-12 공식 확인): 플러그인이 켜져 있으면 사용자 선택 없이 자동 적용 — 사용자 `outputStyle` 설정을 덮고, 여러 플러그인이 걸면 먼저 로드된 쪽이 이긴다. 개인·프로젝트 스타일 파일엔 해당 없음 |

예시(코딩 병행 + 출력 형식 변경):

```markdown
---
name: diagrams-first
description: 구조 설명은 항상 Mermaid 다이어그램부터
keep-coding-instructions: true
---
코드·아키텍처·데이터 흐름을 설명할 때는 Mermaid 다이어그램을 먼저 제시하고 산문 설명을 잇는다.
다이어그램은 15노드 이하, 제어 흐름은 flowchart TD, 요청 경로는 sequenceDiagram.
```

생성 후 **STEP 2 #1에서 "지금 켠다"고 답했을 때만** §1의 전환 방법으로 활성화(`/config` 또는 `outputStyle` 키). 만들고 켜지 않는 것도 유효한 선택이다. 적용은 새 세션부터.

### 주의 (설치 시 사용자에게 고지)
- **서브에이전트에는 적용되지 않는다**(자체 시스템 프롬프트 사용) — 02 로스터의 에이전트 동작은 불변.
- **토큰**: 스타일 본문은 매 세션 시스템 프롬프트에 실리는 고정 비용 — 짧게 유지(수십 줄 이내 권장). 스타일을 바꾸면 프롬프트 캐시가 무효화된다. 절약 프로필(00 STEP 3)이면 특히 본문을 아낀다.
- **팀 공유**: 프로젝트 스코프는 커밋 시 팀 전원에게 노출(**공유 `.claude/settings.json`에 `outputStyle` 키를 두지 않으면** 선택은 각자) — 회사 repo면 커밋 전 합의. 조직은 managed 설정으로 스타일을 배포할 수도 있다.
- CLAUDE.md와 역할 분리: 스타일에 프로젝트 사실·규칙을 넣지 않는다(그건 CLAUDE.md 몫 — 중복 금지).

### 검증
**파일 기반 증거를 먼저 보고**한다 — 생성된 스타일 파일 경로 + frontmatter + settings의 `outputStyle` 키 값. `/config` 피커 표시까지가 그 자리에서 확인 가능한 범위다. 지시한 형식이 실제로 나타나는지와 `keep-coding-instructions: true`의 코딩 동작 유지는 **새 세션에서 사용자가** 확인할 항목이므로 STEP 4 기록에 대기로 남긴다.

### 제거
해당 스코프의 `output-styles/<이름>.md` 삭제 + **세 곳(`~/.claude/settings.json`·`.claude/settings.json`·`.claude/settings.local.json`)의** `outputStyle` 키를 조회해 그 이름을 가리키는 키를 전부 제거(파일만 지우면 남은 키가 없는 스타일을 가리켜 세션 시작 시 무효 설정이 된다).

---

## L. 유지보수 (6개월마다 재검증)
- `outputStyle` 설정 키·스코프 저장 위치, `/config` 내 메뉴 위치 — code.claude.com/docs/en/output-styles.
- frontmatter 키(`keep-coding-instructions`·`description`·`force-for-plugin`) 유지 여부, 기본값(false) 변화.
- 내장 스타일 목록(현행 5종: Default·Proactive·Explanatory·Learning·Concise) 증감.
- 🟡 **개인↔프로젝트 스코프 간 동명 스타일 우선순위**가 공식 문서에 명시됐는지(2026-08-20 미명시 — §2 감지가 그 공백을 전제한다).
- (구)`/output-style` 명령이 부활·변경됐는지.
- 갱신 후 이 문서 "최종 갱신"·"최근 재검증" 날짜 수정.

---

**문서 정보** — 출력 스타일 **v1.11** · 최종 갱신: 2026-08-20 · 최근 재검증: 2026-08-20 · 참조: Claude Code v2.1.237.
역할 경계: 시스템 프롬프트 수준의 역할·톤·형식은 이 문서, 프로젝트 사실·기록 규칙은 01(CLAUDE.md), 에이전트별 시스템 프롬프트는 02 로스터 — 중복 서술하지 않는다.
