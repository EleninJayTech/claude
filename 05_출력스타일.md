# Claude Code 출력 스타일(Output Styles) — 드롭인 적용

> **문서 버전: v1.2** · 최종 갱신: **2026-08-12** · **최근 재검증: 2026-08-04** · 기준: Claude Code v2.1.221
>
> | 버전 | 날짜 | 변경 내용 |
> | --- | --- | --- |
> | v1.0 | 2026-08-04 | 초판 — 내장 스타일 4종 안내 + 커스텀 스타일 생성 체크리스트(스코프·keep-coding-instructions 질문). 당일 공식 문서 조회로 작성 |
> | v1.1 | 2026-08-10 | **적용 기록 표기 자기 참조화**(DEC-20260810-bsjeong87-07, 00~06 공통): STEP 4 하드코딩 → "최상단 문서 버전 표기에서 읽어 기록" — 내용 변경 없음 |
> | v1.2 | 2026-08-12 | **/audit 반영**(docs/audit-20260812.md): §2 frontmatter 표에 `force-for-plugin` 행 추가(h13 — §L에만 있던 키, 당일 공식 문서로 역할 확인: 플러그인 전용 자동 적용) + 01 §D-2 "## 5. Output Style" 동명 혼동 주의 반 줄(의심 항목 확정) |
>
> ※ 갱신 시: 이 표에 한 줄 추가 + 하단 "문서 정보" 날짜 수정 + §L 재검증.

> **사용법**: 이 파일을 아무 프로젝트 루트(또는 `docs/`)에 넣고 Claude에게
> **"이 문서를 학습해서 이 PC(또는 이 프로젝트)에 적용해줘"** 라고 하면 된다.
> Claude는 ① 현재 스타일 구성을 감지 → ② **체크리스트로 무엇을 할지 확인 요청** → ③ 선택된 것만 생성·설정 → ④ 검증 후 보고 한다.
>
> **무엇**: 출력 스타일은 Claude Code의 **시스템 프롬프트를 수정**해 응답의 역할·톤·형식을 세션 전체에 걸쳐 바꾼다. CLAUDE.md(사용자 메시지로 주입되는 사실·규칙)와 층이 다르다 — 역할 자체를 바꾸려면 스타일, 프로젝트 사실·컨벤션은 CLAUDE.md. (이름 겹침 주의: 01 §D-2 글로벌 CLAUDE.md 예시의 "## 5. Output Style" 절은 행동 규칙 항목명일 뿐 이 기능과 무관.)
> **기본 원칙**: 1순위 = 최적의 결과물, 토큰 절약 = 품질 유지 전제의 2순위 (01 §0과 동일).

---

## A. Claude 실행 프로토콜 (03 §A와 같은 방식)

**STEP 1 — 감지.**
- 현재 활성 스타일: settings의 `outputStyle` 키(`.claude/settings.local.json`·`~/.claude/settings.json` 등 각 스코프) — 없으면 Default.
- 커스텀 스타일 존재: `~/.claude/output-styles/`·`.claude/output-styles/`의 `.md` 파일 목록. 있으면 "유지/재구성/교체" 질문(덮어쓰지 않음).

**STEP 2 — 체크리스트 확인 요청** (AskUserQuestion, 감지 결과 병기):
1. **무엇을 할까** (multiSelect): §1 내장 스타일 전환(파일 생성 없음, 설정만) / §2 커스텀 스타일 생성.
2. §2 선택 시 — **용도**(어떤 역할·톤·출력 형식을 원하나: 예. 한국어 보고서 톤, 다이어그램 우선, 데이터 분석가 역할), **스코프**(글로벌 `~/.claude/output-styles/` = 이 PC 전 프로젝트 / 프로젝트 `.claude/output-styles/` = 이 repo, 커밋해 팀 공유), **코딩 지침 유지 여부**(아래 🔴 주의 — 코딩 작업을 병행하면 반드시 유지).

**STEP 3 — 선택된 것만 생성·설정.**

**STEP 4 — 검증·보고.** §"검증"을 실행해 **증거(출력)로 보고**. 프로젝트 스코프 파일은 커밋 안내. **적용 기록**: 대상 `CLAUDE.md` 맨 아래 `<!-- dropin-applied: … -->` 한 줄에 이 문서의 **현재 버전**(최상단 "문서 버전" 표기에서 읽음)을 `05 vX.Y(항목)` 형식으로 추가/갱신한다(형식·재적용 규칙은 00 §A — 단독 적용 시에도 남긴다).

> 원칙: 승인 없이 기존 구성을 덮어쓰지 않는다. settings 수정은 해당 키만 병합.

---

## 1. 내장 스타일 4종 — 전환만 (파일 생성 없음)

| 스타일 | 용도 |
| --- | --- |
| **Default** | 기본 소프트웨어 엔지니어링(코딩 최적) |
| **Proactive** | 자율 실행 강화 — 즉시 실행·루틴 결정은 추정 선호(권한 모드는 그대로 적용됨) |
| **Explanatory** | 학습 지원 — 구현 선택·패턴을 "Insights"로 설명하며 진행 |
| **Learning** | 협력 학습 — 설명 + 일부 코드를 사용자가 직접 구현하도록 `TODO(human)` 유도 |

### 전환 방법 (2026-08-04 현행)
- **`/config` 메뉴 → Output style** 선택(선택은 `.claude/settings.local.json`의 `outputStyle` 키에 저장), 또는 settings에 `"outputStyle": "<이름>"` 직접 기입.
- ⚠️ **(구)`/output-style` 슬래시 명령은 제거됨**(v2.1.73 deprecated → v2.1.91 제거) — 옛 자료의 안내를 따르지 않는다.
- ⚠️ **세션 중 변경은 현재 세션에 적용되지 않는다** — 시스템 프롬프트는 세션 시작 시 1회만 읽힌다. 변경 후 `/clear` 또는 새 세션.

### 검증
`/status`(또는 `/context`)로 활성 스타일 확인 + 새 세션에서 응답 형태가 바뀌었는지 1턴 시연을 증거로 보고.

### 제거(기본 복귀)
settings의 `outputStyle` 키 제거(또는 `/config`에서 Default 재선택).

---

## 2. 커스텀 스타일 생성

**무엇**: 선택된 스코프의 `output-styles/<이름>.md` 한 파일. frontmatter + 본문(시스템 프롬프트 끝에 추가될 지시).

### 감지
양 스코프 `output-styles/` 폴더의 동명·유사 목적 파일. 있으면 유지/교체 질문. 같은 이름이 여러 스코프에 있으면 **작업 디렉터리에 가까운 쪽이 우선**(v2.1.178+).

### 설치
사용자의 용도 답변으로 본문을 작성해 생성한다. frontmatter 키(2026-08-04 확정):

| 키 | 기본값 | 설명 |
| --- | --- | --- |
| `name` | 파일명 | 표시 이름 |
| `description` | 없음 | `/config` 피커에 표시 — 권장 |
| `keep-coding-instructions` | **false** | 🔴 **false면 Claude Code의 내장 코딩 지침이 통째로 제거**된다(검증·수술적 변경 습관 포함). **코딩을 병행하는 스타일은 반드시 `true`** — false는 비코딩 역할 전환(작가·분석가 등)에만 |
| `force-for-plugin` | false | **플러그인 배포 스타일 전용**(2026-08-12 공식 확인): 플러그인이 켜져 있으면 사용자 선택 없이 자동 적용 — 사용자 `outputStyle` 설정을 덮고, 여러 플러그인이 걸면 먼저 로드된 쪽이 이긴다. 개인·프로젝트 스타일 파일엔 해당 없음 |

예시(코딩 병행 + 출력 형식 변경):

```markdown
---
name: diagrams-first
description: 구조 설명은 항상 Mermaid 다이어그램부터
keep-coding-instructions: true
---
코드·아키텍처·데이터 흐름을 설명할 때는 Mermaid 다이어그램을 먼저 제시하고 산문 설명을 잇는다.
다이어그램은 15노드 이하, 제어 흐름은 flowchart TD, 요청 경로는 sequenceDiagram.
```

생성 후 §1의 전환 방법으로 활성화(`/config` 또는 `outputStyle` 키). 적용은 새 세션부터.

### 주의 (설치 시 사용자에게 고지)
- **서브에이전트에는 적용되지 않는다**(자체 시스템 프롬프트 사용) — 02 로스터의 에이전트 동작은 불변.
- **토큰**: 스타일 본문은 매 세션 시스템 프롬프트에 실리는 고정 비용 — 짧게 유지(수십 줄 이내 권장). 스타일을 바꾸면 프롬프트 캐시가 무효화된다. 절약 프로필(00 STEP 3)이면 특히 본문을 아낀다.
- **팀 공유**: 프로젝트 스코프는 커밋 시 팀 전원에게 노출(선택은 각자) — 회사 repo면 커밋 전 합의. 조직은 managed 설정으로 스타일을 배포할 수도 있다.
- CLAUDE.md와 역할 분리: 스타일에 프로젝트 사실·규칙을 넣지 않는다(그건 CLAUDE.md 몫 — 중복 금지).

### 검증
`/config` 피커에 새 스타일이 뜨는지 + 활성화 후 새 세션 1턴에서 지시한 형식(예: 다이어그램 선행)이 나타나는지 출력으로 보고. `keep-coding-instructions: true`면 코드 수정 요청 1건으로 코딩 동작이 유지되는지도 확인.

### 제거
해당 스코프의 `output-styles/<이름>.md` 삭제 + `outputStyle` 키가 그 이름이면 키 제거.

---

## L. 유지보수 (6개월마다 재검증)
- `outputStyle` 설정 키·스코프 저장 위치, `/config` 내 메뉴 위치 — code.claude.com/docs/en/output-styles.
- frontmatter 키(`keep-coding-instructions`·`description`·`force-for-plugin`) 유지 여부, 기본값(false) 변화.
- 내장 스타일 목록(현행 4종: Default·Proactive·Explanatory·Learning) 증감.
- (구)`/output-style` 명령이 부활·변경됐는지.
- 갱신 후 이 문서 "최종 갱신"·"최근 재검증" 날짜 수정.

---

**문서 정보** — 출력 스타일 **v1.2** · 최종 갱신: 2026-08-12 · 최근 재검증: 2026-08-04 · 참조: Claude Code v2.1.221.
역할 경계: 시스템 프롬프트 수준의 역할·톤·형식은 이 문서, 프로젝트 사실·기록 규칙은 01(CLAUDE.md), 에이전트별 시스템 프롬프트는 02 로스터 — 중복 서술하지 않는다.
