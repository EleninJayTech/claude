# Claude Code 검증 자동화 — 드롭인 적용

> **문서 버전: v1.5** · 최종 갱신: **2026-08-10** · **최근 재검증: 2026-08-04** · 기준: Claude Code v2.1.226
>
> | 버전 | 날짜 | 변경 내용 |
> | --- | --- | --- |
> | v1.0 | 2026-07-28 | 초판 — 검증 3종(hooks 품질 게이트 · gh/glab CLI · CI 통합) 체크리스트 설치. git 호스트 감지로 GitHub/GitLab·기타 분기 |
> | v1.1 | 2026-07-28 | 절약 프로필 주의 한 줄 — hooks 게이트는 절약 프로필에서도 유지(재작업 감소가 곧 절약) |
> | v1.2 | 2026-08-04 | **적용 기록 한 줄 추가**(STEP 4: 대상 CLAUDE.md에 `dropin-applied` 버전 기록 — 재적용 시 버전 비교용, 형식·재적용 규칙은 00 §A) |
> | v1.3 | 2026-08-04 | **전면 재검증**(당일 조회, v2.1.221): hooks 스키마·`claude-code-action` v1 유지(패치만 잦음)·gh/glab winget 설치 명령 전부 유효 확인 — 내용 변경 없음. "최근 재검증" 표기 분리 |
> | v1.4 | 2026-08-10 | **보안 스택 위치 정리**(당일 조회, v2.1.226 — DEC-20260810-bsjeong87-02): 역할 경계에 공식 보안 5층 스택(세션 중 security-guidance → 온디맨드 `/security-review` → claude-security 딥 스캔 → 관리형 PR Code Review(Team·Ent) → CI) 한 줄 — 이 문서의 hooks·CI 게이트가 어느 층인지 명시 |
> | v1.5 | 2026-08-10 | **적용 기록 표기 자기 참조화**(DEC-20260810-bsjeong87-07, 00~06 공통): STEP 4 하드코딩(`04 v1.3` 구버전) → "최상단 문서 버전 표기에서 읽어 기록" |
>
> ※ 갱신 시: 이 표에 한 줄 추가 + 하단 "문서 정보" 날짜 수정 + §L 재검증.

> **사용법**: 이 파일을 아무 프로젝트 루트(또는 `docs/`)에 넣고 Claude에게
> **"이 문서를 학습해서 이 프로젝트에 적용해줘"** 라고 하면 된다.
> Claude는 ① 스택·git 호스트·기존 구성 감지 → ② **체크리스트로 무엇을 설치할지 확인 요청** → ③ 체크된 항목만 설치 → ④ 검증 후 보고 한다.
>
> **왜 이 문서인가**: "검증은 증거(출력)로 보고한다"는 규칙(01 §F-1)은 사람이 잊으면 무너진다. 이 문서는 그 규칙을 **hooks(커밋 전)와 CI(머지 전) 2중 게이트로 기계적으로 강제**한다 — 안전과 결과물 품질이 1순위, 재작업 감소로 토큰 절약은 따라온다.
> **정직한 고지**: hooks 스키마·CI 액션은 바뀐다. 설치 시점에 공식 문서(`code.claude.com/docs/en/hooks`, `github.com/anthropics/claude-code-action`)를 확인한다.

---

## A. Claude 실행 프로토콜 (03 §A와 같은 방식)

**STEP 1 — 감지.**
- **스택/검증 명령**: 포맷터·린터·테스트 러너 감지(`package.json` scripts, `pyproject.toml`, `Makefile`, `gradlew` 등). 없으면 STEP 2에서 질문.
- **git 호스트**: `git remote -v` — `github.com` / 사내·클라우드 GitLab / 기타. **호스트가 §3의 분기를 결정한다.**
- **기존 구성**: settings.json의 `hooks`(PostToolUse 등), `gh`/`glab` 설치 여부(`--version`), `.github/workflows/`·`.gitlab-ci.yml` 존재 여부. 있으면 "유지/재구성/교체" 질문(덮어쓰지 않음).

**STEP 2 — 체크리스트 확인 요청** (multiSelect, 감지 결과 병기):
§1 hooks 품질 게이트 / §2 호스트 CLI(gh 또는 glab) / §3 CI 통합. 각 항목의 스코프(프로젝트/글로벌)와 강도(차단 vs 경고만)도 함께 묻는다.

**STEP 3 — 체크된 항목만 설치.** 선택 안 된 항목은 손대지 않는다.

**STEP 4 — 검증·보고.** 각 항목 §"검증"을 실행해 **증거(출력)로 보고**. 프로젝트 파일은 커밋 안내. **적용 기록**: 대상 `CLAUDE.md` 맨 아래 `<!-- dropin-applied: … -->` 한 줄에 이 문서의 **현재 버전**(최상단 "문서 버전" 표기에서 읽음)을 `04 vX.Y(설치 항목)` 형식으로 추가/갱신한다(형식·재적용 규칙은 00 §A — 단독 적용 시에도 남긴다).

> 원칙: 승인 없이 기존 구성을 덮어쓰지 않는다. `settings.json` 수정은 해당 키만 병합. 시크릿(API 키·토큰)은 읽지도 커밋하지도 않는다.

---

## 1. hooks 품질 게이트 — 편집 즉시 포맷·검증

**무엇**: Claude가 파일을 고칠 때마다 포맷터·린터가 **자동 실행**되도록 hooks를 건다. 실패 출력은 Claude에게 피드백되어 스스로 고친다 — "고치고 → 검증되고 → 통과해야 다음" 루프의 1차 게이트.
**스코프**: 팀 공유면 프로젝트 `.claude/settings.json`(커밋), 개인이면 `~/.claude/settings.json`. **강도**: 게이트는 검증·차단만 한다 — 자동 "수정 커밋"은 하지 않는다(안전 우선).

### 감지
settings.json(양 스코프)의 `hooks.PostToolUse` 존재 여부 + 스택의 포맷/린트 명령(없으면 질문: "이 프로젝트의 린트/테스트 명령은?").

### 설치
감지한 스택 명령으로 `<...>`를 채워 해당 스코프 settings.json에 **병합**(hooks 스키마는 설치 시점에 공식 문서로 확인):

```json
{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [{ "type": "command", "command": "<수정 파일 대상 포맷터/린터 명령>" }]
      }
    ]
  }
}
```

- 테스트가 빠른 프로젝트(수 초)는 lint 자리에 테스트까지 포함 가능. 느린 테스트는 hooks에 넣지 않는다(§3 CI 몫) — 세션이 느려지고 토큰만 태운다.
- **절약 프로필(00 STEP 3)에서도 이 게이트는 빼지 않는다** — 재작업 감소가 가장 큰 토큰 절약이다.
- 회사 repo에서 프로젝트 스코프로 커밋하면 **팀원 전원에게 적용**됨을 고지하고 승인받는다.

### 검증
파일 하나를 일부러 포맷 어긋나게 편집 → hook이 돌아 교정/실패 피드백이 나오는지 출력으로 보고.

### 제거
settings.json에서 해당 hook 항목만 제거(다른 hooks는 손대지 않음).

---

## 2. 호스트 CLI — gh(GitHub) / glab(GitLab)

**무엇**: Claude가 PR 생성·이슈 조회·CI 로그 읽기를 직접 하게 하는 CLI. 검증 루프의 연결고리(예: "CI 실패 로그 읽고 고쳐줘").
**호스트 분기**: GitHub → `gh` / GitLab(사내 포함) → `glab`(셀프호스트는 `glab auth login --hostname <사내호스트>`) / 기타 호스트 → 설치 생략(고지만).

### 감지
`gh --version`·`glab --version`, 인증 상태(`gh auth status` 등). 이미 설치·인증돼 있으면 건너뜀.

### 설치
- gh: `winget install GitHub.cli`(Windows) / `brew install gh`(macOS) → `gh auth login`.
- glab: `winget install glab.glab` / `brew install glab` → `glab auth login`(사내 호스트면 `--hostname`).
- **인증은 사용자가 직접**(브라우저 로그인) — Claude가 토큰을 만들거나 저장하지 않는다. 회사 계정 정책(SSO 필수 등)을 따른다.

### 검증
`gh pr list`(또는 `glab mr list`) 실행 출력으로 보고.

### 제거
CLI 언인스톨 + `gh auth logout`/`glab auth logout`.

---

## 3. CI 통합 — 머지 전 2차 게이트

**무엇**: PR/MR 단계에서 Claude가 리뷰·검증에 참여한다. **호스트에 따라 방식이 다르다**:

### 3-A. GitHub → `claude-code-action`
- 공식 액션(`anthropics/claude-code-action@v1`, GA): PR에 `@claude` 멘션으로 리뷰·수정 요청, 이슈에서 구현 지시 가능.
- 설치: `.github/workflows/claude.yml` 생성 — 최신 템플릿·권한 설정은 설치 시점에 `github.com/anthropics/claude-code-action` README로 확인. API 키는 **repo Secrets**(`ANTHROPIC_API_KEY`)로만.
- **회사 주의** 🔴: CI에서 코드가 Anthropic API로 전송된다 — 조직의 외부 AI 사용 정책·데이터 반출 규정을 먼저 확인. 조직 승인 없으면 이 항목은 설치하지 않는다.

### 3-B. GitLab·기타 → headless 폴백
- 공식 액션이 없으므로 CI 스테이지에서 `claude -p "<리뷰/검증 프롬프트>" --allowedTools ...`로 직접 호출. 헤드리스 운용의 정본은 **02 §F(`claude -p` 팬아웃)** + 공식 headless 문서 — 여기선 "CI 스테이지에 넣는다"는 위치만 정의한다.
- 러너에 Claude Code 설치 + API 키를 CI 변수(마스킹)로 주입. 사내망 러너면 Anthropic API 접근 가능 여부부터 확인.

### 감지
`.github/workflows/`에 claude 관련 워크플로 존재 여부 / `.gitlab-ci.yml`의 claude 스테이지 여부.

### 검증
테스트 PR/MR을 하나 만들어 액션(또는 스테이지)이 실행된 로그를 증거로 보고.

### 제거
워크플로 파일(또는 CI 스테이지) 삭제 + Secrets/변수에서 API 키 제거.

---

## L. 유지보수 (6개월마다 재검증)
- hooks 이벤트·스키마(`PostToolUse` matcher 문법) 유지 여부 — code.claude.com/docs/en/hooks.
- `claude-code-action` 메이저 버전·권장 워크플로 템플릿 변화 — github.com/anthropics/claude-code-action.
- `gh`/`glab` 설치 명령·인증 방식 변화.
- 갱신 후 이 문서 "최종 갱신" 날짜 수정.

---

**문서 정보** — 검증 자동화 **v1.5** · 최종 갱신: 2026-08-10 · 최근 재검증: 2026-08-04 · 참조: Claude Code v2.1.226.
역할 경계: 편집 중 게이트(§1)와 CI 게이트(§3)는 이 문서가 정본, 헤드리스 실행 기법은 02 §F가 정본, 리뷰 명령(`/code-review`)은 02 §E 주석 참조 — 중복 서술하지 않는다.
보안은 공식 5층 스택과 상호보완: ① 세션 중 security-guidance 플러그인 → ② 온디맨드 `/security-review`(내장 명령, 현재 브랜치 1회 점검) → ③ claude-security 딥 스캔 → ④ 관리형 PR Code Review(Team·Enterprise, 리뷰당 ~$15–25) → ⑤ CI 정적 분석. 이 문서의 §1 hooks·§3 CI는 ⑤층의 정본이고, 플러그인 2종(①③)은 03 부록 참조.
