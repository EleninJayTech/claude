# Claude Code 검증 자동화 — 드롭인 적용

> **문서 버전: v1.20** · 최종 갱신: **2026-09-02** · **최근 재검증: 2026-08-20** · 기준: Claude Code v2.1.237
>
> | 버전 | 날짜 | 변경 내용 |
> | --- | --- | --- |
> | v1.0 | 2026-07-28 | 초판 — 검증 3종(hooks 게이트·gh/glab CLI·CI 통합), git 호스트 감지 분기 |
> | v1.1 | 2026-07-28 | 절약 프로필에서도 hooks 게이트 유지 한 줄(재작업 감소가 곧 절약) |
> | v1.2 | 2026-08-04 | 적용 기록 한 줄 추가(STEP 4 — 형식은 00 §A) |
> | v1.3 | 2026-08-04 | 전면 재검증(v2.1.221) — 내용 변경 없음. "최근 재검증" 표기 분리 |
> | v1.4 | 2026-08-10 | 역할 경계에 공식 보안 5층 스택 한 줄 — 이 문서의 hooks·CI가 어느 층인지 명시 |
> | v1.5 | 2026-08-10 | 적용 기록 표기 자기 참조화(하드코딩 제거) |
> | v1.6 | 2026-08-12 | 강도 축 통일(차단/경고만 — STEP 2 질문+§1 설정법), §1 스코프 `.claude/settings.local.json` **3택**, STEP 4 조치 대기 메모, 푸터 5층 정리, hooks="편집 즉시" 표현 통일 |
> | v1.7 | 2026-08-12 | **조직 승인 게이트를 STEP 1·2 절차에 배선** + 차단 시 §1·§2는 진행하고 `게이트 차단(조직 미승인)` 기록, §3-A Secrets 등록은 사용자 직접, §3 검증을 실행 가능한 형태로 |
> | v1.8 | 2026-08-12 | §3 검증에 확인 명령(`gh secret list`/GitLab CI/CD 변수), STEP 4 `CLAUDE.md` 부재 시 최소 골격 위임, 게이트 서술 통일("그 항목만" 미설치), 제거 주체·GitLab 축 병기 |
> | v1.9 | 2026-08-12 | §1 감지를 설치 스코프 **3곳 전부**로, §1 검증 파일 기반 증거 선행+세션 재로딩 불확실 시 대기 처리, §2 gh/glab에 Linux·WSL 경로 |
> | v1.10 | 2026-08-13 | **최적화**(릴리즈 정비): 버전 표·본문 서술 압축(규칙 불변) + 권장·전체 모드 위임 시 **§1 기본 체크** 명시, remote 없는 repo의 §2·§3 **항목 단위 `해당 없음`** 분기 |
> | v1.11 | 2026-08-20 | **§3-B GitLab 공식 CI/CD 통합(beta) 반영**("공식 액션 없음" 폐기 — `.gitlab-ci.yml` 정본 템플릿·`@claude` 멘션·마스킹 변수), §3-A 인증 2종 병기(`CLAUDE_CODE_OAUTH_TOKEN`·`/install-github-app`) |
> | v1.12 | 2026-08-20 | remote 없음을 **가역 어휘**로(영구 제외 오용 해소)·미지원 호스트 기록 어휘, §1 제거를 **3곳 전부**로·검증에 강도별 통과 기준, §2 검증에 **인증 대기 분기**, §3 `/install-github-app`은 **PR=푸시라 안내만**·감지에 원격/PR·`include:` 축·검증에 `@claude` 멘션 트리거·제거에 **OAuth 토큰 병기**, §3-B 셋째 불릿 귀속·주어 명시, §L GitLab beta 3개월 |
> | v1.13 | 2026-08-20 | 전면 재검증(v2.1.237) — hooks 세션 즉시 반영·GitLab CI/CD beta 유지·`claude-code-action@v1`·gh/glab 설치 명령 유효 확인 |
> | v1.14 | 2026-08-20 | §L의 hooks 세션 재로딩 🟡 해소 — 세션 중 편집한 hook은 file watcher가 즉시 반영하므로 §1 검증 전제 성립(문구 변경 불요) |
> | v1.15 | 2026-08-20 | 재검증 2회차: §3-A 구독 인증을 **Pro·Max·Team·Enterprise** 4종으로 정정(+공유 시크릿엔 API 키), §3-B에 **유지 주체=GitLab·API 키 전용** 명시, §2 glab **공식(brew)↔커뮤니티(winget) 구분** |
> | v1.16 | 2026-08-21 | **/audit 7차(회귀) 반영** — 인증·설치 대기처럼 **그 PC에서만 참인 메모는 글로벌 기록에만**(프로젝트 줄엔 팀 사실만) |
> | v1.17 | 2026-08-31 | §1 강도에 **종료코드 2의 이벤트별 효력** 주석 — `PostToolUse`는 종료코드 2로도 막히지 않으며 여기서의 "차단"은 되돌림이 아니라 피드백이다. 편집 전 차단은 `PreToolUse` |
> | v1.18 | 2026-08-31 | §3-A 게이트를 **4항목으로 확장**(관리자 권한·시크릿 등록 방식·권한 집합 전체 수용·러너와 토큰 양쪽 비용) + 감지 시 **시크릿은 존재 여부만**, §3 제거에 **시크릿을 지워도 자격증명은 유효** |
> | v1.19 | 2026-08-31 | §3에 **`### 설치` 소제목** 신설(호스트별 3-A/3-B 분기 안내 — 4단 계약 충족), §L에 **자기 점검(갱신 의식)** 한 줄 |
> | v1.20 | 2026-09-02 | **/audit 8차(회귀) 반영** — STEP 2 승인 게이트 질문이 반출 3문항뿐이라 §3-A의 나머지 4축(관리자 권한·시크릿 등록 방식·권한 집합·비용 양면)을 같은 자리에서 묻도록 — §3-A가 "반출 축 하나가 아니다"라고 선언해 놓고 질문 단은 안 자라 있었다 |
> ※ 갱신 시: 이 표에 한 줄 추가 + 하단 "문서 정보" 날짜 수정 + §L 재검증.

> **사용법**: 이 파일을 아무 프로젝트 루트(또는 `docs/`)에 넣고 Claude에게
> **"이 문서를 학습해서 이 프로젝트에 적용해줘"** 라고 하면 된다.
> Claude는 ① 스택·git 호스트·기존 구성 감지 → ② **체크리스트로 무엇을 설치할지 확인 요청**(§3은 조직 승인 게이트 포함) → ③ 체크된 항목만 설치 → ④ 검증·**적용 기록** 후 보고 한다.
>
> **왜 이 문서인가**: "검증은 증거(출력)로 보고한다"는 규칙(01 §F-1)은 사람이 잊으면 무너진다. 이 문서는 그 규칙을 **hooks(편집 즉시)와 CI(머지 전) 2중 게이트로 기계적으로 강제**한다 — 안전과 결과물 품질이 1순위, 재작업 감소로 토큰 절약은 따라온다.
> **정직한 고지**: hooks 스키마·CI 액션은 바뀐다. 설치 시점에 공식 문서(`code.claude.com/docs/en/hooks`, `github.com/anthropics/claude-code-action`)를 확인한다.

---

## A. Claude 실행 프로토콜 (03 §A와 같은 방식)

**STEP 1 — 감지.**
- **스택/검증 명령**: 포맷터·린터·테스트 러너 감지(`package.json` scripts, `pyproject.toml`, `Makefile`, `gradlew` 등). 없으면 STEP 2에서 질문.
- **git 호스트**: `git remote -v` — `github.com` / 사내·클라우드 GitLab / 기타. **호스트가 §3의 분기를 결정한다.** remote가 없으면(신규 로컬 repo) §2·§3은 **`미선택: 04 §2·§3(remote 없음 — 추가 시 재질문)`**으로 기록하고 §1만 진행한다 — remote는 나중에 붙는 **가역 상태**라 `해당 없음`(구조적 적용 불가, 00 상태 어휘 표에서 ②③ 둘 다 제외)을 쓰면 영영 다시 제안되지 않는다.
- **회사·사내 repo 여부**: remote 도메인·조직명으로 추정하되 코드로 단정할 수 없으므로 STEP 2에서 확인한다. **§3 CI는 코드가 외부 API로 나가므로 조직 승인 게이트의 대상**(§3-A 🔴).
- **기존 구성**: settings.json의 `hooks`(PostToolUse 등), `gh`/`glab` 설치 여부(`--version`), `.github/workflows/`·`.gitlab-ci.yml` 존재 여부. 있으면 "유지/재구성/교체" 질문(덮어쓰지 않음).

**STEP 2 — 체크리스트 확인 요청** (multiSelect, 감지 결과 병기):
§1 hooks 품질 게이트 / §2 호스트 CLI(gh 또는 glab) / §3 CI 통합. 00의 권장·전체 모드로 위임받으면 **§1이 기본 체크**, §2·§3은 감지 결과(호스트·CI 설정 존재)가 있을 때만 기본 체크. **§1에 한해** 스코프(3택 — §1 참고)와 강도(차단 vs 경고만 — §1 "강도" 설정법 참고)도 함께 묻는다(§2 CLI는 PC 단위, §3은 repo 고정이라 해당 없음).
- 🔴 **§3을 체크했으면 조직 승인 게이트를 반드시 묻는다**(어떤 설치 모드에서도 생략 불가 — 00 §A STEP 3 불변 ②): "회사·사내 repo인가 / 외부 AI 사용·데이터 반출 정책을 확인했는가 / 승인받았는가" — 그리고 §3-A의 나머지 4축(관리자 권한·시크릿 등록 방식·권한 집합 전체 수용·비용 양면)도 같은 자리에서 묻는다. **승인이 없으면 §3만 설치하지 않고** §1·§2는 그대로 진행한 뒤, STEP 4 기록에 `04 vX.Y(hooks·CLI만 — CI 게이트 차단(조직 미승인))`로 남긴다(다음 재적용·`/dropin-check`가 재확인 대상으로 읽는다).

**STEP 3 — 체크된 항목만 설치.** 선택 안 된 항목은 손대지 않는다.

**STEP 4 — 검증·보고.** 각 항목 §"검증"을 실행해 **증거(출력)로 보고**. 프로젝트 파일은 커밋 안내. **적용 기록**: 대상 `CLAUDE.md` 맨 아래 `<!-- dropin-applied: … -->` 한 줄에 이 문서의 **현재 버전**(최상단 "문서 버전" 표기에서 읽음)을 `04 vX.Y(설치 항목)` 형식으로 추가/갱신한다(**인증·설치 대기처럼 그 PC에서만 참인 메모는 `~/.claude/CLAUDE.md`에만** 남긴다 — 프로젝트 줄엔 팀 사실만. 00 §A STEP 5)(형식·재적용 규칙은 00 §A — 단독 적용 시에도 남긴다. **대상에 `CLAUDE.md`가 없으면** 00 §A STEP 5의 최소 골격 규칙대로 승인 후 만들어 기록한다 — 단독 드롭인이면 그 규칙이 읽히지 않아 기록이 통째로 증발한다). **부분 완료·사용자 조치 대기도 괄호에 남긴다**(예: `04 vX.Y(gh 설치, 인증 대기)`) — 이 문서의 §2 인증은 사용자 직접 단계라 대기 상태가 자주 생긴다.

> 원칙: 승인 없이 기존 구성을 덮어쓰지 않는다. `settings.json` 수정은 해당 키만 병합. 시크릿(API 키·토큰)은 읽지도 커밋하지도 않는다.

---

## 1. hooks 품질 게이트 — 편집 즉시 포맷·검증

**무엇**: Claude가 파일을 고칠 때마다 포맷터·린터가 **자동 실행**되도록 hooks를 건다. 실패 출력은 Claude에게 피드백되어 스스로 고친다 — "고치고 → 검증되고 → 통과해야 다음" 루프의 1차 게이트.
**스코프**(3택): 팀 공유면 프로젝트 `.claude/settings.json`(커밋), 개인·전 프로젝트면 `~/.claude/settings.json`, **개인+이 repo만이면 `.claude/settings.local.json`**(스택별 명령이라 글로벌 훅은 다른 스택 repo에서 깨질 수 있다). **강도**: 기본은 **차단**(실패 출력이 Claude에 피드백돼 통과할 때까지 수정). **경고만**을 원하면 hook 명령이 실패해도 성공 종료하도록 감싼다(예: `<명령> || echo "[경고] 검사 실패(진행 허용)"`) — 출력은 남고 진행은 막지 않는다. 어느 쪽이든 자동 "수정 커밋"은 하지 않는다(안전 우선).

> **여기서 "차단"은 편집을 되돌린다는 뜻이 아니다.** 종료코드 2는 **차단을 지원하는 이벤트에서만** 도구 호출을 막는다(`PreToolUse`·`UserPromptSubmit`·`Stop`·`PreCompact` 등). `PostToolUse`는 **종료코드 2로도 막히지 않으며**, 편집은 이미 일어난 뒤다 — 실패 출력이 Claude에 피드백돼 **뒤이어 고치게** 하는 것이 이 겹의 실제 동작이다. 편집 자체를 **일어나기 전에** 막아야 하면 `PreToolUse`에 건다.

### 감지
`hooks.PostToolUse` 존재 여부를 **설치 스코프 3곳 전부**에서 확인한다 — `~/.claude/settings.json`·프로젝트 `.claude/settings.json`·**`.claude/settings.local.json`**(아래 스코프 3택과 같은 범위. local을 빼면 개인 스코프에 이미 걸린 hook을 못 보고 중복으로 얹는데, local은 커밋되지 않아 팀원 PC마다 다르게 터진다). + 스택의 포맷/린트 명령(없으면 질문: "이 프로젝트의 린트/테스트 명령은?").

### 설치
감지한 스택 명령으로 `<...>`를 채워 해당 스코프 settings.json에 **병합**(hooks 스키마는 설치 시점에 공식 문서로 확인):

```json
{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [{ "type": "command", "command": "<수정 파일 대상 포맷터/린터 명령>" }]
      }
    ]
  }
}
```

- 테스트가 빠른 프로젝트(수 초)는 lint 자리에 테스트까지 포함 가능. 느린 테스트는 hooks에 넣지 않는다(§3 CI 몫) — 세션이 느려지고 토큰만 태운다.
- **절약 프로필(00 STEP 3)에서도 이 게이트는 빼지 않는다** — 재작업 감소가 가장 큰 토큰 절약이다.
- 회사 repo에서 프로젝트 스코프로 커밋하면 **팀원 전원에게 적용**됨을 고지하고 승인받는다.

### 검증
먼저 **파일 기반 증거**로 보고한다 — 설치한 스코프의 `settings.json`에서 `hooks.PostToolUse` 항목(매처·명령)을 출력. 그다음 파일 하나를 일부러 포맷 어긋나게 편집 → **강도가 "차단"이면 교정/실패 피드백이, "경고만"이면 경고 문구가** 출력에 찍히는지 보고한다(강도를 안 나누면 "경고만"을 고른 정상 설치가 성공 종료라 피드백이 없어 매번 미완으로 다시 물어진다). **트리거 시연이 안 나오면 실패로 단정하지 말 것** — 세션 시작 이후 추가한 hooks 구성이 현재 세션에 즉시 반영되는지는 버전에 따라 다르다(🟡 §L 재검증 항목). 반응이 없으면 `/hooks`로 로드 상태를 확인하고, 그래도 없으면 "새 세션에서 재확인" 대기로 남긴다(STEP 4 기록 괄호).

### 제거
**위 감지와 같은 3곳(`~/.claude/settings.json`·`.claude/settings.json`·`.claude/settings.local.json`)을 모두 조회해** 해당 hook 항목만 제거한다(다른 hooks는 손대지 않음) — 한 곳만 보면 개인 스코프에 얹힌 훅이 남아 다른 repo에서 계속 터진다.

---

## 2. 호스트 CLI — gh(GitHub) / glab(GitLab)

**무엇**: Claude가 PR 생성·이슈 조회·CI 로그 읽기를 직접 하게 하는 CLI. 검증 루프의 연결고리(예: "CI 실패 로그 읽고 고쳐줘").
**호스트 분기**: GitHub → `gh` / GitLab(사내 포함) → `glab`(셀프호스트는 `glab auth login --hostname <사내호스트>`) / 기타 호스트 → 설치 생략(`미선택: 04 §2(해당 없음—미지원 호스트)`로 남긴다 — 고지만 하면 기록에 흔적이 없다).

### 감지
`gh --version`·`glab --version`, 인증 상태(`gh auth status` 등). 이미 설치·인증돼 있으면 건너뜀.

### 설치
- gh: `winget install GitHub.cli`(Windows) / `brew install gh`(macOS) / Linux·WSL은 배포판 패키지 또는 공식 릴리스 바이너리(`cli/cli` 릴리스) → `gh auth login`.
- glab: `brew install glab`(**공식 지원 경로** — macOS·Linux·WSL) / Windows는 `winget install glab.glab`(**공식 지정 밖 — 커뮤니티 유지보수 묶음**, 사내 승인 대상일 수 있다) 또는 공식 릴리스 바이너리 → `glab auth login`(사내 호스트면 `--hostname`).
- **인증은 사용자가 직접**(브라우저 로그인) — Claude가 토큰을 만들거나 저장하지 않는다. 회사 계정 정책(SSO 필수 등)을 따른다.

### 검증
`gh pr list`(또는 `glab mr list`) 실행 출력으로 보고. **인증이 미완이면 설치 실패가 아니다** — `gh auth status`(또는 `glab auth status`) 출력을 증거로 내고 `04 vX.Y(gh 설치, 인증 대기(YYYY-MM-DD~))`로 남긴다(인증은 사용자 직접 단계라 대기가 자주 생긴다 — 메모가 없으면 00 STEP 3 질문 ③이 읽을 것이 없어 영영 미완으로 남는다).

### 제거
CLI 언인스톨 + `gh auth logout`/`glab auth logout`.

---

## 3. CI 통합 — 머지 전 2차 게이트

**무엇**: PR/MR 단계에서 Claude가 리뷰·검증에 참여한다. **호스트에 따라 방식이 다르다**:

### 설치
호스트에 따라 아래 둘 중 **하나만** 따른다 — GitHub는 3-A, GitLab·기타 호스트는 3-B. 어느 쪽이든 **시크릿 등록은 사용자가 직접** 하고 Claude는 워크플로 파일만 만든다.

### 3-A. GitHub → `claude-code-action`
- 공식 액션(`anthropics/claude-code-action@v1`, GA): PR에 `@claude` 멘션으로 리뷰·수정 요청, 이슈에서 구현 지시 가능.
- 설치: `.github/workflows/claude.yml` 생성 — 최신 템플릿·권한 설정은 설치 시점에 `github.com/anthropics/claude-code-action` README로 확인(퀵스타트 정본은 CLI의 `/install-github-app` — 단 이 명령은 **브랜치·커밋·푸시를 수반하는 PR을 만들므로** STEP 4의 "커밋은 안내만" 방침에 걸린다. 실행은 사용자에게 안내만 한다). 인증은 **repo Secrets**로 — API 키(`ANTHROPIC_API_KEY`) 또는 **Pro·Max·Team·Enterprise 구독이면 `CLAUDE_CODE_OAUTH_TOKEN`**(`claude setup-token`으로 발급) 중 하나. 단 OAuth 토큰은 **발급자 개인 구독에 묶이므로** 여러 repo가 공유하는 시크릿에는 API 키를 쓴다.
- **회사 주의** 🔴: CI에서 코드가 Anthropic API로 전송된다 — 조직의 외부 AI 사용 정책·데이터 반출 규정을 먼저 확인. 조직 승인 없으면 **§3만** 설치하지 않는다(§1·§2는 진행 — 절차·기록 형식은 STEP 2 참조. 04 전체를 미설치로 기록하지 않는다).
- **게이트는 데이터 반출 축 하나가 아니다** — 아래 넷을 함께 확인한다. 어느 하나라도 막히면 설치 대신 `게이트 차단(사유)`로 남긴다.
  - **관리자 권한**: repo·조직에 관리자 권한이 있어야 앱을 설치할 수 있다.
  - **시크릿 등록 방식**: 조직이 허용하는 방식인지 먼저 본다(개인 토큰 금지·조직 시크릿만 허용 등의 정책이 있다).
  - **권한 집합은 전체 수용**: 앱을 설치하면 그 앱의 **권한 집합 전체를 받아들이게 된다.** 부분 수용은 불가능하므로, 조직이 최소 권한을 요구하면 필요한 권한만 가진 별도 앱을 직접 만드는 경로를 택한다.
  - **비용은 양쪽**: 러너 사용 시간과 토큰 사용량 **둘 다** 든다. 한쪽만 보고 승인받으면 나중에 다른 쪽에서 막힌다.
- **감지 시 시크릿은 존재 여부만 확인한다** — 값을 읽지 않는다. 검증하려다 시크릿을 읽으면 이 문서의 보안 원칙을 스스로 어긴다.
- **Secrets 등록은 사용자 직접**(§2 인증과 동일) — API 키·OAuth 토큰 어느 쪽이든 Claude는 읽지도 넣지도 않는다. 워크플로 파일만 만들고, 등록은 안내한다.

### 3-B. GitLab → 공식 CI/CD 통합(beta) · 기타 → headless 폴백
- **GitLab은 공식 통합이 있다**(beta): `.gitlab-ci.yml` 정본 잡 템플릿·`@claude` 멘션 트리거(이슈·MR·리뷰 스레드)·마스킹 CI/CD 변수(`ANTHROPIC_API_KEY` + `GITLAB_ACCESS_TOKEN` 또는 `CI_JOB_TOKEN`)를 제공 — 설치 시점에 `code.claude.com/docs/en/gitlab-ci-cd`의 템플릿을 정본으로 쓴다. **이 통합의 유지 주체는 GitLab**이라 지원 창구가 §3-A(Anthropic 공식 액션)와 다르다. 인증도 비대칭이다 — GitLab은 **API 키 전용**(또는 Bedrock·Google Cloud OIDC)이고 구독 OAuth 토큰을 받지 않는다.
- **그 외 호스트**는 CI 스테이지에서 `claude -p "<리뷰/검증 프롬프트>" --allowedTools ...`로 직접 호출. 헤드리스 운용은 **02 §F(`claude -p` 팬아웃) 주석 + 공식 headless 문서** 참조 — 여기선 "CI 스테이지에 넣는다"는 위치만 정의한다.
- **(headless 폴백 전용)** 러너에 Claude Code를 설치하고, **사용자가** API 키를 CI 변수(마스킹)로 등록한다(Claude는 키를 넣지 않는다 — §3-A와 같은 규칙). 사내망 러너면 Anthropic API 접근 가능 여부부터 확인.

### 감지
`.github/workflows/`에 claude 관련 워크플로 존재 여부(**`/install-github-app`으로 깔았으면 워크플로가 미머지 PR에만 있어 로컬이 비는 것이 정상** — `gh workflow list` 또는 열린 PR 조회로 함께 확인) / `.gitlab-ci.yml`의 claude 스테이지 **또는 공식 템플릿 `include:`** 여부.

### 검증
워크플로 파일이 생성됐는지 확인하고(파일 존재·경로 출력 — `/install-github-app` 경로로 만들었으면 로컬 파일 대신 **생성된 PR·원격 파일 링크가 증거**다), 키 등록은 **GitHub면 `gh secret list`**(§2를 설치했을 때. 이름만 표시되고 값은 안 나온다) **GitLab이면 CI/CD 변수 목록**으로 확인한다 — CLI가 없으면 사용자에게 웹 확인을 요청한다. 실행 증거는 **사용자가 테스트 PR/MR을 만들고 거기에 `@claude` 멘션을 남긴 뒤**(GitHub 공식 액션·GitLab 공식 통합 모두 멘션이 트리거다 — 파일만 두면 아무 일도 일어나지 않는다) 액션(또는 스테이지) 로그로 확인한다 — PR 생성은 브랜치·커밋·푸시를 수반해 STEP 4의 "커밋은 안내만" 방침에 걸리고, Secret 미등록 상태면 반드시 실패한다. 실행 확인 전이면 기록에 `04 vX.Y(CI 설치, Secret 등록·첫 실행 대기)`로 남긴다.

### 제거
워크플로 파일(또는 CI 스테이지) 삭제. **Secrets/변수의 자격증명 제거는 사용자 직접** — `ANTHROPIC_API_KEY`뿐 아니라 **구독으로 넣었으면 `CLAUDE_CODE_OAUTH_TOKEN`도 함께** 지운다(§3-A가 둘 중 하나를 쓰므로 한쪽만 지우면 구독 자격증명이 repo에 남는다). 설치 때와 같은 이유로 Claude는 키를 읽지도 지우지도 않는다 — 안내만 한다.
**시크릿을 지워도 자격증명 자체는 유효하다** — repo에서 사라질 뿐 키는 살아 있다. 완전히 폐기하려면 발급처(콘솔·계정 설정)에서 그 키를 삭제하라고 함께 안내한다. 이 한 줄이 없으면 "지웠으니 안전하다"는 오해가 남는다.

---

## L. 유지보수 (6개월마다 재검증 — **GitLab 통합이 beta인 동안 그 항목만 3개월**)
- hooks 이벤트·스키마(`PostToolUse` matcher 문법) 유지 여부 — code.claude.com/docs/en/hooks.
- ✅ **세션 중 추가한 hooks는 현재 세션에 즉시 적용된다**(2026-08-20 확인 — settings 파일 직접 편집은 file watcher가 자동으로 집어 간다). §1 검증이 "방금 건 hook"을 실제로 검사한다는 전제가 성립하므로 문구 변경 불요. 이후엔 이 동작이 유지되는지만 본다.
- `claude-code-action` 메이저 버전·권장 워크플로 템플릿 변화 — github.com/anthropics/claude-code-action.
- **GitLab CI/CD 통합(beta)**: beta→GA 전환·`.gitlab-ci.yml` 정본 템플릿·필요 변수 변화 — code.claude.com/docs/en/gitlab-ci-cd.
- `gh`/`glab` 설치 명령·인증 방식 변화.
- **자기 점검(갱신 의식)** — 재검증·개정은 기억이 아니라 **당일 공식 문서** 조회로 한다(확인 못 한 항목엔 🟡을 단다). 고쳤으면 ① 버전 표에 한 줄(무엇을 왜) ② 헤더·푸터의 버전과 날짜 ③ 저장소 `CLAUDE.md` 구성물 표 ④ 이 체크리스트 ⑤ 기계 검사 통과를 빠짐없이 한다. 버전은 네 곳에 있고 **하나만 빠져도 4축이 어긋난다** — 골격 규칙의 정본은 `docs/DOC_CONTRACT.md`다.

---

**문서 정보** — 검증 자동화 **v1.20** · 최종 갱신: 2026-09-02 · 최근 재검증: 2026-08-20 · 참조: Claude Code v2.1.237.
역할 경계: 편집 중 게이트(§1)와 CI 게이트(§3)는 이 문서가 정본, 헤드리스 실행 기법은 02 §F 주석+공식 headless 문서 참조, 리뷰 명령(`/code-review`)은 02 §E 주석 참조 — 중복 서술하지 않는다.
보안은 공식 5층 스택과 상호보완: ① 세션 중 security-guidance 플러그인 → ② 온디맨드 `/security-review`(내장 명령, 현재 브랜치 1회 점검) → ③ claude-security 딥 스캔 → ④ 관리형 PR Code Review(Team·Enterprise, 리뷰당 ~$15–25) → ⑤ CI 정적 분석. 이 문서의 **§3 CI가 ⑤층의 정본**이고, **§1 hooks는 5층 스택 이전의 "편집 즉시" 게이트**로 상호보완한다. 플러그인 2종(①③)은 03 부록 참조.
