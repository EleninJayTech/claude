# Claude Code 확장기능 설치 — 체크리스트 (드롭인 적용)

> **문서 버전: v1.11** · 최종 갱신: **2026-08-12** · **최근 재검증: 2026-08-04** · 기준: Claude Code v2.1.226
>
> | 버전 | 날짜 | 변경 내용 |
> | --- | --- | --- |
> | v1.0 | 2026-07-23 | 초판 — 확장 3종(claude-hud · svg-design 스킬 · frontend-design 스킬) 체크리스트 설치 |
> | v1.1 | 2026-07-28 | 참조 버전 갱신(v2.1.220, Opus 5 출시) — 내용 변경 없음 |
> | v1.2 | 2026-07-28 | 항목 추가: **§4 MCP 서버**(팀 공유 `.mcp.json`·개인 스코프, 회사 데이터 유출 주의) · **§5 브라우저 검증**(Claude in Chrome·인앱 브라우저 — frontend-design 시너지) + **부록: 추천 플러그인**(우리 구성과의 중복 판정 포함). (구)§4 규약은 §6으로 |
> | v1.3 | 2026-07-28 | **절약 프로필 연동**: 프로필=절약이면 claude-hud를 기본 체크로 추천(한도 가시화가 절약의 전제) |
> | v1.4 | 2026-08-04 | **적용 기록 한 줄 추가**(STEP 4: 대상 CLAUDE.md에 `dropin-applied` 버전 기록 — 재적용 시 버전 비교용, 형식·재적용 규칙은 00 §A) |
> | v1.5 | 2026-08-04 | **전면 재검증**(당일 조회, v2.1.221): 소스 5종 전부 생존 확인(claude-hud·anthropics/skills·Chrome 확장·플러그인 2종). 갱신 — §3 frontend-design **플러그인 마켓플레이스 설치 병기**(현행 공식 경로), §1 플러그인 즉시 활성화(v2.1.221) 한 줄, §5 Chrome 표기 정리(GA→전 유료 플랜 제공). "최근 재검증" 표기 분리 |
> | v1.6 | 2026-08-10 | **중복 재판정**(당일 조회, v2.1.226 — DEC-20260810-bsjeong87-02): 부록 갱신 — security-guidance **전용 공식 문서 페이지 승격**(3층 검사: 편집 패턴/턴 종료/커밋, v2.1.144+, 전 플랜) 반영, **claude-security**(멀티에이전트 딥 스캔+패치 제안) · **LSP code intelligence**(공식 11종) 행 추가. claude-hud 생존 재확인(2026-08-07 push, 27.2k★) — §1 정본 유지, 내장 statusline은 여전히 스크립트 DIY라 흡수 아님 |
> | v1.7 | 2026-08-10 | **적용 기록 표기 자기 참조화**(DEC-20260810-bsjeong87-07, 00~06 공통): STEP 4 하드코딩(`03 v1.5` 구버전) → "최상단 문서 버전 표기에서 읽어 기록" |
> | v1.8 | 2026-08-11 | §1 **추천 표시 구성 추가**(작성자 실사용 검증 구성): 판단에 필요한 항목만 켜는 `config.json` 예시 — 모델+effort·컨텍스트%·사용량%+리셋·git dirty·agents, 소음 항목(tools·todos·cost 등) off |
> | v1.9 | 2026-08-12 | **/audit 2차 반영**(docs/audit-20260812-2.md, M22): §2·§3 감지에 **동명 그림자 경고**(양 스코프 동시 존재 시 개인이 이김 — 프로젝트에 설치·커밋해도 그 PC에선 실행되지 않는다. v1.23이 확정한 personal > project가 01에만 반영돼 있던 것) + §3 검증에 "뜬 것이 **설치한 스코프의 것인지**" 조건 추가(개인 쪽이 떠서 실패한 설치가 통과하던 오탐) |
> | v1.10 | 2026-08-12 | **/audit 3차 반영**(docs/audit-20260812-3.md, L18): §1 claude-hud의 `/reload-plugins` 안내를 조건절("v2.1.221+는 …할 수 있다")에서 현행 서술로 — 문서 기준이 v2.1.226이라 그 분기는 항상 참이고, 독자에게 불필요한 버전 확인을 요구했다 |
> | v1.11 | 2026-08-12 | **/audit 4차(앵커 없는 전수조사) 반영**(docs/audit-20260812-4.md): **§4 MCP·§5 브라우저의 회사 게이트를 STEP 1·2에 배선**(S2 — 🔴 주의가 본문 산문에만 있고 절차엔 없어, 사내 repo에 전체 모드로 돌리면 대화 데이터 외부 전송·로그인 세션 접근 구성이 질문 없이 설치됐다. 04는 v1.7, 06은 진작 배선됨 — 03만 남아 있던 것), STEP 2 스코프 질문에 **§4 MCP 3택**(M14 — `.mcp.json`은 커밋되면 팀 전원 배포), STEP 4에 **부분 완료·조치 대기 표기**(M21 — 03이 사용자 실행 항목이 가장 많은 문서인데 대기 어휘가 없었다)+`CLAUDE.md` 부재 시 최소 골격 위임(M19), §2 검증에 **설치 스코프 확인**(M20 — v1.9가 §3에만 넣은 것), 버전 표 v1.10 행의 표 이탈 정정(M2)·§1 설치 블록의 `/reload-plugins` 조건화와 `(v2.1.221+)` 제거(L6)·사용법 요약에 적용 기록(L7)·§6 6요소 실태 반영(L25) |
>
> ※ 갱신 시: 이 표에 한 줄 추가 + 하단 "문서 정보" 날짜 수정 + §L 재검증.

> **사용법**: 이 파일을 아무 프로젝트 루트(또는 `docs/`)에 넣고 Claude에게
> **"이 문서를 학습해서 이 PC(또는 이 프로젝트)에 적용해줘"** 라고 하면 된다.
> Claude는 ① 현재 설치 상태를 감지 → ② **체크리스트로 무엇을 설치할지 확인 요청**(§4·§5는 조직 승인 게이트 포함) → ③ 체크된 항목만, 사용자가 고른 스코프·방식으로 설치 → ④ 검증·**적용 기록** 후 보고 한다.
>
> **기본 원칙**: 1순위 = 최적의 결과물, 토큰 절약 = 품질 유지 전제의 2순위 (01 통합마스터 §0과 동일).
> **정직한 고지**: 외부 저장소(플러그인·스킬)는 버전이 바뀐다. 설치 전 §L의 소스 URL이 살아있는지 확인한다.

---

## A. Claude 실행 프로토콜 (적용하면 이 순서대로)

**STEP 1 — 현황 감지 (먼저 조사, 질문은 그다음).** 각 항목의 §"감지" 절차로 **이미 설치됐는지 / 유사한 것이 있는지** 확인한다:
- claude-hud: `~/.claude/settings.json`의 `statusLine` 키, `~/.claude/plugins/` 아래 claude-hud 존재 여부. **다른 statusLine 구성이 이미 있으면 "유사 구성"으로 분류.**
- svg-design·frontend-design: `~/.claude/skills/`와 `<프로젝트>/.claude/skills/`의 동명 폴더, 그리고 **비슷한 목적의 스킬**(이름·description에 svg/design/frontend/ui 포함) 존재 여부.
- MCP: 프로젝트 `.mcp.json` 유무 + `claude mcp list` 출력.
- **회사·사내 repo 여부**(§4·§5 게이트의 전제): git remote 도메인·조직명으로 1차 판별하되 **코드로 단정할 수 없으므로** STEP 2에서 확인한다.
- 브라우저 검증: Desktop 앱 사용 여부·Chrome 확장 설치 여부(코드로 알 수 없으면 STEP 2에서 함께 질문).

**STEP 2 — 체크리스트 확인 요청.** AskUserQuestion으로 묻는다(감지 결과를 각 선택지 설명에 표시):
1. **설치 항목 선택** (multiSelect): §1 claude-hud / §2 svg-design / §3 frontend-design / §4 MCP 서버 / §5 브라우저 검증 — 각 항목에 현재 상태를 병기한다. 예) "svg-design — 미설치", "claude-hud — ⚠️ 이미 다른 statusLine 사용 중". **운용 프로필이 절약(Pro·한도 관리, 00 STEP 3)이면 claude-hud를 기본 체크로 추천** — 한도 소모율 가시화가 절약의 전제다.
2. **이미 설치됐거나 유사 구성이 감지된 항목**은 반드시 처리 방법을 묻는다: **유지(건너뜀) / 재구성(설정만 갱신) / 교체(기존 제거 후 설치)**. 묻지 않고 덮어쓰지 않는다.
3. **스킬 항목(§2·§3)은 스코프**를 묻는다: **글로벌**(`~/.claude/skills/` — 이 PC 전 프로젝트) / **프로젝트**(`.claude/skills/` — 이 repo만, 커밋해 팀 공유 가능). claude-hud는 구조상 **글로벌 전용**이므로 묻지 않고 고지만 한다. **§4 MCP도 스코프를 묻는다**(3택 — §4 본문): 프로젝트 공유 `.mcp.json`(커밋되면 팀 전원 배포) / 개인 `--scope user` / 기본 `--scope local`.
3-1. 🔴 **§4·§5를 체크했고 사내·회사 repo면 조직 승인 게이트**를 묻는다(00 §A STEP 3 불변 ② — 어떤 모드에서도 생략하지 않는다): MCP는 대화 데이터를 **외부로 전송**하고, 브라우저 검증은 **로그인된 사내 세션**에 접근할 수 있다. 승인되지 않았으면 **그 항목만 제외**하고 나머지는 설치하며, STEP 4 기록에 `03 vX.Y(설치분 — MCP 게이트 차단(조직 미승인))` 형식으로 남긴다(00 §A STEP 5 차단 기록 ② 항목 단위 형식 — 나중에 승인되면 재적용 질문 ③이 다시 묻는다).
4. **스킬 항목은 호출 방식**을 묻는다:
   - **자동 판단** — Claude가 관련 작업을 감지하면 스스로 로드해 적용 (frontmatter 기본값)
   - **수동 호출 전용** — `/svg-design`처럼 사용자가 부를 때만 (frontmatter에 `disable-model-invocation: true`)

**STEP 3 — 체크된 항목만 설치.** 각 항목의 §"설치" 절차 실행. 선택 안 된 항목은 손대지 않는다.

**STEP 4 — 검증·보고.** 각 항목의 §"검증"을 실행해 **증거(출력)로 보고**. 프로젝트 스코프로 설치한 파일이 있으면 커밋 안내(글로벌 설치는 커밋 대상 아님). **적용 기록**: 대상 `CLAUDE.md` 맨 아래 `<!-- dropin-applied: … -->` 한 줄에 이 문서의 **현재 버전**(최상단 "문서 버전" 표기에서 읽음)을 `03 vX.Y(설치 항목)` 형식으로 추가/갱신한다(형식·재적용 규칙은 00 §A — 단독 적용 시에도 남긴다. **대상에 `CLAUDE.md`가 없으면** 00 §A STEP 5의 최소 골격 규칙대로 승인 후 만들어 기록한다 — 단독 드롭인이면 그 규칙이 읽히지 않아 기록이 통째로 사라지고, 00 STEP 5가 든 대표 사례가 하필 "03만 설치"다). **부분 완료·사용자 조치 대기도 괄호에 남긴다** — 이 문서는 `/plugin install`·`/claude-hud:setup`·Chrome 확장처럼 **사용자가 직접 실행해야 하는 항목이 가장 많으므로** 대기가 기본값에 가깝다(예: `03 vX.Y(svg-design — hud는 /plugin install 사용자 실행 대기)`). 게이트로 막힌 항목은 `게이트 차단(사유)`로 구분해 남긴다(둘 다 재적용 질문 ③의 대상).

> 원칙: 승인 없이 기존 구성을 덮어쓰지 않는다. `settings.json` 수정은 **해당 키만 병합**(통째 교체 금지). 시크릿은 읽지도 커밋하지도 않는다.

---

## 1. claude-hud — CLI 사용량 실시간 HUD

**무엇**: 입력창 아래에 상태줄(statusLine)을 그려주는 플러그인. 컨텍스트 사용량 바(초록→노랑→빨강), 구독 사용량(rate limit) 소모율, git 브랜치, 도구/에이전트 활동, todo 진행률 표시.
**스코프**: 글로벌 전용(`~/.claude/plugins/` + user `settings.json`의 `statusLine`) — 이 PC의 모든 세션에 적용.
**요구사항**: Claude Code v1.0.80+, Node.js 18+(Windows 포함. 없으면 `winget install OpenJS.NodeJS.LTS`).

### 감지
- `~/.claude/settings.json`에 `statusLine` 키가 있는가? 있다면 그 값이 claude-hud인가, 다른 도구인가(다른 도구면 **교체 여부를 반드시 질문**).
- `~/.claude/plugins/` 아래 `claude-hud` 폴더(또는 `/plugin` 목록)가 있는가? 있으면 설치 대신 **재구성**(`/claude-hud:configure`)을 제안.

### 설치 (세션 안에서 순서대로)
```text
/plugin marketplace add jarrodwatts/claude-hud
/plugin install claude-hud
/claude-hud:setup        ← 프리셋(Full/Essential/Minimal) 선택 + statusLine 자동 구성
/reload-plugins          ← 평소엔 불필요(설치 즉시 활성화). HUD가 안 보일 때만
```
- 설정 파일: `~/.claude/plugins/claude-hud/config.json` (언어·표시 항목·색상). 이후 조정은 `/claude-hud:configure`.
- 현행은 플러그인이 설치 즉시 활성화되어 `/reload-plugins`가 불필요하다 — 위 블록에서도 HUD가 안 보일 때만 실행한다.
- 출처: `github.com/jarrodwatts/claude-hud`

### 추천 표시 구성 (선택 — 작성자 실사용, 2026-08-11 기준)
`setup` 프리셋 대신 세밀하게 맞추려면 `config.json`에서 아래 키만 조정한다(`/claude-hud:configure`로도 동일). 기준: **판단에 필요한 것만 켠다** — 무엇이 돌고 있나(모델·effort·agents), 컨텍스트가 얼마나 찼나(compact 대비), 한도를 얼마나 썼나(사용량+리셋 시각), 미커밋이 있나(git dirty). tools·todos·cost·speed 같은 소음 항목은 끈다.
```json
{
  "lineLayout": "expanded", "showSeparators": false, "pathLevels": 1,
  "gitStatus": { "enabled": true, "showDirty": true, "showAheadBehind": false, "showFileStats": false },
  "display": {
    "showModel": true, "modelFormat": "full", "showEffortLevel": true,
    "showProject": true, "showContextBar": true, "contextValue": "percent",
    "showUsage": true, "usageValue": "percent", "usageBarEnabled": true, "showResetLabel": true,
    "showAgents": true
  }
}
```
(위에 없는 `display.*` 표시 항목은 전부 `false` 기준 — 기본 off인 키는 그대로 두면 된다.)

### 검증
새 세션(또는 `/reload-plugins` 후)에서 입력창 아래 HUD 줄이 보이는지 확인. `~/.claude/settings.json`에 `statusLine` 키가 생겼는지 확인해 보고.

### 제거
`/plugin uninstall claude-hud` + `settings.json`의 `statusLine` 키 제거(다른 키는 손대지 않음).

---

## 2. svg-design — SVG 필터 디자인 스킬

**무엇**: 웹 UI의 시각 효과(손그림 떨림·Gooey·Liquid Glass·네온 타이포)를 **SVG 필터로 정확하게** 구현하게 하는 스킬. AI가 흔히 틀리는 지점(ID 중복·Safari 클리핑·성능·접근성·XSS)을 규칙으로 강제한다.
**적용 시점**: 사용자가 고른 호출 방식에 따라 — 자동 판단(관련 작업 감지 시) 또는 `/svg-design` 수동 호출.

### 감지
`~/.claude/skills/svg-design/`·`.claude/skills/svg-design/` 존재 여부 + 유사 스킬(svg·filter·effect 계열). 있으면 유지/교체 질문.
> ⚠️ **양 스코프에 동명이 있으면 개인(글로벌)이 이긴다**(enterprise > personal > project — 01 §F-2 주의). 개인 쪽에 이미 있는 채로 프로젝트에 설치하면 커밋·공유까지 해도 **그 PC에선 실행되지 않는다** — 개인 쪽을 교체하거나 이름을 분리한다.

### 설치
선택된 스코프의 `skills/svg-design/SKILL.md`로 아래 내용을 생성한다. (수동 호출 전용을 골랐으면 frontmatter에 `disable-model-invocation: true` 한 줄 추가.)

```markdown
---
name: svg-design
description: 웹 프론트엔드에서 SVG 필터 기반 시각 효과(손그림 떨림·Gooey/Metaball·Liquid Glass·네온 텍스트 등)를 구현·수정할 때 사용. 인라인 SVG 필터 코드 작성, React/Next.js 컴포넌트화, Safari 호환·성능·접근성 문제 해결에 적용.
---
# SVG 필터 구현 규칙 (Staff-Level Frontend 기준)

너는 React/Next.js·SVG 그래픽 전문 아키텍트다. 아래 규칙을 예외 없이 지켜 결함 없고 접근성 있는 고성능 인라인 SVG 필터를 생성한다.

## 필수 제약 (위반 금지)
1. **SSR/Hydration**: 컴포넌트가 반복 렌더링될 수 있으면 `<filter id>`를 하드코딩하지 말 것. React 18 `useId()`로 고유 ID 생성(`useId().replace(/:/g,'')`) 후 인라인 style로 `filter: url(#${id})` 연결. `Math.random()` ID는 Hydration 에러를 유발하므로 금지.
2. **Safari(WebKit) 클리핑 버그**: `feGaussianBlur`·`feDisplacementMap` 적용 요소는 효과가 경계 밖으로 나가면 잘린다(`overflow: visible`로 해결 안 됨). 래퍼 div에 여유 padding 또는 `transform: translateZ(0)` 부여. 필터에 `x="-10%" width="120%"` 여백이나 `filterUnits="userSpaceOnUse"`를 명시.
3. **성능**: `feTurbulence`의 `numOctaves`는 3 이하(모바일 1 권장). 매 프레임 React state로 SVG 속성을 바꾸지 말 것 — GPU 캐시가 무효화된다. 애니메이션은 CSS transform 또는 SMIL(`<animate>`)로 컴포지터 스레드에서. 확대/축소 왜곡 방지엔 `primitiveUnits="userSpaceOnUse"`.
4. **접근성**: 텍스트에 필터를 걸 때 `<path>` 아웃라인화 금지 — DOM 텍스트(`<h1>` 등)를 유지한 채 CSS `filter: url()`만 적용(선택·복사·스크린리더 보존). 시각 전용 `<svg>` 정의부엔 `aria-hidden="true"`, width/height 0.
5. **보안**: 외부 입력이 SVG에 들어오면 DOMPurify(`USE_PROFILES: { svg: true, svgFilters: true }`) 필수. 사용자 값을 `href`·`in2`에 비이스케이프로 넣지 말 것(픽셀 탈취·XSS 벡터).

## 핵심 레시피
- **손그림 떨림(Squiggle)**: `feTurbulence`(baseFrequency 0.02, numOctaves≤3) → `feDisplacementMap`(scale 5, R/G 채널) + `<animate attributeName="seed" values="0;1;2;3;4" dur="0.4s" calcMode="discrete" repeatCount="indefinite">` — JS 개입 없이 렌더 파이프라인 안에서 애니메이션.
- **Gooey/Metaball**: 그룹 컨테이너에 `feGaussianBlur(stdDeviation 6~10)` → `feColorMatrix`(alpha 행 `0 0 0 18 -7` 류로 대비 극대화) → `feComposite(operator="atop")`. 알파를 0/1로 양극화해 달라붙는 점성 연출. `colorInterpolationFilters="sRGB"` 명시.
- **Liquid Glass**: 렌즈형 변위 맵 이미지를 `feImage`로 로드 → `feDisplacementMap`(R=x축, G=y축 굴절) + `feComponentTransfer`로 하이라이트. CSS는 `backdrop-filter: url('#id') blur(12px)` — Chromium 우선, Safari/Firefox엔 순수 CSS 블러 폴백 필수.
- **네온/마스킹 타이포**: 숨긴 `<svg aria-hidden>`에 `feGaussianBlur → feMerge(blur + SourceGraphic)` 필터 정의, 실제 `<h1>`에 `filter: url()` 적용.

## 출력 지침
Gooey·Squiggle·Liquid Glass 등 요청 시 Tailwind 클래스를 쓰는 TypeScript React(.tsx) 컴포넌트 코드를 바로 출력하고, 적용된 수학 원리(feColorMatrix 임계값 클램핑, 변위 공식 등)를 2~3문장으로 덧붙인다. 프레임워크가 React가 아니면 그 스택 관용구로 변환하되 위 제약은 동일하게 지킨다.
```

### 검증
`/` 자동완성(또는 `/context`)에 svg-design이 뜨는지 확인. **뜬 것이 방금 설치한 스코프의 것인지까지 확인한다** — 양 스코프에 동명이 있으면 개인 쪽이 떠서 프로젝트 설치가 실패해도 검증을 통과한다(설치 경로의 `SKILL.md`를 직접 열어 대조하면 확실하다). 스킬을 호출해 "Gooey 버튼 컴포넌트"를 생성시켜 **useId 사용·Safari 대응·aria-hidden**이 코드에 들어갔는지 확인해 보고.

### 제거
해당 스코프의 `skills/svg-design/` 폴더 삭제.

---

## 3. frontend-design — AI 티 안 나는 프론트엔드 디자인 스킬

**무엇**: Anthropic 공식 스킬. 흔한 "AI가 만든 UI" 룩(뻔한 폰트·보라 그라데이션·과한 애니메이션)을 피하고, 주제·톤에 맞는 **의도적 디자인**(타이포·컬러·모션·공간 구성)의 프로덕션급 HTML/CSS/JS·React·Vue 코드를 만들게 한다.
**적용 시점**: 사용자가 고른 호출 방식에 따라 — 자동 판단 또는 수동 호출.

### 감지
`skills/frontend-design/` 존재 여부(양 스코프) + 유사 스킬. **§2 svg-design과는 역할이 다름**(frontend-design=디자인 방향·전체 UI, svg-design=SVG 필터 효과 구현) — 둘 다 설치해도 충돌하지 않음을 사용자에게 고지.
> ⚠️ **양 스코프에 동명이 있으면 개인(글로벌)이 이긴다**(§2와 동일 — 01 §F-2 주의). 프로젝트 설치를 고르기 전에 개인 쪽 동명 유무를 먼저 확인한다.

### 설치
- **플러그인 방식(현행 공식 경로, 2026-08-04 확인)**: `/plugin marketplace add anthropics/skills` → `/plugin install example-skills@anthropic-agent-skills`(frontend-design 포함 묶음, 글로벌 적용). frontend-design 하나만 원하면 아래 개별 설치.
- **프로젝트 스코프(개별)**: 루트에서 `npx skills add https://github.com/anthropics/skills --skill frontend-design` 실행(`.claude/skills/frontend-design/` 생성).
- **글로벌 스코프(개별)**: 위 명령이 프로젝트에 설치하므로, 생성된 `frontend-design/` 폴더를 `~/.claude/skills/`로 이동(또는 `github.com/anthropics/skills`의 `frontend-design` 폴더를 받아 복사).
- npx 실행이 실패하면(네트워크/권한) 저장소에서 스킬 폴더를 직접 내려받아 복사하는 폴백을 쓴다.
- 수동 호출 전용 선택 시 SKILL.md frontmatter에 `disable-model-invocation: true` 추가.
- 출처: `skills.sh/anthropics/skills/frontend-design` · `github.com/anthropics/skills`

### 검증
`/` 자동완성에 frontend-design이 뜨는지 + SKILL.md frontmatter가 온전한지 확인해 보고. **뜬 것이 방금 설치한 스코프의 것인지까지 확인한다** — 양 스코프에 동명이 있으면 개인 쪽이 떠서 프로젝트 설치가 실패해도 검증을 통과한다. (Claude Code에 내장 frontend-design 플러그인 제안이 뜨는 경우가 있는데, 이 스킬을 설치했으면 중복 설치하지 않는다.)

### 제거
해당 스코프의 `skills/frontend-design/` 폴더 삭제.

---

## 4. MCP 서버 — 외부 도구·데이터 연결

**무엇**: MCP(Model Context Protocol) 서버로 Claude에 외부 능력을 연결한다 — 예: GitHub(PR·이슈·CI 로그), Context7(라이브러리 최신 문서 주입), Playwright(브라우저 조작), Sentry·Linear 등 파트너 서버 다수. **어떤 서버를 붙일지는 사용자에게 목록으로 질문**(프로젝트 스택·용도 기반 추천 병기).
**스코프**: **프로젝트 공유** = `.mcp.json`(repo에 커밋, 팀원 각자 첫 실행 때 승인 — 01 §F-1 공유 목록과 일치) / **개인** = `claude mcp add --scope user`(이 PC 전 프로젝트) / 기본 `--scope local`(이 repo에서 나만).
**회사 업무 주의** 🔴: MCP 서버는 대화 중 데이터를 **외부로 전송**할 수 있다 — 사내 코드·데이터를 다루는 repo라면 조직이 허용한 서버만 연결하고, 사내 호스팅(GitLab 등) 환경에선 해당 호스트용 서버가 따로 있는지 확인한다. 인증 토큰은 환경변수로 주입(`.mcp.json`에 시크릿 직접 기입 금지).

### 감지
`.mcp.json` 존재 + `claude mcp list` 출력(등록·연결 상태). 같은 목적의 서버가 이미 있으면 유지/교체 질문.

### 설치
- 서버별 명령이 다르므로 **설치 시점에 해당 서버 공식 README·`code.claude.com/docs/en/mcp`를 확인**해 `claude mcp add ...`(전송 방식 stdio/HTTP에 맞게) 실행. 팀 공유면 `--scope project`로 `.mcp.json`에 기록.
- 인증이 필요한 서버는 `claude mcp login <서버>`(v2.1.185+, 해제는 `claude mcp logout`).

### 검증
`/mcp` 목록에서 서버가 **connected** 상태인지 + 대표 도구 1개 호출(예: GitHub MCP로 이슈 1건 조회)을 증거로 보고.

### 제거
`claude mcp remove <이름>`(개인) / `.mcp.json`에서 항목 삭제 후 커밋(공유).

---

## 5. 브라우저 검증 — 만든 화면을 Claude가 직접 확인

**무엇**: 프론트엔드 작업 결과를 Claude가 **직접 브라우저로 열어 확인·조작**하게 한다 — §2·§3 디자인 스킬과 시너지("만들고 → 스스로 열어 보고 → 고친다" 루프). 경로 3종 중 환경에 맞는 **하나만** 설치:
- **Desktop 인앱 브라우저**(v2.1.202+): Desktop 앱이면 내장 — 설치 불요, 안내만.
- **Claude in Chrome**(공식 확장 — 전 유료 플랜 제공): CLI/IDE 사용자 중 Chrome 사용자.
- computer use(CLI 연구 프리뷰): 위 둘이 안 될 때의 참고만 — 이 문서에선 설치하지 않음.

**회사 업무 주의** 🔴: 브라우저 연결 시 로그인된 세션·사내 페이지에 접근할 수 있다 — 확장의 **사이트 권한을 개발 대상(localhost·스테이징)으로 최소화**하고, 사내 시스템 페이지는 권한에서 제외한다.

### 감지
Desktop 앱 사용 여부, Chrome 확장 설치 여부를 사용자에게 확인(코드로 감지 불가).

### 설치
- Chrome: 공식 확장(Claude in Chrome) 설치 → 확장 설정에서 허용 사이트 지정(최소 권한). 상세는 `code.claude.com/docs`의 Chrome 문서를 설치 시점에 확인.
- Desktop: 별도 설치 없음 — 사용법(로컬 dev 서버 미리보기·페이지 상호작용) 안내만.

### 검증
로컬 dev 서버(또는 테스트 페이지)를 열어 스크린샷/확인 결과를 증거로 보고.

### 제거
Chrome 확장 제거 또는 사이트 권한 회수. Desktop은 해당 없음.

---

## 6. 항목 추가 규약 (이후 확장용)

새 확장을 이 문서에 추가할 때는 §1~5와 같은 형식으로 쓴다 — **무엇 / 감지 / 설치 / 검증 / 제거** 5요소 필수 + **스코프 선택지가 있는 항목은 스코프도**(§2·§3 스킬 = 글로벌/프로젝트, §4 MCP = 3택. §1 claude-hud는 글로벌 전용, §5 브라우저는 확장 설치라 스코프 개념이 없어 "적용 시점"으로 대신한다 — 없는 항목에 억지로 절을 만들지 않는다). "감지"에는 유사 구성 판별 기준을, "검증"에는 성공 기준(증거로 보고할 출력)을 반드시 포함한다. **추가 전 중복 판정**: 내장 기능·기존 문서(01·02·04)와 겹치면 넣지 않거나 부록으로 내린다. 추가 후 최상단 버전 표에 한 줄 기록.

---

## 부록. 추천 플러그인 — 중복 판정 포함 (설치 항목 아님, 참고만)

커뮤니티 인기 플러그인 다수가 **이 구성과 기능이 겹친다**. 원칙: 겹치면 우리 정본을 쓰고, 아래는 필요할 때만 개별 설치(`/plugin marketplace` — 공식 마켓 200+종).

| 플러그인 | 용도 | 중복 판정 |
| --- | --- | --- |
| Superpowers | 계획→스펙→TDD 워크플로 팩 | ⚠️ **02 로스터·plan mode와 겹침** — 02가 정본. 02를 안 쓰는 프로젝트에서만 고려 |
| Claude Mem | 영속 메모리 | ⚠️ **내장 auto-memory와 겹침** — 내장이 정본 |
| Context7 | 라이브러리 최신 문서 주입 | ✅ 겹침 없음 — §4 MCP로 설치 가능, 외부 라이브러리 많은 프로젝트에 추천 |
| Caveman | 간결 응답으로 토큰 절약 | 참고만 — 절약은 품질 유지 전제의 2순위(§기본 원칙) |
| security-guidance | 작업 중 보안 취약점 리뷰 — **3층 검사**(편집 시 패턴 매치·턴 종료 diff 리뷰·커밋/푸시 에이전트 리뷰), hooks 기반, v2.1.144+·전 플랜 | ✅ 겹침 없음(공식, 전용 문서 `docs/en/security-guidance`) — 보안 민감 프로젝트에 추천, 04 검증 자동화와 상호보완 |
| claude-security | 온디맨드 **멀티에이전트 취약점 딥 스캔+패치 제안**(`/claude-security` — 리포트·패치는 `CLAUDE-SECURITY-<ts>/`에 남고 자동 적용 없음), v2.1.154+·dynamic workflows 필요(Pro는 `/config`에서 켬) | ✅ 겹침 없음(공식) — security-guidance(작성 중)와 층이 다름(딥 스캔). 스캔당 토큰 소모 큼 |
| LSP 플러그인 11종 | 언어 서버 연동 **code intelligence** — 편집 직후 타입 오류 자동 진단 + 정의/참조 정밀 탐색(`typescript-lsp`·`pyright-lsp`·`gopls-lsp` 등, 언어 서버 바이너리는 별도 설치) | ✅ 겹침 없음(공식) — 타입 언어·대형 코드베이스에 추천. 대형 repo에선 언어 서버 메모리 주의 |

---

## L. 유지보수 (6개월마다 재검증)
- 소스 생존 확인: `github.com/jarrodwatts/claude-hud`(설치 명령 변화), `github.com/anthropics/skills`(frontend-design 위치·설치 CLI 변화).
- 스킬 frontmatter 스펙(`disable-model-invocation`·`description` 자동 로드 규칙) 유지 여부 — code.claude.com/docs/en/skills.
- svg-design 규칙 중 브라우저 종속 항목(Safari 클리핑 버그·backdrop-filter 지원 편차)이 여전히 유효한지.
- §4 MCP: `claude mcp` 명령 체계·`.mcp.json` 스키마·`mcp login` 유지 여부. §5: Chrome 확장·인앱 브라우저 제공 상태.
- 부록 플러그인 목록: 생존 여부·중복 판정 재확인(내장 기능이 흡수했으면 행 삭제).
- 갱신 후 이 문서 "최종 갱신" 날짜 수정.

---

**문서 정보** — 확장기능 설치 체크리스트 **v1.11** · 최종 갱신: 2026-08-12 · 최근 재검증: 2026-08-04 · 참조: Claude Code v2.1.226.
근거: claude-hud README(github.com/jarrodwatts/claude-hud) · skills.sh/anthropics/skills/frontend-design · SVG 필터 구현 가이드 분석(feTurbulence/feDisplacementMap/feColorMatrix 원리, Safari WebKit 클리핑 버그, useId Hydration, DOMPurify svgFilters 프로파일 — 2026-07-23 당일 정리).
