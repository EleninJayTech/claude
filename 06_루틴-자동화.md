# Claude Code 루틴(Routines) — 클라우드 스케줄 에이전트 드롭인 적용

> **문서 버전: v1.15** · 최종 갱신: **2026-08-31** · **최근 재검증: 2026-08-20** · 기준: Claude Code v2.1.237
>
> | 버전 | 날짜 | 변경 내용 |
> | --- | --- | --- |
> | v1.0 | 2026-08-04 | 초판 — 루틴 3종 트리거(스케줄·API·GitHub)+repo 준비물+정의 기록(docs/ROUTINES.md) |
> | v1.1 | 2026-08-10 | 적용 기록 표기 자기 참조화 — 내용 변경 없음 |
> | v1.2 | 2026-08-10 | §1에 **계정 레벨 GitHub 연결 확인** 추가(미연결이면 마지막 루틴 저장이 거부됨) |
> | v1.3 | 2026-08-12 | §4 `.gitattributes`에 `docs/ROUTINES.md merge=union` 배선, STEP 1·2 게이트 요약을 §1 4종 전부로 |
> | v1.4 | 2026-08-12 | STEP 4 **차단 시에도 `06 게이트 차단(사유)` 기록**+부분 완료 표기, §4 검증에 `.gitattributes` 확인, §2·§4 제거 절 신설, "편집 즉시 hooks" 표현, 기준 v2.1.226 |
> | v1.5 | 2026-08-12 | STEP 1 중단 두 곳에 "기록은 남긴다" 인라인 병기, 게이트 ④ 차단 범위 명시(§3 루틴 저장만 — §2 준비물은 진행), `CLAUDE.md` 부재 시 최소 골격 위임, 루틴 감지 "이 repo 대상" 한정 |
> | v1.6 | 2026-08-12 | §2 검증을 준비물 4종 전부로 확장(MCP 안 쓰면 "해당 없음" 분기) |
> | v1.7 | 2026-08-12 | 검증 ③ 시크릿 기준 모순 해소(환경변수 주입은 통과 기준이 아님 — 비권장 유지), 검증 4종을 준비물 4종과 1:1 재정렬 |
> | v1.8 | 2026-08-13 | **최적화**(릴리즈 정비): 버전 표·본문 서술 압축(규칙 불변) |
> | v1.9 | 2026-08-20 | **GitHub 트리거 CLI 생성 반영**(v2.1.225+ — "웹에서만" 폐기, 웹 전용은 API뿐), §1 401 서술 현행화(`/schedule` 선확인+`/web-setup` 안내·한계), §3 검증에 **커넥터 최소화 확인**, 비용에 one-off 상한 불산입·usage credits |
> | v1.10 | 2026-08-20 | §1에 **GitHub App 선설치 게이트**(미설치면 첫 이벤트까지 미발견), §2 검증 ①을 세션 로그 축으로·③을 조회 가능한 두 증거로·④에 `CLAUDE.md` 골격 분기, §4 검증에 커넥터 최소화 확인, STEP 4 기록 예시를 `게이트 차단(사유)` 정본 어휘로 |
> | v1.11 | 2026-08-20 | 전면 재검증(v2.1.237) — routines research preview 유지, `/schedule` 조회·수정·즉시실행 3종과 중지·삭제 웹 전용 유효 확인 |
> | v1.12 | 2026-08-20 | 재검증 2회차: STEP 1에 **`/schedule` 은닉 조건** 추가(`DISABLE_TELEMETRY`·`DO_NOT_TRACK`·`CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC`·`DISABLE_GROWTHBOOK` 중 하나라도 켜져 있으면 게이트 4종을 통과해도 명령이 없다), 관리에 **실행 이력 자연어 질의**(v2.1.227+) |
> | v1.13 | 2026-08-21 | **/audit 7차(회귀) 반영** — 게이트 차단을 **사유로 스코프 분기**(조직·정책=팀 / 요금제·계정=글로벌), §2 검증에 **§3 미수행 분기**(연결·환경변수 확인은 대기로 남기고 `.mcp.json` 증거만) |
> | v1.14 | 2026-08-31 | §1 게이트에 **작업 위험도 축**(무인 실행을 감당할 범위인가)과 **명령 은닉 조건**(프라이버시 환경변수와 자기모순), §3 검증에 **"초록색은 성공이 아니다"**, §4 검증에 **`/schedule list` 대조**(기록 → 드리프트 탐지) |
> | v1.15 | 2026-08-31 | §2·§3·§4에 **`### 감지`·`### 설치`** 신설(4단 계약 충족 — 기존 구성을 말없이 덮어쓰지 않는 질문 포함), §1·§5 제목에 **`설치 항목 아님`** 선언(번호는 배포된 `dropin-applied` 기록이 참조하므로 다시 매기지 않는다), §L에 **자기 점검(갱신 의식)** 한 줄 |
> ※ 갱신 시: 이 표에 한 줄 추가 + 하단 "문서 정보" 날짜 수정 + §L 재검증. **research preview 기능이라 변동이 잦다** — 적용 시점에 공식 문서(code.claude.com/docs/en/routines) 확인 필수.

> **사용법**: 이 파일을 아무 프로젝트 루트(또는 `docs/`)에 넣고 Claude에게
> **"이 문서를 학습해서 이 프로젝트에 적용해줘"** 라고 하면 된다.
>
> **무엇**: 루틴은 **Anthropic 클라우드에서 무인 실행되는 스케줄 에이전트**다 — 내 PC가 꺼져 있어도 정해진 시간(또는 API 호출·GitHub 이벤트)에 repo를 clone해 작업하고 PR·커밋·알림으로 결과를 남긴다. 예: 야간 PR 리뷰, 주간 문서 드리프트 점검, 배포 후 스모크 테스트.
> **상태(2026-08-04)**: **research preview** — 동작·한도·명령이 바뀔 수 있다. 요구: **Pro·Max·Team·Enterprise** 요금제 + Claude Code on the web 활성.
> **회사 업무 주의** 🔴: 루틴은 **repo 전체를 Anthropic 클라우드로 clone**하고 프롬프트도 클라우드에 저장된다. 전용 시크릿 저장소가 없다(환경변수는 환경 사용자에게 보임). Team·Enterprise는 Owner가 admin 설정의 Routines 토글로 조직 전체를 끌 수 있다 — **조직 정책 확인 없이는 회사 repo에 설치하지 않는다**(04 CI 통합과 같은 게이트).

---

## A. Claude 실행 프로토콜 (03 §A와 같은 방식)

**STEP 1 — 감지.**
- 요금제·활성화: 코드로 알 수 없다 → STEP 2에서 질문(Pro+ 여부 · 회사 repo면 조직 승인 — 🔴 승인 없으면 여기서 중단(**중단해도 STEP 4의 `06 게이트 차단(사유)` 기록은 남긴다**) · **계정 레벨 GitHub 연결(§1 넷째 항목)** — 이 게이트는 **§3 루틴 저장만 막는다**(§2 준비물은 진행 가능)).
- git 호스트: `git remote -v` — **GitHub가 아니면** repo 연동 루틴은 불가(현행 GitHub 전용) → 안내 후 중단(**이때도 `06 게이트 차단(GitHub 아님)` 기록은 남긴다**).
- **`/schedule` 명령이 보이는가**: 요건 미충족이면 CLI가 이 명령을 **숨긴다**. `DISABLE_TELEMETRY`·`DO_NOT_TRACK`·`CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC`·`DISABLE_GROWTHBOOK` 중 하나라도 설정돼 있으면 사라지므로, 게이트 4종을 다 통과하고도 `Unknown command`로 막힌다 — 절약·프라이버시 설정을 쓰는 환경에서 먼저 확인한다.
- 기존 루틴: `/schedule list`에서 **대상 repo가 이 repo인 루틴만** + `docs/ROUTINES.md` 존재 여부. 있으면 "유지/재구성" 질문 — 루틴은 **계정 스코프**라 타 repo용 루틴이 있다고 이 프로젝트가 적용된 것은 아니다.
- repo 준비물: `.mcp.json`(프로젝트 스코프 MCP) 유무 — 루틴은 **CLI로 등록한 개인 MCP를 쓸 수 없다**.

**STEP 2 — 체크리스트 확인 요청** (AskUserQuestion, 감지 결과 병기):
§1 사전 확인(**게이트 4종 전부** — 요금제 Pro+·조직 승인·repo GitHub 연동(GitHub 전용)·계정 GitHub 연결) → §2 repo 준비물 / §3 루틴 생성(어떤 용도·어떤 트리거) / §4 정의 기록(docs/ROUTINES.md — 기본 체크 권장). **운용 프로필이 절약이면** 스케줄 빈도를 보수적으로(주간부터) 제안 — 루틴 실행도 구독 사용량을 소모한다.

**STEP 3 — 체크된 항목만 실행.**

**STEP 4 — 검증·보고.** 각 항목 §"검증"을 **증거(출력)로 보고**. 프로젝트 파일(`.mcp.json`·docs/ROUTINES.md)은 커밋 안내. **적용 기록**: 대상 `CLAUDE.md` 맨 아래 `<!-- dropin-applied: … -->` 한 줄에 이 문서의 **현재 버전**(최상단 "문서 버전" 표기에서 읽음)을 `06 vX.Y(설치 항목)` 형식으로 추가/갱신한다(형식·재적용 규칙은 00 §A — 단독 적용 시에도 남긴다). **부분 완료·사용자 조치 대기도 괄호에 남긴다**(예: `06 vX.Y(준비물만 — 루틴 생성 게이트 차단(계정 GitHub 미연결))` — 게이트가 §3만 막는 **항목 차단**이므로 00 상태 어휘 표의 `게이트 차단(사유)` 형식을 쓴다. `확인 대기`로 적으면 재적용 때 §3 루틴 생성이 통째로 미설치라는 사실이 질문에 들어오지 않는다).

> 🔴 **STEP 1·2 게이트로 중단됐어도 기록은 남긴다** — `06 게이트 차단(사유)` 형식으로 한 줄. **사유로 스코프를 가른다**(00 §A STEP 5) — 조직 승인·GitHub 아님은 **팀 사실**이라 대상 `CLAUDE.md`에(예: `06 게이트 차단(조직 미승인)` · `06 게이트 차단(GitHub 아님)`), **요금제·계정 GitHub 연결은 그 사람 것**이라 `~/.claude/CLAUDE.md`에(예: `06 게이트 차단(요금제 미대상)`) 남긴다 — 개인 사유를 팀 줄에 적으면 전원이 상속한다. **대상에 `CLAUDE.md`가 없으면** 00 §A STEP 5의 최소 골격 규칙대로 승인 후 만들어 기록한다 — 단독 드롭인이면 그 규칙이 읽히지 않아 차단 사실이 통째로 사라진다. 중단이 STEP 4보다 앞이라 그냥 끝내면 기록이 없어 **다음 재적용 때 "미선택"과 구분되지 않고**, 조직이 나중에 승인해도 재확인 대상으로 잡히지 않는다(00 §A STEP 3 재적용 질문 ③이 이 어휘를 읽는다).

> 원칙: 승인 없이 루틴을 만들지 않는다. 시크릿은 루틴 환경변수에 넣지 않는다(전용 저장소 없음).

---

## 1. 사전 확인 (게이트 — 통과 못 하면 설치하지 않음 · 설치 항목 아님)

- [ ] 요금제 Pro 이상 + `claude.ai/code` 사용 가능(Claude Code on the web).
- [ ] **회사 repo면**: 조직이 클라우드 실행(코드 반출)을 허용하는가? Team·Enterprise는 admin의 Routines 토글이 켜져 있는가? — 확인 전 진행 금지 🔴.
- [ ] repo가 GitHub에 있고 Claude GitHub App 연동 가능한가(루틴은 매 실행 기본 브랜치를 clone, `claude/` 접두 브랜치로 푸시). **GitHub 이벤트 트리거를 쓸 거면 App이 이미 설치돼 있어야 한다** — 미설치여도 루틴 생성·수동 검증은 통과하고 **첫 실제 이벤트에서야 발화하지 않는 것을 알게 되므로**, 여기서 설치 여부를 먼저 확인한다.
- [ ] **claude.ai 계정에 GitHub 연결이 끝나 있는가**(claude.ai/code 설정 또는 Claude GitHub App 설치) — repo 연동 가능 여부와 별개인 **계정 레벨** 연결. 없으면 프롬프트·스케줄을 다 만든 뒤 마지막 **루틴 저장이 HTTP 401**(`Connect your GitHub account…`)로 거부된다. 현행 `/schedule`은 연결 여부를 **먼저 확인하고 `/web-setup` 실행을 안내**한다 — 단 `/web-setup`은 clone 권한만 주고 **GitHub App 설치·웹훅 전달은 별도**다.
- [ ] **이 작업을 무인으로 돌려도 되는가** — 위 넷은 전부 **자격·승인** 축이고 이것만 **작업 위험도** 축이다. 루틴 세션에는 권한 모드 선택도 승인 프롬프트도 없어서, 실행 중에 물어보지 않는다. **되돌리기 어려운 일**(배포·삭제·외부 발송·비가역 API 호출)을 프롬프트에 넣지 않는다. 자격이 다 갖춰졌다고 해서 맡겨도 되는 일인 것은 아니다.
- [ ] **명령이 실제로 보이는가** — 텔레메트리·추적을 끄는 환경변수가 하나라도 켜져 있으면 위 게이트를 다 통과해도 `/schedule`이 **아예 없는 명령처럼 숨는다**(`Unknown command`). 이 저장소는 절약·프라이버시 설정을 권하므로 **자기모순이 나기 쉬운 자리**다 — 원인 불명으로 막히면 게이트를 다시 뒤지기 전에 여기를 먼저 본다.

---

## 2. repo 준비물 (루틴이 클라우드에서 제대로 돌기 위한 구성 — 이 문서의 생성물 ①)

### 감지
네 준비물이 이미 있는지 각각 본다 — `.mcp.json`이 있고 **프로젝트 스코프로 커밋**돼 있는지(`git ls-files .mcp.json`) · `CLAUDE.md`와 `.claude/settings.json`이 커밋돼 있는지 · `git ls-files`에 `.env`류가 올라와 있지 않은지 · `CLAUDE.md`에 테스트·린트·빌드 명령이 적혀 있는지. **이미 있는 것은 말없이 덮어쓰지 않고 유지/재구성/교체를 묻는다** — 특히 `.mcp.json`은 루틴과 무관한 서버를 이미 담고 있을 수 있다.

### 설치
- **MCP**: 루틴이 쓸 MCP 서버는 **`.mcp.json`(프로젝트 스코프)으로 커밋**돼 있어야 한다(`claude mcp add --scope project`) — 개인 스코프(CLI 로컬 등록)는 클라우드에서 안 보인다. 03 §4 절차 재사용.
- **CLAUDE.md·`.claude/settings.json`**: clone에 포함되므로 그대로 적용된다 — 01의 기록 규칙·deny가 클라우드 실행에도 유효(루틴 작업도 PROGRESS에 남기게 하려면 CLAUDE.md 규칙이 곧 지시서).
- **시크릿**: 루틴 환경변수는 비권장(가시적) — 커넥터 인증은 Anthropic 프록시 경유가 원칙, 그 외 자격증명이 필요한 작업은 루틴에 맡기지 않는다.
- **검증 명령**: 루틴 프롬프트에 "증거로 보고" 규칙(01 §F-1)을 포함할 수 있도록 테스트·린트 명령이 repo 문서(CLAUDE.md)에 적혀 있는지 확인.

### 검증
위 준비물 4종을 **순서대로 하나씩** 증거로 보고한다 — ① **MCP를 쓰는 루틴이면** `.mcp.json`이 커밋돼 있는지 + **연결까지는 §3 `/schedule run` 세션 로그에서 그 MCP 도구 호출이 실제로 성공했는지**로 확인한다(`claude mcp list`는 등록만 보이는 감지 명령이라 연결 실패를 못 잡는데, 무인 실행은 사람이 볼 자리가 없다). **안 쓰는 루틴이면**(예: 문서 드리프트 점검) "MCP 해당 없음"을 명시한다 — 준비물이 없는 것과 확인하지 않은 것은 다르다 ② `CLAUDE.md`·`.claude/settings.json`이 **repo에 커밋돼 clone에 포함되는지**(클라우드 실행은 clone만 받는다 — 미커밋이면 기록 규칙·deny가 통째로 빠진 채 돈다) ③ **시크릿이 평문으로 없는지** — 조회 가능한 두 증거로 낸다: **루틴 환경변수 목록이 비어 있음**(사용자 화면 확인)과 **`git ls-files`에 `.env`류가 없음**. 시크릿 파일을 열어 확인하지 않는다(커넥터 인증은 프록시 경유, 그 외 자격증명이 필요한 작업은 애초에 루틴 대상이 아니다) ④ 루틴이 실행할 **테스트·린트·빌드 명령이 `CLAUDE.md`에 적혀 있는지**(무인 실행에는 사람이 옆에서 알려줄 수 없다) — **01 미적용이라 `CLAUDE.md`가 없거나 최소 골격뿐이면** 명령을 루틴 프롬프트에 직접 적고 그 사실을 STEP 4 기록에 남긴다(06 단독 설치가 표준 경로라 이 분기 없이는 항상 실패한다). `docs/ROUTINES.md`·`.gitattributes`는 §4의 생성물이라 **§4 검증에서 확인한다** — 여기서 요구하면 §2 시점엔 아직 없어 항상 실패한다. 같은 이유로 **§3을 이번에 하지 않으면**(미선택·게이트 차단 — 계정 게이트는 §3만 막으므로 §2 단독은 정식 경로다) ①의 연결 확인과 ③의 환경변수 목록은 `루틴 생성 시 확인 대기(사유, YYYY-MM-DD~)`로 남기고 `.mcp.json` 커밋·`git ls-files` 증거만 낸다.

### 제거
루틴을 폐기했으면 `.mcp.json`에서 **그 루틴 전용으로 추가한 서버만** 제거한다(다른 용도로도 쓰는 서버는 남긴다). 파일이 비면 삭제하고 커밋 안내.

---

## 3. 루틴 생성 (계정 단위 — 파일이 아니라 절차)

### 감지
`/schedule list`로 **이 repo를 대상으로 하는 루틴이 이미 있는지** 본다(계정 전체 목록이므로 다른 repo 것과 섞인다 — 대상 repo로 걸러 판단한다). 있으면 새로 만들지 않고 **유지/재구성(`/schedule update`)/교체**를 묻는다. `docs/ROUTINES.md`(§4 생성물)가 있으면 그 기록과 실제 목록을 함께 대조한다 — 둘이 어긋나 있으면 그 사실 자체가 먼저 보고할 발견이다.

### 설치
**트리거 3종** (스케줄·GitHub는 CLI로 생성 가능 — GitHub는 v2.1.225+. **웹 전용은 API 트리거뿐** — 웹 `claude.ai/code/routines`):

| 트리거 | 생성 | 용도 예 |
| --- | --- | --- |
| **스케줄** | `/schedule <자연어>` (별칭 `/routines`) — 최소 간격 1시간, 일회성도 가능. cron 식은 `/schedule update` | 주간 문서 드리프트 점검, 백로그 정리 |
| **API** | 웹에서 생성 → `POST .../routines/{id}/fire` + 전용 bearer 토큰(트리거 전용·루틴별) | 모니터링 알림 → 스택트레이스 조사 → draft PR |
| **GitHub** | 웹 또는 `/schedule`(v2.1.225+, Claude GitHub App 선설치) — PR opened/merged·release 등 이벤트 + 필터(author·label·정규식) | PR 열릴 때 팀 체크리스트 리뷰, 머지 시 타 SDK 포팅 |

- 생성 시 함께 정하는 것: 대상 repo(복수 가능)·프롬프트·필요 커넥터(**기본 전체 포함이므로 불필요한 커넥터는 제거** — 최소 권한)·클라우드 환경(네트워크 기본 Trusted: 패키지 레지스트리+GitHub만).
- 프롬프트 작성 규칙: 완료 조건과 산출물(PR/코멘트/요약)을 명시하고, **01 §F-1의 "증거로 보고"를 프롬프트에 포함**한다. API 트리거의 payload는 프롬프트에서 명시적으로 참조해야 반영된다.
- 관리: `/schedule list`(조회) · `/schedule run`(즉시 실행) · `/schedule update`(수정) · 웹에서 중지/삭제. 실행 이력은 CLI에서 자연어로 물을 수 있다(예: `/schedule 어젯밤 루틴이 왜 아무것도 안 했나?` — v2.1.227+).

### 검증
`/schedule run`(또는 웹의 Run now)으로 1회 실행 → 세션 URL(`claude.ai/code/session_<id>`)과 산출물(PR·코멘트 등)을 증거로 보고. **커넥터가 최소화됐는지도 확인한다** — 기본이 전체 포함이라 확인하지 않으면 불필요한 반출 경로가 검증을 통과한다(제거 결과는 §4 기록에 남긴다).

> **실행 목록의 초록색은 "성공"이 아니다.** 세션이 인프라 오류 없이 시작해 끝났다는 뜻일 뿐이다. 프롬프트가 시킨 일이 실제로 됐는지는 **세션을 열어 전사를 읽어야** 안다 — 차단된 네트워크 요청, 없는 커넥터 도구, 작업 자체의 실패는 전부 상태 표시가 아니라 전사에 나타난다. 세션 URL을 증거로 낼 때 **그 URL에서 무엇을 확인했는지**까지 함께 보고한다.

### 제거
웹 `claude.ai/code/routines`에서 루틴 삭제(API 토큰도 함께 무효화됨). docs/ROUTINES.md의 해당 항목엔 "폐기" 표시(append 원칙).

---

## 4. 정의 기록 — `docs/ROUTINES.md` (이 문서의 생성물 ②, 기본 권장)

### 감지
`docs/ROUTINES.md`가 이미 있는지, `.gitattributes`에 `docs/ROUTINES.md merge=union` 줄이 이미 있는지 본다. 파일이 있으면 **덮어쓰지 않는다** — append 전용 기록이라 교체는 이력을 지우는 것과 같다. 항목만 이어 붙인다.

### 설치
루틴 정의는 **claude.ai 계정에만 저장**되고 repo에 남지 않는다 — 계정 이전·삭제·팀원 공유 시 재생성할 근거가 없다. 그래서 만들 때마다 repo에 기록한다:

```markdown
# ROUTINES (루틴 정의 백업 · append 전용)
## <루틴 이름>
- 트리거: <스케줄 식 / API / GitHub 이벤트+필터> · 생성: 2026-08-04 · 소유: @<계정>
- repo·커넥터: <대상 repo, 허용한 커넥터만>
- 프롬프트: (전문 붙여넣기)
- 비고: <한도·중지 조건 등>
```

- 루틴은 **개인 계정 소유**(팀 공유 없음) — 팀원이 같은 루틴이 필요하면 이 기록으로 각자 생성한다.
- 수정 시 항목을 고치지 말고 새 항목 append + 구 항목에 "Superseded" 표시(01 기록 원칙과 동일).
- append 전용 선언의 충돌 안전망: **`.gitattributes`에 `docs/ROUTINES.md merge=union` 한 줄**을 추가한다(01 §F-4 기본 4줄엔 이 파일이 없다 — 다중 PC·팀 동시 append 대비).

### 검증
`docs/ROUTINES.md`가 생성·커밋되고 §3에서 만든 루틴이 기록돼 있는지 + **`.gitattributes`에 `docs/ROUTINES.md merge=union` 줄이 실제로 들어갔는지** + **정의 템플릿의 `repo·커넥터` 줄에 허용한 커넥터가 실제로 적혔는지**(§3에서 최소화한 결과가 여기 안 남으면 계정 이전·재생성 때 전체 커넥터 기본값으로 되돌아간다) 확인해 보고(이 줄이 빠지면 다중 PC 동시 append에서 충돌한다 — 위에서 새로 배선한 항목이라 검증에서 빠지기 쉽다).

**그리고 `docs/ROUTINES.md`의 항목과 `/schedule list`의 실제 목록을 대조한다** — 생성 여부만 보면 이 파일은 만들어진 그날만 맞고 그 뒤로는 조용히 낡는다. 파일에는 있는데 계정에 없는 루틴(누가 지웠다), 계정에는 있는데 파일에 없는 루틴(누가 웹에서 만들었다) 양쪽을 보고한다. 이 대조가 이 절을 **기록**에서 **드리프트 탐지**로 만든다.

### 제거
파일은 **지우지 않는다**(append 전용 기록 — 폐기한 루틴도 이력이다). 폐기 시 해당 항목에 "폐기" 표시만 남긴다(§3 제거와 짝). `docs/ROUTINES.md`가 남아 있는 한 `.gitattributes`의 `merge=union` 줄도 남긴다 — 파일은 남는데 안전망만 없애면 다중 PC 동시 append에서 충돌이 부활한다. 파일까지 지울 때만 그 줄을 함께 정리한다.

---

## 5. 비용·한도와 로컬 대안 (안내만 — 설치 항목 아님)

- **비용**: 실행마다 구독 사용량 소모 + 계정별 **일일 실행 횟수 상한**(요금제별, 수치는 공식 미공표 — `claude.ai/settings/usage`에서 확인). 일회성(one-off) 실행은 일일 상한에 셈하지 않고, 상한 초과분은 usage credits로 실행 가능(Team·Enterprise는 admin 활성화). 절약 프로필이면 빈도를 주간부터 시작해 필요 시 좁힌다.
- **로컬 대안과의 경계**: 로컬 파일·로컬 도구가 필요하면 루틴이 아니라 **Desktop 예약 작업**(로컬 실행, 최소 1분)이나 세션 내 **`/loop`**(프로토타이핑·단기 폴링)를 쓴다. 루틴은 "머신이 꺼져 있어도 돌아야 하는 것 + GitHub/API 이벤트 반응"에만.
- 실행 환경 참고: Ubuntu VM(4 vCPU·16GB·30GB), 주요 런타임 사전 설치, 권한 프롬프트 없는 무인 실행(auto 승인 아님 — 사전 설정된 범위만).

---

## L. 유지보수 (research preview — 3개월마다 권장, 최소 6개월)
- 기능 상태(preview→GA 전환, 요금제 범위), `/schedule` 명령 체계 유지 여부 — code.claude.com/docs/en/routines.
- 트리거 종류·최소 간격(현행 1시간)·GitHub App 요건, 일일 실행 상한 정책.
- 클라우드 환경 스펙(네트워크 정책·시크릿 저장소 신설 여부 — 신설되면 §2 시크릿 절 갱신).
- 조직 admin 토글 위치·기본값.
- **자기 점검(갱신 의식)** — 재검증·개정은 기억이 아니라 **당일 공식 문서** 조회로 한다(확인 못 한 항목엔 🟡을 단다). 고쳤으면 ① 버전 표에 한 줄(무엇을 왜) ② 헤더·푸터의 버전과 "최종 갱신"·"최근 재검증" 날짜 ③ 저장소 `CLAUDE.md` 구성물 표 ④ 이 체크리스트 ⑤ 기계 검사 통과를 빠짐없이 한다. **절 번호는 배포된 `dropin-applied` 기록이 참조하므로 다시 매기지 않는다** — 설치하지 않는 절은 제목에 `설치 항목 아님`을 단다(`docs/DOC_CONTRACT.md` §5).

---

**문서 정보** — 루틴 자동화 **v1.15** · 최종 갱신: 2026-08-31 · 최근 재검증: 2026-08-20 · 참조: Claude Code v2.1.237 (routines: research preview).
역할 경계: 클라우드 무인 실행은 이 문서, 편집 즉시 hooks·머지 전 CI 게이트는 04, 헤드리스 CLI 실행은 02 §F — 중복 서술하지 않는다. 루틴 정의는 계정 소유라 repo에 파일로 커밋할 수 없으므로 §4 기록이 재생성의 정본이다.
