# 모델 자동 분담 — 범용 적용 플레이북

> **문서 버전: v1.31** · 최종 갱신: **2026-08-20** · **최근 재검증: 2026-08-20** · 기준: Claude Code v2.1.237 (Opus 5 · Sonnet 5 · Fable 5)
>
> | 버전 | 날짜 | 변경 내용 |
> | --- | --- | --- |
> | v1.0~v1.16 | 2026-07-03~2026-08-12 | 전용 플랜 → 범용 플레이북 초판 → 재검증 반복(advisor·1M·`availableModels`·opusplan·Opus 5·중첩 스폰 깊이) → 병렬 실행(`isolation: worktree`)·팬아웃 대안 → §0 기본 원칙·문서 번호 재편 → 부분 선택+**리뷰 정본=내장 `/code-review`** → §F 절약 배정(Sonnet+advisor) → 적용 기록(도입·자기 참조화·부분 적용 제외 사유) → 비개발 repo 가드, STEP 3 parity-tester 기본 해제·동명 에이전트 질문. 행별 상세는 저장소 git 이력 |
> | v1.17 | 2026-08-12 | 역할 중복 감지를 **이름 무관으로 확장**(기존 에이전트 `description` 대조 후 병기 질문), §E 개인(글로벌) 배치 시 `~/.claude/CLAUDE.md` 기록 병기 |
> | v1.18 | 2026-08-12 | STEP 4 **§검증 신설**(에이전트 로드·산출물 경로 증거), §E **spec-writer 템플릿**+활동 유형별 로스터 매핑, 핸드오프 산출물 파일명 고정(v1.20이 완화), 비개발 가드 `해당 없음` 기록, **§L 신설** |
> | v1.19 | 2026-08-12 | 역할 중복 대조 기준을 **ⓑ 로스터 전체**로(개수 고정 금지), STEP 4 순서 정정(승인→검증·기록→착수)+버그헌팅형 병기, §F에 요구 확정(spec) 행 |
> | v1.20 | 2026-08-12 | 핸드오프 산출물 **이름 고정 완화** — 신규는 기본 이름, 기존 이름이 있으면 유지하고 기록 괄호에 실제 이름(다음 감지가 그 값을 씀) |
> | v1.21 | 2026-08-12 | STEP 4 검증의 핸드오프 이름 하드코딩 제거(ⓓ에서 정한 실제 이름으로 검증) |
> | v1.22 | 2026-08-13 | **활동 유형 어휘 통일**(§E 이름을 정본으로 §C·§D-1 동기화 — 이 값이 STEP 3 ⓑ 기본 체크를 정함) |
> | v1.23 | 2026-08-13 | STEP 3 ⓑ **배치 스코프 질문**(프로젝트/개인)+**병렬 제안**(§C 기준 충족 시 `isolation: worktree` 선택지), §C 병렬 분할 기준, implementer 템플릿 worktree 주석, §F 절약 불변에 spec+advisor 불가 환경 폴백 |
> | v1.24 | 2026-08-13 | **최적화**(릴리즈 정비): 버전 표·본문 서술 압축(규칙 불변) |
> | v1.25 | 2026-08-20 | STEP 4 검증에 **`isolation: worktree` 키 실측**(병렬 선택 시 — name·model만 보면 누락이 통과), §I **Fable 5 가격 $10/$50 기재**(공식 확인) + §L에 가격 축 |
> | v1.26 | 2026-08-20 | 로스터 **개인(글로벌) 배치의 PC 전역 전파 경고** — 스택 고정 명령이 다른 repo에서 실행된다(01 §F-2 스킬 교체 경고와 같은 취지) |
> | v1.27 | 2026-08-20 | 전면 재검증(v2.1.237): §E 서두에 서브에이전트 **동시 실행 상한 20·중첩 스폰 깊이 3** 배선(§L이 가리키던 자리에 서술이 없었다). §I 라인업·advisor 수치 유효 |
> | v1.28 | 2026-08-20 | 재검증 2회차: §E 상한 초과는 **큐 대기가 아니라 스폰 실패**(정정), §F advisor 기준선 **Sonnet 단독 대비**로 정정·**Fable 5 advisor 복귀**(v2.1.232), §C 1M 목록을 §I와 일치, §I에 **Sonnet 5 $2/$10**(9/1 인상 취소)·Haiku 4.5 단가·토크나이저 보정·`ANTHROPIC_DEFAULT_MODEL` |
> | v1.29 | 2026-08-20 | 확장 후보 편입: §E 말미에 **다이내믹 워크플로**(로스터=누구에게 / 워크플로=어떤 순서로 몇 명을)·§F에 규모 상한, §H에 **에이전트 팀 경고**(로스터 분담 무력화), §L에 소비자 축 2건 |
> | v1.30 | 2026-08-20 | **최적화**: 버전 표·본문 압축(규칙 불변) |
> | v1.31 | 2026-08-20 | **최적화**: R20 적용 — 구 행을 v1.0~v1.16을 축 요약 한 줄로 |
> ※ 갱신 시: 이 표에 한 줄 추가 + 하단 "문서 정보" 날짜 수정 + §L 재검증 체크리스트(§I 모델 라인업 최신화는 §L 항목에 포함 — 여기서 중복 열거하지 않는다).

> **사용법**: 이 파일을 아무 프로젝트의 루트(또는 `docs/`)에 넣고 Claude에게
> **"이 플레이북을 이 프로젝트에 적용해줘"** 라고 말하면 된다.
> Claude는 ①프로젝트를 스스로 학습 → ②모르는 것만 질문 → ③이 프로젝트에 맞춘 구성 생성 → ④검증·**적용 기록** 후 착수 한다.

**이게 뭐 / 왜 / 결과**
- **무엇**: 어려운 추론(분석·설계)은 **상위 모델(예: Opus)**, 잘 정의된 실행은 **하위 모델(예: Sonnet/Haiku)** 로 자동 분담하는 작업 파이프라인을, **이 프로젝트에 맞게** 세팅한다.
- **왜**: **1순위는 최적의 결과물, 토큰/비용 절약은 품질을 해치지 않는 전제의 2순위다.** 그래서 어려운 추론은 아끼지 않고 상위 모델에 주고, 잘 정의된 실행만 싸게 돌린다. 프로젝트마다 스택·기능·성격이 다르므로 **고정 템플릿이 아니라 학습+질문으로 맞춘다.**
- **결과**: `.claude/agents/`(모델별 서브에이전트) + `docs/`(난이도 기준·핸드오프 규약)이 만들어지고, 1단계부터 실행 가능.

---

## A. 불변 원칙 (모든 프로젝트 공통)

이 5가지는 프로젝트가 무엇이든 항상 지킨다.
1. **문서 고정 핸드오프** — 단계 산출물을 파일로 남기고 다음 단계는 `@파일`로 읽는다. 대화 요약으로 넘기지 않는다(컨텍스트 손실이 최대 실패 원인).
2. **트리아지는 상위 모델** — "무엇이 어렵고 무엇이 쉬운가" 분류가 최고 레버리지. 여기서 틀리면 하류 전체가 오염된다.
3. **검증 스테이지 필수** — 상위 모델의 diff 리뷰 + 정답 대조(테스트/파리티). "검증기가 모델보다 중요."
4. **난이도 기반 라우팅** — 강한 모델=추론(분석/설계/엣지), 약한 모델=실행(정형/반복). 잘 정의된 것만 싸게.
5. **자동화 천장 인정** — 완전 자동은 없다(~70~85%). 계획·비기능요구(성능/보안/구조)·최종 리뷰는 사람이 소유.

---

## B. Claude 실행 절차 (이 문서를 "적용"하면 이 순서대로 한다)

### STEP 1 — 프로젝트 자동 학습 (스스로)
**적용 대상 가드(선행)**: 이 문서는 **개발 프로젝트 전용**이다. 스캔 결과 코드·빌드가 없는 문서 전용/비개발 repo면 로스터를 만들지 말고 그 사실을 고지하고 중단한다(필요한 건 보통 01·04). **중단해도 기록은 남긴다** — 대상 `CLAUDE.md`에 `미선택: 02(해당 없음—비개발 repo)`(00 §A STEP 5 어휘). 안 남기면 재적용 질문 ②가 구조적으로 적용 불가한 02를 매번 다시 묻는다. **대상에 `CLAUDE.md`가 없으면** 00 §A STEP 5의 최소 골격 규칙대로 승인 후 만들어 기록한다 — 단독 드롭인이면 그 규칙이 읽히지 않아 중단 사실이 통째로 사라진다.

아래를 스캔해 프로젝트를 파악한다. **먼저 코드/설정으로 알 수 있는 건 질문하지 말고 직접 확인한다.**
- **스택/런타임**: 설정 파일 감지 — `package.json`·`pom.xml`·`build.gradle`·`composer.json`·`requirements.txt`·`go.mod`·`Cargo.toml` 등 → 언어·프레임워크·버전.
- **빌드/테스트/CI**: 빌드·테스트 명령, 기존 테스트 유무, CI 설정.
- **구조/도메인**: 디렉터리 트리, 주요 모듈, 엔트리포인트, README·CLAUDE.md·docs.
- **규모/성격**: 대략 파일 수·크기, 사내툴/외부서비스/라이브러리 여부.
- **경계/시크릿**: 건드리면 안 되는 영역, 시크릿 위치(읽지 않는다).
- **git**: 브랜치·원격·미커밋 여부.

### STEP 2 — 부족분 질문 (모르는 것만 물어본다)
코드로 확인 **불가능한 것만** 골라 §D의 질문을 **한 번에 묶어** 묻는다. 각 질문엔 **추천 기본값**을 함께 제시해 사용자가 빠르게 고르게 한다. (다 물어보지 말 것 — 이미 파악한 건 "이렇게 이해했다"고 확인만.)

### STEP 3 — 구성 항목 선택 확인 → 이 프로젝트에 맞춘 구성 생성
생성 전에 아래를 **선택 목록(다중 선택)으로 제시**하고 체크된 것만 만든다. 로스터도 통짜가 아니라 **에이전트별로 선택**하게 한다(활동 유형에 맞는 것만 기본 체크 — 예: 신규 개발이면 surveyor/triage 기본 해제, **테스트 명령 미감지면 parity-tester 기본 해제** — 돌릴 오라클이 없다. 해제 사유는 STEP 4 기록 괄호에 병기). **이미 동명 `.claude/agents/` 파일이 있으면 유지/재구성/교체를 묻는다**(말없이 덮어쓰지 않음 — 00 없이 단독 드롭인일 때도 적용). **이름이 달라도 역할이 겹치면 같이 묻는다** — 기존 에이전트의 `description`을 읽어 **ⓑ의 로스터 전체**와 대조해(예: `code-explorer`≈surveyor, `qa-runner`≈parity-tester, `spec-author`≈spec-writer) 겹치는 쌍을 병기해 확인받는다 — **개수를 고정해 적지 않는다**(로스터가 늘면 이 문장만 낡아 새 에이전트의 중복을 못 거른다). 파일명만 보면 기능 중복 에이전트가 말없이 추가돼 라우팅이 갈린다(00 §A STEP 3 불변 ①의 "유사 구성" 판정이 여기서 이뤄진다).
- ⓐ 난이도 rubric(§C) ⓑ 로스터 — surveyor / triage / analyst / implementer / **spec-writer** / parity-tester 개별 선택(§E) + **배치 스코프 질문**(팀 공유=프로젝트 `.claude/agents/`(커밋) / 개인 실험=`~/.claude/agents/` — §E 서두. 개인이면 적용 기록도 글로벌 병기) ⓒ 커스텀 reviewer(기본 해제 — 내장 `/code-review`로 충분한지 §E reviewer 주석 기준으로 판단) ⓓ 핸드오프 문서 규약
- **ⓐ → 난이도 rubric**을 이 프로젝트 기준으로 작성(§C 참고) → `docs/난이도-매트릭스.md` 골격.
- **ⓑ → 선택된 에이전트 + 모델 배정** → `.claude/agents/` 에 생성(§E 템플릿을 이 스택에 맞게 채움: `tools`·빌드/테스트 명령·용어).
- **ⓒ → 커스텀 reviewer**를 골랐을 때만 `.claude/agents/reviewer.md` 생성(기본은 내장 `/code-review`가 정본이라 만들지 않는다).
- **병렬 제안(ⓑ에 딸림)**: 규모가 크고 슬라이스가 독립적이면(§C 규모 행의 기준) implementer 병렬 투입(frontmatter `isolation: worktree` — §E 서두)을 선택지에 함께 제시한다 — 여기서 제안하지 않으면 병렬 구성은 설치 절차 어디서도 만들어지지 않는다.
- **ⓓ → 오케스트레이션 순서 + 핸드오프 문서 규약**(어느 문서에 무엇을 남기는지) → 신규는 **`docs/핸드오프-규약.md`**를 기본 이름으로 쓴다(00 §A STEP 2 흔적 감지의 기본 후보). **이미 다른 이름으로 있으면 그대로 둔다** — `docs/모델-분담-규약.md`처럼 기존 적용본이 쓰는 이름이 있고, 이름을 바꾸면 그 프로젝트의 기존 참조가 전부 끊긴다. 그때는 **기록 괄호에 실제 이름을 남겨**(예: `02 vX.Y(핸드오프=docs/모델-분담-규약.md)`) 다음 감지가 그 값을 쓰게 한다.

### STEP 4 — 확인 후 착수
구성 요약을 보고하고 **승인**을 받는다. 승인 없이 대량 변경하지 않는다. **먼저 검증**: 생성한 에이전트가 실제로 로드되는지 목록(`/agents` 또는 `.claude/agents/` 파일 경로 + 각 frontmatter의 `name`·`model`)과 산출물 경로(`docs/난이도-매트릭스.md` · **핸드오프 규약 문서는 ⓓ에서 정한 실제 이름**으로 — 기본은 `docs/핸드오프-규약.md`이나 기존 적용본은 다를 수 있어 여기서 이름을 고정하면 검증이 "없음"을 오보한다)를 **출력으로 보고**한다. **양 스코프에 동명 에이전트가 있으면 개인 쪽이 이기므로**(enterprise > personal > project) 뜬 것이 방금 만든 스코프의 것인지까지 확인한다. **병렬(worktree)을 골랐으면 implementer frontmatter의 `isolation: worktree` 키도 실측한다** — `name`·`model`만 보면 병렬 선택이 빠져도 검증을 통과한다. **검증·적용 기록을 마친 뒤** 활동 유형의 1단계부터 착수한다(전환/리팩터형=survey·분석, 신규개발형=spec, 감사형=finder, **버그헌팅형=재현(surveyor)** — §C·§E 서두 매핑). **적용 기록**: 대상 `CLAUDE.md` 맨 아래 `<!-- dropin-applied: … -->` 한 줄에 이 문서의 **현재 버전**(최상단 "문서 버전" 표기에서 읽음)을 `02 vX.Y` 형식으로 추가/갱신한다(형식·재적용 규칙은 00 §A — 단독 적용 시에도 남긴다. **대상에 `CLAUDE.md`가 없으면** 00 §A STEP 5의 최소 골격 규칙대로 승인 후 만들어 기록한다 — 기록이 없으면 `/dropin-check`가 문서 비교를 생략해 영영 최신화 대상에서 빠진다). **부분 적용(일부 에이전트만)이면 어느 에이전트를 왜 뺐는지 괄호에 남긴다**(예: `02 vX.Y(analyst·implementer만 — parity-tester는 테스트 프레임워크 부재)`) — 02 산출물은 로컬 전용일 수 있어 이 기록이 없으면 나중에 적용 범위를 알 수 없다.

---

## C. 프로젝트마다 "알아서 채우는" 적응 규칙

| 프로젝트 변수 | 어떻게 조정 |
| --- | --- |
| **지배적 활동** (`전환/리팩터형` · `신규개발형` · `감사형` · `버그헌팅형` — §D-1이 이 이름으로 묻고 §E가 이 이름으로 로스터를 매핑한다) | 에이전트 로스터 형태를 바꾼다. 전환·리팩터=survey→triage→analyst→implement→review. 신규 개발=spec→implement→review. 감사=finder→verifier. 버그헌팅=재현→원인→수정→회귀. **각 형태를 §E 로스터의 어느 에이전트로 채우는지는 §E 서두의 매핑을 따른다**(로스터에 없는 이름을 즉석에서 만들지 않는다). |
| **스택/빌드** | 에이전트 `tools`와 빌드·테스트·린트 명령을 그 스택에 맞춘다(예: `./gradlew test`, `npm test`, `pytest`). |
| **규모** | 크면 슬라이스를 잘게·병렬로, 작으면 단일 흐름. **병렬로 나누는 기준**: 슬라이스끼리 같은 파일·모듈을 건드리지 않고 순차 의존이 없을 때만 — 겹치면 순차가 싸다(충돌 해소 비용이 병렬 이득을 지운다). **병렬 도구**: 파일을 고치는 에이전트 병렬은 frontmatter `isolation: worktree`(각자 격리 checkout — 충돌 원천 차단), 사람 주도 세션 병렬은 `claude --worktree <이름>`. 초대형 코드베이스는 1M 컨텍스트 활용 — 단 **Anthropic API에선 Fable 5·Sonnet 5·Opus 4.7 이상(4.7·4.8·5)이 기본 1M**이라 suffix 불필요(구독 Max·Team·Enterprise는 Opus 자동 1M). `[1m]` suffix(`opus[1m]`·`opusplan[1m]`)는 게이트웨이·구형 모델·비자동 티어에서만 의미. |
| **성격** (사내툴 / 외부 서비스 / 라이브러리) | 검증의 NFR 가중치 조정(외부 서비스=보안·성능↑, 라이브러리=API 호환·문서↑). |
| **난이도 高 정의** | 그 스택의 위험 요소로 채운다. 예) 암호화·인증·동시성·상태머신·외부 연동 규약·거대·강결합 파일·성능 임계 경로. 易=CRUD·조회·정형 변환·보일러플레이트. |

---

## D. 질문 템플릿 (부족하면 이걸 묻는다 · 추천값 포함)

> 이미 코드로 확인된 항목은 **질문 대신 "이렇게 이해했다"로 확인**만. 아래는 보통 코드로 알 수 없는 것들.

1. **목적/활동**: 이 작업의 목적은? — **§E 로스터 매핑과 같은 이름으로 답을 받는다**: `전환/리팩터형` · `신규개발형` · `감사형` · `버그헌팅형`(전환과 리팩터는 같은 로스터라 한 값이다). 이 답이 STEP 3 ⓑ의 기본 체크를 정하므로, 여기서 다른 낱말을 쓰면 매핑이 갈린다.
2. **타깃/제약**: 목표 스택·버전이 있나? 반드시 지켜야 할 제약(DB 스키마·외부 API·프로토콜·규약)은?
3. **범위/우선순위**: 먼저 할 영역과 제외 영역은? (추천: 저위험·읽기중심부터)
4. **모델/비용**: 쓸 수 있는 모델은? (예: opus/sonnet/haiku — 요금제에 따라 다름, `/model` 목록으로 확인) **운용 프로필은 표준/절약 중 무엇인가?**(00에서 이미 골랐으면 재확인만. Pro·한도 관리는 절약 권장 — §F 절약 배정 적용)
5. **검증(정답)**: 기존 테스트/CI가 있나? "정답(oracle)"을 무엇으로 삼나? (기존 동작 재생·골든테스트·수동확인 중)
6. **팀/기록**: 다중 개발자인가? 작업 기록을 어디에 남기나? (추천: `docs/` append 전용)

---

## E. 에이전트 로스터 템플릿 (Claude가 `<…>`를 채운다)

> 배치: 팀 공유면 프로젝트 `.claude/agents/`, 개인 실험이면 `~/.claude/agents/`.
> ⚠️ **개인(글로벌) 배치는 그 PC의 모든 프로젝트에 로드된다** — 로스터 본문에 스택 고정 명령(`./gradlew test` 등)이 들어가면 다른 스택의 repo에서 그대로 실행된다. 기본은 프로젝트 배치로 보고, 개인 배치는 스택 무관한 역할에만 쓴다(§F-2 스킬 교체 경고와 같은 취지).
> **개인(글로벌) 배치를 골랐으면 적용 기록도 `~/.claude/CLAUDE.md`에 병기**한다(00 §A STEP 5의 글로벌 병기 규칙) — 프로젝트 기록에만 `02 vX.Y`가 남으면 파일은 그 repo에 없어 다른 PC 점검에서 "적용됐다는데 흔적 없음" 모순이 된다. 흔적 감지도 양 스코프를 본다.
> 모델 값: 별칭 `opus`·`sonnet`·`haiku`·`fable`, 전체 ID, 또는 `inherit`(생략 시 기본 = 메인 세션 상속). `tools`는 스택에 맞게 조정.
> 강도 차등이 필요하면 frontmatter에 **`effort:`**(low/medium/high/xhigh/max)를 추가 — 세션 effort를 그 에이전트 실행 중에만 오버라이드(공식 확정 키).
> 파일을 고치는 에이전트(implementer 등)를 **병렬**로 돌릴 땐 frontmatter에 `isolation: worktree` 추가 — 각자 격리된 checkout에서 작업해 파일 충돌이 원천 차단된다(변경 없으면 자동 정리). 격리 worktree는 기본적으로 **기본 브랜치에서 분기**한다(세션의 HEAD가 아님 — 미커밋 작업 위에서 이어 갈 거면 유의). 동시 실행 상한은 기본 20, 서브에이전트의 중첩 스폰은 **깊이 제한 기본 3**(v2.1.219+ — v2.1.217~218은 기본 1이었음).
> **실행 상한**: 서브에이전트 동시 실행 기본 **20**(`CLAUDE_CODE_MAX_CONCURRENT_SUBAGENTS`), 중첩 스폰 깊이 기본 **3**(`CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH`) — 상한을 넘긴 스폰은 **대기하지 않고 `Concurrent subagent limit reached`로 실패한다**(재시도 말라는 지시가 함께 전달되고, 실행 수가 내려가면 다시 스폰된다). 그래서 팬아웃 슬라이스를 20 초과로 짜면 초과분이 밀리는 게 아니라 **누락된다**. ultracode 세션은 이 상한에서 면제되고, fork·재개 서브에이전트는 계수되지 않는다.
> 아래는 **"전환/리팩터형" 기본 로스터**다. §C의 다른 활동 유형은 이 로스터로 이렇게 채운다 — **신규개발형**: survey/triage를 빼고 **spec-writer**(아래 템플릿)부터 → implementer → 리뷰. **감사형**: `finder`=surveyor·`verifier`=analyst로 **역할 프롬프트만 바꿔 쓴다**(파일을 늘리지 않는다). **버그헌팅형**: surveyor(재현 경로 수집) → analyst(원인 규명) → implementer(수정) → parity-tester(회귀). **로스터에 없는 이름을 즉석에서 창작하지 않는다** — STEP 3 ⓑ의 선택 목록과 어긋나 기록·감지가 갈린다.

```markdown
---
name: surveyor
description: <프로젝트> 구조 survey 전문. 모듈·의존성·엔트리포인트를 목록화. 넓은 조사에 적극 사용. 파일 수정 안 함.
tools: Read, Grep, Glob, Bash
model: sonnet
---
넓고 기계적으로 훑어 docs/survey/<area>.md 에 표·목록으로 남긴다. 판단·설계는 하지 않는다.
```
```markdown
---
name: triage
description: 난이도 분류 전문. survey + rubric으로 모듈별 난이도(高/低)·근거·권장모델·순서를 매긴다.
tools: Read, Grep, Glob
model: opus
---
입력 @docs/survey/*.md, rubric(<高 정의>)에 따라 판정 → docs/난이도-매트릭스.md. 애매하면 高로 올리고 이유를 남긴다.
```
```markdown
---
name: spec-writer
description: 신규개발형 1단계. 요구사항을 구현 가능한 스펙(인터페이스·데이터 모델·수용 기준)으로 확정. 코드는 쓰지 않음.
tools: Read, Grep, Glob
model: opus
---
요구·제약(§D 2·3)을 docs/spec/<module>.md 에 남긴다 — 공개 인터페이스, 데이터 모델, **수용 기준(무엇으로 완료를 판정하나)**, 미결 질문. 모호한 요구는 추측해 채우지 말고 미결 질문으로 올린다(§A "almost right"가 가장 비싼 실패다).
```
```markdown
---
name: analyst-hard   # (쌍) analyst-light 는 동일 형식에 model: sonnet
description: 高난이도 모듈 상세 분석·<타깃 스택> 설계. 구현 spec(성공기준·샘플 입출력·위험) 작성.
tools: Read, Grep, Glob
model: opus
---
입력 @docs/난이도-매트릭스.md 해당 모듈 + 관련 코드 → docs/spec/<module>.md. 기존 버그 보존 여부는 "사람 확인 필요" 표시.
```
```markdown
---
name: implementer
description: spec 기반 <타깃 스택> 구현 전문(정형 모듈). spec 준비되면 적극 사용.
tools: Read, Write, Edit, Bash
model: sonnet
# 병렬 투입 시 isolation: worktree 추가(§E 서두 — 파일 충돌 원천 차단)
---
입력 @docs/spec/<module>.md(대화 요약 아님). 성공기준·샘플 만족하게 구현, 범위 밖 손대지 않음. 변경 요약 보고.
```
> **reviewer는 기본적으로 만들지 않는다(중복 정리)** — 일반적인 정확성 리뷰는 내장 **`/code-review`**(수동 호출·백그라운드 실행·설치 0)가 정본이다. 아래 커스텀 reviewer는 **프로젝트 특화 관점이 필요할 때만** 선택: spec 대조(의미 오역), 스택 고유 NFR 가중치, 도메인 규약 검증 등 내장이 모르는 기준이 있는 경우. 참고: 클라우드 멀티에이전트 확장판 **`/code-review ultra`**((구)`/ultrareview` — **전 플랜**: Pro·Max 무료 3회 후 usage credits 과금(회당 ~$5–25), claude.ai 인증 필요·Bedrock/Vertex/Foundry·ZDR 조직 불가. **관리형 PR 자동 리뷰 "Code Review"만 Team·Enterprise**)와 품질 정리 전용 **`/simplify`**(단순화·재사용만, 버그 탐지 아님, v2.1.154+)도 내장이다. `/review`는 `/code-review`의 별칭(v2.1.223+).

```markdown
---
name: reviewer   # 선택 — 내장 /code-review로 부족할 때만 (위 주석 참고)
description: 변환/구현 코드 diff 검증. 타입·널·관용구·상태·NFR(성능·보안) 관점. 구현 직후 적극 사용. 읽기 전용.
tools: Read, Grep, Glob, Bash
model: opus
---
spec과 diff를 대조해 의미 오역을 잡는다. 코드는 고치지 않고 무엇이/왜/어떻게 지적만.
정확성·요구사항에 영향 주는 것만 지적하고 스타일 취향은 제외한다(빈틈을 찾으라면 멀쩡한 코드에서도 찾아내므로 — 과설계 방지).
```
```markdown
---
name: parity-tester
description: 정답 대조(테스트/재생) 실행. 빠르고 저렴하게 통과/차이만 보고.
tools: Bash, Read
model: haiku
---
<테스트 명령>으로 검증하고 diff만 보고. 원인 분석은 상위 단계로.
```

> **영속 메모리(선택)**: 서브에이전트 frontmatter `memory: user|project|local`을 켜면 에이전트별 지식 디렉터리(`~/.claude/agent-memory/<이름>/` 등)가 생겨 세션을 넘어 패턴을 축적한다 — reviewer·analyst처럼 반복 투입되는 에이전트에 유용. auto-memory를 끄면(`autoMemoryEnabled: false`) 이 필드도 무효.

> **여러 에이전트를 코드로 지휘(선택)** — 팬아웃 순서·검증 단계를 **고정**하고 싶으면 다이내믹 워크플로: `.claude/workflows/<이름>.js`(프로젝트 공유)·`~/.claude/workflows/`(개인)에 스크립트를 두면 그 순서대로 서브에이전트를 돌린다(v2.1.154+, 전 유료 플랜 — **Pro는 `/config`의 Dynamic workflows 행에서 켠다**). 로스터가 "누구에게 맡길까"의 정본이라면 워크플로는 **"어떤 순서로 몇 명을"의 고정 장치**다 — 같은 절차를 반복 실행할 때만 값어치가 있고, 1회성 작업은 로스터 디스패치가 싸다. 조직 차단은 `disableWorkflows`.

---

## F. 기본 모델 배정 (사용자가 다르게 원하면 덮어씀)

| 단계 | 기본 모델 | 이유 |
| --- | --- | --- |
| survey / 정형 실행 / 테스트 | sonnet / haiku | 기계적·반복 → 싸게 |
| 요구 확정(spec) | **opus** | 스펙이 틀리면 하류 전부 재작업 |
| 난이도 분류(triage) | **opus** | 최고 레버리지 |
| 상세 분석·설계 | 低 sonnet / 高 **opus** | 난이도별 |
| 高난이도 구현 | **opusplan**(메인 세션) 또는 **`/advisor`** | opusplan=계획 Opus/실행 Sonnet 자동 · advisor=실행 중에도 Opus 온디맨드 자문 |
| 리뷰 | 내장 **`/code-review`** (기본) / 커스텀 reviewer는 선택 | 정확성 리뷰는 내장으로 충분 — 커스텀은 spec 대조 등 특화 관점 필요 시(§E 주석) |

> **절약 프로필 배정** (Pro 요금제·한도 관리 — 00 STEP 3 또는 이 문서 §D 질문 4에서 절약 선택 시 위 표 대신 적용). 원칙: **기본값 하향 + 낭비 제거**로만 아낀다 — 품질 결정 지점은 그대로.
> - **1순위 대안 — Sonnet 메인 + 상위 advisor**: 고난이도 작업을 상위 모델 메인 대신 Sonnet 메인 + `/advisor <상위>`로 운용. 공식 측정(§F 주석의 "The advisor strategy")상 **Sonnet 단독 대비** 비용 −11.9%·품질 +2.7%p(SWE-bench Multilingual, 측정은 Sonnet 4.6 실행자 기준) — 절약 프로필의 핵심 패턴. advisor는 결정 시점에만 호출되므로 상시 상위 모델보다 싸다. **advisor 불가 환경**(Bedrock·Claude Platform on AWS·Google Cloud·Foundry, 또는 feature-flag 조회가 막힌 세션 — 아래 제약 주석)에선 이 대안을 쓰지 않고 **高난이도만 표준 배정(상위 모델)을 유지**한다 — 보상 없는 하향은 품질 불변 위반이다.
> - **워크플로 규모**: 다이내믹 워크플로(§E)는 런당 최대 1,000 에이전트까지 퍼질 수 있다 — 절약 프로필에선 `workflowSizeGuideline`(기본 `medium`)을 낮춰 팬아웃을 묶고, 25개 초과·150만 토큰 예상 시 뜨는 `Large workflow` 경고를 그냥 넘기지 않는다.
> - 정형 실행·테스트는 **haiku 적극**, 분석·구현은 sonnet. 로스터도 축소(surveyor·implementer·parity-tester **+ triage** 중심) — 에이전트가 많을수록 컨텍스트 재구축 비용이 든다. **triage는 축소 대상이 아니다**(아래 불변) — 로스터에서 빼려면 그 판정을 메인 세션이 직접 수행한다고 명시해야 하며, 그냥 없애면 불변 조항이 무효가 된다.
> - 백그라운드 서브에이전트 팬아웃·`ultracode`는 자제. 대량 정형 전환은 `claude -p` 팬아웃(위 주석)이 오히려 통제·비용 모두 유리.
> - **불변(여기서 아끼지 않는다)**: 난이도 분류(triage)·요구 확정(spec — 신규개발형이면 spec-writer도 축소 대상이 아니다)과 최종 리뷰의 상위 모델 — 여기서 틀리면 하류 전체 재작업으로 더 비싸진다(§A-2·§H "almost right" 함정).

> **opusplan 주의**: `opusplan`은 **메인 세션 모드 별칭**(Plan Mode=Opus → 실행=Sonnet)이다. 서브에이전트 frontmatter의 `model:` 값으론 못 쓴다(`sonnet`/`opus`/`haiku`/`fable`/전체 ID/`inherit`만 유효, 기본값 `inherit`). 어려운 구현은 서브에이전트 대신 메인 세션 `/model opusplan`으로. `availableModels` 정책과의 상호작용은 v2.1.205+에서 개선 — Opus 최신이 제외돼도 **허용된 최신 Opus로 대체**해 plan을 돌리고, **모든 Opus가 제외된 경우에만** Sonnet에 머문다.
> **opusplan vs advisor**: opusplan은 plan 경계에서 모델을 바꾸고, `/advisor`는 **실행 중 주모델이 막힐 때 상위 모델에 자문**하는 방식(모델이 자문 시점을 스스로 판단, 전체 대화를 advisor가 읽음). 공식 측정(블로그 "The advisor strategy"): Sonnet+Opus advisor가 SWE-bench Multilingual **+2.7%p·태스크당 비용 −11.9%**(Sonnet 단독 대비), BrowseComp에선 Haiku+Opus advisor 41.2%(단독 19.7%). 계획이 명확한 정형 전환엔 opusplan, 실행 중 난관이 예상되는 탐색적 작업엔 advisor가 유리.
> **advisor 제약**: 실험적 기능·**Anthropic API 전용**(Bedrock·Claude Platform on AWS·Google Cloud·Foundry 불가). feature-flag 조회가 필요해 `DISABLE_TELEMETRY` 류가 걸린 세션에선 advisor가 꺼진다. 설정: `/advisor <모델>`·`advisorModel` 설정·`--advisor` 플래그. advisor는 주모델보다 **같거나 더 유능**해야 하며(페어링 검증), 서브에이전트도 자기 모델 기준으로 페어링 검사 후 상속. **Opus 4.7 이상(4.7·4.8·5)은 동급으로 취급**되어 서로 advisor 지정 가능(단 Opus 4.7+ 메인에 Sonnet 5·Opus 4.6 advisor는 거절). **Fable 5는 advisor로 복귀**(v2.1.232) — Fable 접근권 있는 조직에서 지정 가능하되, 일부 플랜은 `/model fable`로 **usage credits 1회 동의**를 마쳐야 적용된다(동의 전엔 저장·적용 안 됨). Fable 5 메인은 여전히 Fable만 advisor로 허용.
> **비용 제2축 — effort**: 모델 선택과 별개로 `/effort`(low~max)로 추론 강도를 조절할 수 있다. "라우팅으로 모델을, effort로 강도를" 2축 운용이 최종 형태. 에이전트별 차등은 **frontmatter `effort:` 키**(공식 확정)로 건다.
> 다만 **강도를 프로젝트 설정에 고정하지는 말 것**(`effortLevel` 비권장). 세션 단위 단일값이라 작업별 최적이 안 되고, 서브에이전트 차등에도 쓸 수 없다(그건 에이전트 frontmatter `effort:` 몫). **기본값 + 필요할 때 `/effort`** 가 최적이다.
> **대량 정형 전환의 대안 — `claude -p` 팬아웃**: 파일 수백 개짜리 마이그레이션은 세션 안 서브에이전트보다 헤드리스 루프가 더 통제 가능하다(파일당 독립 컨텍스트 + `--allowedTools`로 권한 좁게). 순서: 대상 파일 목록 생성 → 2~3개로 프롬프트 튜닝 → 전체 루프 실행(`--allowedTools`로 권한 좁게, `--output-format json`으로 결과 수집). 상세는 공식 headless 문서(`code.claude.com/docs/en/headless`).
> **긴 작업의 완주 — `/goal`**: 완료 조건을 걸면 조건이 충족될 때까지 턴을 넘겨 계속 작업한다(공식, v2.1.139+). "단계가 아니라 결과를 기술"하는 Fable 5 운용(§I)과 같은 철학 — 파이프라인 전체를 한 번에 맡길 때 유용. 더 굵직한 멀티에이전트 오케스트레이션은 `/effort ultracode`(01 §D-6).
> **병렬 체감 — 백그라운드 서브에이전트**: 서브에이전트는 기본 백그라운드 실행이라(v2.1.201+) 메인 세션이 기다리지 않고 이어 작업한다. 권한 프롬프트는 메인 세션에 떠서 놓치지 않음. 전 세션 현황은 `claude agents`(에이전트 뷰).
> ⚠️ 세션이 계속 xhigh로 시작한다면 모델 기본값이 아니라 **프로젝트 `.claude/settings.json`의 `effortLevel`** 때문일 가능성이 크다(Project > User, 경고 없음). 01 통합마스터 §D-6 참조.

---

## G. Claude 자기 점검 (착수 전 확인)
- [ ] 프로젝트 스택·구조·성격을 파악했다(모르는 건 질문했다).
- [ ] 이 프로젝트용 **난이도 rubric**을 확정했다.
- [ ] **핸드오프 문서 규약**(어느 파일에 무엇)을 정했다.
- [ ] **검증 방법(정답)** 이 정해졌다.
- [ ] 사용자 **승인**을 받았다.

---

## H. 주의 (기대치 관리)
- 완전 자동 아님(~75% 천장): 숨은 비즈로직·아키텍처·보안 엣지는 사람이 판단.
- "almost right" 함정: 어려운 걸 싼 모델로 밀면 재작업이 더 비쌀 수 있음 → rubric으로 걸러 高는 상위 모델.
- 비기능요구(성능/보안/구조)는 자주 누락 → reviewer가 명시 점검.
- "N% 자동 보존" 류 벤더 수치 불신 → 정답 대조로 직접 검증.
- **내장 라우팅과 중복 주의**: Claude Code는 탐색에 내장 Explore 서브에이전트를 자동 사용한다. ~~Haiku 고정~~은 구정보 — 현행은 **메인 세션 모델을 상속(Opus 상한)**. 탐색을 싸게 고정하고 싶으면 `Explore`라는 이름의 커스텀 에이전트를 `model: haiku`로 정의(내장을 덮어씀). 단순 코드 탐색까지 커스텀 surveyor로 감싸면 과설계 — surveyor는 "문서 산출물이 필요한 survey"에만. (내장 Explore·Plan을 끄려면 `CLAUDE_CODE_DISABLE_EXPLORE_PLAN_AGENTS=1`, v2.1.198+) 또 하나 — **Explore·Plan은 CLAUDE.md·git status를 로드하지 않는다**(속도·비용 최적화, 다른 서브에이전트는 전부 로드): 특정 규칙이 탐색에 꼭 필요하면 위임 프롬프트에 재명시. 결과는 메인 세션이 CLAUDE.md 맥락으로 해석하므로 대부분은 문제없다.
- **에이전트 팀은 켜지 않는다** 🔴(실험적 — `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`로 기본 꺼져 있다): 켜면 이름 붙은 서브에이전트가 **팀원으로 떠 결과를 반환하지 않아** §E 로스터 기반 분담이 **오류 없이 조용히** 무력화된다(답이 안 오는 것으로만 나타나 원인 추적이 어렵다). 덧붙여 `/resume`·`/rewind`로 팀원 복원 불가, 분할 페인은 **Windows Terminal·VS Code 통합터미널 미지원**(tmux·iTerm2 필요), `~/.claude/teams/`·`tasks/`는 런타임 생성물이라 손으로 고치지 않는다.

---

## I. 모델 라인업 참고 (2026-08-20 재검증 · 별칭이 자동 추적하므로 표는 그대로 유효)
- 별칭: `haiku` / `sonnet` / `opus` / `fable` / `best` / `default`. **`best` = 조직에 Fable 5 접근권 있으면 Fable 5, 없으면 최신 Opus**. `default`는 모델 오버라이드를 지우고 계정 기본값 복귀(별칭 아님).
- 별칭 해석은 프로바이더별로 다름: `opus`=**Opus 5**(v2.1.219+, API·Claude Platform on AWS·Bedrock·Google Cloud 공통), `sonnet`=Sonnet 5(API, v2.1.197+ — AWS는 Sonnet 4.6, Bedrock/Google Cloud는 Sonnet 4.5). **Foundry만 `opus`=Opus 4.6·`sonnet`=Sonnet 4.5.** 특정 버전 핀은 전체 ID나 `ANTHROPIC_DEFAULT_OPUS_MODEL`/`ANTHROPIC_DEFAULT_SONNET_MODEL`/`ANTHROPIC_DEFAULT_HAIKU_MODEL`/`ANTHROPIC_DEFAULT_FABLE_MODEL`로. 새 세션의 기본 모델 자체는 `ANTHROPIC_DEFAULT_MODEL`(v2.1.236+ — `/model` 저장값·`ANTHROPIC_MODEL`·설정 `model`이 있으면 무시된다).
- **Claude Opus 5**(v2.1.219+): 현행 기본 Opus. Opus 4.8과 동일 가격($5/$25 MTok)·1M 컨텍스트·128K 출력. 자체 안전 분류기 보유 — **사이버 플래그 시 Opus 4.8로 자동 폴백, 바이오는 폴백 없이 거절**. fast mode(`/fast`)는 **Opus 5·4.8 전용**(4.7은 v2.1.219에서 제거, Opus 5 fast는 $10/$50).
- **Claude Fable 5**(v2.1.170+): 최상위 모델, **가격 $10/$50 MTok(Opus 5의 2배 — 2026-08-20 공식 확인)**, 기본값 아님 → `/model fable`로 선택. 장시간 자율 세션·자체 검증 강화형 — "단계가 아니라 결과를 기술"하는 프롬프트에 최적. 안전 분류기 발동 시 자동 폴백이 카테고리별로 분화 — **바이오→Opus 5, 사이버→Opus 4.8**(해당 요청 재실행, `/model fable`로 복귀).
- **컨텍스트**: Anthropic API에선 Fable 5·Opus 5·Sonnet 5·Opus 4.8·4.7이 **기본 1M**. 구독(Max·Team·Enterprise)은 Opus 자동 1M 업그레이드. `[1m]` suffix는 그 외 경로용.
- 기본 모델(계정별): Max·Team Premium·**Enterprise pay-as-you-go**·API·**Claude Platform on AWS·Bedrock·Google Cloud** = **Opus 5**(v2.1.219+) / Pro·Team Standard·Enterprise 구독 시트 = Sonnet 5 / Foundry = Sonnet 4.5.
- **단가 참고**(API, 입력/출력 MTok): Fable 5 $10/$50 · Opus 5 $5/$25 · **Sonnet 5 $2/$10**(2026-08-20 확정 — 런칭 때 예고됐던 9/1 $3/$15 인상은 **취소**되고 인트로가가 표준가가 됐다) · **Haiku 4.5 $1/$5**. 1M 구간 프리미엄 없음. 단 **Opus 4.7 이상은 신규 토크나이저로 같은 텍스트에 ~30% 더 많은 토큰**이 나오므로, 단가만 비교하면 하위 모델 대비 실비용을 과소평가한다.
- 가용성 폴백: `--fallback-model sonnet,haiku` 또는 `fallbackModel` 설정(과부하·서버 오류 시 체인 순서로 대체, 최대 3개).

---

## L. 유지보수 (재검증 체크리스트)
> **6개월 주기**(01 §L과 동일. 06만 research preview라 3개월 권장으로 별도). `/reverify`가 이 목록을 조사 축으로 쓴다.
- §I 모델 라인업 — **별칭(`opus`·`sonnet`·`haiku`·`fable`)이 가리키는 실제 모델**, 요금제별 가용 모델, **§I 가격 수치**(비용 비교 질의·절약 배정의 근거라 낡으면 판단이 틀어진다).
- §F 절약 배정의 근거 수치(Sonnet 메인 + 상위 advisor 패턴의 공식 측정치)와 페어링 권고가 여전히 유효한지.
- 1M 컨텍스트 정책(§C 규모 행) — 기본 1M 대상 모델·`[1m]` suffix가 필요한 경로.
- 서브에이전트 동시 실행 상한·**중첩 스폰 깊이 기본값**(§E 서두), `isolation: worktree` 동작.
- frontmatter 키 생존 — `effort:`·`isolation:`·`model:`·`tools:`.
- §E 로스터 이름이 §C 활동 유형 매핑·STEP 3 ⓑ 선택 목록과 일치하는지(셋 중 하나만 늘면 감지·기록이 갈린다).
- **다이내믹 워크플로**(§E 말미) — 파일 위치·`workflowSizeGuideline`·`disableWorkflows`·요금제 게이트(Pro는 `/config`에서 켬)가 유지되는지.
- **에이전트 팀**(§H) — 실험 플래그가 유지되는지, **GA로 승격되면 로스터 간섭 서술을 재판정**한다(지금의 "켜지 않는다"는 실험 단계 전제다).
- 갱신 후 최상단 버전 표 + 아래 "문서 정보" 날짜 수정.

---

**문서 정보** — 범용 플레이북 **v1.31** · 최종 갱신: 2026-08-20 · 최근 재검증: 2026-08-20 · 참조: Claude Code v2.1.237 (변경 이력은 문서 최상단 버전 표 참조).
