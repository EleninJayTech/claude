---
name: kr-ui-guide
description: 한국 사용자 대상 웹·앱의 UI/UX 설계, 아이콘 제작·선정, 색상·팔레트 결정을 할 때 쓴다. 국내 서비스 벤치마킹 리서치(UX 패턴·아이콘 체계·색채 체계)에서 추린 원칙과 체크리스트를 작업 종류에 맞게 불러와 설계를 제안하고 결과를 자가 점검한다. 화면 설계, 입력 폼, 버튼 배치, 내비게이션, 로딩·에러 상태, 마이크로카피, 아이콘, 색 조합, 브랜드 컬러, 다크모드, 명도 대비, 디자인 토큰을 언급하면 쓴다.
---

# kr-ui-guide — 한국 사용자 대상 UI 설계 기준

국내 주요 서비스(토스·카카오·네이버·배달의민족·당근·KRDS 등)를 벤치마킹한 리서치 3종에서 추린 기준이다. **사례를 베끼는 도구가 아니라 판단 기준이다.**

## 무엇을 읽나
작업 종류에 맞는 정리본만 읽는다. 여러 종류가 섞이면 해당하는 것을 모두 읽는다.
- 화면·흐름(입력 폼, 버튼 배치, 내비게이션, 로딩·에러 상태, 마이크로카피) → `${CLAUDE_SKILL_DIR}/references/ux-patterns.md`
- 아이콘(제작·선정·교체, 탭바, 상태 표현) → `${CLAUDE_SKILL_DIR}/references/icons.md`
- 색(브랜드 컬러 적용, 팔레트, 시맨틱 색, 다크모드, 토큰) → `${CLAUDE_SKILL_DIR}/references/color.md`

특정 서비스의 사례가 필요할 때만 `${CLAUDE_SKILL_DIR}/references/source/`의 원문(`ux-report.md`·`icon-report.md`·`color-report.md`)에서 **해당 절만** 찾아 읽는다 — 원문은 파일마다 수만 토큰이라 통째로 읽지 않는다(`### [사례 NN]` 제목으로 찾는다).

## 반드시 지키는 기준 (작업 종류와 무관)
1. **명도 대비** — 본문 텍스트 4.5:1 이상, 큰 텍스트(24px 이상 또는 약 18.7px 이상 볼드)·아이콘·UI 경계 3:1 이상(WCAG 2.1 AA). **색 쌍을 제안할 때마다 계산한 값을 함께 적는다.** 채널값 c(0~255)를 s=c/255로 두고, s≤0.04045면 s/12.92, 아니면 ((s+0.055)/1.055)^2.4 → 상대 휘도 L=0.2126R+0.7152G+0.0722B → 대비=(밝은 쪽 L+0.05)/(어두운 쪽 L+0.05). 참조 문서의 대비값은 출처 보고서의 계산이라 그대로 인용하지 않는다.
2. **밝은 유채색은 면으로만** — 흰 배경 대비 4.5:1 미만인 색(노랑·밝은 초록·민트·주황 등)은 텍스트·얇은 선에 쓰지 않고 버튼·배경 면으로 쓴다. 그 면 위 글자는 어두운 색으로 대비를 확보한다.
3. **상태는 색과 형태로 함께** — 활성/비활성·선택/미선택을 색만으로 구분하지 않는다(라인↔채움, 굵기, 기호를 함께 바꾼다).
4. **터치 영역** — 보이는 크기와 무관하게 최소 44×44pt(iOS)·48×48dp(Android).
5. **아이콘 라벨** — 공통 기호(뒤로·닫기·검색·공유) 외에는 텍스트 라벨을 붙이고, 아이콘만 있는 버튼엔 `aria-label`(또는 플랫폼의 접근성 라벨)을 준다.
6. **강조 액션은 한 화면에 하나** — 보조 액션은 무채색·아웃라인으로 낮춘다.
7. **다크패턴 금지** — 해지·탈퇴·취소를 가입·결제보다 어렵게 만들거나, 그 버튼을 흐리게·작게 숨기지 않는다.

## 순서
1. 작업 종류를 정하고 해당 정리본을 읽는다.
2. 대상 서비스의 업종(금융·커머스·지도·공공 등)을 정리표에서 찾아 **국내 관습**을 확인한다(예: 한국 주식 시세는 상승 빨강·하락 파랑).
3. 설계를 제안할 때 근거를 정리본의 원칙·체크리스트 항목으로 단다.
4. 결과물을 해당 체크리스트로 자가 점검하고, 통과하지 못한 항목은 이유와 함께 보고한다.

## 하지 않는 것
- 사례에 나온 브랜드의 색·아이콘을 그대로 가져다 쓰지 않는다 — 브랜드 자산이다. 원리만 가져온다.
- 효과 수치(전환율·완료율 등)를 근거로 인용하지 않는다 — 출처 보고서가 2차 자료를 모은 것이라 검증되지 않았다.
- 미적 방향(타이포·무드·모션)은 이 스킬의 몫이 아니다. `frontend-design`(설치할 때 별칭을 붙였으면 그 이름)이 있으면 그쪽이 방향을, 이 스킬이 제약·관습을 맡는다 — 둘이 부딪히면 위 "반드시 지키는 기준"이 이긴다.
