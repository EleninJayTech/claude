# 내부 정합성 전수조사 결과 — 2026-08-12 (/audit 첫 수행)

> 상태: **전 항목 미수정 — 수정 대기**(사용자 지시: 상·중·하 전부 수정 예정, 계정 변경 후 진행).
> 조사: 렌즈 6종(A 규칙 정합성 · B 버전/참조 · C 케이스 누락 · D 04/05/06 정밀 · E 실물↔백업↔명세 · F 자작 스킬) 병렬 에이전트 → 원문 대조 재검증 완료.
> 수정 시 규칙: 경량 반영(A안), 문서 수정 = 버전 표 한 줄+bump+푸터, 글로벌 스킬 수정 = global-config/ 미러+해시 확인, 기록 = PROGRESS·DEC·PROJECT_PLAN. 이 파일은 수정 진행에 따라 각 행 앞 체크박스를 갱신한다.

## [상] 2건

- [ ] **S1. 스킬 스코프 우선순위 모순** — `01` §F-2 B 헤더·§D-4·§E-4: "프로젝트 `.claude/skills/`가 글로벌 기본형을 **덮어씀**" ↔ 실제 `~/.claude/skills/resume`(8-11행)·`wrap`(19-20행): "**개인 스코프가 프로젝트를 덮으므로**(enterprise > personal > project) 통합 절차를 이 파일에 내장". 둘 중 하나는 거짓 — 스킬 쪽이 맞다면 B형(MSA) 템플릿은 설치해도 실행 안 됨. **수정 전 공식 문서로 우선순위 확정 필수**(스킬/커맨드 동명 시 우선순위) → 01의 "덮어씀" 서술 4곳(§D-4·§E-3·§E-4·§F-2 B/C)을 확정 방향으로 통일.
- [ ] **S2. raw→사본 전환 시 `출처=` 기록 미갱신** — `~/.claude/skills/dropin-update` raw 분기 ⓐ(사본 내려받기) 실행 후 기록이 raw로 남아 다음 판별이 현실과 모순. 수정: ⓐ에 "`출처=사본`으로 기록 갱신" 포함 + 판별에 "기록=raw인데 사본 존재 시 사본으로 재판별" 한 줄. (+백업 미러)

## [중] 22건

### 구번호 재편 잔재 — repo CLAUDE.md
- [ ] M1. `CLAUDE.md` 환경 절 "단일 git repo(**00 §B**의 B형)" → 실제 B형 판별표는 **01 §B**.
- [ ] M2. `CLAUDE.md` "확인 못 한 항목은 🟡로 남긴다(**00 §L** 원칙)" → 00 §L(117-120행)엔 🟡 원칙 없음(3줄 유지보수뿐) → **01 §L**로.
- [ ] M3. `CLAUDE.md` 구성물 표 global-config 행 "**2026-08-10 스냅샷**" → 실제 최신 미러 2026-08-12.

### 백업 부속 정체
- [ ] M4. `global-config/README.md` 전면 낡음 — 3행 날짜 "(2026-07-20)", 표가 4종(**`output-styles/` 행 누락** → 새 PC 복원 시 `outputStyle` 키가 가리키는 파일 누락), 9행 skills 나열 5종(frontend-design·svg-design 누락). 표에 output-styles 행+skills 7종(또는 "등")+날짜 갱신.
- [ ] M5. `01` §D-3 deny 예시가 실물보다 3건 약함 — `PowerShell(Get-Content *.pem)`·`PowerShell(Get-Content *.key)`·`Read(**/coverage/**)` 누락(신규 설치 PC가 약한 보안을 받음). §D-3에 3건 추가.

### 문서↔스킬 사본 드리프트
- [ ] M6. `~/.claude/skills/resume`가 **최근 DEC 3건을 안 읽음** — 01 §F-1·repo CLAUDE.md·§F-2 B는 읽으라 함(A·C 템플릿도 없음). 실스킬 2단계+템플릿 A(01:267)·C(01:312)에 "docs/DECISIONS.md 최근 3건(Read limit)" 추가. (+백업 미러)
- [ ] M7. `~/.claude/skills/wrap` 9행 "새 결정은 **DEC-NNN**으로" — 구형 id(동시 발번 충돌 방지 규칙 위반 유도). `DEC-YYYYMMDD-작성자`로. 글로벌 CLAUDE.md §7 "last 3 DEC-NNN"도 동반 갱신. (+백업 미러)
- [ ] M8. 통합형 resume에서 **루트 docs/INDEX.md 읽기 소실** — wrap:25는 계속 기록하는데 아무도 안 읽음(01 §F-2 C:312엔 있음). 실스킬 통합 절에 복원. (+백업 미러)
- [ ] M9. `01` §G(415행) "→ git pull (동료 기록 동기화)" — 무조건 pull이 /resume pull-first(clean 검사) 안전장치 우회+이중 실행. "→ /resume (원격 최신화 포함)"으로 축약.
- [ ] M10. "최상단 append"(01:229·CLAUDE.md) vs "포인터 제목 바로 아래"(01:246·CLAUDE.md) **자리 경합 미정의** — 길이 관리 절에 "append는 포인터 줄 아래부터(포인터 고정)" 명시(01·CLAUDE.md·wrap 스킬 3곳).

### dropin-check/update 커버리지
- [ ] M11. check·update 둘 다 **통합 워크스페이스(A-1/A-2) 미지원** — 루트 CLAUDE.md만 파싱. "통합이면 루트+하위 repo 각각" 절 추가(resume/wrap 패턴). (+백업 미러)
- [ ] M12. `00` STEP 5 — 통합 환경에서 dropin-applied가 루트/하위 어느 CLAUDE.md에 남는지 미정의. "repo별 적용분은 각 repo, 루트 레이어분은 루트" 한 줄.
- [ ] M13. check §4 — §3이 차이 방향을 판단하라면서 권고는 "global-config에서 복사" 단방향(백업이 구버전이면 **최신 로컬을 구백업으로 덮는 사고**). 방향별 2분기 권고로.
- [ ] M14. check — "기록 없음" 보고 후 다음 행동 부재. §4에 "기록 없음 → 00 재적용으로 기록 수립 권장" 분기.
- [ ] M15. check §1 — 더티 클론이면 pull 생략만 하고 fetch도 없어 낡은 기준으로 "최신" 오보 가능. "더티면 fetch로 behind 병기".
- [ ] M16. update 사본 분기 — GitHub API 단일 경로(오프라인이면 클론 있어도 막힘). "로컬 클론 우선, API 차선" 폴백(check §1과 동일 사다리).
- [ ] M17. `00` STEP 5 — 프로젝트 설치에 글로벌(01 §D) 포함 시 기록이 프로젝트 CLAUDE.md에만 남음 → PC 차원 글로벌 버전을 알 수 없음(프로젝트 삭제 시 소실). "글로벌 항목 적용 시 ~/.claude/CLAUDE.md에도 글로벌분 병기".

### 04 집중 (2026-08-10 전수 점검이 04 본문엔 덜 반영된 흔적)
- [ ] M18. `04` STEP 2(33행) "강도(차단 vs 경고만)도 묻는다" ↔ §1(46행)은 강도를 "자동 수정 없음" 축으로 재정의+경고만 모드 설정법 부재. 질문·본문 축 통일.
- [ ] M19. `04` §1 스코프 2택 — "개인+이 repo만 = `.claude/settings.local.json`" 케이스 누락(글로벌 훅은 타 스택 repo에서 깨짐). 한 줄 추가.
- [ ] M20. `04` STEP 4 — 부분 완료 메모 규칙(00 v1.9, 실사례가 04의 gh 미인증) 미반영 — 단독 드롭인 시 도달 불가. "사용자 조치 대기도 괄호에" 한 줄.
- [ ] M21. `04` 134행 — "§1 hooks·§3 CI는 ⑤층(CI 정적 분석)의 정본" ↔ 133행·§1 제목(편집 즉시)과 자기모순. "§3=⑤층 정본, §1=스택 이전의 편집 중 게이트"로 분리.

### 06·자작 스킬·환경
- [ ] M22. `06` §4 ROUTINES.md — "append 전용·01 원칙과 동일" 선언인데 merge=union 미배선(다중 PC 충돌 무방비). "`.gitattributes`에 `docs/ROUTINES.md merge=union` 추가" 한 줄.
- [ ] M23. `.claude/skills/audit` 3·15행 "**5종**" — F 렌즈 추가 후 미갱신(수량 제거 권장: "렌즈 전부"). auto-memory 2곳(MEMORY.md·audit-command.md)도 동반.
- [ ] M24. `.claude/skills/reverify` 26행 pages 열거에 **learn.html 누락**(29행엔 pages/ 자체 누락 — 하급 h17과 함께). "(demo·intro·options·learn)" 또는 "pages/ 전부".
- [ ] M25. `~/.claude/commands/skill-setup.md` — frontmatter 전무(장문 본문이 통째로 description화+변경형 명령이 자동 호출 개방). name·한 줄 description·`disable-model-invocation: true` 추가. (+백업 미러)
- [ ] M26. 비-git 폴더 — `01` §B 판별표·resume/wrap 어디에도 없음. §B에 "비-git → git init 권장 또는 docs 체계만" 행 + 스킬에 폴백 한 줄.

(중은 22건으로 집계했으나 세분하면 M1~M26 — 그룹 병합 차이. 전부 확정.)

## [하] 확정 (수정 대상)

- [ ] h1. `04` 20행 "hooks(**커밋 전**)" → 실체는 편집 즉시. "(편집 즉시)"로.
- [ ] h2. `00`:3 기준 v2.1.221 vs `01`:3 v2.1.226 — 같은 날 갱신 문서 간 불일치. 00 헤더 동기화+§L에 동기화 규칙 한 줄.
- [ ] h3. `01` 푸터(503행) 변경 요약이 v1.15에서 정체 — 02 방식("변경 이력은 최상단 버전 표 참조")으로 교체해 이중 관리 제거.
- [ ] h4. check §1 "앞 5줄이면 충분" ↔ §4 "버전 표 해당 행 인용" 재료 부족 — "헤더+버전 표(앞 ~30줄)"로.
- [ ] h5. `00` 재적용 분기 — stale 사본이면 diff 공백. "출처=사본이면 비교 전 /dropin-update 권장" 한 줄.
- [ ] h6. `00` STEP 1 — 클론 자동 탐색 없음(클론 보유자도 raw로 기록됨). check와 동일한 후보 탐색 한 줄.
- [ ] h7. `00` STEP 5 — 재적용 "기존 줄 갱신(교체)" 시 기존 문서 항목·메모 보존 규칙 명시.
- [ ] h8. `00`/06 — 게이트 차단으로 미설치된 문서가 "미선택"과 구분 없음 → 괄호 어휘 `게이트 차단(사유)` 추가.
- [ ] h9. check — 오프라인+클론 없음 종단 문구("점검 불가 보고 종료").
- [ ] h10. `01` — push 권한 없는 remote에서 커밋 안내 후 실패 경로("로컬 커밋 유지+권한자 경유" 한 줄).
- [ ] h11. `02` STEP 3 — 테스트 명령 미감지 시 parity-tester 기본 해제+사유 병기.
- [ ] h12. `02` 181행 "00/§D-4" 표기 모호 → "00 STEP 3/이 문서 §D 질문 4".
- [ ] h13. `05` §L의 `force-for-plugin` — 본문(§2 표)에 정의 없음. 표 한 줄 추가 또는 §L에서 제거.
- [ ] h14. `06` STEP 1(25행)·STEP 2(31행) 게이트 요약이 4종 중 2종만(계정 GitHub 연결·GitHub 전용 누락).
- [ ] h15. `00` 52행 불변 ② 06 게이트 열거 → "06 §1 게이트 전부"로.
- [ ] h16. `04` STEP 2 스코프 질문 — §1에만 성립(§2 CLI=PC, §3=repo 고정). "스코프는 §1에 한해" 한정.
- [ ] h17. reverify 29행 신규 문서 동기화 목록에 pages/ 누락 + §4에 "다음 재검증" 예정일·🟡 체크리스트 갱신 단계 부재(audit §4엔 있음).
- [ ] h18. audit 23행 F 렌즈 — skill-setup 실위치(`commands/`) 명시 + "우리가 만든"에 humanizer(외부 저작) 포함 표현 손질.
- [ ] h19. `01` §F-2 A wrap 템플릿에 WBS 파생 뷰 동기화 단계 부재(실스킬에만 존재) — 한 줄 반영.
- [ ] h20. repo CLAUDE.md 커밋 양식 예시의 구체 버전("01 v1.5, 02 v1.4") → vX.Y 플레이스홀더.
- [ ] h21. skill-setup 후보 카탈로그 정체 — humanizer 단일 파일 힌트 오류·superpowers 기각 판정(DEC-20260810-bsjeong87-02) 미반영·frontend-design 구경로·외부 URL 6종이 아무 재검증 축에도 없음. 카탈로그 개정 또는 03 부록 흡수(정리 후보 — 방향 결정 필요).
- [ ] h22. (선택 — 사람용 예외) options/demo의 dropin-applied 예시 구버전·`출처=` 미반영, learn 시뮬레이터 `출처=` 미반영, options "v1.7 기준" 표기, index 날짜 3원 불일치 — 다음 페이지 갱신 기회에 일괄.
- [ ] h23. `01` §D-3 — 실물에만 있는 `attribution.sessionUrl`·SessionStart `startup` 매처 문서 미반영(선택).

## [의심] (수정 전 확인 필요)
- humanizer frontmatter `allowed-tools` 공백 구분 표기 — 파서 동작 공식 문서 확인 후(다음 /reverify B축).
- `04` 110행 "헤드리스 정본은 02 §F" — 실체는 주석+공식 문서 위임. "정본" 완화 여부.
- `05` ↔ 01 D-2 "## 5. Output Style" 동명 혼동 — 반 줄 병기 여부.
- **미확정 1건**: `02` 단독 적용 시 동명 `.claude/agents/` 파일 유지/재구성/교체 규칙 존재 여부 — 02 STEP 3 원문 정독 후 판정.

## 기각·오탐 (기록용)
"audit이 자작 스킬 미감사"(F 렌즈로 기소진) · "reverify description 00~06 하드코딩"(§0 자가 치유 문구 존재) · CLAUDE.md:78 dropin-applied의 01 v1.19(적용 당시 정당한 기록 — /dropin-check 안내 대상이지 결함 아님) · settings.json 머신 종속 키 차이(정상 드리프트).
