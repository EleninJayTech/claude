# PROGRESS (최상단 append)

> 이전 기록: `docs/archive/PROGRESS-20260720-20260812.md` (2026-07-20~2026-08-12 · 80항목). 과거 이력 검색은 `docs/` 폴더 단위로. **append는 이 줄 아래부터.**

- [config][Done] **조치 대기 3건 → `미선택` 전환**(사용자 판단 — 사내 도입 예정 없음). 글로벌 `04 §2 호스트 CLI 인증`(gh 인증·glab 사내 호스트 등록), 프로젝트 `06 §3 루틴 생성`. **어휘는 상태 4종 표를 따랐다** — 사용자가 처음 말한 `보류`는 정의된 상태가 아니고, 00 §A STEP 3 ③이 보류를 **`미선택` 전환**으로 규정한다(v1.22에서 일반화). 세 가지 판단: **ⓐ `해당 없음`을 쓰지 않았다** — 그건 구조적 적용 불가라 질문 ②③ 둘 다에서 영구 제외되는데, "아직 예정 없음"은 조건이 바뀌면 되살아나므로 평범한 `미선택`이라야 다음 재적용의 질문 ②가 다시 묻는다. **ⓑ 문서 전체가 아니라 항목(`§2`·`§3`) 단위**로 내렸다 — 04는 gh 2.97.0 설치분이, 06은 `docs/ROUTINES.md` 틀+`.gitattributes` 준비물이 이미 적용돼 있어 줄 전체를 `미선택: 04, 06`으로 바꾸면 **이미 만든 산출물이 기록에서 사라져** 다음 재적용이 없는 것으로 알고 다시 만든다. **ⓒ 괄호 안에도 문서 번호를 병기**(`미선택: 04 §2`) — 00 §A가 "여기서 다른 표기를 쓰면 질문 ②의 미선택 스캔에 걸리지 않는다"고 경고하고, `04 §2` 자신이 `미선택: 04 §2(해당 없음—미지원 호스트)`라는 같은 형식의 선례를 갖는다. **부수 효과로 위치 어긋남 1건이 해소**됐다 — 06 대기는 *계정 GitHub 연결*(=머신 로컬)인데 커밋되는 프로젝트 줄에 있어 팀 전원 상속 위험이 있었다(그 스코프 분기는 어제 06 v1.13·00 v1.30에야 생겼고 이 메모는 2026-08-12자였다). 전환 후 남는 건 `미선택`뿐이고 그건 팀 사실이라 프로젝트 줄이 제자리다. 검증: 두 파일 `대기` 문자열 **0건**, `check.ps1` **이상 없음(R01~R25 OK·INFO 0)**, 커밋 `f79f293` — @bsjeong87 (main) 2026-08-21

- [maint][Pending] **관찰 — `/resume` 예정일 안내가 조건부 트리거를 읽지 못한다**. R09·R19가 규정한 형식은 **달력형**(`다음 ○○: YYYY-MM경`) 하나뿐인데, `PROJECT_PLAN:91` 정기 실적용은 **`(또는 대량 개정 직후)`**라는 조건을 괄호에 달고 있다. 오늘 그 조건은 이미 참이다 — 어제~오늘 대량 개정(00 v1.33·01 v1.52·04 v1.16·06 v1.13) 뒤 회귀 감사(7차)까지 끝났고 DEC-20260812-bsjeong87-16 ①이 "대량 개정 뒤에는 회귀 감사 **다음에** 실적용을 한 번 돌린다"고 정한다. 그런데 달력값이 `2026-11경`이라 이번 `/resume`은 아무것도 안내하지 않았고, 나 자신도 "정기 축은 모두 여유 있다"고 보고했다 — **드러난 건 절차가 발화해서가 아니라 사용자가 되물어서**였다. 짝 축인 환경형 시나리오 대입은 오늘 3회차를 돌았으나 "실제 적용" 절반은 미수행이다. 처방 후보: ⓐ 조건을 기계가 읽는 형식으로 승격(예: `다음 ○○: YYYY-MM경 | 조건=대량 개정 직후`) ⓑ `/wrap`이 대량 개정을 감지해 예정일을 앞당겨 적는다. 파급은 R09·R19·`~resume`·`~wrap`·`check.ps1` — @bsjeong87 (main) 2026-08-21

- [backup][Pending] **관찰 — `/wrap` 미러 대조의 `settings.json` 비교가 키 순서에 민감하다**. 이번 대조에서 구성 성격 키 `hooks`가 DIFF로 떴으나, 실제 차이는 **PreCompact `manual` 훅 객체의 JSON 키 순서**뿐이었다(정본 `type·command·shell·timeout·statusMessage` / 미러 `type·shell·timeout·statusMessage·command`) — 값은 전부 동일해 재귀 정렬 후 재비교로 일치를 확인했다. 절차 문안은 "키 단위로 비교"라고만 적혀 **직렬화 순서까지 내용 차이로 읽힌다**. 이미 명문화된 **줄바꿈 정규화와 같은 실패 계열**(표현 차이를 내용 차이로 오판)인데 그쪽만 규정돼 있다. 방치하면 매 대조마다 거짓 DIFF가 뜨고, 절차가 이어서 "어느 쪽이 최신인지 판정 → 복사 제시"로 가므로 **멀쩡한 미러를 덮어쓰는 권고**로 이어진다. 같은 대조에서 **`jq` 미설치로 네 키가 전부 거짓 `OK`**로 나온 일도 있었다(양쪽 다 빈 출력이라 해시가 일치) — 비교는 **도구 부재를 통과와 구분**해야 한다. 처방: `~wrap` 미러 대조 절에 "JSON은 키 순서를 정규화한 뒤 비교" + "도구 부재는 실패로 취급" 두 줄, `global-config/README.md` 동기. 이번 회차는 복사·`global-config/` 행 날짜 갱신 모두 불요 — @bsjeong87 (main) 2026-08-21

- [maint][Done] **`DECISIONS.md` 아카이브 분리 — 50항목 → `docs/archive/DECISIONS-20260720-20260812.md`** (`/wrap` 부피 점검, 사용자 승인). 111KB(임계 120KB의 **92%**)로 아직 넘지 않았으나 DEC 하나가 2~4KB라 다음 세션이면 넘는 상태였다. **경계는 달력이 아니라 목표치로** 잡았다 — 활성이 임계의 절반(60KB) 이하가 되는 지점을 계산하니 2026-08-12 **한복판**이었고, 그날 하루에 결정이 몰려 있어 날짜 경계에 맞춰 **2026-08-13**으로 당겼다(활성 24항목 **55KB·45%**, 아카이브 50항목 56KB). 포인터는 제목 바로 아래 **최상단 고정**(/resume이 최근 항목만 읽어 아래 포인터는 보이지 않는다). 활성 항목이 인용하는 `[[DEC-2026-08-12-*]]`는 아카이브로 내려갔으나 **폴더 단위 검색** 규칙이 이를 흡수한다 — @bsjeong87 (main) 2026-08-21

- [audit][Done] **`/audit` 7차(회귀 모드) — 확정 31건 전건 수정, `check.ps1` FAIL 0·INFO 0** — 렌즈 A~F 6종 병렬, 원시 36 → **확정 31(상 7·중 15·하 9)** + 인계 6 + **오탐 1 기각**. 보고서 `docs/audit-20260821.md`. 델타는 `bd37602^..HEAD`(4커밋). **확정 상 7건 중 3건이 이 델타의 회귀이고 2건은 같은 날 wave형 반영(`cef9f29`)이 만든 것**이다 — 회귀 모드를 고른 판단이 그대로 값을 냈다. **ⓐ S2(내 회귀)**: "기록 3종 중 **일부만** 있으면 미설치"가 **정상 단위분할 repo를 매 세션 오보**한다 — §E-3은 `PROJECT_PLAN`만 `docs/` 루트고 `PROGRESS`·`DECISIONS`는 `docs/<단위>/`라 어느 폴더로 세도 "일부만"이 항상 참이다. **수정 전엔 오발하지 않았다**(옛 조건은 `docs/` 존재만 봤다) — `/wrap`이 append를 건너뛰어 기록이 소실되고 실물 스킬도 같은 문구라 배포 상태였다. 판정을 **경로 기준**으로 바꿔 A·B·C+실물+미러 전부 수정. **ⓑ S6(내 회귀)**: 00에 게이트 차단 사유 분기를 넣으면서 **소비자 06을 보지 않아** 계정·요금제 사유가 팀 기록에 남는 상태 그대로. **ⓒ S3(내 미완)**: 셋째 증거 경로를 넣고도 바로 뒤 "설치·검증 성공 뒤에만 제거"를 남겨, **설치할 게 없는 대기**는 해소를 관찰해도 못 닫았다. **ⓓ S1(렌즈 4종 독립 수렴)**: `§F-2` **A wrap에 R21·R23이 없었다** — 대장은 "01 안 4곳"으로 A를 명시해 **표가 거짓 보증**했고, 어제 v1.50의 진단("A에만 넣어 누락")도 R21에 대해선 틀렸다(R22만 A에 있었다). **처방을 사람이 아니라 기계로 옮겼다** — `check.ps1`이 규칙 열의 `NN 안 N곳`을 파싱해 **실제 출현 수**를 대조한다(M13). 그 검사는 **넣자마자 R23을 FAIL**시켰는데 원인이 결함이 아니라 **마커 선정**(`Reverted`는 상태값·뱃지 표에도 쓰여 셀 수 없다)이라, 개수 선언은 정밀한 마커를 가진 규칙에만 두는 것으로 정리했다. 그 밖에 `프로필=`의 예시↔규칙 모순, 머신 로컬 메모의 **이관 절차**와 **글로벌 줄 생성 트리거**, 04·06의 스코프 배선, 06 §2 검증의 §3 의존, 신규 합류 판정, 확보 사다리 rung, §B 표·§H의 동시성 3분기 동기, `[Pending]` **종결자**(R25 신설 — 이번 `/resume`이 실제로 오보한 축), `/optimize`의 예정일 생산자, skill-setup↔reverify 모순을 반영. **J1·J2 승인분**: `/audit` 렌즈 C 여정에 다인 축 + `다음 시나리오 대입: 2026-11경`, 4축의 "실적용"에 시나리오 대입을 하위 방법으로 명시. **부수 발견**: `04`에만 남아 있던 빈 인용 줄(`>`)이 새 bump 행을 만나 발화 — 08-20에 5문서를 고칠 때 04가 목록에 없었고 그 줄이 표 끝이라 잠복해 있었다. **렌즈 E 오판 1건**도 교차 확인으로 잡았다(`core.autocrlf`를 "부합"이라 했으나 실측은 `input`·워킹트리 LF). 산출 **00 v1.33 · 01 v1.52 · 04 v1.16 · 06 v1.13** + 스킬 6종 + `check.ps1` 2검사 + R25 — @bsjeong87 (main) 2026-08-21

- [다인축][Done] **wave형 시나리오 검증 → 4건 반영 + 오탐 1건 기각** — 미정의 분기 **4건(상 2·중 2)** 전건 처방, `check.ps1` 이상 없음(INFO 0). 전제부터 틀렸다 — 어제 sfa형이 남긴 "wave는 sfa와 동일 구조로 가정"을 실물 대조로 열었더니 **동일하지 않았다**: §B **혼합형**(하위 3 repo + `wave-local` 비-git이 루트 repo에 추적됨)·루트 `docs/` 기록 3종 부재(얇은 라우터)·프로젝트 스킬 1종·hooks 2종·`ROUTINES.md`·로스터 7종 — sfa가 밟지 않은 축이 6개다. **ⓐ 상 — 기록 체계 부분 설치를 아무도 못 잡는다**: `wave-web`은 `docs/PROGRESS.md`만 있고 DECISIONS·PROJECT_PLAN이 없는데, wrap 폴백이 **`docs/` 유무만** 보므로 발동하지 않아 조용히 통과한다. PROJECT_PLAN이 없으면 미해결 승격(R08)의 집행 대상이 아예 없고 아무도 알리지 않는다 → 폴백을 **파일 단위**로(3종 중 없는 것을 지목, 있는 파일엔 기록). **사본 4벌 전부**(§F-2 A·B·C + 실물 스킬·미러)에 반영 — 어제 A에만 넣어 만든 회귀를 그대로 되풀이하지 않으려고 먼저 사본을 세고 시작했다. **ⓑ 상 — 해소된 조치 대기가 안 닫힌다**: 루트 기록의 `wave PROGRESS 4,003줄 아카이브 대기(2026-08-12~)`는 **이미 끝난 일**이었다(아카이브 2본 분리, 활성 87줄). 게이트도 §검증 명령도 없는 대기라 00 STEP 3 ③의 두 증거 경로 어디에도 안 걸린다 — PROJECT_PLAN이 2026-08-13에 "1회 재발하면 배선"으로 예고한 그 재발이다. 설계분은 `사용자 확인`이었으나 **`직접 확인 → 관찰 불가면 질문`** 순으로 넣었다(이번 건은 파일 줄 수·포인터로 즉시 관찰됐다 — 물었으면 자기신고에 의존했을 것). **ⓒ 중 — 게이트 차단이 통째로 팀 사실**이라 `06 루틴 생성 게이트 차단(요금제·계정 GitHub 연결 미확인)`처럼 개인 계정 사유가 프로젝트 줄에 남아 **전원에게 상속**된다 — 그 규칙이 막으려던 현상 자체다 → 사유로 분기(조직·정책=팀 / 요금제·계정=글로벌). **ⓓ 중 — 미완 메모의 생산 어휘가 자유**라 소비자(문자열 탐색)가 못 읽는다(02의 `미정 — 규약 §3`이 실례) → `… 대기(사유)` 형식 고정. **오탐 1건**: "STEP 2의 통합 워크스페이스 각각 읽기가 소실됐다"고 봤으나 긴 불릿 **안쪽**에 실재했다 — `cut`으로 잘린 출력을 근거로 삼은 내 실수이고, git 이력 대조로 자기 기각했다. **관찰 2건 등재**(혼합형 비-git 단위의 기록 경로 두 갈래 — 사례 0건이라 처방 보류 / PROGRESS `[Pending]` 마커의 종결자 부재 — 이번 `/resume`이 실제로 오보했다, `/audit` 렌즈 A ④로 인계). 산출 **00 v1.32 · 01 v1.51** — @bsjeong87 (main) 2026-08-21

- [다인축][Done] **sfa형 시나리오 검증 → 7건 반영(자작 회귀 1건 포함)** — 미정의 분기 **7건(상 4·중 2·하 1)** 전건 처방, `check.ps1` 이상 없음. 같은 날 nastvad형(단일 repo) 반영 직후 sfa형(개인 root + 회사 하위 3 + 층위 혼재 + 크로스-repo + 정책 전환기)을 9단계로 대입했고, **직전 커밋이 만든 회귀가 첫 발견**이었다. **ⓐ S4 — 자작 회귀**: R21·R22를 `§F-2` **A 템플릿에만** 넣고 **B·C를 빠뜨렸는데 `check.ps1`이 OK를 냈다**. 마커 판정이 **파일 단위**라 `01`에 한 번만 있으면 통과하기 때문이고, nastvad형이 단일 repo(A 경로)만 밟아 안 걸렸다 — [[DEC-20260812-bsjeong87-15]]("대량 수정 직후 회귀가 절대다수")가 그대로 재현. 처방은 B·C 반영 + **`RULES.md` 읽는 법에 "마커는 파일 단위"** 명시 + R21·R22 규칙 열에 파일 안 사본 위치(§F-1·§F-2 A·B·C) 병기 — 검사기로는 못 막는 층이라 사람 체크리스트로 넘겼다. **ⓑ S6 — 위임 표기의 정본 부재**: `02·05·06은 루트 참조` 위임이 달린 하위 repo만 클론하면, 규칙상 재적용 질문 ①~③에서 **제외**되는데 실제로는 적용된 적이 없어 **영영 설치되지 않는다**. 개별 repo 기본 정책이 이 상황을 흔하게 만든다 → 루트 부재면 위임을 무시하고 **미적용으로 취급**(정본 없는 위임은 "적용됨"이 아니라 확인 불가). **ⓒ S5 — A/B/C 상위집합 미명시**: §F-2가 *"환경에 맞는 것 하나 선택"*인데 글로벌은 PC당 하나라, sfa-server(B 필요)와 wave-root(C 필요)가 한 PC에 있으면 **고를 근거가 없었다**. 실제로는 **C ⊃ B ⊃ A**(각 변형이 폴백을 갖춤)이므로 "가장 넓은 것"을 명시 — 정책 전환기에 A·B·C가 섞이는 기간의 유일한 판단 근거다. 그 외 **S3** 방법 A 알림이 나에게만 뜨는 문제(도달 채널이 그 repo git뿐이라 **PR·이슈에도 한 줄**), **S1** §I 5가 루트 레이어 사용을 전제(→ "쓸까(기본 아니오)"+소유 2단), **S2** C 폴백의 `(=A와 동일)`이 단위 판별까지 끄는 것으로 읽힘(→ INDEX만 생략·**단위 판별 유지**), **S7** 갈래는 멀티레포에서 repo가 아닌 **단위**(repo는 파일 경로가 이미 구분). 산출 **00 v1.31 · 01 v1.50**, RC 동기화. **wave는 sfa와 동일 구조로 가정**해 별도 대입 없음 — repo 구성이 다르면 그 차이만 추가 검증 필요 — @bsjeong87 (main) 2026-08-21

- [다인축][Done] **nastvad형 시나리오 검증 → 다인 축 배선 일괄 반영(R21~R24 신설)** — 미정의 분기 **10건(상 7·중 3, 그중 신규 7)** 전건 처방, `check.ps1` **이상 없음(INFO 0)**. 발단은 사용자 질의 — 회사 솔루션 3종(nastvad 단일 회사 repo / sfa·wave 통합형)에서 ① 팀원마다 기록 위치가 다르고 ② 검수 브랜치 병합 시 기록이 엉키고 ③ 상호 가시성이 없고 ④ 정말 범용인가. 진단: **환경 축(구조)은 범용이나 팀 축(사람 수)은 얇다** — 문서 전체의 기본 시점이 "1인이 자기 PC에 적용"이고, 검증 4축 어디에도 "여러 사람이 동시에"가 없어 구조적으로 발견될 경로가 없었다(실적용 표본도 1인 다PC). **정책 결정**: root 폐지(개별 repo 기본) 채택 — 다만 root는 구조 축만 지우므로 **정책+문서를 한 몸으로** 진행(①은 nastvad가 반증 — 이미 root 없는데 병합 문제가 거기서 나왔다). **8단계 여정 대입**(설치→타 팀원 설치→feature `/resume`→`/wrap`→release-inspect 병합→검수 탈락 되돌리기→main→신규 합류) 결과 신규 7건이 전부 **브랜치·사람 수 축**이었다. 핵심 셋 — **ⓐ F1**: 동시성 질문이 *"같은 단위를 동시 편집"*이라 서로 다른 프로젝트를 하는 팀은 정직하게 "아니오"가 나와 단위분할이 미적용된다(§E-3이 멀쩡한데 **분기가 다른 길로 보낸** 사례 — 규칙이 아니라 **질문 문언**이 뿌리) **ⓑ F3**: 단위분할은 §F-2 B를 전제하는데 B는 **글로벌 교체**라, A를 쓰는 팀원은 `docs/PROGRESS.md`를 못 찾고 **조용히 빈손**으로 끝난다(B엔 폴백이 있어 교체는 안전한데 **A엔 역방향 폴백이 없었다**) **ⓒ F6**: `git revert`가 PROGRESS 줄을 지우면 append-only 원칙이 **git 조작으로** 깨지는데 어휘조차 없었다(이 저장소는 *결정*의 번복만 다뤘고 *작업*의 되돌림은 미정의). 처방 — **R21** 기록 양식에 **갈래·날짜**(양식에 날짜가 없어 `/resume`의 "최상단=최근" 전제가 순서에만 의존했다 — 정본과 실무가 갈려 있었다) · **R22** `/resume` 갈래 우선(R21의 소비자 축) · **R23** 되돌림 `[Reverted]` append+원항목 표시, revert가 지운 줄은 되살린다 · **R24** `PROJECT_PLAN`·`CLAUDE.md` union 금지 명시(줄 수정이라 양쪽이 다 남아 파손 — 안 적으면 누가 넣어서 깬다). 그 외 STEP 1에 **기여자 수 감지**(`git shortlog`로 "혼자냐"를 묻지 않고 안다), 동시성 **3분기**, §E-3 팀원 전원 B 필수, A 폴백에 단위분할 감지, §E-2 **권장 기본값=개별 repo**, §F-5 조건을 소유→**git 중첩**으로(실물 결함), 00에 **신규 합류 분기**·**머신 로컬은 글로벌이 정본**(한 사람의 `gh 인증 대기`가 괄호 메모 보존을 타고 전원에게 상속되던 자리). 산출 **00 v1.30 · 01 v1.49**, RC·`~resume`·`~wrap`·미러 2종 동기화. **하향 2건**: 소유 축 분리(§E-4 개인 하드코딩 4곳)는 범용성엔 필요하나 root 폐지로 이 팀엔 무관 → 관찰 등재 / 팀원 배치 질문은 3분기에 흡수돼 보류 — @bsjeong87 (main) 2026-08-21

- [docs][Done] **적대적 diff 검토 결과 반영 — 의미 왜곡 2건 정정·소실 규범 2건 복원·임계 문언 통일·DEC-14 Superseded 표시**(검증 2건 중 나머지 1건 귀환, 판정 **FINDINGS**). 검토는 삭제 **64행 전수**를 `git show HEAD:`로 읽어 축 요약 3행과 대조하고, 백틱 식별자 전량 + 비-식별자 규칙 문구 37종을 개별 grep했다(식별자 누락 0, 문구 미회수 2). **① 의미 왜곡 2건 — 둘 다 내가 만든 것**: ⓐ `02` 병합 행에 `§0 기본 원칙`이라 썼는데 **02엔 §0이 없다**(절 구성이 `## A.`~`## L.`) — 01의 `§0` 패턴을 02에 복사해 **없는 절 참조를 신규 생성**한 것이다. ⓑ `01` 병합 행의 `/wrap 미러 대조 배선(`출처=`·4키 판정)`에서 `출처=`는 v1.20(점검·최신화 스킬의 설치 유형 판별) 소관이고 /wrap 미러 대조는 v1.25, 4키 판정은 v1.28이다 — **주체 변경**이자 `/optimize` §1이 명시적으로 금지한 실패 유형. 아이러니하게도 **R12 마커 담지체를 늘리려고 `출처=`를 끼워 넣은 것이 왜곡을 만들었다** — 마커 보존과 서사 정확성이 충돌할 수 있다는 실사례. **② 규범 손실 2건**: `01 v1.14`의 "커스텀 /resume·/wrap+`docs/` 기록 체계는 **내장 Tasks 대비 정본 유지**"와 `02 v1.13`의 "**§H Explore·Plan은 CLAUDE.md 미로딩**". `Tasks`·`미로딩`은 00~06 본문에 **0회**라 그 표 행이 유일 담지체였고, **이전 최적화 회차가 본문에서 걷어낸 뒤 표 행만 남은 상태**여서 이번 병합이 마지막 사본을 지웠다 — 압축이 2회에 걸쳐 규범을 잘라내는 경로가 실재한다. 병합 행에 복원. **③ 임계 모호(F8)**: "15행 이하"가 실제로는 물리 16행(버전 15+병합 1)이고 `check.ps1`의 `$vs.Count`는 병합 행을 안 세므로 다음 발동이 한 행씩 늦어진다 → **"버전 행 15개 이하(병합 행은 세지 않는다 — `check.ps1` 계수와 같다)"**로 3곳 통일. **④ Superseded(F9)**: DEC-14의 결정 ②("버전 표는 `/optimize` 권한 밖")를 DEC-15가 같은 날 뒤집었는데 표시가 없어 CLAUDE.md 규율 위반이었다 → `[결정 ② Superseded by DEC-…-15]` 표시(①③은 유효). 조사 중복(`구 행을 …을`) 3문서·01 푸터의 죽은 `v1.0 행` 라벨도 정정. 산출 **01 v1.48·02 v1.32**(00은 조사 수정뿐이라 bump 없음 — 재적용 델타 가치 0). **관찰 유지**: `/dropin-check`·`/dropin-update`가 표 첫 행인 병합 행을 못 보는 건(F5·F6) **두 검토가 독립적으로 같은 지점을 지적**했고, R20 필수 목록에 `~check`·`~update`가 없어 배선해도 드리프트를 못 잡는다(F7)는 지적이 더해졌다 — @bsjeong87 (main) 2026-08-20

- [docs][Done] **재적용 시뮬레이션 결과 반영 — 표 분리 5문서 복구 · `check.ps1` 검사 추가(발화 시험 통과) · 00 폴백 정밀화**(백그라운드 검증 2건 중 1건 귀환, 판정 **성립(조건부)**). **F2 — 실물 결함**: 03에서 고친 빈 인용 줄(`>`)이 **00·01·02·05·06에도** 있어 최신 행들이 **헤더 없는 둘째 표**로 렌더되고 있었다. 06은 `HEAD~3`에 이미 있던 기존 결함이고, 나머지 넷은 빈 줄이 표 **끝**에 있던 탓에 **오늘 내 bump 삽입(`※ 갱신 시` 줄 앞)이 그 뒤로 들어가 악화**시킨 것이다 — 즉 절반은 기존, 절반은 자책. 5문서 복구. **`check.ps1`에 "인용 표 안의 비-표 줄" 검사 추가** — 오늘 "1회 재발하면 배선"으로 등재한 관찰이 **5회 재발**했다. 검사기는 06에 결함을 주입해 **exit 0→1→0으로 발화를 실측**했다(시험 안 한 검사기는 없는 것과 같다). **W1 — 문구 결함**: 오늘 내가 쓴 폴백이 "상세 델타 **대신** 그 요약을 제시"로 읽혀 **읽을 수 있는 상세 15행까지 버리는 독법**이 성립했다 → "**구간분만 축 요약으로 갈음하고 구간 뒤 상세는 그대로 제시**"로 못 박고, 정수 비교(`v1.9`는 `v1.16`보다 낮다)·`전체 모드`와의 어휘 충돌 분리·거절 경로·`출처=클론`이면 `git log -p` 복원 경로를 함께 명시. **00 v1.29**(내용 변경). 01·02·05·06은 **공백 제거뿐이라 bump 없음** — 재적용 델타 가치가 0인 변경으로 방금 줄인 표에 5행을 되늘리는 건 자기모순이다(4축은 그대로 일치). **미반영 → 관찰 등재**: ⓐ **F1** `/dropin-update`·`/dropin-check`가 "로컬 버전 이후 행"으로 스캔하는데 병합 행은 **표의 첫 행**이라 걸리지 않아 구간분이 **경고 없이 조용히 사라진다**(R20 필수 목록 3곳에 이 둘이 없다) ⓑ 축 요약에 **버전 앵커가 없어** 구간 안 사용자에게 "당신에게 새로운 부분"을 특정해 줄 수 없다 — R20이 구조적으로 수용한 손실이며 요약을 더 잘 써서 고칠 수 있는 종류가 아니다. **적대적 diff 검토는 아직 미귀환** — @bsjeong87 (main) 2026-08-20

- [docs][Pending] **R20 신설(버전 표 병합) + 00·01·02 병합 실행 — 7종 −3.8%(137,799→132,551자), `check.ps1` 통과. 검증 2종 미완**(사용자 지시 "25→15행으로 진행"). `/optimize` 2회차가 관찰로 넘긴 최대 부피 축을 같은 날 규칙으로 승격. **검토 중 내 설계 전제 2건이 틀렸다**: ⓐ "`check.ps1` 결번 검사가 삭제를 막는다"는 부정확 — 검사는 **연속한 두 행만** 비교해 표가 `v1.0`에서 시작하는지는 보지 않으므로 **머리 자르기는 그대로 통과**한다(막히는 건 중간 행 삭제뿐) ⓑ 어제 등재한 트리거 "표 비중 25%"는 **본문이 줄어도 비중이 오르는** 모순이라, 최적화가 성공할수록 트리거가 당겨진다 → **행 수 기준**으로 교체. **설계**: 외부 아카이브 파일이 아니라 **문서 안 병합 행**. 근거 — 표 파손 검사가 연속 표 블록의 **파이프 수 일치**를 요구해 "이전 이력: …" 포인터 줄은 표를 두 동강 낸다(오늘 03에서 고친 결함이 정확히 그 실패 유형이다). 병합 행은 파이프 수가 같아 통과하고 **드롭인 자족성도 유지**된다(md 한 장만 받아도 성립). **병합 ≠ 삭제** — 01은 기록 v1.26에서 **8일 만에 20행 뒤처졌다**. 그냥 지우면 그만큼 뒤처진 설치의 재적용 델타가 사라지므로 구 행을 **축 요약 한 줄**로 남긴다(R18 "요지만"을 표 자신에게 재귀 적용 — 재적용 델타 소비자는 이미 행들을 축으로 묶어 읽고 있었다). **배선 5곳**: `docs/RULES.md` R20 행(마커 `버전 표 병합` — 후보 3종 중 충돌 0인 것으로, 필수 `00`·`RC`·`p:optimize`) · `CLAUDE.md` 문서 관리 규칙 · `00 §A STEP 3` **폴백**(기록 버전이 병합 구간 안이면 축 요약 제시 + 전체 재적용 권장) · `/optimize` §1의 "행 수 보존"에 R20 예외 + 집행자 지정 · `check.ps1` **임계 감지**(25행 초과 INFO — 규칙만 세우고 생산자를 안 두면 조용히 썩는다, R09 교훈. 현재 7문서 모두 25행 미만이라 잠복). **실행**: 00 14행·01 33행·02 17행 → 축 요약 3행(**−5,248자**). 삭제 직전 **마커 생존 정밀 검사**를 따로 돌렸다 — "마커가 표 행에만 있는가"가 아니라 "**지울 바로 그 행들**의 마커가 다른 데도 있는가"로 질문을 바꿔야 했고, 9종 전부 잔존 확인. 잔존이 **1곳뿐**이던 `merge=union`·`enterprise > personal > project`·`YYYY-MM경`·`출처=`는 요약문에 **의도적으로 포함**해 담지체를 늘렸다. 중간에 bump 행의 "R20 버전 표 병합" 문구가 마커 미등록 사본으로 INFO에 걸려 — 규칙 사본이 아니라 적용 기록이고 03~06 병합 때마다 재발할 문구라 — 허용 목록을 늘리는 대신 "R20 적용 —"으로 문구를 바꿔 해소. 산출: **00 v1.28·01 v1.47·02 v1.31**. **미완**: 적대적 diff 검토·재적용 시뮬레이션(백그라운드 실행 중 세션 종료) — 통과 전까지 커밋 보류 ([[DEC-20260820-bsjeong87-15]]) — @bsjeong87 (main) 2026-08-20

- [optimize][Done] **`/optimize` 첫 스킬 실행(2회차) — 7종 순 −0.20%(138,074→137,799자), 검증 3종 통과**(사용자 `/optimize`). 절차 자체는 2026-08-13에 수동 완주했고 **스킬 호출은 이번이 처음**이다. 측정: 1회차 직후 117,691자 → 7일 만에 **138,074자(+17.3%)** 로, 최적화 *전*(133,912자)보다도 커졌다 — 그 주에 늘리는 축만 3회(audit 54건 → reverify 1·2회차 → 편입 3건) 돌았다. **압축 24건(−711자)**: 버전 표 12행(경위 라벨·과거 상태 서술·중복 귀결) + 본문 12곳 — 이 중 값진 건 **§L이 정본인 인라인 재검증 날짜**였다(`deny 현행 동작(2026-08-04 재검증)`·`auto mode (2026-07-28 확인)`은 §L의 "정기 확인 2026-08-20 전부 유효"와 갈린 낡은 값이라 부피이자 모순이었고, 지우니 둘 다 해소됐다). **검증 3종**: ⓐ `check.ps1` 통과(R01~R19 마커 무손상) ⓑ **적대적 diff 검토** 결함 1건 — `00 v1.23`의 `(루트 레이어 중복 설치 차단)`이 **드롭인 페이로드 내 유일 담지체**였다(규칙 도입 커밋이 00엔 표 행만 넣어 STEP 2 본문에 담지체가 없다. md만 받은 프로젝트는 "역방향 감지"만 남고 무엇을 막는지 사라진다) ⓒ **재적용 시뮬레이션** 성립 — 단 `02 v1.29`가 실질 상실(§H 본문엔 온존하나 **델타 독자는 그 사이 행만 읽는다**). **두 검토의 합격 기준이 갈렸다**(페이로드 내 담지체 유무 vs 델타 소비자 도달) — 00 §A가 "그 사이 행만 읽으면 된다"고 못 박은 이상 후자가 더 강한 시험이라 **4건 복원(+35자)**. 별건: `03` v1.16 앞 **빈 인용 줄로 버전 표가 두 표로 갈려** v1.16~v1.19가 헤더 없이 렌더되고 있었다(`check.ps1` 표 검사 사각) — 복구. **핵심 실측**: 7일 성장분 19,672자 중 **32%(6,291자)가 버전 표**인데 신규 38행이 이미 R18을 지켜 압축 여지가 없고, `check.ps1` 시간순·결번 검사가 삭제를 막는다. 게다가 **bump가 문서당 1행(+401자)을 도로 넣어** 총 −711 압축이 순 −275로 줄었다. 산출: 00 v1.27·01 v1.46·02 v1.30·03 v1.20·05 v1.13 + 00 §A 델타 경계 명시(시험이 찾은 한 줄 처방 1건), 관찰 4건 등재 ([[DEC-20260820-bsjeong87-14]]) — @bsjeong87 (main) 2026-08-20

- [docs][Done] **`/wrap` 점검 — `/optimize` 예정일 형식 누락 1건 수정**(세션 마무리). 유지보수 4축 중 **`/optimize`만 `다음 ○○: YYYY-MM경` 형식이 없었다** — 나머지 셋(실적용 2026-11 · audit 2027-02 · 재검증 2027-02)은 전부 보유. R09가 규정한 그 형식이 없으면 `/resume`의 예정일 안내가 **발화하지 않으므로**, 오늘 등재한 항목이 안내 대상에서 빠진 채 미해결 목록에만 남는 상태였다. `다음 /optimize: 2026-09경` 부여(조건은 이미 충족했으므로 **상한이지 대기선이 아니라는 단서**를 함께 적었다 — 날짜만 보고 9월까지 미루면 등재 취지가 뒤집힌다). 부피 실측: PROGRESS 43,378B·DECISIONS **95,515B**(임계 120KB의 80% — 오늘 DEC이 13건 늘어 다음 세션 전후로 닿는다) — @bsjeong87 (main) 2026-08-20

- [docs][Done] **확장 후보 결정 — 편입 3건 반영·신설 1건 보류, `check.ps1` 통과**(사용자 지시 "추천대로 진행"). 판단 자료로 신입 개발자 눈높이 HTML 1종을 먼저 만들어 Artifact로 발행(기존 사람용 페이지 시스템 승계 — `--paper`/`--ink`/Noto Serif KR + IBM Plex Mono, Pretendard는 CSP상 로드 불가라 Noto Sans KR 폴백 추가, 다크 팔레트는 신설). **편입 3건**: ① `02` §H **에이전트 팀 경고** — 켜면 이름 붙은 서브에이전트가 **팀원으로 떠 결과를 반환하지 않아** 로스터 분담이 오류 없이 죽는다(답이 안 오는 것으로만 나타남). 셋 중 유일하게 "쓰지 말라"는 항목 ② `01` §J **격리 3단**(내장 샌드박스→dev container→VM) — §J가 "네이티브 Windows 미지원"에서 서술이 끊겨 **이 저장소 주 환경 사용자를 "격리를 못 쓴다"는 틀린 결론으로 보내고 있었다**. 보강이 아니라 사실상 정정 ③ `02` §E 말미 **다이내믹 워크플로**(로스터=누구에게 / 워크플로=어떤 순서로 몇 명을) + §F 규모 상한. **세 곳 모두 §L에 소비자 축을 함께 달았다** — 신규 서술은 정정과 달리 근거 요구가 안 걸려 어떤 검사도 안 거친 채 커밋된다(DEC-20260820-12 ①의 직접 적용). **신설 1건(플러그인 제작) 보류** — 값어치가 아니라 ⓐ 수요 미실증(해시 대조로 현재 방식이 작동 중, 배포 대상 1인) ⓑ 파급이 `global-config/`·`check.ps1`·`/dropin-check`·`/dropin-update`까지 미침 ⓒ 이날 늘리는 축만 3회 돌고 `/optimize` 미실행. **재평가는 `/optimize` 1회 후**, 기준은 "수동 복사가 실제로 사고를 냈나"로 고정. PROJECT_PLAN에 `/optimize` 첫 실행을 미해결로 신규 등재. 산출: 01 v1.45·02 v1.29 + `docs/reverify-20260820-2.md` 결정 절 + Artifact 1종 ([[DEC-20260820-bsjeong87-13]]) — @bsjeong87 (main) 2026-08-20

- [reverify][Done] **전면 재검증 2회차 — 🟡 6건 중 5건 종결·[상] 정정 8건 반영, 확장 후보 10건 파일 보존, `check.ps1` 통과**(사용자 `/reverify` 재실행). 같은 날 2회차라 D축 신규분은 적을 것이 예상됐으나, **1회차 산출물 자체가 검사 대상이 되어 수확이 더 컸다**. 4축 병렬(A 모델·effort / B 권한·hooks·메모리 / C 외부 소스·중복 재심 / D 신규 후보), 축 간 판정 충돌 **0건** — 오히려 B축이 재구성한 `sandbox.credentials` 키(`extract`·`decode`·`sigv4`)를 D축이 changelog v2.1.224 항목으로 독립 확인해 판정이 굳었다. **🟡 종결 5건**: ① `sandbox.credentials` 스키마 확보 — **1회차가 못 찾은 원인은 소스 부재가 아니라 조회 도구 절단**(settings 페이지가 길어 fetch가 알파벳 중간에서 잘림 → `settings.md` 원문 직접 수신). 01 §L의 🔴🟡가 **소진**됐다 ② 03 플러그인 경로 3종 — 감지 정본은 `enabledPlugins`이고 `installed_plugins.json`은 **공식 문서 0회 등장 내부 파일** ③ schemastore 301 — **고치지 않는다**(스키마 `$id`가 `json.`으로 자기 선언, `www.`는 호스팅 계층). 질문이 틀렸던 유형이라 근거를 남겨 닫음 ④ glab 공식(brew)↔커뮤니티(winget) 구분 ⑤ GitLab 구독 인증 비대칭(API 키 전용). 출력 스타일 스코프 우선순위 1건만 🟡 유지(여전히 공식 미명시). **[상] 8건**: `02:123` 상한 초과가 "큐 대기"가 아니라 **스폰 실패**(1회차가 §L 빈 자리를 메우며 새로 쓴 문장 — `git log -S`로 `555bce4` 유입 확인) · `02:210` advisor 기준선이 "상위 단독"인데 같은 절 `02:216`은 "Sonnet 단독"으로 **내부 모순** · Fable 5 advisor **복귀**(v2.1.232) · Read deny가 **Write까지 차단**(v2.1.228+, `01`에 **3곳** — B축은 2곳만 지목) · auto mode는 Team·Ent도 **기본 제공**(옵트아웃) · **Pro·Max·Team은 8/14부터 auto가 기본 시작 모드**·`Shift+Tab` 순환 순서 정정 · `04:117` 구독 인증 "Pro/Max"→**4개 플랜**(회사 repo가 04의 주 대상이라 오분기 비용 큼) · 부록 **Caveman이 내장 Concise에 흡수**(부록 §L이 요구하는 이번 회차 유일 재판정 사례). **[중]** Context7 공식 마켓 등재·LSP **11→13종**·"200+종"→카탈로그 위임+공식/커뮤니티 마켓 구분·`02` §C 1M 목록 §I와 일치·**Sonnet 5 $2/$10 확정**(9/1 인상 취소)·Haiku 단가·토크나이저 ~30% 보정·`ANTHROPIC_DEFAULT_MODEL`·`attribution.sessionUrl` 기본 true·hooks 매처 `,` 허용·`npx skills`는 **서드파티 CLI**(vercel-labs)·Chrome은 **직계약 플랜만**·`05` §L에 "changelog 정본, 레퍼런스 낙후" 경고·`06`에 **`/schedule` 은닉 조건**(`DISABLE_TELEMETRY` 등)·audit 렌즈 F "전용" 어휘 정정. **확장 후보 10건은 `docs/reverify-20260820-2.md`에 보존**(신설 추천 1·편입 3·관찰 2·기각 4) — 1회차 5건이 이름 없이 소실된 재발 방지로 `/reverify` §3에 산출물 파일 규칙 배선. 산출: 01 v1.44·02 v1.28·03 v1.19·04 v1.15·05 v1.12·06 v1.12 + `reverify`·`audit` 스킬 + 후보 파일 1종 ([[DEC-20260820-bsjeong87-12]]) — @bsjeong87 (main) 2026-08-20

- [docs][Done] **미해결 3건 일괄 종결 — R19 신설·R07 재설계·PROGRESS 아카이브 1차 실행, `check.ps1` 통과**(사용자 지시 "필요한 권장 수정 전부 진행"). ① **A(R19 신설)**: `/resume` 예정일 안내에 소비자 필터를 배선 — **미완료 항목만**(줄 머리 `- [x]`·취소선 제외). **R09를 확장하지 않고 별도 행으로 둔 것이 판단의 핵심** — 마커를 공유하면 소비자 사본(`~resume`)이 빠져도 생산자 3곳만으로 검사가 통과해, 결함의 원인이던 "소비자 미추적"이 검사 자신에게서 재현된다. 필수 `01` `~resume` 2곳. 실측: 필터 전 7줄 중 **3줄이 이미 종결된 회차**(106·107·109) → 필터 후 4줄·경과 0. 검증 중 **줄 머리 판정으로 정밀화 1건** — 본문에서 `[x]`를 *언급*하는 미완료 항목(123행)이 종결로 오판돼, "줄 머리 체크박스로 판정"을 명시했다. ② **B(R07 재설계)**: 임계를 `약 800줄` **또는** `약 120KB`(먼저 닿는 쪽)로 — **교체가 아니라 축 추가**다(1항목=여러 줄로 적는 타 repo에선 줄 수가 여전히 먼저 닿으므로 범용성이 깨지지 않는다). 더 큰 변경은 **자를 양을 달력에서 목표치로** 옮긴 것: "가장 오래된 분기"로 자르면 2026-07은 25항목이지만 **11.7KB뿐**이라 활성이 104KB(임계의 87%)로 남아 한두 세션 뒤 재발화한다 — 임계로 촉발되는 규칙이 임계 아래로 못 내려가면 매 세션 잡음이 된다. 그래서 **활성 파일이 임계의 절반 이하가 될 때까지**(가능하면 날짜 경계). 이 목표를 만족하는 경계는 2026-08-12 앞 **하나뿐**이었다(08-12 하루가 22항목 40KB 덩어리라 그 사이에 경계가 없다). **실행**: 80항목을 `docs/archive/PROGRESS-20260720-20260812.md`로 분리, 활성 **116,593B → 35,236B**(항목 100 = 활성 20 + 아카이브 80, 전수 보존 확인). ③ **C(DECISIONS)**: "규칙 부재"를 규칙 편입으로 닫음 — R07 대상에 DECISIONS 포함(같은 임계·같은 절차). **지금 조치는 없다**(86,331B = 임계의 72%). 등재된 정정 근거(/resume이 항목 단위로 읽어 읽기 비용에 안 잡힘)는 유효하므로 긴급성은 여전히 낮고, 바뀐 건 임계에 닿았을 때 무엇을 할지가 정의됐다는 점이다. **배선**: `docs/RULES.md`(R07 정정·**R19 신설** — 대장 19행) · `01` v1.42→**v1.43**(§F-1 길이 관리·세션 워크플로 + §F-2 A/B/C 페이로드 6곳) · repo `CLAUDE.md`(길이 관리+구성물 표) · `~resume` §3 · `~wrap` + `global-config/skills/{resume,wrap}` 미러 2종. 검증: `check.ps1` **이상 없음(INFO 0)** — R07 필수 3곳·R19 필수 2곳·미러 25개 일치·버전 4축 일치 — @bsjeong87 (main) 2026-08-20

- [reverify][Done] **확장 후보 superpowers 재판정 — 기각 유지·근거 갱신(03 v1.18), `check.ps1` 통과**(사용자 승인 "재판정 진행"). `/reverify` §3 승인 경로대로 전용 에이전트 스펙 조사 → 재판정 → 반영. **출발점 정정**: C축이 "공식 마켓 등재 = 역전"으로 올렸으나 03 부록의 실제 기각문은 **기능 중복**이었다 — 축이 다르다. **조사 결과**: 스킬 14종·commands 0·agents 0·hooks 1(SessionStart). 겹침 2축 → **4축**(02 로스터 — `.claude/agents/`를 안 만들고 `general-purpose`로 디스패치해 로스터를 무력화 / plan mode — 앞단 게이트로 감싸기 / **내장 `/code-review` 우회**(신규) / **skills 호출 거버넌스 재정의**(신규)). 고유는 `systematic-debugging` 1종. 🔴 **SessionStart 훅 충돌 실측 확인** — 우리 글로벌 `compact` 훅은 "PROGRESS·PROJECT_PLAN을 다시 **읽어라**", superpowers `compact` 훅은 "**파일 확인**보다 스킬 호출이 먼저"로 같은 시점에 반대 지시를 주입한다. **공식 등재의 의미**: 공식 문서가 *신청 절차 없음*·*검증 불가*를 명문화 — 심사가 아니라 재량 큐레이션이라 판정 축을 못 움직인다. **결정타는 우리 환경** — 이 저장소는 코드·빌드·테스트가 없어 TDD·SDD·worktree의 산출값이 0인데 훅 충돌 비용만 낸다. 반영: 03 부록 행을 4축 겹침·훅 충돌·워킹트리 오염·텔레메트리·공식 등재의 의미로 교체하고 조건을 **"02를 안 쓰는 코드 프로젝트에서만"**으로 명시. `skill-setup` 설치 경로에 공식 마켓 우선 병기(구식 벤더 마켓만 안내하던 것)+판정 정본을 03 부록으로 가리킴(R14). **에이전트 보고 절반 정정** — "skill-setup이 여전히 권장 후보로 싣는다"는 과했다: 조건부 비권장과 SessionStart 훅 중복 게이트가 이미 있었고 실제 갱신 대상은 설치 경로 하나였다. PROJECT_PLAN 🟡 종결 ([[DEC-20260820-bsjeong87-10]]) — @bsjeong87 (main) 2026-08-20

- [reverify][Done] **재검증 마무리 점검 — 미처리 2건 발견·수정, `/reverify`에 §L 갱신 배선**(사용자 질의 "확장 후보 외 모두 처리됐나" → "진행"). 답하려 커밋된 파일을 열어 확인한 결과 **2건 미처리**였다. ① **각 문서 §L 미갱신** — `PROJECT_PLAN` 🟡만 재작성하고 정본인 §L은 안 고쳤다. `04 §L` hooks 🟡(오늘 해소)를 ✅로 내리고, `01 §L`의 🔴🟡 5건 중 **유효 확인된 4건**(deny 우회·샌드박스 Windows 미지원·`attribution`·auto-memory 한도)을 분리해 🔴🟡는 `sandbox.credentials`만 남겼다. ② **출력 스타일 우선순위 🟡의 종결 근거 부재** — 근거를 쓰려 원문을 다시 열자 **`05:77`이 실제로 틀려 있었다**: 공식은 "가까운 쪽 우선"을 *프로젝트 스코프 안 중첩 폴더*에만 규정하는데 05가 "여러 **스코프**에 있으면"으로 일반화했다. 정정하고 스코프 간 우선순위 미명시를 §2 본문·05 §L·PROJECT_PLAN 🟡에 재등재. **🟡가 옳았고 앞선 종결이 성급했다.** **재발 방지**: `/reverify` §2에 "§L 갱신은 반영의 일부 + 해소는 값 확인/질문 철회 두 종류" 배선 — §0이 §L을 정본으로 읽는데 쓰기 지시가 없던 게 원인이라 한 줄로 닫았다. **부수**: 05에 인용한 `enterprise > personal > project`가 R06 마커여서 `check.ps1` INFO 발생 → 대장에 허용 행을 늘리지 않고 문자열을 빼고 포인터만 남겨 INFO 0 복귀. 산출: 01 v1.42·04 v1.14·05 v1.11 + `/reverify` 스킬 ([[DEC-20260820-bsjeong87-09]]) — @bsjeong87 (main) 2026-08-20

- [reverify][Done] **전면 재검증(v2.1.237) — 정정 5건 반영·🟡 17건 해소, 7문서 bump, `check.ps1` 통과**(사용자 `/reverify`). 조사: 4축 병렬(A 모델·effort / B 권한·hooks·메모리 / C 외부 소스·중복 재심 / D 신규 후보), §0에서 **PROJECT_PLAN 🟡 22항목 + 00~06 §L 41항목**을 범위로 합침. **정정 5건**: ① CLI 기준 v2.1.226 → **v2.1.237**(7문서 헤더·푸터 14곳) ② 05 내장 출력 스타일 **4종 → 5종**(Concise, v2.1.237 당일 추가) ③ 02 §E 서두에 **동시 실행 상한 20·중첩 스폰 깊이 3** 배선 — §L이 가리키던 자리에 서술이 없었다 ④ 03 §3 frontend-design 글로벌 설치를 `npx skills add … -g` 한 줄로 교체(수동 이동 지시 폐기) ⑤ `skill-setup`의 `/skills`는 **실재하지 않는 명령** → `/` 자동완성·`/context`로 교체. 부수로 `/audit` 렌즈 F의 frontmatter 구분자 서술 정정(검사 대상을 `paths`·`arguments`로 이동). **6차 audit 인계 3건 해소**: hooks 세션 즉시 반영(file watcher, 04 §1 전제 유효) · PreCompact `exit 2` stderr는 **사용자 화면에 표시**(override 안내 살아 있음) · `autoMode.soft_deny`는 override 가능한 정책이라 **보안 경계 아님**(미러 4키 제외 근거). **유효 확인**: 샌드박스 네이티브 Windows 미지원 유지 · auto-memory 200줄/25KB · `attribution` · deny 서브프로세스 우회 한계 · 모델 별칭 6종·계정별 기본·폴백 3개 · effort 5단계·`ultracode`·fast mode · advisor −11.9%/+2.7%p(1차 출처 직접 확인) · **외부 URL 12종 전건 200** · claude-hud 설치 명령 4단계 · GitLab CI/CD beta 유지·API 키 전용(04가 정확) · routines preview 유지·`/schedule` 3종(중지·삭제 웹 전용, 06이 정확) · Chrome GA · `/team-onboarding` 실재 · frontend-design 내장 아님(03이 정확). **교차 검증에서 2건이 갈렸다** — B축이 두 번 낡은 레퍼런스 페이지를 봤고 changelog·원문을 가진 쪽이 옳았다 ([[DEC-20260820-bsjeong87-08]]). **확장 후보 5건은 보고만**(신설 없음, 선택 대기). PROJECT_PLAN 🟡을 잔여 6건으로 재작성하고 다음 재검증 **2027-02경** 등재. 산출: 00 v1.26·01 v1.41·02 v1.27·03 v1.17·04 v1.13·05 v1.10·06 v1.11 + `skill-setup`·`audit` 스킬 + 미러 1종 — @bsjeong87 (main) 2026-08-20

- [docs][Done] **`/resume` PROGRESS 읽기 단위 재정의(줄 수 → 최근 ~5항목·항목당 앞 ~700자) — R02 교체, `check.ps1` 통과**(사용자 지시 "1, 2 둘다 진행"). PROJECT_PLAN이 "다음 개정 세션에서 단독 처리"로 이월해 둔 항목. **이번 세션의 `/resume` 자신이 결함을 실행**해 `head -40`이 80KB로 밀려난 것이 착수 계기. 설계는 사용자 선택(항목 수 단독 / 항목 수+항목당 상한 / 총 문자량 3택 중 **2축**). 배선: `docs/RULES.md` R02 규칙문+**마커 `40줄`→`5항목`** → 필수 4사본(`01` v1.39→**v1.40**·repo `CLAUDE.md`·`~resume`·`~wrap`) → `global-config/skills/{resume,wrap}` 미러 재복사 → `check.ps1` 전 항목 OK(R02 필수 4곳·허용 1, INFO 0, 미러 25개 일치). 치환 21곳 + 버전표 1행. 워크스페이스 루트의 하위 repo 훑기도 `~10줄`→`~2항목` 동반 교체. **실측 19,853자 → 3,364자(17%·5.9배)** — 상위 5항목 중 4개가 700자에서 잘려 항목당 상한이 실제로 작동. **조사 중 발견**: 1차 사본 조사가 `grep -v`로 경로를 걸었는데 본문이 매칭돼 `01`의 2곳(`:304`·`:313`)과 resume 미러가 누락됐다 — 경로 필터를 필드 기준으로 바꿔 재조사해 전수 확보(이 저장소 결함의 다수가 "사본 누락"이라 조사 방법 자체가 위험원이다). **stale 체크박스 2건 정리**: `PROJECT_PLAN:107`·`:137` — 본문은 취소선+종결 기록인데 박스만 `[ ]`였다(107은 줄 자신이 "수정을 마치고 check.ps1 통과해야 체크"라 적었고 그 조건은 어제 충족). **DECISIONS 아카이브는 보류 유지 + 근거 정정**(읽기가 이미 항목 단위라 무증상 — 등재 근거가 사실과 달랐다). **미종결로 분리 등재**: R07(800줄 임계)의 줄 수 축은 이 형식에서 여전히 발화하지 않는다(191줄/108KB=임계의 24%) — 크기 축 재기준은 즉시 Q2 아카이브 실행을 부르므로 다음 개정 세션으로 ([[DEC-20260820-bsjeong87-07]]) — @bsjeong87 (main) 2026-08-20

- [audit][Done] **/audit 6차 회귀 재도출 — 확정 54건 전건 수정, check.ps1 통과**(사용자 지시 "회귀 모드로 다시 돌려 34건을 재도출" → 저장·전체 수정 승인). 6차가 산출물 파일을 안 남겨 [하] 21건이 소실된 상태에서, PROJECT_PLAN:131이 예정한 복구 경로대로 재도출. 렌즈 A~F 6종 병렬 + **발견 전건 원문 재검증** → 원시 69(A 12·B 8·C 13·D 17·E 6·F 13) → **확정 54(상 3·중 27·하 24)·인계 4·기지 2·오탐 0**. 6차 34건 대비 **+20**. 교차 지목 6건(`00:144`·`00:91`은 A·B, wrap-gate는 A·C, `01 §D-3`은 A·E·F, §L 미배선은 B·F). **[상] 3건**: ① wrap-gate가 `$dirty`·`$hasToday` **두 축 독립 실패** — 타 repo에선 `/wrap`+커밋을 마쳐도 `(미커밋 0, 오늘 기록 False)`로 차단(6차는 [상]1+[중]1로 나눠 봤다). 등재된 처방 "좁히기"는 **악화시키므로 기각**하고 `docs/**/PROGRESS.md` 변경 유무 단일 축으로 교체 ② `/reverify`가 00~06 §L을 한 번도 안 읽어 **§L 신규 축이 도착 즉시 사망**(6차의 04 GitLab 1건은 증상 — 02 가격·03 Chrome도 이미 죽어 있었다). §0에 §L 읽기 배선으로 `02:253`의 기존 주장을 참으로 만듦 ③ `/audit` §3이 미승인 회차 산출물을 보존하지 않음 — **오늘 재도출 자체가 이 결함의 비용**이라 §3에 "파일 먼저 쓰고 승인 대기" 배선. **부수 — 검사 도구 자신의 결함 2건**: `check.ps1` 미러 절이 백업만 순회해 "실물에만 있는 파일"을 못 봤고(새 PC 복원 손실 방향이 사각지대), 4키 비교가 키 순서에 민감했다. 둘 다 고쳐 미러 대조 24 → **25개**(`LICENSE.txt` 포함). 산출: **7문서 bump**(00 v1.25·01 v1.39·02 v1.26·03 v1.16·04 v1.12·05 v1.9·06 v1.10) + 스킬 6종(audit·reverify·optimize·resume·wrap·dropin-update/check) + `check.ps1`·`RULES.md`(R09 허용 열)·`CLAUDE.md`(`global-config/` 날짜 8일 낡음 — M27, 통합 중 발견)·`README.md`·`options.html`·`skill-setup` + 미러 7파일. 6차 미열거 [중] 6건(03 hud 스코프 검증·03 표시 구성 3택·06 GitHub App 게이트·04 OAuth 제거·04 멘션 트리거·04 §3-B 귀속)도 함께 처리. 보고서 정본 `docs/audit-20260820-6.md`. 인계 4건은 PROJECT_PLAN 🟡 등재. **`a13adf5` 관련(L24)**: 제목이 "skill-setup 정본 동기화"인데 산출물이 없었다 — 실물·미러가 이미 같아 diff 0이었던 것으로 판단(방향 미판별). 앞으로 diff 0인 동기화는 커밋 제목이 아니라 여기 본문에 방향과 함께 적는다 ([[DEC-20260820-bsjeong87-06]]) — @bsjeong87 (main) 2026-08-20

- [audit][Pending] **/audit 6차(회귀 모드) — 확정 34건 보고, 수정은 미착수(승인 대기)**(사용자 `/audit`, PROJECT_PLAN 등재 절차 이행 — 08-20 6문서 대량 개정 직후). 렌즈 6종 병렬 + 발견마다 원문 재검증(4판정) → 원시 39 → **확정 34(상 1·중 12·하 21)·인계 3·오탐 1·기지 1**. check.ps1은 전 항목 통과(기계 축은 이미 확정이라 렌즈 B·E가 판단 축만 봄). **34건의 절대다수가 어제 수정 자신이 만든 회귀** — 3·5차에 이은 세 번째 재현이라 DEC-20260812-bsjeong87-15 ①(대량 수정 뒤 회귀 감사는 절차)이 다시 값을 했다. **[상] 1건**: `/compact` wrap-gate 통과 조건이 `미커밋 0 AND 오늘 기록`인데 **/wrap 정본은 커밋하지 않는다**(글로벌 wrap 스킬 "미커밋이 남으면 경고하라") — 이 repo는 커밋 상시 허용이라 어제 4상태 실측이 통과했으나 hook은 글로벌이라 **타 repo에선 정석 순서(작업→/wrap→/compact)가 항상 차단·오보**되고 5분 override가 상시 탈출구로 굳는다. 겹치는 [중] 1건: 날짜 판정(`yyyy-MM-dd` grep)의 생산자가 없다 — 기록 양식 정본(01 §F-1·CLAUDE.md)에 **날짜 필드가 없고** 이 repo의 병기는 관행이다. **[중] 12건 요지**: 03 `/claude-hud:*` 검증 축이 설치한 그 프로젝트에서 돌아 스코프 실패를 못 잡음(03 §1 정의와 모순) · 06 GitHub App **선설치 게이트 부재**(렌즈 C·D 독립 지목 — 수동 검증까지 통과해 첫 실제 이벤트까지 미발견) · 06 §4 검증이 게이트 차단 분기 없이 §3 산출물 요구(v1.6/v1.7이 §2에서 고친 것과 같은 결함) · 04 제거 절이 OAuth 토큰 누락(구독 자격증명 잔존) · 04 검증이 `@claude` 멘션 트리거 미반영 · 04 §3-B 셋째 불릿 귀속 상실+"주입" 주어 부재 · 00 §B 04행이 폐기된 "headless 폴백" 전제 · 01 §D-3 hooks 예시가 실물·미러와 3자 드리프트(렌즈 E·F 독립 지목) · **04 §L의 GitLab 축이 소비자 두 곳(/reverify C축·PROJECT_PLAN 🟡)에 모두 없음** · 05 글로벌 병기 미인라인 · 03 표시 구성 3택 미배선(막다른 길). **인계 3건**은 PROJECT_PLAN 🟡 등재(플러그인 경로 3종 관계·`autoMode.soft_deny` 구성 성격 여부·GitLab 러너 요건). 수정 방식은 사용자 승인 후 결정 — 특히 [상]은 좁히기/되돌리기/고치기 세 방향이 갈린다. **부수 — /wrap의 미러 대조가 감사 6차가 놓친 35번째 결함을 잡았다**: `scripts/check.ps1:149-151`이 `ConvertTo-Json -Compress` **문자열 비교**라 **JSON 키 순서에 민감**하다. 이번 세션 중 `/model` 전환으로 정본 `settings.json`이 재직렬화되며 `command`↔`statusMessage` 순서만 바뀌었는데 `settings.hooks 실차이` FAIL이 났다(정규화 비교로는 **내용 동일** 실측 — 복사 불요). 01 §D-3 ⑤가 deny에 대해 "집합이라 배열 순서는 무관"을 이미 명시했는데 같은 원리가 키 순서엔 미적용이다. 방치하면 **오검출이 상시화돼 검사 자체를 불신하게 되고**, 방향 판정을 거쳐 불필요한 미러 복사를 권하게 된다(정규화 비교로 교체 권장 — 승인 대기) ([[DEC-20260820-bsjeong87-05]]) — @bsjeong87 (main) 2026-08-20

- [config][Done] **`/compact`에 /wrap 선행 게이트 배선 — 글로벌 PreCompact(manual) hook 신설**(사용자 요청 "/compact 시 /wrap 확인 + /wrap>/clear>/resume 추천"). 공식 hooks 레퍼런스 당일 조회로 **PreCompact exit 2 = 압축 실차단** 확정 후 구현: manual 게이트(PROGRESS 없으면 추천만 / 미커밋 0+오늘 기록=통과+추천 / 미충족=차단+5분 내 재실행 override) + 기존 auto 경고·SessionStart(compact) 문구 보강. 스크립트는 settings.json **인라인**(hooks/ 폴더가 백업 화이트리스트 밖이라 외부 파일은 새 PC 복원 누락). 검증: 4상태 실측 전부 통과(exit 0/2/0/0·마커 생성-소비 확인), 미러 재복사 후 check.ps1 settings 4키 일치. 실전 차단은 다음 /compact 입력이 첫 시험 ([[DEC-20260820-bsjeong87-04]]) — @bsjeong87 (main) 2026-08-20

- [docs][Done] **문서 전수 확인(claude-hud형 3패턴 소탕) — 6문서 bump + 적대적 diff 검토 13건 반영**(사용자 요청 "이번처럼 문서의 문제가 없게 문서들을 확인"). 3렌즈 병렬(외부 현행 대조 15출처·권장 절 배선 전수·실사례 미백포트 전수) → **채택 18·참고 확정 5·기각(기추적) 다수·등재 2**. 최대 발견 3: ① 04 §3-B "GitLab 공식 액션 없음"이 틀림 — 공식 CI/CD 통합(beta, 정본 템플릿·`@claude` 멘션) 존재 ② 03 §5 Chrome이 확장 설치에서 끝남 — 현행은 `--chrome`/`/chrome` 활성화+`/login` 필수, `/chrome` 상태 패널이 감지·검증 수단 ③ 01 §검증이 "빼지 않는다"로 필수화한 ⓑ(글로벌 deny)를 안 보던 비대칭. 그 외: 06 GitHub 트리거 CLI 가능(v2.1.225+)·커넥터 최소화 검증, 02 Fable 5 가격 $10/$50 기재(08-18 관찰 미백포트 해소, 당일 공식 확인)·`isolation:` 실측, 03 §1 스코프 배선 완성(감지 `installed_plugins.json`·검증 `/claude-hud:*` 명령 축). **적대적 diff 검토(커밋 전)가 15건 적발, 13건 채택** — 반영 자신이 만든 회귀(§6 "글로벌 전용" 잔존·STEP 1 감지 요약 미갱신·refreshInterval 조건부 검증 약형 등) 포함. 산출: **00 v1.24·01 v1.38·02 v1.25·03 v1.15·04 v1.11·06 v1.9**(05 무변경) + options.html 3곳·PROJECT_PLAN 등재 2(읽기 상한 단위 불일치·DECISIONS 아카이브 보류 추적)+hooks 실측 근거 부기. 검증: check.ps1 2회 전 항목 통과. **다음 세션 `/audit` 회귀 모드 1회 권장 등재**(6문서 대량 개정 — DEC-15 ①) ([[DEC-20260820-bsjeong87-03]]) — @bsjeong87 (main) 2026-08-20

- [docs][Done] **claude-hud 설치·추천 구성 결함 수정 — 03 v1.14, options.html 동기화**(사용자 보고 "새 PC마다 설치가 제대로 안 되고 추천 구성도 적용 안 됨" → 문서 확인 요청). 조사 2렌즈(문서 전수 / 실환경+업스트림 README 당일 조회) 후 판정 **채택 5·기각 1**: ① 설치 순서가 현행과 반대 — 문서는 `install→setup→reload`인데 현행 README는 `reload`가 `setup`보다 먼저(명령 미로드 상태에서 setup이 그 자리에서 실패 — "설치 안 됨"의 직접 원인) ② user 스코프 확인 부재 — 2026-08-11 실사례(project 스코프 설치로 `/claude-hud:*` 미표시, PROGRESS 기록됨)가 03에 미백포트 ③ **추천 표시 구성 절이 절차에 미배선** — STEP 2 질문·§검증 어디에도 안 걸려 표준 경로에선 적용될 수 없고 검증(`statusLine` 키만)이 미적용을 통과시킴("추천 구성 미적용"의 직접 원인) ④ setup 프리셋 서술이 버전 종속(구버전 0.3.0 실측 — setup에 프리셋 없음, configure 전용) → configure 폴백 병기 ⑤ `refreshInterval: 5` 권장 누락. 기각 1: 새 PC 플러그인 재설치 필요 자체는 기추적(PROJECT_PLAN 미해결·README 기재 — 기배선). 관찰(조치 없음): 이 PC hud 0.3.0 vs 업스트림 0.8.0. 검증: check.ps1 전 항목 통과 ([[DEC-20260820-bsjeong87-02]]) — @bsjeong87 (main) 2026-08-20

- [docs][Done] **2026-08-18 이월 미판정 2건 종결 — ⓐ 회차 불산입 · ⓑ 수확 4건 판정(채택 1·기각 3), 01 v1.37**(사용자 결정 "안 셈"·"승격·판정 진행" — gh 인증 대기 건은 "필요할 때 따로 요청"으로 보류, 메모 유지). ⓐ 08-13 재적용은 계측·판정·기록 절차 없이 진행돼 검증 회차 품질 기준 미달 — 재적용 기록으로만 남기고 다음 실적용 2026-11경 유지. ⓑ 기각 3은 전부 **기배선이 작동해 잡힌 사례**(글로벌 스킬 낡음=재적용 감지 · 그림자 스킬=/dropin-check §3 · `.gitattributes`=01 §검증 ⓓ), 채택 1은 팀 공유 `settings.json` 타 PC 절대경로 — §F-3이 플레이스홀더만 다루고 절대경로 금지·감지는 생산자·소비자 둘 다 공백이던 것을 01 v1.37 경량 2곳(§F-3 상대 경로만+`settings.local.json` 안내 / §검증 ⓔ 경로 실재·상대 확인)으로 배선. 부수: check.ps1이 이 PC 정본 `commands/skill-setup.md` 구버전(미러 대비 08-12 감사분 누락 — **기각 1번과 같은 패턴의 이 PC 실증**, 08-13 재적용이 스킬 4종만 교체하고 `commands/`는 대상 밖이던 것)을 적발 → 승인 후 미러→정본 복사. 검증: check.ps1 재실행 전 항목 통과 ([[DEC-20260820-bsjeong87-01]]) — @bsjeong87 (main) 2026-08-20

- [maint][Pending] **2026-08-13 재적용 세션이 이 저장소에 미기록 — 오늘 `/resume`가 발견**. 그날 3개 프로젝트(sfa-project·wave-project·nastvad)에 00을 재적용해 각 `CLAUDE.md`의 `dropin-applied`와 대상 repo 커밋(`39855a3`·`e11a414`·`9f7d6697`)은 남았으나 **문서 저장소 PROGRESS엔 한 줄도 없다** — /wrap 통합 워크스페이스 규칙 ①이 "드롭인 재적용 세션은 문서 저장소가 주 대상"으로 지정하는데 그 반대로 끝났다. 소실 위험 수확 4건: 글로벌 스킬 4종이 미러보다 낡아 **v1.36 규칙이 미작동 중이었음**(교체로 해소) · sfa 그림자 프로젝트 스킬 제거 · nastvad `.gitattributes` 부재(3인 공유 repo인데 append 기록이 merge=union 무방비였음) · nastvad 팀 공유 `settings.json`에 타 PC 절대경로 커밋. 재적용 **4회차**(릴리즈 기록 실증 범위가 3회차까지)에 해당하나 PROJECT_PLAN 미갱신 — 정기 실적용 검증 회차(2026-08-12 기준 2회차)로 셀지는 미판정 — @bsjeong87 (main) 2026-08-18

- [docs][Done] **`/model`·`/effort` 2축 질의 응답 + 신입용 설명 페이지 1건**(사용자 질의 "opus 5+high vs fable 5+medium vs opus 5+medium"). 근거는 전부 기존 문서(01 §D-6·02 §I) — 재조사 없음. 요지: 두 축은 대체재가 아니라 능력 상한(모델)×추론 깊이(effort)이고, **Fable 5+medium은 두 축을 반대로 미는 조합**(advisor 불가 + effort hold + 자율성의 연료인 추론 깊이를 깎음). 산출물은 아티팩트 1건(스크래치패드, **저장소 무변경** — 사용자가 `pages/` 등재 의사 없음을 명시). 관찰: 02 §I에 Opus 5 가격은 있으나 **Fable 5 가격 기재 없음**(조치 없음) — @bsjeong87 (main) 2026-08-18

- [release][Done] **첫 릴리즈 — 태그 `r2026.08.13`(기준선 7054057) 생성·푸시**(사용자 선언 "이제 릴리즈 하겠습니다"). 스냅샷: 드롭인 7종(00 v1.23·01 v1.36·02 v1.24·03 v1.13·04 v1.10·05 v1.8·06 v1.8)+사람용 안내물 6종 현행. 실증 범위: Windows(신규 설치 완주 3회·재적용 3회차·혼합형 워크스페이스·sfa형 시나리오), 배포 채널: GitHub Pages 랜딩. 고지 3종 유지(전 팀 Windows 확인으로 OS 고지는 해당 없음, 미실행 경로·06 preview) — @bsjeong87 (main) 2026-08-13

- [infra][Done] **유지보수 스킬 정비 — /audit·/reverify 개정 + /optimize 신설(유지보수 4축 완성)**(사용자 제안 "스킬도 업데이트 필요 + 최적화 스킬 신설" → 설계 승인 "진행"). 사용자가 지적한 3문제가 전부 "DEC엔 있는데 스킬엔 미배선"이던 교훈들과 일치 — 생산자 없는 규칙=미완의 자기 사례. **/audit**: 모드(전수/회귀) 정의·교대와 "대량 반영 직후엔 회귀가 절차" 명문화(§서두), §4에 처방 5후보 동등(추가·좁히기·되돌리기·분해·안 하기 — 1회 재발 관찰 규칙 포함)·반영은 요지만(경위·발견 ID 금지+RULES 마커 보존)·파일 통독+검증 절은 준비물과 1:1 대조·check.ps1 재실행=종결 조건. **/reverify**: §2 반영 요지만·마커 보존·check.ps1 종결, §1-D 후보 기준 ⑤ 기각·관찰을 채택과 동등. **/optimize 신설**(`.claude/skills/optimize/`, 드롭인 아님): 이번 세션의 최적화 루프를 절차화 — 측정(자수·표 비중·경위 밀도)→압축(표=요지·본문=규칙+귀결, §F 페이로드·마커 불변)→기계 검사→적대적 diff 검토(왜곡·손실·소비자 단절)→실적용 테스트→bump·기록, 가드레일에 "삭제도 추가만큼 위험"(첫 실행 회귀 4건 실측). 배선: RULES.md **R18**(요지만 — 마커 `경위·발견 ID`, 필수 p:audit·p:reverify)+파일 코드 표, check.ps1 맵에 p:reverify·p:optimize, CLAUDE.md에 4축 한 줄. 사람용 안내물 6종은 스킬 언급 0건 실측 — 유지보수 도구는 사용자 서술 범위 밖이라 갱신 대상 없음. 검증: check.ps1 전 항목 통과(R18 필수 2곳·optimize frontmatter OK·INFO 0) — @bsjeong87 (main) 2026-08-13

- [docs][Done] **사람용 안내물에 워크스페이스 개정분 반영 — options 어긋남 1건(유형 표 4행→6행)+보강 2건, README 팀 지원 요지 1건**(사용자 지시 "readme, 각 html 등 사용자 확인 문서 업데이트" — 전원 Windows 확인, mac/Linux 사용자 없음). options.html: 프로젝트 유형 표에 **혼합형·비-git 행 추가**(01 v1.31이 신설했는데 표가 4행인 채 남아 있던 실제 어긋남)+MSA 행에 "실제로 나눠 맡는 단위만", 표 아래 **저장소마다 따로 확인**(회사/개인·하위 설치 여부 — 00 v1.23 단위별 판정) 문단, 재적용 FAQ에 보류→미선택 전환 한 줄(00 v1.22). README: "팀·여러 저장소 환경도 지원" 불릿 1개(팀원별 설치 위치 상이 무충돌·저장소별 확인·공유/개인 구분). learn·index·demo·intro는 오늘 델타에 해당 범위 없음(무변경). 검증: check.ps1 통과 — @bsjeong87 (main) 2026-08-13

- [check][Done] **11축 재검증 + sfa형 워크스페이스 시나리오 검증 — 충족 11/11 확인, 미정의 분기 12건 중 9건 수정·2건 기각·1건 관찰**(사용자 요청 "여전히 잘 되어있는지 분석 + sfa 예제 환경 확인" → "권장 수정 사항이 생기면 수정하고 릴리즈 이상 없는지 확인·보고"). **① 11축 재검증**: 최적화(−12.1%) 이후에도 전 축 충족 — 보강 7건 전부 원문 실재, 압축이 끊은 참조 0(교차 확인 6건), 이음매 2건만(§B 표 02행 대상 열·01행 유형 열거 잔존 → 수정). **② sfa 대입**(개인 git 루트 + 회사 repo 2개 중첩 + MSA, 팀원별 설치 위치 상이·개인 구성 혼용 — 7단계 여정): 층위 공존(C5 배선)·재적용 존중·다PC 규칙·개인 구성 우선순위 고지는 정의돼 있었고, **미정의 12건 발견**. 수정 9건 — 00 STEP 2에 **회사/개인 단위별 판정**(루트 remote 하나로 답하면 하위 회사 repo가 게이트 없이 통과하던 최심각 공백)·**하위→상위 역방향 감지**·**clone 직후 하위 부재 오판 방지**, 01 §E-2 **하위 적용 여부 단위별 질문**(§E-2 "각자 적용"↔§E-4 "하위 불침습"의 층 구분), §E-3 단위 도출 기준(Java 멀티 모듈=동시 편집 단위만), §E-4 개인 원격 반출 확인+A-1 변형 포인터, §F-2 C+`/resume` 실물 **루트 대상 시 하위 각 repo PROGRESS 훑기**(sfa 요구 "하위에서 생긴 기록을 루트에서 봐야 한다"가 pull까지만 배선되고 읽기에서 끊겨 있던 것), `/dropin-check` **프로젝트 구성 그림자 감지**(개인 구성 팀원 PC에서 팀 커밋 구성이 가려진 상태 보고 — R06 사본 등록). 기각 2(작성자 필드·차단 기록 단위 축 — 사유는 DEC), 관찰 1(CLAUDE.md 규칙 충돌 우선순위). 산출: **00 v1.23·01 v1.36** + 스킬 2종·미러 동기. 검증: check.ps1 전 항목 통과(미러 24개 일치 포함) — @bsjeong87 (main) 2026-08-13

- [docs][Done] **설치 문서 최적화(릴리즈 정비) — 7종 −12.1%(133,912→117,691자), 규칙 유실 0, 실적용 테스트 3종 통과**(사용자 지시 "여러 번 루프를 돌듯 최적화 자율 진행, 릴리즈 배포 수준 목표"). 루프 4회: **①** 버전 표 압축(문서의 17~30% — 행마다 감사 파일·발견 ID·경위가 통째로 들어가 있던 것을 "무엇이 바뀌었나" 요지로, 행 수·시간순·결번 보존) **②③** 본문 경위 괄호 제거(00 4곳·01 3곳·02 3곳·04 1곳·06 1곳 — 규칙+한 줄 귀결만 유지, §F-1 정본·§F-2 템플릿은 불변) **④** 검증 3종 병렬 — ⓐ 적대적 diff 검토가 **압축 회귀 4건 적발·수정**(R17 마커 `비-git` 소실로 check.ps1 FAIL, 02 v1.18 "고정→기본화" 서사 왜곡, 01 v1.23 정정→확정 주체 변경, 01 v1.13 remote 조건 소실) ⓑ 신규 설치 완주(scratchpad 빈 repo, 권장+표준) — **절차 공백 0**, 모호 3건 ⓒ playground 재적용 시뮬레이션(읽기 전용) — 혼합형 판별·기록 5곳 파싱·압축 표만으로 재적용 요약 생성 전부 성공, 기존 절차 공백 6건 발견. 테스트 발견 중 한 줄 처방 7건 반영: 00(클론+사본 병기 시 읽기=헤더 버전 높은 쪽 · 보류 시 `미선택` 전환 일반화 · 기록에 00 자신 버전 포함 · 통합 워크스페이스 **위임 표기** 정의+`/dropin-check` 소비자 배선[미러 동기]) · 01(§검증 ① 비대화형 대체 증거) · 04(권장 모드 §1 기본 체크 · remote 없음 분기). 산출: **00 v1.22·01 v1.35·02 v1.24·03 v1.13·04 v1.10·05 v1.8·06 v1.8** ([[DEC-20260813-bsjeong87-02]]). 검증: check.ps1 전 항목 통과(4축·시간순·마커 65곳·미러) — @bsjeong87 (main) 2026-08-13

- [docs][Done] **사람용 페이지·README 최신화 — 사실 어긋남 options 8건·learn 3건 수정, 안내물 4종 보강 3건**(사용자 지시 "Readme, 각 html들 등 최신화 진행"). 조사 렌즈 2개 병렬(README·index·demo·intro / options·learn) 후 수정 지점 전수 원문 재검증. README·index·demo·intro는 **사실 오류 0**(링크·버전 하드코딩·절차 서술 전부 현행 일치) — 보강만 3건(index·README의 01 설명에 `/dropin-check`·`/dropin-update` 추가, demo 검증 시연에 프로젝트 산출물 축 한 줄+번들 5종). **options.html은 실제로 낡아 있었다**: 로스터 5종(spec-writer 부재)·"5종 전부" → 6종+활동별 요령, 02 배치 스코프·병렬 제안 통누락 → ⓑ에 추가, ⓓ 만들 파일이 구 이름 `docs/모델-분담-규약.md` → 현행 기본 `docs/핸드오프-규약.md`(기존 이름 유지 단서 — 에이전트 보고 밖에서 재검증 중 추가 발견), 04 hooks 스코프 2택 → 3택+강도 질문, MCP 스코프 2택 → 3택(기본 local), 05 추가 질문에 켤 위치 축+동시 선택 시 활성화 대상, 06 게이트 ③→④(계정 GitHub 연결 — 미연결 시 저장 거부 실측), 재적용 질문 2개 → 3개(대기 메모 해소 확인), 최소 모드 산출물에서 글로벌 CLAUDE.md 제외(최소=ⓑⓒⓓ). **learn.html 3건**: 로스터 표 6종화, 시뮬레이터 모순 2건 — "권장+이미 01 있음"이 사용자 모드 문구를 출력(SITUS.applied.docs=null → 01 재적용 행으로), 사용자 모드에서 번들이 계획에 미반영인데 라벨·스탬프엔 +디자인(합집합 규칙 위반 → 번들 union을 모드 무관 적용+custom 분기 신설). 검증: 인라인 JS `node --check` 2종 통과, check.ps1 통과, 푸터 날짜 4종·CLAUDE.md 표 동기화(intro는 무변경이라 유지) — @bsjeong87 (main) 2026-08-13

- [check][Done] **적용 시나리오 검증(사용자 확인 사항 11축) — 충족 5·부분 5·공백 1, 발견 6건 전부 수정**(사용자 검증 요청 → "추천은 진행"). 조사 렌즈 3개 병렬(환경·팀 공유 / 워크스페이스 형태·다중 설치 / 품질·모델·병렬) 후 공백 주장 전건을 원문 재검증. 유일한 완전 공백 = **C5 팀원 층위 혼재**(어떤 팀원은 상위 폴더 루트에서, 어떤 팀원은 하위 repo에서 설치 — 어디에도 없던 시나리오) → 01 §E-4에 공존 규칙(루트 레이어는 하위 불침습이라 충돌 없음 · 상위 폴더가 팀 공유 repo면 루트 산출물 커밋 전 팀 합의). 부분 충족 5축의 수정: ② 병렬이 참고 주석에만 있고 절차 미배선 → 02 STEP 3 병렬 제안+§C 분할 기준(파일·모듈 비겹침·순차 의존 없음)+implementer 템플릿 worktree 주석(서두 지시의 소비자 도달) ③ **"프로젝트 기록의 글로벌 항목은 이 PC 실구성 증거가 아님"이 이 PC 글로벌 메모에만 있고 문서 미승격**(2026-08-11 실증분) → 00 STEP 2 승격 ④ 02 로스터 배치 스코프 질문 부재(STEP 3 ⓑ+00 STEP 4 목록에서 02만 누락)+00 단독 조합의 개인 파일 축(`settings.local.json`) 미안내 → 3곳 배선 ⑤ 01 §D만 Windows 하드코딩(04는 OS 3분기 보유) → OS 주석+D-2·D-3 `~` 표기 ⑥ 하 3건 — 05 outputStyle 기입 측 전파 경고(제거 측만 있던 것)·02 절약 불변에 spec 추가·advisor 불가 환경(Bedrock/Vertex/Foundry·Fable 5 메인) 폴백. 산출: **00 v1.21·01 v1.34·02 v1.23·05 v1.7**(03·04·06 해당 없음, 글로벌 스킬 무변경 — 미러 동기화 불요). 검증: check.ps1 전체 통과(FAIL 0·INFO 0, 규칙 대장 17종 OK) — @bsjeong87 (main) 2026-08-13

- [infra][Done] **결함 예방 3축 — 규칙 대장 + 기계적 검사 + 미실행 경로 2종 실행**(사용자 지시 "1순위부터 2, 3까지 권하는 순서 모두 진행" + 저장소 목적 재확인 "범용·개인/팀 적용·토큰 절약·품질·병렬·모델 분담 구성 설치 repo — 이 점을 잊으면 안 됨"). 목적 재확인이 2순위 방향을 뒤집었다 — §F-2·global-config는 **범용 자족성이 요구하는 설치 페이로드·백업**이라 사본을 줄일 수 없고, 대신 **추적한다**. ① **`docs/RULES.md` 신설**: 공유 규칙 17개 × 필수 사본 65곳, id/마커/필수/허용. 만드는 행위 자체가 결함 1건을 잡았다 — R08(미해소 `[Pending]` 승격)이 문서 2곳에만 있고 실행할 `/wrap`엔 배선 0(생산자 없는 규칙). 마커 교훈: `autoMemoryEnabled` 같은 키 이름은 우연 일치를 낳는다 — 규칙 고유 표현으로. ② **`scripts/check.ps1` 신설**(드롭인 아님): 버전 4축·표 시간순/결번·표 파손·BOM·frontmatter·미러 해시·규칙 대장 7종 — `/audit` 렌즈 B·E의 기계적 부분 대체, `/audit` §0·CLAUDE.md에 배선. **첫 실전에서 내 실수(01 버전 표 v1.31→v1.33→v1.32 역순)를 즉시 잡았다.** ③ **신규 설치 경로 첫 실행**(빈 widget-api repo, 권장+검증강화+회사 · 표준): 회사 게이트·R11 항목 차단 어휘 최초 실전. 발견 2건 — **H3 [중]** 01의 검증이 §D-5(글로벌 스킬 자동완성)뿐이라 **자기가 만드는 프로젝트 산출물을 하나도 확인하지 않음**(재적용에선 파일이 이미 있어 무증상, 빈 repo에서만 드러남 — 02가 v1.18에서 고친 것과 같은 결함이 31개 버전 생존) → 01 §A STEP 4 §검증 신설(두 축)+00 대표 검증 동기화 · **H2 [중]** §F-3 `additionalDirectories` 플레이스홀더를 안 지우면 존재하지 않는 경로가 팀 공유 파일에 커밋 → 경고 줄(JSON엔 주석 불가) · H1 오탐(활동 유형 생산자는 §D-1에 실재) + 잔여 어휘 3벌을 §E 이름으로 통일. ④ **03·05·06 재적용**(playground, 4버전 드리프트): 신설 감지 전부 실행 — 그림자 0·오버라이드 0·06 검증 4종 통과, **구성 변경 0 · 발견 0건**(이번 세션 세 번째 적용 회차이자 첫 무발견). 산출: **00 v1.20·01 v1.33·02 v1.22** + RULES.md·check.ps1 + `/wrap` 미러. 검증: check.ps1 전체 통과(FAIL 0·INFO 0) — @bsjeong87 (main) 2026-08-13

