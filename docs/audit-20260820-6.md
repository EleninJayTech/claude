# /audit 6차 재도출 (회귀 모드) — 2026-08-20

> 6차(2026-08-20) 조사·보고는 산출물 파일을 남기지 않아 확정 34건 중 **[하] 21건이 소실**됐다.
> 사용자 지시로 **회귀 모드 재도출**(PROJECT_PLAN:131이 예정한 복구 경로). 렌즈 A~F 6종 병렬 + 발견마다 원문 재검증.
> 6차 요지는 앵커가 아닌 **커버리지 하한**으로 취급 — 미열거분 재도출이 주 목적.

## 결과 요약

| 구분 | 건수 |
| --- | --- |
| 원시 발견 | 69 (A 12 · B 8 · C 13 · D 17 · E 6 · F 13) |
| **확정** | **54** ([상] 3 · [중] 27 · [하] 24) |
| 외부 확인 필요(인계) | 4 |
| 기지(이미 PROJECT_PLAN 등재) | 2 |
| 오탐 | 0 |
| 6차 대비 | 34 → 54 (+20) |

교차 지목(독립 확인): `00:144`(A·B) · `00:91`(A·B) · wrap-gate [상](A·C) · wrap-gate 경로 고정(A·C) · `01 §D-3` 드리프트(A·E·F) · `/reverify` §L 미배선(B·F)

## §0 기계 검사

`pwsh -File scripts/check.ps1` — 전 항목 통과, FAIL 1건은 **오검출로 실측 확정**
(`check.ps1:149-151`이 `ConvertTo-Json -Compress` 문자열 비교라 JSON 키 순서에 민감. 재귀 키 정렬 정규화 비교 결과 내용 동일, 양쪽 1510자). 아래 M26으로 등재.

---

## [상] 3건

### H1. `/compact` wrap-gate가 `/wrap` 정본과 **이중으로** 모순 — 타 repo에선 어떤 정석 조치로도 통과 불가
- **위치**: `~/.claude/settings.json` PreCompact matcher `manual` (미러 `global-config/settings.json`)
- **통과 조건**: `if($dirty -eq 0 -and $hasToday){ ... exit 0 }`
- **① `$dirty` 축**: `~/.claude/skills/wrap/SKILL.md:22-23` "미커밋 변경이 남으면 **경고하라**" — `/wrap`은 커밋하지 않는다. 정상 완료 직후 = PROGRESS·DECISIONS·PROJECT_PLAN 미커밋 = `$dirty>=1`. 사본 전부 동일(`01:272`·`01:311`·`01:338`·`01:367`·`CLAUDE.md:71`). `--porcelain`은 untracked 포함이라 무관한 빌드 산출물로도 dirty.
- **② `$hasToday` 축(①과 독립 실패)**: 기록 양식 정본 `01:258`·`01:449`·`CLAUDE.md:57`에 **날짜 필드가 없다**. 이 repo의 `— @bsjeong87 (main) 2026-08-20` 병기는 문서화되지 않은 관행. → 신규 프로젝트에선 `$hasToday` 영구 false.
- **실해**: 타 repo에서 `작업 → /wrap → 커밋 → /compact`를 정확히 수행해도 `[wrap-gate] /compact 차단 - /wrap 미확인(미커밋 0, 오늘 기록 False)`. **/wrap을 했는데 안 했다고 한다.** 차단 메시지가 권하는 `/wrap > /clear > /resume`엔 커밋 단계가 없어 안내를 따를수록 `$dirty`가 0→3으로 늘어난다(커밋을 별도 단계로 규정한 곳은 `01:460-461` §G뿐인데 게이트는 인용하지 않는다). 5분 override가 상시 탈출구로 굳는다.
- **이 repo가 무증상인 이유**: 커밋 상시 허용 + 날짜 병기 관행. hook은 글로벌이라 이 예외가 전 프로젝트에 적용된다.
- **처방**: 판정을 "`docs/**/PROGRESS.md`가 **직전 커밋에 포함**되거나 **미커밋 목록에 있다**"는 단일 축으로 교체(양식·커밋 관행 무관, 생산자 존재). ⚠️ **PROJECT_PLAN:131에 등재된 "좁히기=오늘 기록 단독 판정"은 채택하면 안 된다** — M8 참조.

### H2. `/reverify`가 00~06의 §L 재검증 체크리스트를 **한 번도 읽지 않는다** — §L 신규 축이 도착 즉시 사장
- **위치**: `.claude/skills/reverify/SKILL.md` §0·§1
- **근거**: §0이 읽는 것은 `git status` / `CLAUDE.md` 구성물 표 / `PROJECT_PLAN` "다음 재검증" 🟡뿐. `grep §L` → 0건. §1 A~D축은 7개 §L을 손으로 복제한 병렬 목록이라 이미 갈라졌다.
- **반대편 거짓 주장**: `02:253` "`/reverify`가 이 목록을 조사 축으로 쓴다" — `grep reverify 0*.md` 결과 **전 문서에서 이 한 줄뿐**이고, 사실이 아니다.
- **이미 죽은 축 3종**(전부 `1c5cc9d`가 08-20에 §L에 추가): `02:254` §I 가격 수치 / `03:250` Chrome 활성화 경로·`/login` 인증 요건 / `04:138` GitLab CI/CD beta→GA
- **실해**: 04 GitLab 축은 6차가 뒤늦게 PROJECT_PLAN 🟡에 등재했으나 그것도 절반이다 — `reverify:33`이 🟡을 "**잔여 항목으로 재작성**"하므로 1회 조회 후 목록에서 빠지는데, beta→GA·템플릿 변화는 상시 축이다. 02·03분은 🟡에도 없다. 다음 /reverify(2027-01)가 Fable 5 가격을 조회 대상에 넣지 않아 낡은 수치로 모델 배정을 권고한다.
- **처방**: `reverify` §0에 "00~06 각 문서의 §L을 읽어 조사 범위에 합친다" 한 줄 + C축에 GitLab CI/CD를 상시 축으로. **둘 중 하나만 하면 다음 축이 또 죽는다.**

### H3. `/audit`이 **미승인 회차의 확정 발견을 보존하지 않는다** — 6차 [하] 21건 소실의 직접 원인
- **위치**: `.claude/skills/audit/SKILL.md` §3·§4
- **근거**: §3이 "보고(수정 전 — 여기서 멈추고 **승인 대기**)"로 끝나고, 기록 절은 §4("수정 — **승인된 항목만**") 안에만 있다. 산출물 파일을 쓰라는 지시가 §3·§4 어디에도 없다. 그런데 §1 전수 모드는 "과거 감사 산출물(`docs/audit-*.md`) 열람 금지"라고 그 파일들의 **존재를 전제**한다(1~5차엔 있고 6차엔 없다).
- **실해**: 승인이 나지 않으면 그 회차 산출이 통째로 증발한다. 실제로 `b22d9e2`가 건드린 파일은 PROGRESS·DECISIONS·PROJECT_PLAN 3개뿐이고, [상]1·[중]12만 열거돼 **[하] 21건은 어디에도 없다**. 오늘의 재도출 전체가 이 결함의 비용이다.
- **처방**: §3 끝에 "확정 발견 표를 `docs/audit-YYYYMMDD[-N].md`로 **먼저 쓰고** 승인을 기다린다" 한 줄. — **이 문서가 그 처방의 선반영이다.**

---

## [중] 27건

| # | 위치 | 문제 | 처방 |
| --- | --- | --- | --- |
| M1 | `~/.claude/settings.json` manual hook | wrap-gate가 `docs/PROGRESS.md` 고정 상대경로라 MSA 단위분할(`docs/<단위>/`)·워크스페이스 루트에서 fail-open. 게다가 "기록 체계 없는 폴더"라고 **사실과 반대로** 보고 (A·C 독립 지목) | `docs/**/PROGRESS.md` 재귀 탐색, 부재 메시지 완화 |
| M2 | `01:191` | §D-6 절약 프로필이 "긴 세션은 `/compact <초점>`"을 권하는데 같은 날 신설한 글로벌 hook이 그걸 차단하고 `/clear`를 권한다(절약 의도와 정반대) | §D-6에 게이트 전제 한 줄 |
| M3 | `01:176` | §D-5 확인이 "자동완성 4종이면 성공" 그대로 — `01:83` §검증 ①이 필수화한 글로벌 `deny` 실측이 없다. 스킬 4종은 파일 복사만으로 뜨므로 deny 블록이 통째로 빠져도 "성공" | §D-5에 deny 실측 추가 |
| M4 | `00:91` | STEP 5 대표 검증 01행이 01 v1.37·v1.38 §검증 개정 미반영(같은 커밋 `1c5cc9d`이 03행은 동기화 — 행마다 정책이 갈렸다) (A·B 독립 지목) | 열거를 지우고 "01 §A STEP 4 §검증 그대로"로 위임 일원화 |
| M5 | `00:144` | §B 04행이 폐기된 "GitHub Actions 또는 headless 폴백" 2분기 전제 유지. 04 v1.11은 GitLab 공식 CI/CD 통합(beta)을 정본화 (A·B 독립 지목) | 3분기로 교체 |
| M6 | `01 §D-3` hooks 예시 | PreCompact `manual`(wrap-gate) 전무 — 실물 4매처 vs 문서 2매처. `grep wrap-gate 0*.md` → 0건 (A·E·F 독립 지목) | 정본 포인터 한 줄 + 선택 확장으로 명시 |
| M7 | `01:162` | auto hook 문구가 **실행 불가능한 구버전** — 문서 "임박 → 작업 끊고 /wrap" vs 실물 "**발동** → 압축 후 즉시 /wrap". PreCompact(auto)는 압축이 이미 발동한 뒤 발화한다 | 실물 문구로 교체 |
| M8 | `docs/PROJECT_PLAN.md:131` | 등재된 처방 후보 "**좁히기=오늘 기록 단독 판정**"이 H1 ②와 결합하면 결함을 **악화**시킨다(생산자 없는 축만 남아 타 repo 항상 차단). 이 repo는 날짜 관행 덕에 통과해 **무증상 배포** | H1 채택으로 항목 종결 |
| M9 | `00 STEP 1` 클론 항목 | 신규 설치의 "로컬 클론" 확보 경로에만 뒤처짐 가드가 없다(`grep behind\|origin/main 00` → 0건). `/dropin-check`·`/dropin-update`는 둘 다 보유 | `git fetch` + behind>0 질문 한 줄 |
| M10 | `00:91`·`01:83` | 글로벌 전용 설치(`01:79` ⓓⓔ 제외, 00 §C-1 "새 PC → 01+03")에서 §검증 "두 축 다"가 구조적으로 충족 불가한데 예외가 없다 | "글로벌 전용이면 ②는 '해당 없음' 명시로 갈음" |
| M11 | `02:115` | 로스터 개인 배치(`~/.claude/agents/`)의 PC 전역 전파 경고가 없다(`grep` 0건). `01:293`은 스킬에 대해 같은 경고를 갖는다 | `01:293`과 같은 취지 한 줄 |
| M12 | `~/.claude/skills/dropin-update/SKILL.md` | "저장소 원본에서 실행 금지" 가드가 PWD에 걸려 있는데 최신화 대상은 하위 repo별 → 워크스페이스 루트 실행 시 하위 원본 클론이 가드를 통과, **미커밋 개정 작업이 raw 최신본에 덮인다** | 워크스페이스 줄에 원본 제외 한 구절 |
| M13 | `00:116` | `확보 실패(사유)`에서 회복할 때 모드·번들을 다시 고르는 경로가 없다. 줄에 문서 항목이 0건이라 질문 ①②③ 전부 발화하지 않아 절차가 멈춘다 | "항목 0건 기록은 신규 설치로 취급" |
| M14 | `04:114` vs `04:127` | 같은 문서가 같은 행위를 한 절에선 "퀵스타트 **정본**", 다른 절에선 금지로 규정. 금지가 테스트 PR에만 붙었다 | `:114` 괄호에 "실행은 안내만" 한정 |
| M15 | `04:124` | §3 감지가 이번 델타로 새 정본이 된 두 설치 경로를 모두 못 본다(`/install-github-app` PR 경로는 로컬 파일 부재가 정상 · GitLab `include:` 템플릿) | 원격·PR·`include:` 확인 한 구절 |
| M16 | `04:35` | remote 없는 repo(가역 상태)에 `해당 없음` = **영구 제외** 어휘를 쓴다(`00:117` "②③ 둘 다에서 제외"). 나중에 remote를 붙여도 gh·CI가 영영 재제안되지 않는다 | 가역 어휘(`미선택: … 추가 시 재질문`)로 교체 |
| M17 | `04:101` | §2 검증이 `gh pr list` 한 줄뿐 — 같은 문서 `:45`가 "**§2 인증은 사용자 직접 단계라 대기 상태가 자주 생긴다**"고 선언했는데 검증에 대기 분기가 없다(§1·§3엔 있다) | `gh auth status` 증거 + 대기 기록 반 줄 |
| M18 | `04:83` | §1 제거가 "settings.json"(단수, 스코프 미특정) — 감지(`:57`)는 3곳 전부. 05는 같은 구조에서 이미 교정됐는데 04엔 백포트 안 됨 | 3곳 전부 조회로 교체 |
| M19 | `05:37` | STEP 2 #3 활성화 키 스코프가 2택인데 감지(`:31`)·제거(`:65`)는 3곳 — **팀 공유 경로가 질문에서 통째로 빠졌다**. `:65`(팀 활성 가능)와 `:103`("선택은 각자")도 서로 다른 말을 한다 | 세 번째 선택지 + `:103` 한정 |
| M20 | `05:31`·`05:62` | 감지·검증이 settings 세 곳만 보고, 그걸 **덮어쓰는** 두 출처를 안 본다 — `:84` `force-for-plugin`, `:103` managed. **03으로 플러그인을 깐 PC(표준 조합)에서 증거까지 붙은 통과 보고가 나오는데 응답은 안 바뀐다** (claude-hud 실패와 동형) | 감지·검증에 두 출처 확인 한 줄씩 |
| M21 | `06:64`·`:67` | 00이 두 곳(`:146`·`:149`)에서 "**완전 독립**"이라 주장하는 06이 실제로는 01 산출물(CLAUDE.md 기록 규칙·§F-1 증거 규칙)을 준비물·통과 기준으로 요구. "무인 자동화" 번들은 06 단독 설치가 표준 경로 | 00 "독립" 한정 + 06 검증 ④에 골격뿐일 때 분기 |
| M22 | `00:93` vs `00:96` | **같은 표 안 두 행이 한 명령에 상반된 지위**를 준다 — 03행 "`claude mcp list`는 감지 명령이지 **검증이 아니다**" / 06행은 그걸 검증 증거로 쓴다. 루틴은 무인 실행이라 연결 실패를 사람이 볼 자리가 없다 | 06 검증에 세션 로그 축 추가 + 00:96 동기 |
| M23 | `scripts/check.ps1` 미러 절 | 미러 대조가 **백업 쪽만 순회**하고 루프 안엔 `정본 없음` 판정만 있다 → "**실물에만 있는 파일**"을 구조적으로 못 본다. 새 스킬을 `~/.claude`에만 만들고 미러를 잊는 방향 = 새 PC 복원 때 실제로 손실 나는 방향이 영구 사각지대. `-Include '*.md'`라 `LICENSE.txt`도 무검사 | 실물 ∪ 백업 합집합 순회, 확장자 필터 제거 |
| M24 | `.claude/skills/audit/SKILL.md` §0 | "재조사 금지" 면제가 **렌즈 B·E만** 지목 — F ③은 check.ps1이 이미 확정한 BOM·name↔폴더를 다시 돌린다. 이번 재도출에서 운영자가 수동으로 주입해야 했다 | "렌즈 B·E·**F**"로 확장 |
| M25 | `.claude/skills/audit/SKILL.md` §4 | 회귀 회차가 **자기 예정일 줄을 닫는 단계가 없다**. PROJECT_PLAN에 회귀·전수 두 줄 공존인데 §4는 후자만 갱신하라 한다 → 회귀 줄이 안 닫혀 /resume이 상시 오보 | "이번 회차를 촉발한 예정일 줄을 먼저 닫는다" |
| M26 | `scripts/check.ps1` settings 4키 비교 | `ConvertTo-Json -Compress` **문자열 비교**라 JSON 키 순서에 민감. `/model` 전환 등으로 정본이 재직렬화되면 가짜 FAIL(이번 회차 실측). `01 §D-3 ⑤`가 deny 배열에 대해 정한 "순서 무관"의 키 버전 | 재귀 키 정렬 정규화 비교로 교체 |
| M27 | `CLAUDE.md:25` | 구성물 표 `global-config/` 행의 "최종 갱신" 날짜가 **8일 낡음**(문서 2026-08-12 / `git log -- global-config/` 최신 `47840e9` 2026-08-20). 이 행은 자기 의미를 "이 폴더의 최종 갱신일"로 명시 정의했으므로, 다른 PC가 "08-13 이후 백업 안 됨"으로 읽고 자기 `~/.claude`를 정본으로 판정해 미러를 덮어쓴다. check.ps1 버전 4축은 00~06만 보므로 기계 검사에 영영 안 걸린다 (통합 중 발견) | 날짜 정정 + `/wrap` 미러 대조에 갱신 배선 |

---

## [하] 24건

| # | 위치 | 문제 | 처방 |
| --- | --- | --- | --- |
| L1 | `01 §D-3` SessionStart | 예시가 실물의 마지막 문장 누락(`압축 전 미기록 결정이 있었다면 먼저 /wrap을 안내하라.`). M7과 짝인 2단 배선이 양쪽 다 끊긴다 | 실물과 일치 |
| L2 | `01:168` | §D-3 ⑥ 매처 열거가 `auto`/`compact` 2종뿐인데 같은 절이 `startup`을 소개하고 실물은 `manual`까지 4종 | 열거를 일반화 |
| L3 | `global-config/README.md` 제외 절 | 잔재 파일명 하드코딩이 이 PC에서 반증(문서 `…20260721-111024` / 실물 `…20260708-154730`·`-165651`·`-172350`). `dropin-applied`와 같은 실패 유형 | 파일명 제거, 패턴만 |
| L4 | `~/.claude/settings.json` manual hook | 5분 override 마커가 `$env:TEMP\claude-compact-override` **PC 전역 단일 파일** — repo A의 창을 repo B 세션이 소비. 통과 경로에선 마커를 안 지워 낡은 마커도 남는다 | 파일명에 PWD 해시 부착 |
| L5 | `global-config/README.md` 복원 절 | 새 PC 복원용 미러에 PowerShell 전용 hook이 들어갔는데 `01:117`은 "만드는 내용물 D-2~D-4는 **OS 무관**"이라 규정하고 README는 `hooks`를 병합 대상으로 지정 → macOS·Linux 복원 시 실행 불가 셸 호출 | 복원 절에 OS 한정 한 줄 |
| L6 | `01:98` | 비-git 폴더 면제 열거가 v1.38이 ⓓ에 편입한 `§F-5 .gitignore`를 담지 않는다. RULES R14가 겨냥한 형태 | 열거를 묶음 표현으로 |
| L7 | `00 STEP 5` 순서 | [의심] ② 커밋 안내 → ③ 적용 기록이라, 방금 만든 `dropin-applied` 줄이 안내 대상에서 빠진다. 다음 /resume의 git status가 잡을 가능성이 높아 낮게 봄 | ②에 기록 줄 포함 명시 |
| L8 | `global-config/skills/dropin-check/SKILL.md` | [의심] 실행 지시문에 실제 버전 숫자(`00 v1.10+`) 하드코딩. DEC-20260810-bsjeong87-07 금지 형태이나 "도입 시점 주석" 의도일 수 있다 | 숫자 제거 |
| L9 | `pages/options.html` 03 §5 항목 | [의심] v1.15 핵심 정정(확장 설치만으론 연결 안 됨·`/chrome` 활성화·`/login` 필수·WSL 미지원) 미반영. 같은 커밋이 §1·04·06은 동기화 — 사람용 예외 문면과 실제 관행이 어긋난다 | 한 구절 추가(절차 전제 변경은 예외 대상 아님) |
| L10 | `~/.claude/skills/resume/SKILL.md`·`dropin-update` | "00 재적용" 권고에 00 문서를 **어디서 얻는지**가 없다. 00의 raw URL은 00 §B 안에만 있어 00을 못 읽은 상태에선 참조 불가(raw 유형 막다른 길) | raw 경로 괄호 병기 |
| L11 | `00:71` vs `:76`·`:117` | 전체 모드가 `해당 없음`을 매번 재판정하는지 기록대로 영구 제외하는지가 두 문장에서 갈린다 | 한쪽으로 확정 |
| L12 | `01:83` §검증 ⓔ | v1.37이 신설한 절대경로 폴백(`settings.local.json`)에 검증 축이 없다 — 폴백 경로의 오타·부재가 안 잡힌다 | ⓔ에 폴백 파일 확인 추가 |
| L13 | `~/.claude/settings.json` manual hook | 비-git 폴더에서 `$dirty`가 무조건 0(git 실패, stderr 폐기) → 판정이 생산자 없는 축 단독으로 축소 | H1 처방으로 함께 해소 |
| L14 | `04:134` | beta 의존(GitLab CI/CD)을 새로 들였는데 재검증 주기는 "6개월" 그대로. 06은 같은 성격에 "3개월 권장"으로 따로 잡아뒀다 | beta 항목만 3개월 한정 |
| L15 | `04:80` | §1 검증 통과 기준이 STEP 2의 두 강도 중 "차단"만 전제 — "경고만"을 고른 사용자는 정상 동작해도 실패 피드백이 안 온다 | 강도별 통과 기준 반 줄 |
| L16 | `05:41` | 단독 드롭인을 이미 고려한 STEP 4가 "개인 파일은 커밋하지 않는다"만 인라인 안 함. 05의 기본 활성화 경로가 바로 그 개인 파일 | 반 줄 추가 |
| L17 | `06 §4` 검증 | 델타가 §3에 "커넥터 최소화 확인 — **제거 결과는 §4 기록에 남긴다**"를 넣었는데 §4 검증은 그 항목을 확인 목록에 안 넣었다 | 확인 항목 추가 |
| L18 | `06 §2` 검증 ③ | 검증 ③만 확인 **방법과 증거 형태**가 없다(①②④는 명령·경로 보유). "없음을 증명"인데 방법이 없어 "확인했습니다"로 끝나거나 시크릿 파일을 열게 된다 | 조회 가능한 두 증거로 치환 |
| L19 | `06:42` | 항목 차단 기록 예시가 00 정본 어휘와 형식이 다르다(`확인 대기` vs `게이트 차단(사유)`). `00:110`이 "어휘 4종은 섞지 않는다"고 못박은 상태 | 정본 형식으로 교체 |
| L20 | `.claude/skills/optimize/SKILL.md` | 세 유지보수 스킬 중 optimize만 아카이브 포인터 규칙(R01)이 빠졌다 | audit/reverify와 같은 문구 |
| L21 | `audit`·`reverify` 기록 절 | 예정일 생산자 둘이 `다음 ○○: YYYY-MM경` **리터럴 형식을 갖고 있지 않다**. RULES R09 필수 사본에 두 코드가 미등록이라 지금 넣으면 "extra"로 FAIL 난다 | R09 허용 열 추가 **후** 형식 삽입(순서 중요) |
| L22 | `docs/PROJECT_PLAN.md` skill-setup 인용 | 줄번호가 낡음(`146` → 실제 `156`). `24cb6ff`에서 밀렸다 → 다음 /reverify가 무관한 문장을 보고 "항목 소멸"로 오판해 🟡을 지운다 | 줄번호 대신 인용 문구로 |
| L23 | `~/.claude/commands/skill-setup.md` 0단계 | [의심] 인벤토리에 통합 워크스페이스 분기가 없다(cwd 한 겹만). 다만 skill-setup은 드롭인 자산이 아니라 개인 커맨드라 적용 대상인지가 설계 판단 | "단일 repo 전용" 명시(범위 축소 = 안 하기에 가까운 처방) |
| L24 | 커밋 `a13adf5` | [의심] 제목이 "skill-setup 정본 동기화"를 주장하나 산출물이 없다(실물·미러 현재 동일). 방향이 미러→실물이었다면 정상이나 커밋만으론 판별 불가 | 과거 커밋은 수정 불가 — PROGRESS에 방향 기록으로 종결 |

---

## 외부 확인 필요 (인계 — 수정 대상 아님, PROJECT_PLAN 🟡 등재)

| # | 위치 | 확인할 것 |
| --- | --- | --- |
| X1 | `~/.claude/settings.json` manual hook | PreCompact exit 2의 **stderr가 사용자 화면에 표시되는지** Claude에게만 가는지. 통과·비대상 경로는 stdout, 차단만 stderr로 채널이 다르다. Claude 경유라면 override 안내가 실행 불가능한 지시가 된다 |
| X2 | `06 §5`·`§6` | `/schedule`에 **중지·삭제 하위명령**이 생겼는지(델타가 생성 축만 CLI로 현행화하고 관리·제거는 "웹에서" v1.0 서술 그대로) |
| X3 | `04 §3-B` | GitLab 공식 통합이 **`CLAUDE_CODE_OAUTH_TOKEN`(구독 인증)을 받는지**. GitHub 축엔 병기됐고 GitLab 축엔 없다 |
| X4 | `skill-setup` ↔ `03 §3` | **frontend-design이 Claude Code 내장인지**. skill-setup만 "내장이면 제외" 힌트를 갖고 03 §3은 없다 |

## 기지 (이미 PROJECT_PLAN 등재 — 상태 유지)

| # | 위치 | 비고 |
| --- | --- | --- |
| K1 | `~/.claude/skills/humanizer/SKILL.md` frontmatter | `allowed-tools` 공백 구분 표기의 파서 동작(2026-08-12 인계, 2차에서도 미해소) |
| K2 | `~/.claude/commands/skill-setup.md` | `/skills` 명령 실재 여부(줄번호는 L22로 정정) |

---

## 이상 없음 (확인한 축 — 무엇을 봤는지가 증거)

- **§참조 실재 전수**(렌즈 B·F 독립): 00~06·CLAUDE.md·스킬 7종·RULES.md·사람용 html 5종에서 뽑은 §참조 전부 실재. **`1c5cc9d~1 → HEAD` 6문서 섹션 ID diff 0 — 08-20 개정이 § 번호를 옮기지 않았다**. 끊긴 참조 0건
- **파일명·raw URL·스킬 이름 실재**: 00 §B 파일명 6종·raw URL 경로·`scripts/`·`docs/RULES.md`·`.claude/skills/` 3종·`global-config/` 전부 실재
- **예시 속 버전 하드코딩**: 드롭인 7종·CLAUDE.md·README·사람용 5종·스킬 전수 스캔에서 실수치는 헤더·버전 표·푸터·구성물 표·실제 기록뿐(정상). 예외 후보 1건은 L8
- **상태 어휘 4종의 3자 배선**: `미선택`/`게이트 차단(사유)`/`소스없음·확보 실패`/`해당 없음` 전부 생산자·소비자·종결자 확인
- **미러 완전성**: 실물 24파일 : 미러 24파일, 목록·내용 diff 0. humanizer 내부 경로 인용 14/14 실재
- **글로벌/프로젝트 동명 중복**: 글로벌 7 ∩ 프로젝트 3 = **공집합**. commands↔skills 중복 0
- **`disable-model-invocation`**: 관리자 전용 4종(audit·optimize·reverify·skill-setup) 전부 `true`
- **출력 스타일**: `korean-evidence-report.md`에 `keep-coding-instructions: true` 존재(05 §2 🔴 요건 충족)
- **문서 내 인라인 정본 3자 대조**: `03 §2` 코드펜스 ↔ 실물 ↔ 미러 **완전 일치**. 같은 구조 스킬은 svg-design 1종뿐
- **사용자 여정**(렌즈 C): 신규 설치 4조합·재적용 3분기·`출처=` 없는 옛 기록·특수 환경 5종·02 여정 3종·회사 게이트 거부 후·스킬 핸드오프 4종 — 위 발견 외 이상 없음
- **`additionalDirectories` 상대 경로 규칙(v1.37) 사본 대조**: 규정·검증 사본 누락 0
- **`/dropin-update` ↔ 00 재적용 순환 배선**: 양쪽 소비자 실재 + 무한루프 가드 보유
- **`/resume` 절차 순서 4벌**: pull-first → git status → PROGRESS 40줄 → PROJECT_PLAN → DEC 3건 순서 전 사본 동일

---

## 6차 기지 13건 커버리지 대조

| 6차 항목 | 재도출 | 매핑 |
| --- | --- | --- |
| [상] wrap-gate ↔ /wrap 정본 모순 | ✅ 재현·**승격** | H1(② 축 독립 실패 추가) |
| [중] 날짜 판정 생산자 부재 | ✅ 재현 | H1 ②로 통합 |
| [중] 00 §B 04행 headless 전제 | ✅ 재현 | M5 |
| [중] 01 §D-3 hooks 3자 드리프트 | ✅ 재현·**분해** | M6·M7·L1·L2 |
| [중] 04 §L GitLab 축 소비자 0 | ✅ 재현·**일반화** | H2 |
| [중] 06 §4 게이트 차단 분기 부재 | ⚠️ 인접 | L17·L18 |
| [중] 04 제거 절 OAuth 토큰 누락 | ⚠️ 인접 | M18·X3 |
| [중] 03 표시 구성 3택 미배선 | ❌ 미재현 | 6차 요지로 함께 수정 |
| [중] 03 hud 스코프 검증 무력 | ❌ 미재현 | 6차 요지로 함께 수정 |
| [중] 06 GitHub App 선설치 게이트 부재 | ❌ 미재현 | 6차 요지로 함께 수정 |
| [중] 04 검증 `@claude` 멘션 미반영 | ❌ 미재현 | 6차 요지로 함께 수정 |
| [중] 04 §3-B 셋째 불릿 귀속/주어 | ❌ 미재현(문체 축) | 6차 요지로 함께 수정 |
| [중] 05 글로벌 병기 미인라인 | ❌ 미재현 | 6차 요지로 함께 수정 |

**미재현 6건**은 이번 렌즈 배분에서 빠진 것이지 해소된 것이 아니다 — 수정 시 PROGRESS 2026-08-20 [audit] 항목의 요지를 함께 처리한다.

---

**문서 정보** — /audit 6차 재도출(회귀 모드), 2026-08-20, @bsjeong87 (main). 조사 렌즈 A~F 6종 병렬 + 발견 전건 원문 재검증(오탐 0).
