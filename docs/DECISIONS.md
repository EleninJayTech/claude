# DECISIONS (append 전용 · id = DEC-날짜-작성자-번호)

- **DEC-20260720-bsjeong87-01** — md 파일 3종 유지(삭제 안 함). HTML은 사람용, md는 Claude 학습용 드롭인 — 역할이 다름. 중복은 감수. *(Superseded by [[DEC-20260723-bsjeong87-01]])*
- **DEC-20260720-bsjeong87-02** — 문서 최신화는 기억이 아니라 **당일 공식 문서(code.claude.com/docs) 조회 검증**으로만 반영. 확인 못 한 항목은 🟡로 남김(§L 원칙).
- **DEC-20260720-bsjeong87-03** — `theme` 키를 settings.json 예시에서 제거(문서화 키 아님 확인). 테마는 `/config`로 안내. 개인 설정에 남아 있는 건 무방.
- **DEC-20260720-bsjeong87-04** — 글로벌 .claude 백업은 **화이트리스트 방식**(CLAUDE.md·settings.json·skills·commands만). 통째 커밋 금지 — .credentials.json·대화기록 유출 위험.
- **DEC-20260720-bsjeong87-05** — 문서 3종 역할 분담 확정: **00·01은 드롭인 지시서로 얇게 유지, 세션 운영 기법 상세는 02가 전담**. 02 진단에서 나온 공백은 00·01에 한두 줄 포인터만 심는 **경량 반영(A안)** 원칙 — 드롭인 문서가 길어질수록 적용 정확도가 떨어지고(비대 CLAUDE.md와 동일 원리) 재검증 시 중복 유지보수가 생기므로.
- **DEC-20260721-bsjeong87-01** — 새 PC 글로벌 셋업은 **`global-config/` 정본 복원을 1순위**로 한다. 00 §D의 settings/CLAUDE.md 블록을 보고 수기 재구성하는 건 백업이 없을 때의 **폴백**. 이번에 §D(그것도 낡은 v1.2)만 보고 재구성했다가 `skills/humanizer/`·`commands/` 전체와 CLAUDE.md §9를 누락했다 — §D 블록은 최소 골격이지 실제 구성의 전부가 아니다. 복원 전 `git pull`로 문서 버전부터 확인할 것(당시 로컬은 7/16 상태, 원격은 v1.4).
- **DEC-20260721-bsjeong87-02** — 복원 시 `settings.json`은 **통째 덮어쓰기가 아니라 병합**. 문서·백업 반영분(deny·hooks 등 구성 성격)은 적용하되, **머신·계정 현실에 종속된 값은 현재 것을 보존**한다: `model`(백업은 `claude-fable-5[1m]`이나 사용량 크레딧 미활성 → `opus`), `effortLevel`, `theme`, `enabledPlugins`·마켓플레이스 소스 형식(실제 설치 경로가 다름), `autoUpdatesChannel`. 백업 시점 이후 머신 상태가 달라져 있으므로 통째 복원은 동작하지 않는 설정을 되살린다.
- **DEC-20260721-bsjeong87-03** — `theme`은 백업 동기화 대상에 **포함**하되(개인 설정에 남는 건 무방 — [[DEC-20260720-bsjeong87-03]]), 값이 바뀌면 커밋 전에 사용자에게 고지한다. 이번에 `dark-daltonized`(색각 대응) → `dark`가 백업에 반영됐다 — 접근성 설정이라 무언 반영은 위험. 복원용 백업은 되살릴 값이므로 취향 키라도 변경 시 확인이 필요하다.
- **DEC-20260723-bsjeong87-01** — **html 전면 폐기, md 단일 관리로 전환** ([[DEC-20260720-bsjeong87-01]] 대체). 이 repo 문서의 용도가 "새 PC·새 프로젝트에서 Claude에게 학습시키는 드롭인 지시서"로 확정되면서 사람용 html의 존재 이유가 소멸 — 이중 동기화 유지보수만 남음. 02는 HTML만 있던 것을 md로 변환(v1.1, 내용 동일)해 보존 후 html 3종 삭제(복구는 git 히스토리 197ff1a 이전). PROJECT_PLAN 미해결 항목(02 md 부재)도 함께 해소.
- **DEC-20260723-bsjeong87-02** — 모든 문서의 기본 원칙 확정: **1순위 = 최적의 결과물, 토큰 절약 = 품질을 해치지 않는 범위의 2순위 부가 효과.** 절약을 위해 검증 스테이지·기록·상위 모델 라우팅을 빼지 않는다. 00 §0·01 "왜"에 명문화(00 v1.6, 01 v1.5). 문서가 흔들릴 때(예: "탐색까지 전부 haiku로" 같은 절약 우선 제안) 이 원칙으로 판정한다.
- **DEC-20260723-bsjeong87-03** — **(구)02 진단·확장활용 가이드 제거 + 문서 번호 재편** ([[DEC-20260720-bsjeong87-05]]의 "02 상세 전담" 부분 대체 — "드롭인은 얇게 + 경량 반영(A안)" 원칙은 유지). 근거: 02의 진단부(§1~3)는 경량 반영 완료로 소임을 다한 역사 기록이고, 운영 가이드부(§4~C)는 구성 파일을 생성하지 않는 사람용 습관이라 드롭인 저장소의 목적(범용 구성 적용)에 불필요. 세션 운영 상세는 공식 문서(code.claude.com/docs) 직접 참조로 전환. 번호 재편: 통합구성 00→**01**, 모델분담 01→**02**, **00 슬롯은 신규 문서 예약**. 복구는 git 히스토리에서 가능.
- **DEC-20260723-bsjeong87-04** — **`index.html`(사람용 안내 페이지)은 md 단일 관리([[DEC-20260723-bsjeong87-01]])의 예외**. md 미러가 아니라 별도 목적(개발 입문자~비개발자용 저장소 사용법, GitHub Pages 랜딩)의 독립 산출물이므로 이중 관리에 해당하지 않음. 동기화 의무는 **문서 목록·번호가 바뀔 때만**(md 내용 수정 시엔 없음). 제작 기준: frontend-design 스킬 원칙(AI 기본값 룩 회피) + 대표 이미지는 03 svg-design 레시피로 자체 시연. README도 같은 눈높이(비개발자) 유지.
- **DEC-20260728-bsjeong87-01** — **드롭인 문서 설계 원칙 확장**(기존 [[DEC-20260723-bsjeong87-02]] 기본 원칙에 추가): ① **2단계 선택형** — 00에서 문서 선택 → 각 문서 안에서 항목 선택(01·02 기본 구성도 부분 선택 지원, 단 01 보안 항목은 해제 비권장). ② **중복 기능 금지** — 내장 기능·기존 문서와 겹치면 더 효율 좋은 하나만 정본, 나머지는 부록/추천 한 줄(적용례: 내장 `/code-review` 정본·커스텀 reviewer 선택 강등, 내장 auto-memory 정본·Claude Mem 부록, 02 로스터 정본·Superpowers 부록). ③ **회사 업무용 상시 고려** — 조직 정책(managed settings·admin 토글) 우선, 사내 git 호스트(GitLab) 분기, 외부 데이터 전송(MCP·CI) 항목엔 정책 확인 게이트. 우선순위는 **안전 > 최적의 결과물 > 토큰 절약**. 이 원칙으로 04(검증 자동화)를 신설하고 03을 5종으로 확장함.
- **DEC-20260728-bsjeong87-02** — **절약 프로필은 별도 Pro 문서가 아니라 기존 문서의 선택지로**. 근거: Pro 전용 문서는 01·02와 90% 중복(이중 유지보수 — html 폐기와 같은 문제), 요금제 이름에 종속되면 범용성 훼손. 대신 00 STEP 3에 **운용 프로필(표준/절약)** 질문을 추가하고 하위 문서가 분기(01 §D-6 effort 운용, 02 §F 절약 배정, 03 claude-hud 기본 체크, 04 게이트 유지). 절약 설계 원칙: **기본값 하향 + 낭비 제거만** — 품질 불변 지점(트리아지·최종 리뷰·검증 게이트)은 절약 프로필에서도 줄이지 않는다([[DEC-20260723-bsjeong87-02]]와 정합). 핵심 패턴: Sonnet 메인 + 상위 advisor(공식 측정 비용 −11.9%·품질 +2.7%p). "절약"은 Pro 전용 명칭이 아님 — Max 한도 임박·회사 비용 관리에도 동일 적용.
