# /audit 7차 (회귀 모드) — 2026-08-21

- **모드**: 회귀. 델타 `bd37602^..HEAD` (4커밋 / 9파일 / +95·−41) — nastvad형·sfa형 시나리오 검증, `/wrap` 점검, wave형 시나리오 검증. 00 v1.29→v1.32 · 01 v1.48→v1.51 · R21~R24 신설.
- **선행**: `pwsh -File scripts/check.ps1` 통과(INFO 0) — 기계 축(버전 4축·표 시간순/결번·표 파손·BOM·frontmatter·미러 해시·규칙 대장 존재 여부)은 확정으로 두고 렌즈에서 제외.
- **렌즈**: A(규칙 정합성)·B(버전·참조)·C(절차 케이스)·D(저빈도 03~06)·E(실물↔백업↔명세)·F(자작 스킬) 6종 병렬.
- **재검증**: 전건 원문 대조. **오탐 1건 기각**(§2에 기록).
- **원시 → 확정**: 렌즈 보고 36건 → **확정 31건**(상 7 · 중 15 · 하 9) + 인계 6건 + 오탐 1건.

> ⚠️ **확정 7건 중 3건이 이 델타가 만든 회귀이고, 그중 2건은 오늘 wave형 반영(`cef9f29`)이 만들었다.** 회귀 모드를 고른 판단이 그대로 값을 냈다.

---

## 1. 확정 [상] — 7건

| # | 파일:줄 | 문제 | 실해 시나리오 | 제안 |
| --- | --- | --- | --- | --- |
| S1 | `01:296~300` (+ `docs/RULES.md:55,57`) | **§F-2 A wrap 템플릿에 R21(갈래·날짜)·R23(되돌림)이 없다.** 실측 `갈래·날짜` = 241(§F-1)·323(B)·352(C) **3곳**인데 대장은 "01 안 4곳: §F-1·§F-2 **A**·B·C"로 A를 명시 — **표가 거짓 보증**. `Reverted`도 A에만 없다. **렌즈 A·B·C·E 4종이 독립 수렴.** 어제 v1.50 행의 진단 "R21·R22를 A에만 넣어 누락"은 **R21에 대해 사실과 다르다**(R22만 A에 있었고 R21은 A에 들어간 적이 없다) | 단일 repo 신규 설치자가 받는 것이 A다. A로 깐 다인 팀은 `/wrap`이 갈래·날짜 없이 append하는데 **같은 A의 resume(289)은 "갈래를 우선 읽고"** 를 지시한다 — 소비자만 있고 생산자가 없어 필터가 영구히 빈손. `check.ps1`은 파일 단위 판정이라 영구 OK | A wrap 본문에 B·C와 같은 한 줄(§F-6 양식 + 갈래·날짜 + `[Reverted]`) 추가. R23 규칙 열에도 "01 안 N곳" 명기 |
| S2 | `01:299`·`324`·`352` + `~/.claude/skills/wrap/SKILL.md:21` (+ 미러) | 🔴 **오늘 `cef9f29`가 만든 회귀.** "기록 3종 중 **일부만** 있으면 미설치 알림"이 **정상 단위분할 repo를 매 세션 오보**한다 — §E-3 레이아웃(`01:212~217`)은 `PROJECT_PLAN`만 `docs/` 루트, `PROGRESS`·`DECISIONS`는 `docs/<단위>/`다(`01:239`·`CLAUDE.md:56`이 정본). 어느 폴더를 기준으로 세도 "일부만"이 **항상 참**. 렌즈 A·C 수렴 | **수정 전엔 오발하지 않았다** — 옛 조건 "`docs/`가 없으면"은 MSA repo에서 `docs/`가 존재해 발동 안 함. 지금은 `/resume`이 "B로 바꾸세요", 같은 세션 `/wrap`이 "기록 체계 미설치"라며 **PROGRESS를 append하지 않고 요약만 출력**한다(기록 소실). 이 PC의 실물 스킬도 같은 문구라 **현재 배포 상태** | 3종을 한 폴더로 세지 말 것 — "`PROJECT_PLAN`은 repo `docs/` 루트, `PROGRESS`·`DECISIONS`는 대상 단위"로 경로 기준을 명시해 A·B·C wrap + 실물 스킬 + 미러 전부 수정 |
| S3 | `00:71` | 🔴 **오늘 `cef9f29`가 만든 미완.** 셋째 증거 경로(직접 확인)를 넣었으나 **바로 뒤 "설치·검증이 성공한 뒤에만 STEP 5에서 그 메모를 제거한다"가 그대로 남아** 해소를 관찰해도 메모를 닫을 수 없다. `00:94`(STEP 5 ③)도 같은 게이트를 반복 | 이 경로를 신설한 계기(wave 루트의 `아카이브 대기(2026-08-12~)`)는 **설치할 것이 없는 대기**다. 해소를 직접 관찰해도 제거 조건을 만족시킬 수 없어 메모가 남고, 처방이 막으려던 "`/resume` 상시 오보"가 그대로 재현된다. 앞의 "해소됨 → STEP 4 설치 대상에 넣는다"도 공회전 | 셋째 경로 문장 끝에 "이 경로로 해소가 관찰되면 **설치 없이도** STEP 5에서 메모를 제거한다(근거를 괄호에 남긴다)" |
| S4 | `00:98`·`00:102` ↔ `00:104` | v1.30이 "머신 로컬(조치 대기·`프로필=`)은 글로벌 줄에만"을 넣었는데 **같은 절의 표준 예시와 필드 목록이 안 고쳐졌다** — 예시 `<!-- dropin-applied: … · 프로필=표준 · … -->`는 **프로젝트 줄**이고, 필드 첫 불릿도 `적용일·모드·프로필`을 필수로 열거. 소비자 `~check:22`도 프로젝트 줄에서 프로필을 파싱. 렌즈 A·B·C 수렴 | 실행자는 규범 문장보다 **복붙 가능한 예시 줄**을 따르므로 새 규칙이 사실상 발화하지 않는다. 규칙을 지킨 드문 경우엔 반대로 `/dropin-check`가 결손 보고를 낸다. 이 저장소 자신도 `CLAUDE.md:89`에 `프로필=표준`을 프로젝트 줄로 갖고 있다 | `00:98` 예시에서 `프로필=표준` 제거, `00:102`를 "적용일·모드(프로필은 글로벌 줄)"로, `~check` 파싱을 스코프별로 분리 |
| S5 | `04:49`·`04:105` | 04가 `04 vX.Y(gh 설치, 인증 대기(YYYY-MM-DD~))`를 **대상(프로젝트) `CLAUDE.md`**에 남기라 지시. 04 전체에 `~/.claude/CLAUDE.md` 언급 **0건**. `00:104`가 이 규칙의 대표 예시로 든 문자열이 **정확히 04의 예시**다 | A가 `gh` 인증을 안 끝낸 채 커밋 → 팀 전원의 프로젝트 `CLAUDE.md`에 `gh 인증 대기`가 상속 → 전원의 `/resume`이 남의 대기를 상시 안내하고 재적용의 "괄호 메모 보존"이 매번 되살린다. **04는 단독 드롭인이 표준 경로**라 00을 안 읽으면 교정 기회가 없다 | 04:49에 한 줄 — "인증·설치 대기 등 그 PC에서만 참인 메모는 `~/.claude/CLAUDE.md`에만(프로젝트 줄엔 팀 사실만 — 00 §A STEP 5)" |
| S6 | `06:45`·`06:47`·`06:34` | 🔴 **오늘 `cef9f29`가 만든 회귀.** 00에 게이트 차단 **사유별 스코프 분기**(조직·정책=팀 / 요금제·계정=글로벌)를 넣으면서 소비자 06을 보지 않았다. 06은 게이트 4종(요금제·조직 승인·repo GitHub·**계정 GitHub 연결**) 전부를 프로젝트 기록으로 보낸다(`06 vX.Y(준비물만 — 루틴 생성 게이트 차단(계정 GitHub 미연결))`). 06 자신은 "루틴은 **계정 스코프**"(06:37)·"**개인 계정 소유**"(06:112)를 알고 있다. 06에도 `~/.claude/CLAUDE.md` **0건** | A의 계정 미연결이 팀 repo에 커밋 → B·C의 재적용에서 계속 뜨고, 정작 A의 글로벌 기록엔 흔적이 없어 A가 연결을 끝내도 닫히지 않는다. 한 줄에 팀 사실과 개인 사실이 섞인다 | 06:47에 한 줄 — "요금제·계정 GitHub 연결 사유는 `~/.claude/CLAUDE.md`에, 조직 승인·GitHub 아님은 프로젝트 `CLAUDE.md`에" |
| S7 | `06:70` | §2 검증 ①이 **§3 산출물**(`/schedule run` 세션 로그)을, ③이 **존재하지 않는 루틴의 환경변수 목록**을 요구한다. 06:41은 §2/§3/§4를 개별 선택으로 두고, 06:34는 계정 게이트가 "**§3 루틴 저장만 막는다**(§2 준비물은 진행 가능)"고 명시 — **§3 없이 §2만 성립하는 경로가 정식으로 있다**. 같은 줄이 §4에 대해서만 이 함정을 막아 뒀다 | 게이트 차단으로 §2만 설치한 **표준 시나리오**에서 ①③이 증거를 못 내 "실패"로 보고되거나 조용히 건너뛴다 — 후자면 `.mcp.json` 커밋 여부라는 유일한 실검증도 함께 증발 | ①③ 앞에 분기 — "§3을 이번에 하지 않으면(미선택·게이트 차단) 연결·환경변수 확인은 `루틴 생성 시 확인 대기`로 남기고 `.mcp.json` 커밋·`git ls-files` 증거만 낸다" |

---

## 2. 확정 [중] — 15건

| # | 파일:줄 | 문제 | 실해 | 제안 |
| --- | --- | --- | --- | --- |
| M1 | `docs/PROGRESS.md` 다수 (+ `01:256`·`~resume:22`) | **`[Pending]`·`[Blocked]` 마커의 종결자가 0건.** 생산자 ✅(R08)·소비자 ✅(R02)·종결자 ❌ — 종결은 PROJECT_PLAN에서만 일어나고 PROGRESS 원항목은 영구히 열려 있다. **사례 1건이 아니라 현재 파일에 3건 상존**(`:15`·`:37`·`:47` 전부 이미 종결) | `/resume`이 닫힌 항목을 열린 것으로 보고한다 — **이번 세션이 실제로 그렇게 오보했다** | ⓑ 권고: `/resume`이 PROGRESS의 Pending을 보고하기 전 **이미 읽고 있는 PROJECT_PLAN "미해결/관찰 중"과 대조**하고 없으면 닫힌 것으로 본다(추가 파일 읽기 0). R19와 같은 축. 대장에 별도 행 |
| M2 | `~/.claude/skills/resume/SKILL.md:7~17` | 실물 스킬의 **단위 판별이 "통합 워크스페이스면" 조건 안에 갇혀 있다.** `docs/<단위>/`로 쪼갠 **단일 repo**에서는 22행의 `docs/PROGRESS.md` 고정 경로만 남는다. §F-2 C 폴백("INDEX만 생략, **단위 판별 유지**")도 A 폴백("B로 교체 안내")도 실물엔 없다 | 단위분할 단일 repo에서 `/resume`이 조용히 빈손으로 끝난다 — 두 템플릿이 각각 다른 방식으로 막으려던 그 실패. `01:275`의 **`C ⊃ B ⊃ A`** 주장이 이 PC 실물에선 성립하지 않는다 | 실물 resume에 C 폴백 한 줄 + 미러 동기 |
| M3 | `00:103` ↔ `00:72` | 🔴 오늘 신설한 `… 대기(사유)` 형식이 **같은 문서의 괄호 용법과 충돌**한다 — `00:72`는 같은 괄호에 "**최초 발생일 보존**(`gh 인증 대기(2026-08-06~)`)"을 규정. 게다가 새 형식을 따르는 예시가 문서 전체에 0건(자기 예시조차 `(gh 설치, 인증 대기)`) | 재적용을 반복하면 `대기(사유)`와 `대기(발생일)` 중 하나가 다른 하나를 덮어써, 두 축 중 한쪽이 매번 소실 | 두 축을 한 형식으로 — **`… 대기(사유, YYYY-MM-DD~)`** 로 고정하고 `00:72`·`00:103`·예시 3곳 동기 |
| M4 | `00:104` ↔ `00:124` | "머신 로컬은 **글로벌 기록이 정본**"인데 **글로벌 줄이 생기지 않는 설치**에서 갈 곳이 없다 — 병기 트리거는 "글로벌 스코프 **항목이** 하나라도 포함됐으면"이라 프로젝트 전용 설치는 글로벌 줄을 안 만든다. 그런데 `프로필=`은 STEP 3에서 **항상** 묻고, 요금제·계정 게이트 차단도 글로벌행이다 | 프로젝트 전용 설치에서 프로필·요금제 차단이 **어디에도 기록되지 않는다** → 재적용이 프로필을 매번 다시 묻고, 요금제 차단은 질문 ③에서 사라진다(STEP 3 스스로 "신호 소실이 나쁘다"고 한 것) | 병기 트리거에 "**또는 머신 로컬 항목이 하나라도 생기면**(없으면 최소 골격으로 만들어) 거기 남긴다" |
| M5 | `00:104` ↔ `00:94`·`00:72` | **이미 프로젝트 줄에 적힌 머신 로컬 메모의 이관 절차가 없다** — 새 규칙은 "적지 않는다"(앞으로)만 정하고, 재적용은 "괄호 메모 **보존**"·"메모 **유지**"가 원칙 | 이 규칙을 낳은 관찰 사례 자체(공유 repo에 남은 한 사람의 `gh 인증 대기`)가 그대로 유지된다 — **처방이 발견 사례에 도달하지 않는다** | "재적용 시 프로젝트 줄에서 머신 로컬 메모를 발견하면 **글로벌 줄로 옮기고 프로젝트 줄에선 제거**(이관은 제거 게이트의 예외)" |
| M6 | `~/.claude/skills/dropin-check/SKILL.md:24` | 글로벌 줄 파싱이 **조건부**("프로젝트 기록에 글로벌 항목이 포함됐거나 글로벌만 적용한 PC면")인데, v1.30 이후엔 **글로벌 항목이 없는 프로젝트 전용 설치에서도** 조치 대기·요금제 게이트가 글로벌 줄에만 존재한다 | 조건이 거짓이라 글로벌 줄을 안 읽고 §4가 통째로 비어 "최신 — 조치 불요"를 낸다. `/resume`은 이미 무조건 둘 다 읽는데 `/dropin-check`만 반쪽 | 조건을 "**항상 함께 파싱**"으로 |
| M7 | `~/.claude/skills/dropin-check/SKILL.md:23` 부근 | 00 v1.31의 **위임 표기 "루트 부재 폴백"이 소비자에 미배선** — 00은 "루트가 없으면 위임을 무시하고 미적용 취급"으로 고쳤으나 스킬은 "루트 기록 기준으로 판정한다"만 갖는다 | 하위 repo만 클론한 팀원이 점검하면 위임된 문서가 판정 대상에서 **조용히 빠지고** "최신 — 조치 불요"가 나온다. 00을 거치지 않는 유일한 점검 경로가 그 사각을 통과 | "루트 실물·기록이 없으면 그 문서는 **확인 불가**로 보고" 한 줄 + 미러 |
| M8 | `01:53` ↔ `01:55`·`01:475` | 기여자 수(`git shortlog`)로 "다인이면 STEP 2 (b)는 **확인만**"으로 강등했는데, (b)는 **3분기**(ⓐ 혼자 / ⓑ 다른 갈래 / ⓒ 같은 갈래)이고 기여자 수는 ⓐ와 (ⓑ\|ⓒ)만 가른다. **ⓑ/ⓒ 경계는 커밋 로그에 없다** | ⓒ는 MSA 단위분할 **필수**를 트리거하는 구조 결정이다. "확인만"으로 넘기면 ⓑ가 기본 제시돼 ⓒ인 팀이 단위분할 없이 설치된다 | `01:53`을 "기여자 수는 ⓐ 여부만 정한다 — **ⓑ/ⓒ는 반드시 묻는다**"로 |
| M9 | `01:77`·`01:464` | **동시성 3분기가 §B 판별표·§H 적응 규칙의 옛 이분법에 미배선** — 둘 다 "같은 단위 동시 편집"만 묻는다 | ⓑ(각자 다른 기능을 하는 흔한 팀)가 두 표에서 "동시성 없음"으로 읽혀 **`merge=union` 없이 flat**으로 구성된다 — §I 2가 "필수"로 올린 항목이 빠진다 | §H 동시성 행을 "ⓐ/ⓑ/ⓒ(§I 2) — ⓑ부터 union 필수, ⓒ면 단위분할", §B MSA 행을 "동시성 ⓒ(또는 ⓑ에 갈래 3+)"으로 |
| M10 | `01:75~76` | §B 판별표가 통합 환경에 **§E-4를 필수처럼** 붙인다(`§E-2 + §E-4(루트 레이어)`) — v1.50이 넣은 "루트 레이어는 **선택**, 기본 아니오"(`01:201`·`01:478`)와 충돌 | STEP 1이 유형을 표에서 확정하므로 루트 레이어가 기본 포함으로 제안되고, 딸린 §F-2 C(글로벌 스킬 교체)가 **그 PC의 다른 프로젝트 전부에 전파**된다 | 두 행을 "§E-2 (+ 선택 §E-4 — 기본 아니오)"로 |
| M11 | `00:40~46` | 확보 사다리에 **"프로젝트 밖 폴더의 비-git 사본"** rung이 없는데, 바로 아래 `출처=` 어휘는 `사본 <경로>` 값을 전제한다. `/dropin-check:13`은 "없으면 **사용자에게 경로를 묻고**"를 갖는다 | 폐쇄망에서 프로젝트 밖에 최신 사본이 있어도 rung 2·3이 다 실패해 "확보 0건 → 중단"으로 떨어진다 — **물어보기만 하면 설치되는 환경이 통째로 막힌다** | rung 2·3 사이에 "사용자가 지정한 폴더(git이면 클론·아니면 사본으로 판별)" 추가 |
| M12 | `00:70` | **신규 합류 판정이 대부분의 신규 합류자를 놓친다** — 기준이 "`~/.claude/`에 글로벌 구성이 없으면"인데, 합류자 대다수는 Claude Code를 이미 쓰던 사람이라 글로벌 구성이 **이미 있다** | 이들은 "재적용"으로 분류돼 기본값("이 repo의 기존 구성을 따른다")을 못 받고 재구성·교체를 대등하게 제안받는다 → **팀이 맞춘 구성을 새 사람이 덮는 경로**가 열린 채 남는다 | "이 PC의 글로벌 기록·프로젝트 산출물 실물에 **이 기록의 흔적이** 없으면 신규 합류"로 |
| M13 | `docs/RULES.md:14` + `scripts/check.ps1` | ⑤ **처방이 진단과 다른 자리에 있다** — 실패한 주체는 `check.ps1`(파일 단위 판정)인데 처방은 "사람 체크리스트"에 걸렸다. `check.ps1`은 `-notmatch`(존재 여부)만 보므로 규칙 열의 "01 안 4곳"을 **아무도 집행하지 않는다**. 그 문장을 쓴 델타에서 **하루 만에 S1으로 재발** | 파일 단위 검사를 통과하는 사본 누락이 시나리오 검증 없이는 계속 새어 나간다 | `check.ps1` §규칙에서 규칙 열의 `01 안 (\d)곳`을 파싱해 **출현 수**를 대조(버전 표 인용 줄 `^>\s*\|`는 계수 제외) |
| M14 | `~/.claude/commands/skill-setup.md:74` ↔ `.claude/skills/reverify/SKILL.md:20` | 카탈로그 외부 URL의 재검증 소속이 **정면 모순** — skill-setup은 "정기 재검증 대상이 **아니**어서 실행 시점 확인이 유일한 검증", reverify §1-C는 "skill-setup 카탈로그의 외부 URL 생존"을 조사 대상으로 든다. 두 줄이 같은 배치에서 **2분 간격**으로 들어갔고 뒤 커밋이 앞 문장을 무효화했는데 앞이 안 지워졌다 | 다음 `/optimize`·`/audit`이 "재검증 대상이 아니다"를 근거로 reverify의 그 축을 **군더더기로 걷어낸다** → 카탈로그 URL이 다시 무추적 | skill-setup의 반문장을 "생존 점검은 `/reverify` §1-C가 맡고 정체 확인은 실행 시점 몫"으로(미러 동반) |
| M15 | `.claude/skills/optimize/SKILL.md:25~28` | **`/optimize`만 예정일 생산자(R09)가 없다** — audit §4·reverify §4는 "촉발한 예정일 줄을 닫고 다음 주기를 `다음 ○○: YYYY-MM경`으로 갱신"을 갖는데 optimize엔 없다. 소비자는 이미 배선돼 있다(`PROJECT_PLAN:132`) | `/optimize`를 돌려도 촉발 줄이 안 닫히고 다음 주기가 안 적힌다 → `/resume` 예정일 안내가 상시 오보. **이미 한 번 밟아 손으로 수습했다**(`d0f728b`) | optimize §3 마지막에 한 줄 + R09 필수 목록에 `p:optimize` 등재 |

---

## 3. 확정 [하] — 9건

| # | 파일:줄 | 문제 | 제안 |
| --- | --- | --- | --- |
| L1 | `01:542` | 푸터 "최종 갱신"이 **2026-08-20**인데 헤더는 2026-08-21 — v1.49·v1.50·v1.51 **3연속 bump에서 날짜만 빠졌다**. `CLAUDE.md:31`이 명시적으로 요구하는 항목이고, `check.ps1`은 버전 4축만 보고 날짜는 안 본다(00·02~06은 전부 일치) | `2026-08-21`로 정정 + `check.ps1` §버전에 헤더↔푸터 **날짜** 대조 추가 |
| L2 | `00:140` · `CLAUDE.md:18` · `index.html:276` · `pages/learn.html:446` · `pages/options.html:581` | **"내장 스타일 4종"이 5곳에 남았다**(05 v1.10에서 Concise 추가로 5종). `PROJECT_PLAN:139`엔 `CLAUDE.md` **한 곳만** 등재돼 있어 고쳐도 4곳이 남는다. 00 §B는 "선택 목록의 원본"이라 **Concise가 통째로 빠진 채 안내**된다 | 5곳 동시 정정 + 등재 항목에 사본 위치 병기 |
| L3 | `00:70` | "**아래** 불변 ①"이 가리키는 대상은 바로 **위 줄**(`00:69`) | "위 불변 ①"으로 |
| L4 | `00:123` | 위임 표기를 "무시하고 미적용 취급"한 뒤 **그 표기 자체의 처리(제거/유지)가 미정의**. `00:94`는 "보존"이 원칙이라 옛 위임과 새 설치 기록이 한 줄에 공존한다 | "그 문서를 이번에 설치했으면 **위임 표기에서 그 문서를 뺀다**" |
| L5 | `00:43` | 클론 `behind > 0` 가드에 **pull 실패 폴백이 없다**. 같은 상황을 `/dropin-update:24`는 "`git show origin/main:<파일>` 기준으로 비교"로 처리 | rung 2에 같은 폴백 한 구절 |
| L6 | `01:414` ↔ `01:256`·`289`·`313`·`341` | 장수 통합 브랜치 병합 경고("최상단이 최근이 아니다 → **날짜**로 읽는다")에 **소비자 배선이 없다** — resume 지시는 전부 "최상단 최근 ~5항목"으로 남아 있다 | resume 지시에 "상단 5항목의 날짜가 역순이면 **날짜 기준**으로 다시 고른다" |
| L7 | `~/.claude/skills/dropin-check/SKILL.md:38` | "개인 쪽에 가려짐" **그림자 감지가 보고만 되고 §4 권고 목록에 후속 조치가 없다**(처방은 `01 §F-2`에 이미 있다) | §4 권고에 "그림자 → 글로벌 동명을 그 내용으로 교체(또는 제거)" 추가 |
| L8 | `.claude/skills/audit/SKILL.md:13` ↔ `:25` | §0이 "BOM은 스크립트가 확정"이라 했는데 §1-F가 BOM 재조사를 지시(완전 중복). 반대로 `check.ps1:116`의 frontmatter `name`↔파일명 검사는 **`output-styles`를 안 돈다**는데 §0 문구가 "frontmatter는 확정됨"으로 덮는다 | §1-F ③의 BOM 반절을 "`output-styles`의 `name`↔파일명은 check 밖이라 렌즈 F가 본다"로 교체 |
| L9 | `.claude/skills/audit/SKILL.md:20` | 렌즈 A의 사본 목록이 **`01 §F-1·§F-2`를 한 벌로** 세어, S1을 놓친 그 입도 그대로다. `RULES.md:14`가 "파일 안 사본 위치"를 요구한 형식과 어긋난다 | "`01 §F-1·§F-2(**A·B·C 각각 별개 사본** — RULES 규칙 열과 대조)`"로 |

---

## 4. 판단 필요 — 처방 위치가 스킬이 아닐 수 있음 (2건)

| # | 문제 | 선택지 |
| --- | --- | --- |
| J1 | **다인 축의 "유일한 발견 수단"이 어디에도 없다.** `DEC-20260821-01`이 "팀 축은 앞으로도 **시나리오 대입이 유일한 수단**"이라 했는데 ⓐ 스킬 어디에도 시나리오 대입 절차가 없고 ⓑ `/audit` §1-C 여정 목록에 **다인·신규 합류·브랜치 축이 없다**(R21~R24를 낳은 8단계 여정이 렌즈에 안 실렸다) ⓒ PROJECT_PLAN의 `다음 …경` 9줄 중 시나리오 항목 0건. 3회차가 미정의 21건을 냈는데 **다음 발화 트리거가 없다** | ⓐ `/audit` §1-C에 `다인(합류→갈래 병행→검수 병합→되돌림)` 여정 추가 ⓑ PROJECT_PLAN에 `다음 시나리오 대입: 2026-11경` ⓒ 둘 다 |
| J2 | `CLAUDE.md:36`의 **유지보수 "4축"** 열거가 시나리오 검증(최근 4커밋 중 3커밋·발견 21건)을 담지 않는다. 가상 대입이라 "실적용=경험 검증"과 다른 축이고, R14("자라는 목록은 열거하지 않는다")가 겨냥한 형태의 열거이기도 하다 | ⓐ 5축으로 확장 ⓑ 실적용 축의 하위 방법으로 명시 ⓒ 안 하기 |

---

## 5. 외부 확인 필요(인계) — 6건

`/audit`은 인터넷 조회 없이 도므로 확정도 기각도 할 수 없다. 승인 시 **PROJECT_PLAN "다음 재검증" 🟡 목록**에 올린다.

1. 05 **내장 스타일 종수(Concise 포함 5종)** 재확인 — 내부적으론 05가 정본이라 L2 수정은 외부 확인 없이 진행 가능. 종수 자체만 인계.
2. 05 `keep-coding-instructions` **기본값 `false`** 여부 — 실물이 `true`인 건 작성자가 규칙을 지킨 증거일 뿐 기본값의 증거가 아니다.
3. 05 개인↔프로젝트 **동명 스타일 우선순위**(05가 🟡로 명시적 미명시 처리 중) — 상태 유지 여부.
4. 06 **요금제 범위·research preview 상태·`/schedule` 은닉 env 4종·GitHub App 요건**.
5. 03 **Claude in Chrome 직계약 플랜 제한**(Bedrock·GCP·Foundry 제외) — D의 관련 처방은 이 사실과 무관하게 유효.
6. 04 **구독 OAuth 4종·GitLab 통합 beta 유지 주체·인증 비대칭**(§L에 3개월 주기로 이미 예약).

---

## 6. 오탐 · 의도된 설계 · 보류

<details><summary>펼치기</summary>

**오탐 1건** — "00 STEP 2의 통합 워크스페이스 각각 읽기 지시가 소실됐다"(내가 조사 중 직접 낸 발견). `grep`/`cut`으로 잘린 출력만 보고 판정했으나, 실제로는 `00:50`의 **긴 불릿 안쪽**에 온전히 살아 있었다(git 이력 대조로 자기 기각). 이 함정을 렌즈 프롬프트에 방어 규칙으로 심었고, 6개 렌즈 모두 같은 오탐을 내지 않았다.

**의도된 설계로 판정** —
- `CLAUDE.md:89`에 `00 vX.Y`가 없는 것: 그 규칙(`b62bfc1`, 2026-08-13)이 기록(2026-08-12)보다 **나중**이라 당시엔 규범이 아니었다. 지금 임의로 채우면 "적용하지 않은 버전을 기록"하게 되므로 **다음 재적용 때 실측값으로** 처리한다.
- `CLAUDE.md:73`·`:75`에 R19·R22(소비자 축)가 없는 것: 대장이 `RC`를 필수로 요구하지 않고 R19 선례와 일치. 다만 R21(생산자)은 RC에 있어 **비대칭**이라, 대장 읽는 법에 "RC는 다인 축 소비자 규칙을 싣지 않는다"를 한 줄 명시하는 편이 낫다(L급).
- `06`의 `docs/ROUTINES.md merge=union` ↔ append 원칙: 이 저장소 PROGRESS 규약과 같은 형태라 **의도된 감수**로 본다(R24는 줄 *교체*가 일어나는 파일에 관한 규칙).

**보류(등재만)** — 글로벌 `~/.claude/CLAUDE.md` §9의 "현재 7종" 하드코딩. 실측과 일치하고 `~GC`는 R14 필수가 아니라 `check.ps1`도 안 본다. 스킬 하나만 늘어도 조용히 틀리는 자리.

**렌즈별 오탐률** — A 0/12 · B 0/8 · C 0/16(1건은 기지 관찰로 자진 제외) · D 0/12 · E 1/4(`<!-- dropin-applied` 접두 표기는 실해 미확인 [의심] — L급 정밀화로 강등) · F 0/6. 프롬프트에 심은 "긴 줄 펼쳐 읽기" 방어 규칙이 작동했다.

</details>

---

## 7. 이상 없음으로 확인한 축 (증거)

- **미러**: `~/.claude` 화이트리스트 5종 ↔ `global-config/` **27파일 완전 일치**(skills 7·commands 1·output-styles 1). 한쪽에만 있는 파일 0건. `settings.json` 구성 성격 4키 전부 동일(`hooks` 차이는 키 순서뿐). `CLAUDE.md` 정규화 diff = `dropin-applied` 줄 1건(규정된 예외).
- **명세↔실물**: §D-3 deny **27종 전건 일치**, §D-4 raw URL 2개 실재, `commands`↔`skills` 이름 중복 0건, §F-3의 `additionalDirectories` 정확히 제외됨, §F-4에 `PROJECT_PLAN`·`CLAUDE.md` union 없음 ✔.
- **§참조 실재**: 00~06·CLAUDE.md의 모든 `§` 참조를 헤딩과 대조 — **없는 절 0건**(과거 실사례 `02 §0`은 제거 상태 유지).
- **버전 하드코딩**: 드롭인 7종의 `vX.Y` 플레이스홀더 17개소 정상, 실제 숫자는 표·헤더·푸터·기록에만. **재발 0건**.
- **유령 규범**: 신설 버전 표 행 6개 전수 대조 — 본문 실재. 허위 요약은 `01 v1.50` 행의 괄호 진단 1건뿐(S1).
- **frontmatter**: 10종 전부 `name`↔폴더/파일명 일치, 관리자 전용 4종 `disable-model-invocation: true`, 잘못된 구분자 0건, `keep-coding-instructions: true` 정상.
- **svg-design 3자 대조**: `03 §2` 코드펜스 ↔ 설치본 ↔ 미러 **정규화 후 완전 동일**.
- **외부 저작 설치본**: humanizer의 `references/*` 6 + `examples/*` 8 전건 실재, frontend-design `LICENSE.txt` 실재.
- **여정 대입(온전)**: 신규 설치 2종×확보 4경로 · 재적용(기록 유/무·출처·상태 어휘 4종·병합 구간·역방향) · 특수 환경(비-git·폐쇄망·더티·push 권한 없음·혼합형·루트/하위) · 02 여정(부분 선택·기존재·비개발) · 회사 게이트 "아니오" 이후 · 스킬 핸드오프 왕복.
- **03~06 실행 프로토콜**: 네 문서 모두 감지→질문→설치→검증(증거)→기록 완결, "됐다"로 끝내는 자리 0건, 게이트 우회 경로 0건.

---

**상태**: §4 **수정 완료**(2026-08-21, 전건 승인). `pwsh -File scripts/check.ps1` **FAIL 0 · INFO 0**, 정본↔미러 5종 일치.

### 수정 요약
- **드롭인 4종** — 00 v1.32→**v1.33** · 01 v1.51→**v1.52** · 04 v1.15→**v1.16** · 06 v1.12→**v1.13**.
- **스킬 6종** — `~/.claude/skills/{resume,wrap,dropin-check}` · `~/.claude/commands/skill-setup` (+ `global-config/` 미러) · 프로젝트 `optimize`·`audit`.
- **검사기** — `scripts/check.ps1`에 두 검사 추가: ⓐ 규칙 열의 `NN 안 N곳` 선언 ↔ **실제 출현 수** 대조(M13 — 파일 단위 판정의 사각) ⓑ 헤더↔푸터 **최종 갱신 날짜** 대조(L1).
- **대장** — `R25` 신설(`[Pending]`의 종결자 축) · R09 필수에 `p:optimize` · R23은 개수 선언을 빼고 위치만(마커 `Reverted`가 상태값·뱃지 표에도 쓰여 **셀 수 없다**) · R08 허용에 `~resume` · 읽는 법에 "RC엔 생산자 규칙만" 명문화.
- **J1·J2** — `/audit` 렌즈 C 여정에 **다인 축** 추가 + PROJECT_PLAN에 `다음 시나리오 대입: 2026-11경`; `CLAUDE.md` 4축의 "실적용"에 시나리오 대입을 하위 방법으로 명시.
- **인계 6건** — PROJECT_PLAN "다음 재검증: 2027-02경"의 🟡 목록에 등재.

### 수정 중 새로 드러난 것 2건
1. **새 검사가 즉시 실효** — M13을 넣자마자 R23이 FAIL했다. 원인은 결함이 아니라 **마커 선정**이었다(`Reverted`가 상태값 `[갈래][…Reverted]`와 §F-6 뱃지 표에도 정당하게 나온다). 개수 선언을 R21·R22처럼 **정밀한 마커를 가진 규칙에만** 두는 것으로 정리했다 — 셀 수 없는 마커에 수를 붙이면 검사가 상시 거짓 경보를 낸다.
2. **`04`에만 남아 있던 빈 인용 줄(`>`)** — 2026-08-20에 5문서를 고칠 때 04는 목록에 없었고, 그 줄이 표 **끝**에 있어 뒤따르는 행이 없던 탓에 여태 잠복했다. 이번 bump 행이 그 뒤로 들어가며 `check.ps1` 표 파손 검사가 발화. **잠복 결함은 그 자리에 새 내용이 들어올 때 깨어난다.**

### 렌즈 E의 오판 1건 (감사 중 교정)
렌즈 E는 repo `CLAUDE.md`의 `core.autocrlf=true` 서술을 "실물과 부합"으로 판정했으나, 실측은 **`input`이고 워킹트리도 LF**였다(`git config core.autocrlf`). PROJECT_PLAN에 2026-08-20부터 관찰로 올라 있던 항목이라 교차 확인으로 잡혔다 — **"이상 없음" 판정도 재검증 대상**이라는 사례다.
